# 更新日志

## 2026-09-05

* **前台+后台（音乐专辑封面）**：后台音乐页每行新增「封面」按钮——上传/替换专辑封面（jpg/png/gif/webp/avif ≤2MB，已设显示 ✓ 和移除 ✕），存 media.cover 列（KV covers/，换图删旧，删除歌曲连封面一并清理），新接口 `POST/DELETE /api/admin/media/:id/cover`、/api/admin/media 清单带 has\_cover；前台迷你播放器左侧圆盘升级为**专辑封面显示**：当前曲目带封面时黑胶式旋转（播放转、暂停保持角度、`prefers-reduced-motion` 不转、中心白点固定不随图转），无封面回落原音符图标；封面随 /api/playlist 下发（静态 music/ 曲库与本地文件夹歌曲无封面），曲目合并/缓存恢复时后台清单后到会自动补齐并即时刷新。**顺手修了一个真 bug**：autoLoadMusicFolder 把 /api/playlist 的 music 项映射成 {name,url} 时丢了 lrc 字段——后台曲库配的歌词从未真正到达播放器（一直只有静态 music/ 同名 .lrc 在生效），现一并携带并经 wrangler 全链路实测确认下发。本地实测：登录→传歌→传封面→清单 has\_cover/cover 地址→封面 200 image/png→替换后旧 KV 404 新图 200→配歌词 playlist 带 lrc→移除封面后回落、lrc 保留→删歌曲封面 KV 连带清理，校验脚本全过

* **前台（课表导出）**：个人主页课表卡工具栏在「导入 WakeUp」旁新增「导出课表」按钮——纯前端把当前课表转成 **WakeUp 兼容 JSON** 下载（文件名 `WakeUp课表导出-日期.json`），本站「导入 WakeUp」可直接导回（服务端解析时保留提醒设置/作息；termStart 随文件携带，WakeUp App 忽略未知字段不受影响），WakeUp App 也能识别导入；weeks 转布尔数组（下标 0 = 第 1 周），内部空 weeks（= 每周都上）导出为全 true 保语义无损；无课程时提示不生成文件。本地实测：导出 → 服务端真实 parseWakeUp 解析往返测试，课程名/地点/教师/星期/节次跨度/周次/每周语义全部无损；校验脚本全过

* **前台（账号昵称，展示名与登录名分离）**：登录账号不再直接当昵称用——个人主页身份卡用户名旁新增「改昵称」入口（1~20 字，留空保存即恢复显示用户名），改完**即时同步**顶栏字母徽章/下拉菜单/欢迎语（sessionStorage demoLoginName 改存展示名），登录账号名保持不变（登录仍用账号名）。留言板历史留言的署名取**实时昵称**（改昵称会同步旧留言的显示，署名按 user_id 实时解析）；login/register/me/profile 接口响应均带 nickname；新接口 `POST /api/user/nickname`（仅前台用户会话；users 表补 nickname 列，ensureSchema 自动补列）。本地实测：改昵称后身份卡/顶栏字母/存储名三处同步、重登后昵称保持、留言板署名显示昵称 + 等级徽标

* **前台（留言板）**：顶栏新增「留言」全屏界面（hash 路由 #/board，Ctrl+K 快跳清单同步收录）——前台用户登录后可发布留言（≤500 字，同一用户 60 秒一条防刷），每条留言显示**用户名 + 签到等级徽标（Lv.0~6，与签到共用 lib/levels.js 规则）**；管理员可发布（金色「站长」徽标）并删除任意留言（两段式确认：首点变「确认删除」再点才删）；游客打开可见留言，发布时弹登录卡。实现：新表 messages（is_admin=1 为站长留言），新接口 GET/POST/DELETE /api/messages（双会话鉴权与 AI 同口径；/api/user/me 补 alsoAdmin 字段——同一浏览器同时有用户登录与管理员会话时，留言板据此显示删除入口）；全屏层走统一管理器注册（FS_ROUTE #/board、Esc 逐层退、垫底覆盖机制全部自动生效）。本地实测：游客拦截、登录发布带 Lv.2 徽标、60 秒限速、站长发布、两段式删除后其余留言保留（修了删除后分页偏移 bug）、搜索快跳直达

* **前台（更新日志时间轴）**：文档区《更新日志》改为时间轴渲染——`## 日期` 自动分组为时间轴节点（主题色圆点 + 竖线），每条更新变独立卡片（标题取条目开头的 **粗体** 段，正文支持行内代码/加粗/链接）；超 220 字的长条目默认折叠出「展开全部/收起」按钮，阅读体验不再是一整屏密文字。其余文档仍走原 Markdown 渲染，写日志的格式完全不变（部署说明照常 `## 日期` + `* 条目` 即可）。本地实测：7 个日期分组 64 条条目正确分组、折叠展开切换、Esc 逐层退出

* **后台（管理员安全中心，「我的」页四件套）**：「我的」页在头像/邮箱之外新增**安全卡**——①**修改管理员密码**：补上此前完全缺失的改密入口（原唯一途径是邮箱验证码重置，邮件服务故障即无解），旧密码验证 + 新密码 6-100 位，旧密码试错复用登录限速（scope=pwd，5 次锁 10 分钟），成功后自动踢掉其他设备（当前登录保持）；②**登录设备管理**：列出全部有效管理员会话（IP/UA/登录时间，标记「本设备」，不回传 token），「退出其他设备」一键吊销（sessions 表补 ip/ua 列，登录时记录来源）；③**最近登录记录**：最近 10 条成功/失败登录（✓/✕ + 原因 + IP + 时间），新表 admin_login_logs 自动裁剪只留 100 条；④**管理员登录二次验证（2FA）**：开关存 site_settings `admin_2fa`，开启前提是已绑管理员邮箱且邮件服务已启用——开启后**后台登录页与前台主页管理员直登两个入口**密码通过后都向管理员邮箱发 6 位验证码（purpose=admin2fa），凭一次性票据换会话（票据复用 email_login_pending 表，user_id 取负数=管理员，与 schedule_sent 约定同源；验证码错一次票据即作废防爆破）。**⚠️ 已知风险**：2FA 开启后若邮件服务故障，管理员会被锁在门外（两个入口都止步于发信失败），届时删 site_settings 的 `admin_2fa` 键即可恢复密码登录——开启门槛已因此要求邮件服务必须在线。后台登录卡相应增加验证码输入行（needCode 时出现、自动聚焦、按钮变「验证并登录」）。本地实测：改密错误旧密码 401 / 正确后旧会话失效 / 新旧密码登录边界、双会话踢单、登录日志全类型记录、2FA 未绑邮箱 400 / 两入口 needCode 拦截（不下发会话）/ 错码票据作废 / 对码发会话、UI 安全卡渲染与改密刷新、登录卡验证码行出现逻辑（fetch 桩模拟 needCode）。

* **前台（全站搜索 Ctrl+K）**：新增命令面板式搜索——**Ctrl/Cmd+K** 或顶栏放大镜（`#searchToggle`）唤起，任何界面里可用：搜索**界面**（首页/工具/文档/AI/相册/杂项，走 hash 路由返回键可用）、**工具卡**（打开工具界面后滚动定位到对应卡并高亮脉冲一下，密码生成会自动展开折叠区）、**文档**（docs.json 清单，直达阅读层）、**音乐**（按歌名回曲库定位播放，与收藏同机制，曲库中已删除的置灰）、**图片**（派发 yhuo:open-photo 直开大图）与**相册名**、**视频**（回首页）。实现要点：独立浮层 z-index 350（高于灯箱低于登录卡），**不参与全屏层互斥**——任何界面里唤起、关闭回到原界面；数据源 /api/playlist + docs.json（60 秒缓存，接口失败回退 music|video|images 三个静态清单，坑 2 的伪 200 也走兜底）；键盘 ↑↓ 选择 / Enter 打开 / Esc 关闭，**捕获阶段接管按键**（面板开着时按键不再漏给其他全屏层，stopPropagation 隔离）；匹配词高亮 `<mark>`（DOM textNode+mark 构建防注入，命中位置 0 的 falsy 坑已修）；分组渲染保持组整体不交错（组内按命中位置排序、组间按各组最佳命中排序）；被功能开关关闭的界面/模块（含 AI 全局开关）**不出现在结果里**；`prefers-reduced-motion` 关过渡。本地实测：唤起/关闭/搜歌播放（迷你播放器载入晴天）/搜"更新日志"直达阅读层/搜图开灯箱/快跳清单/方向键导航/AI 未配置时自动隐藏/后台关 docs+album+video 后搜索结果同步只剩音乐组，恢复开关即回。修了一个真 bug：文档项若立即 openDoc 会被 hash 路由拉起 docsView 时的互斥收掉（docViewer 会被其他层关闭），改为等界面拉起后再开阅读层

## 2026-09-04

* **前台（每日签到 + 等级 0~6）**：个人主页新增签到卡（身份卡下方，仅前台注册账号可见，管理员隐藏）——每日点「签到」记一天（北京时间计天，同日重复请求幂等返回 already 不报错），卡片显示**等级徽标 + 等级名 + 距下一级进度条 + 累计/连续天数 + 近 7 日签到圆点**；等级按**累计签到天数**实时计算（不入库）：0/5/15/40/80/150/300 天对应 Lv.0 初来乍到 → Lv.1 渐入佳境 → Lv.2 常客 → Lv.3 老朋友 → Lv.4 中坚力量 → Lv.5 镇站之宝 → Lv.6 传奇（改阈值只需改服务端 LEVELS 数组，存量自动重算；满级徽标金色、进度恒 100%）；连续天数断一天清零（今天未签时按昨天起算显示历史连续，签上即接回）。实现：新表 checkins（(user_id, day) 主键防同日重复，ensureSchema 自动建）；新接口 GET/POST /api/user/checkin（仅前台用户会话，管理员 401 与收藏同口径）；前端纯渲染，进度条百分比由服务端 prev/next 阈值算出。本地实测：未登录 401、注册→首签（total/streak/近 7 日圆点即时更新）→重复签到幂等→回填 16 天历史后自动升 Lv.2 常客（连签 17 天、进度 8%、「再签 23 天升到 Lv.3」）、管理员打开个人主页卡片隐藏，校验脚本全过

* **后台（按钮与动效体检 + 无障碍合规）**：排查全后台不合规代码并优化按钮/动画——①**状态色合规**：删除类按钮此前是"中性灰样式、毫无危险感"（`button.danger` 悬停不变色），且两处硬编码危险色（删除确认弹窗的「确定」钮 `#d64545`、相册 ⋯ 菜单的危险项 `#e0342b`）违反"状态色走 --ok/--warn/--danger 变量（深浅主题各一档）"约定——现 `button.danger` 悬停转危险色（文字/边框/浅红底全走 `--danger` 的 color-mix），两处硬编码改 `var(--danger)`（深色主题自动换亮红档，确认钮文字取 `--bg` 保证两主题对比度）；②**动画切换增强**：询问/预览弹窗此前瞬间弹出无过渡——现背景淡入 + 卡片缩放上滑入场（靠 hidden 切换 display 自动重播 CSS 动画，零 JS 参与）；登录门四张卡（初始化/登录/找回密码/网络错误）互切与进入主界面同样加了入场动效；③**无障碍合规**：`prefers-reduced-motion` 原来只覆盖 3 处动画（列表/下拉/侧边栏/弹窗等全漏），改为全局压缩规则（全部动画/过渡时长瞬时化，全后台无 infinite 动画所以安全）；新增全局 `:focus-visible` 焦点环（Tab 键盘导航可见，鼠标点击不触发）；④**全量排查结论**：模板字符串内 8 处正则反斜杠全部规范双写（坑 18 无回归）、无原生 confirm/prompt/alert（自定义 ask 全覆盖）、行内操作钮无文本字符（仅 toast/日志的状态 ✓✕ 标记属正当使用）、innerHTML 注入点 textContent/escapeHtml 到位。本地 wrangler 实测：13 个页面切换动画全覆盖零报错、删除确认/图片预览弹窗动画与深浅主题危险钮配色正确、danger 悬停变色与键盘焦点环生效

* **前台（工具合集六卡重做）**：工具界面整体换血——砍掉"手机上有更好替代品"的旧倒计时和颜色转换，换成六张更实用的卡：**🎯 重要日子**（考试/纪念日倒计时，多条记录按日期排序，显示"今天/明天/还有 N 天/已过 N 天"，存 localStorage）；**🍅 番茄钟**（原倒计时升级：专注 25 / 短休 5 / 长休 15 分段切换，响铃沿用原倒计时的 Web Audio 三声铃，专注完成自动计入"今日完成 N 个"并切到短休，跨天自动归零，存 localStorage）；**🔄 换算器**（单位 + 进制合一：长度/重量/温度/数据大小四类单位互转，温度走公式、其余按系数；进制二/八/十/十六互转用 BigInt 逐位解析杜绝超精度失真，四进制同行显示、点击行复制，非法输入显示 —）；**📝 文本工具**（字数统计实时四项：字符/不含空格/词数（中文按字、英文数字串按词）/行数；按行整理：去重/排序/去空行/去首尾空格/大小写；时间戳转日期（10 位以内按秒、更长按毫秒）与当前时间戳；JSON 格式化/压缩带报错提示）；**🎲 随机决策**（每行一个选项滚灯式抽取约 1 秒落定，选项自动保存）；**🧮 计算器**保留并新增历史记录（最近 20 条持久化，点击一条把结果填回屏幕，可一键清空；连续相同算式不重复入历史）。密码生成器为低频工具收进底部「更多工具 ▾」折叠区（功能不变）。全部纯本地无外部依赖、随机决策/重要日子/番茄钟计数/计算器历史分别存 localStorage（yhuoDice / yhuoDays / yhuoPomo / yhuoCalcHist），网格仍为 2 列自适应（窄屏单列）。本地实测：六卡添加/换算/进制/统计/整理/时间戳/JSON/抽签/计算/历史/折叠区全链路通过，刷新后持久化正常，hash 路由直达正常

* **前台（切换界面不再闪现主页）**：五个全屏界面本就是半透明毛玻璃（背景 88% 不透明 + blur），此前切换时旧层 250ms 淡出、新层 250ms 淡入，中途两层同时半透明、主页从缝隙里闪出来。现改为"垫底覆盖"：切换时旧层不淡出，原样压在新层下面（新层 z-index 提到 151 垫在最上、旧层加 fs-covered 禁点击），等新层过渡完（320ms）再在遮盖下走正常关闭流程——主页全程被挡住；快速切回被覆盖层会撤销覆盖原样留住（FS_COVER_TIMER 定时器管理）；fsClose 的滚动锁解锁改为"还有别的层开着就保持锁定"（排除自身 .show 未摘的时序），修复覆盖层延迟关闭会提前解锁主页滚动的问题。浏览器逐帧采样实测（189 帧）：切换全程 0 闪现帧，✕/Esc 关闭后滚动锁正确解除

* **前台（hash 路由）**：顶栏五个全屏界面与地址栏双向同步——点工具/文档/AI/相册/杂项，地址变为 #/tools、#/docs、#/ai、#/album、#/misc；**浏览器返回/前进键可用**（相册返回回工具、再返回回首页，修复此前"进了界面按返回直接退出网站"的缺陷）；**刷新/分享直达**：以 #/xxx 打开页面直接拉起对应界面；✕/Esc 手动关闭时路由静默清除（replaceState，不产生历史记录，返回键不会"重开"已手关的界面）；顶栏"首页"/logo/回到顶部等 #home 锚点视为回首页路由，自动关闭打开的界面。实现：FS_ROUTE 路由表挂在统一全屏管理器上——fsOpen 先写路由再互斥关闭其他层（防 fsClose 误清新路由）、fsClose 校验后静默清路由、hashchange 路由器与深链接都经注册表新增的 open 回调拉起界面（界面的实际显示在各自 open 函数里，直接调 fsOpen 不会显示，注册表原本只存 close 不存 open）；个人主页与文档阅读层不参与路由；与功能开关联动：指向已关闭界面的路由静默清掉（缓存已知关闭直接回首页，首访无缓存先拉起、配置到达后就地关闭并清路由），正开着的界面被后台新配置关闭时同样处理

* **前台（功能开关）**：后台「外观」页新增「功能开关」卡片——顶栏四个界面（工具/文档/相册/杂项）与首页三个模块（天气胶囊/歌词横条/首页视频）可逐个关闭，保存后前台**直接隐藏**（顶栏项消失导航自动补位、全屏层彻底打不开、首页模块不渲染，访客感知不到功能存在过）；AI 不在此列、跟随「AI」页的全局启用开关（且全局开关关闭时前台顶栏 AI 入口现在也直接消失，而不是打开后才看到"接入中"）。实现：site\_settings 新增 feature\_flags 键（白名单 tools/docs/album/misc/weather/lyric/video，缺省全开，全部为开时自动清键），/api/settings 下发 flags 字段（接口失败一律全开、不裁功能），前台 fsOpen 管理器与各界面 open 函数入口双重守卫（画廊点照片跳相册等旁路入口一并拦住），/api/admin/appearance PUT 支持 {flags}；开关改完对访客下次进页面生效（settings 60 秒缓存）。**首屏无闪现**：flags 每次成功拉取后存 localStorage（yhuoFlags / yhuoAiOff），head 末尾内联脚本在 body 渲染前读缓存给 html 打 ff-*-off 类（CSS display:none!important 隐藏导航项与视频框），applyFeatureFlags/fetchAiConfig 拿到最新配置后增删同样的类校正并刷新缓存——访客下一次进页面，被关功能从第一帧就不出现；**首次到访（无缓存）改为先藏后放**：head 内联脚本先打 ff-boot-hide / ff-ai-boot-hide 把顶栏可选项与视频框整体藏住，settings/AI 配置到达后放行真实状态，接口失败或 3 秒超时兜底强制放行（缺省全开不裁功能）——被关功能任何情况下都不会闪现；**配套省流量：杂项和相册都关闭时，前台启动不再下载任何图片**（画廊与相册照片墙共用同一批图，只关其一时仍属正当展示照常加载；判断用 head 内联脚本同步存的 window.\_\_FF\_CACHE 加实时 flags 双查，配置到达后经 FF\_APPLY\_HOOKS 钩子校正——管理员重新开启时自动补加载；首次到访无缓存时无从得知开关仍会加载，与首屏"先藏后放"同一取舍）；浏览器实测验证：模拟首访（清缓存 + settings 人为延迟 5 秒）——首帧可选功能全部隐藏、3 秒超时放行、5 秒配置到达后按真实 flags 校正，回访（有缓存）首帧即最终状态；杂项相册双关时画廊 0 张图 0 个图片请求（仅站点背景图正常加载），重新开启后配置到达自动补加载 8 张

* **修复管理员绑定邮箱 / 忘记密码重置（2026-08-31 遗留 Bug 根治）**：后台「我的」页绑定管理员邮箱、登录页「忘记密码」重置后台密码，验证码校验从来通不过——根因：后台整页 HTML+CSS+JS 包在 `const PAGE = \`...\`` 模板字符串里，内联正则 `/^\d{6}$/` 的 `\d` 被模板转义吃成 `d`，浏览器实际执行 `/^d{6}$/`（匹配 6 个字母 d），任何数字验证码都被前端拦截（报"请填写 6 位验证码"/"请填写邮箱和 6 位验证码"），请求根本没发到服务端；服务端/D1/邮件发送全程正常（验证码收得到、curl 直调接口一次通过），所以此前排查走了弯路。现把模板里全部 8 处被吞的反斜杠双写修复（2 处 `\d{6}` 验证码校验 + 6 处 `\.` 歌词后缀/去扩展名正则），`const PAGE` 上方留防回归注释——以后后台页面内联代码写正则必须 `\\d`、`\\.` 双写（校验脚本查不出这类错，它不解释模板转义）。已实测绑定/解绑/错误验证码提示/忘记密码放行全链路正常；前台用户绑定邮箱走 index.html 真实文件、不受影响

* **后台概览（访问趋势）**：访问趋势图支持柱状图 / 环形图切换（右上角图标按钮）——环形图按日期顺时针排段、多色调色板取色（纯色无灰底、细缝分隔；零流量天不画段，占比按有访问的天计）；活动日 ≤8 个全画，更多时只画占比 Top 7、其余合并成灰色「其他」段（颜色恒不复用、图例收敛 ≤8 行，点击可列出所含逐日明细），今天的段默认径向外移 5px 作锚点、图例带「今」徽标；中心显示近 N 天总量与日均、大段环外标注占比；环在左、图例在右双列布局（窄卡片自动换成上下）；两种视图统一悬停交互：柱子抬起 / 扇区径向外移并放大、其余淡出、跟随鼠标的数值气泡（日期·次数·占比）、点击弹出当日访问明细（含星期/占比/峰值标记）——顺带修了两个悬停 bug：扇区 --dx/--dy 漏写 px 单位导致 transform 整条失效（径向外移从未生效过），柱/扇区缩放未设 transform-box 导致绕 SVG 左上原点而非自身中心（右边的柱悬停会横漂）；悬停弹出改"进即弹、离缓回"——鼠标落到柱/扇区瞬间到位（悬停规则内关过渡），移开后 0.2s 平滑弹回，气泡仍跟随鼠标；**命中层与视觉层分离**根治悬停抽搐——此前弹出动的是被悬停元素自己，鼠标贴环内缘（或柱底）时「弹出→鼠标悬空→回落→再弹出」无限循环抽搐，现每个扇区/柱子配一个静止的透明命中层接管指针事件，视觉层 pointer-events:none 纯展示随便弹；切 14/30 天与切视图互不影响

* **后台概览（最近访问·IP 归属地）**：新增"最近访问"卡片——每次访问（每浏览器会话一条）记录 IP / 时间 / 页面路径 / UA，列表展示最近 20 条（悬停看完整 UA、点击无明细弹窗不变）；IP 归属地逐条懒查询（国内走 whois.pconline 返回中文省/市，国外兜底 ipwho.is 经内置中文国家表转中文，接口侧 24 小时内存缓存去重、查询失败显示"—"）；归属地可随时关闭——卡片头部滑动开关（仅站长模式化独立设置，不删数据重开即恢复）

* **后台概览（邮件统计）**：邮件统计卡全面细化——新增发送明细列表（最近 20 条：时间/类型徽标/收件人打码/主题/✓ 成功 ✕ 失败，失败的悬停可看原因；验证码邮件只显示"验证码邮件"不泄露验证码）；新增近 14 天发送趋势迷你柱状图；按类型筛选 chips（全部/验证码/测试/自定义/早报/课前提醒/提醒测试）；卡内新块加载淡入、明细行悬停高亮、趋势柱悬停抬起；每次发送（成功与失败）都记明细，失败带原因（email\_logs 表）

* **后台概览（统计卡）**：新增"网站运行"统计卡（时钟样式、与前台页脚同源基准时间，每秒刷新　X 天 X 时 X 分 X 秒，内容居中展示）；访问量卡改名为"总访问量（历史累计）"，访问趋势卡汇总行新增"历史累计 X 次"前缀；统计卡统一去图标居中

* **后台（上传）**：音乐/视频/图片上传行弃用浏览器原生文件控件——改为自定义"选择文件"按钮（ghost 风格 + 上传图标，保留多选/文件夹/类型限制全部能力），选择后按钮下方淡入"已选 N 个文件：…"提示，上传完成后同步清空

* **后台（动效）**：功能界面切换加入场动效（新面板淡入 + 轻微上移）；图片页相册切换与整表刷新（重命名/解散/批量移入）时列表淡入 + 上移 5px，搜索输入不触发避免击键闪烁

* **后台外观（侧边栏）**：左上角头像（含默认 YH 徽标）改为正圆形；标题改为"管理界面"并移除副标题小字；侧边栏展开/收起动画重做——文字改为"宽度从 0 平滑展开 + 淡入"（与侧边栏宽度过渡严格同步，0.2s 级），按钮间距与图标对齐过渡平滑，彻底消除展开时文字/图标"闪一下"的跳变

* **后台（访问统计口径）**：新增 visit\_logs / email\_logs 两张明细表与对应接口（/api/admin/visit-logs、/api/admin/geoip、/api/admin/email/logs），前端访问带上页面路径上报；均为新数据从上线后开始积累

* **前台（管理员体验）**：管理员用账号密在前台登录后，顶栏直接显示后台「我的」页设置的管理头像（刷新页面静默恢复时同样识别管理员会话）；点击个人主页不再显示"未登录"——改为显示管理员身份卡（用户名/头像/管理员标识与创建时间），并隐藏仅前台账号可用的功能卡（收藏/邮箱/改密/更换头像，头像需在后台改）

* **前台（管理员课表）**：管理员也能使用课表功能——前台个人主页课表卡对管理员开放（启用/导入 WakeUp/手动编辑/提醒设置全部可用），课表数据存 site\_settings.admin\_schedule（独立于用户课表）；每日早报与重点课课前提醒对管理员生效，提醒邮件发到后台「我的」页绑定的管理员邮箱（未绑定则发站长邮箱，仅站长模式下限定站长邮箱）；后台「邮件」页「发送测试提醒」在邮箱命中管理员邮箱时也可测管理员课表；管理员检测诊断与普通账号同列

* **首页（视频播放模式）**：后台视频页顶部新增播放模式设置（顺序循环 / 单视频循环 / 随机播放三个胶囊按钮）——单视频模式可在视频列表任一条点「设为独播」指定循环哪一条（星标高亮，选中项显示在设置栏）——设置存 site\_settings.video\_mode；前台首页右侧视频轮播按模式播放：单视频循环 = 当前视频播完自动重播（指定视频被删除自动回落顺序）；随机 = 每次播完随机换一条（不重复上一个）；顺序 = 原行为依次循环

* **首页（歌词律动条）**：歌词横条右侧的音乐图标改为与底部迷你播放器同款的律动条（三根竖条）——播放时主题色错峰跳动（0.9s 循环，与 .mini-eq 同款动画与延迟），暂停/待机时灰色静止（半透明）；播放状态经 updatePlayIcon 与播放器同步切换，窄屏下与其他条内装饰一样隐藏。窄屏居中修复：左右装饰（待机点/律动条）改为同时隐藏——此前只藏右侧律动条，左侧待机点 opacity:0 仍占约 25px 布局空间，居中歌词被顶向右偏约 12px

## 2026-09-02

* **后台曲库歌词（新功能）**：管理后台音乐页支持歌词——①上传歌曲时同选同名 .lrc 自动配对成附件随歌曲入库（.lrc 不单独成条目，未配对会提示）；②每首歌曲行新增「歌词」按钮，可补传/替换 .lrc（≤200KB），已配显示 ✓ 并附移除按钮；③歌词存数据库随歌单接口直接下发，前台歌词横条播放后台曲库的歌时同步逐句显示（此前后台曲库只能显示歌名占位）；静态 music/ 同名 .lrc 方案不变，后台歌词优先

## 2026-09-01

* **课表提醒**：tick 接口增加访问留痕——每次被访问（无论密钥对错）都记北京时间时间戳与结果到 site\_settings（成功/密钥错误分开记），后台「邮件」页 tick URL 下方显示"最近一次 cron 访问"，外部 cron 没在跑或用过期 URL 敲 401 一眼可见，不用去 cron-job.org 后台查

* **课表提醒**：「立即执行一次」升级为带诊断——逐账号显示"为什么没发/发了什么"（今天没课、邮箱未绑定未验证、仅站长模式不符、早报未开/未到点/今天已发过、重点课是否在提醒窗口、发送错误原因，邮箱打码显示），不再是干巴巴的"发送 0 封"；排查"真实提醒没到点发"从此一眼定位

* **课表提醒**：后台「邮件」页新增「发送测试提醒」——不用等真实到点，按真实课表算出"今天该发什么"并立即发送早报 / 课前提醒样式的【测试】邮件，主动验证 解析→计算→拼邮件→发送 全链路；收件邮箱可指定（留空=站长邮箱，仅站长模式下只能发站长邮箱），需是绑定了已验证邮箱且启用过课表的账号；测试发送不写防重发记录，不影响真实提醒；发送结果回显教学周/星期/今日课程数，测试邮件计入邮件统计（"课表提醒测试"）

## 2026-08-31

* **课表**：新增"移除课表"——提醒设置区底部红色按钮，自定义确认框二次确认后删除服务端的全部课程/作息/提醒设置（提醒邮件随之停止）；没启用过课表的账号个人主页只显示"＋ 启用课表"入口行，不再直接铺开空表格

* **后台**：邮件设置支持移除站长邮箱——清空"站长邮箱"输入框后点"保存配置"即移除（原先清空保存仍保留旧值，无法移除）；若"仅站长使用"处于开启状态会自动关闭，输入框下方提示已说明

* **课表修复**：修复 WakeUp JSON 导入报"没有解析到有效课程"——部分版本导出的字段名不同（name/teacher/room/startSection/endSection、weeks 为文本如"1-16"、"2-16双"），解析器已全部兼容（已用真实导出文件验证 14 门课全部正确解析，双周课周次正确）

* **课表**：导入支持 WakeUp 的 CSV 格式（此前仅 JSON）——按表头名匹配列（课程名/星期/讲师/节数/教室/周次，兼容列序变化和别称），支持"第1,2节"、"周一"等中文写法、带引号转义的字段和 BOM；前端按扩展名+文件内容自动分流两种格式

* **课表**：修复个人主页课表卡「作息时间」展开后布局错乱——格子里"第N节"文字和时间输入框横向溢出互相挤压，改为标签在上、输入框占满整格的纵向排列

* **课表提醒（新功能）**：个人主页新增「课表」卡——周视图展示本学期课程（按教学周过滤、可切周），支持导入 WakeUp 课程表导出的 JSON（解析在服务端，保留提醒设置）+ 手动添加/编辑/删除课程；提醒设置支持每日早报（每天固定时间一封列出当天所有课）和重点课课前提醒（勾选的课程提前 N 分钟单独发邮件），作息时间（各节上课时间）可自定义；后台「邮件」页新增定时任务配置——显示 tick URL、复制/重新生成密钥、手动执行一次，需配合外部 cron（推荐 cron-job.org 每 5 分钟）触发。提醒发给用户绑定的已验证邮箱

* **首页/杂项**：顶栏新增「杂项」全屏界面；首页的图片画廊（折叠牌堆轮播）迁入杂项界面，首页不再直接展示图片（相册界面不受影响）

* **首页**：普通用户登录成功后也弹顶部欢迎浮窗（👋 欢迎回来，用户名；不显示"进入后台"按钮，6 秒自动消失）；管理员浮窗行为不变

* **首页**：退出账号后弹顶部浮窗提示「👋 已退出登录，期待下次再见」；关闭登录卡片改为上飘滑出退场动效（点击空白处/登录成功关闭时卡片向上浮起淡出）

* **首页**：登录卡片的登录/注册/找回密码/验证码模式切换改为平滑过渡——邮箱、验证码、密码等输入行高度渐展开收起，注册按钮宽度渐变，副标题与提交按钮文案淡出淡入（尊重系统"减少动态效果"设置）

* **首页修复**：修复天气胶囊被拉伸占满整行、右侧大片留白的问题，现在按内容自适应宽度

* **后台**：用户列表显示注册用户的头像（没设头像的仍显示用户名首字徽章）

* **首页**：未播放音乐时，歌词横条改为显示一言（管理员寄语 > 每日一言当日缓存 > 待机提示）

* **首页**：一言从歌词横条移出，改为天气胶囊下方的独立胶囊（与天气同款样式，打字机轮播）；点击一言胶囊可立即切换下一句

* **后台**：主页寄语支持保存多条（最多 20 条）；前台寄语/一言打字机轮播（没配寄语或只配一条时拉 6 条一言混合轮播，3 秒切一句，主域名失败自动换国际备用域名）；一言改为天气胶囊同款样式；修复刷新后自动接续播放导致歌词条待机一言被歌名覆盖的问题

* **AI 助手**：对话记录服务端保存（按账号隔离，每人最多 200 条，刷新/换界面后自动恢复）；关闭界面不再清空对话，点「新对话」才清（本地+服务端一起清）；退出登录清空内存对话防止串号

* **AI 助手**：多对话管理——历史对话改为抽屉式（默认收起，顶栏气泡按钮唤出，点聊天区/选完对话自动关闭，全屏宽一致）；新对话/切换历史对话继续聊/删除带确认框，对话自动以第一句命名，最多 50 个；旧的单条历史自动归入"历史对话"

* **邮箱体系**：后台新增「邮件」设置页（Resend/Brevo 双协议，API Key 存 D1 不回显，测试发送）；启用后前台开放——注册需邮箱验证码、登录卡片新增「忘记密码」（邮箱验证码重置，重置后踢全部设备）、个人主页可绑定/换绑邮箱并开启**登录二次验证**（2FA）；验证码 6 位、10 分钟有效、限 5 次尝试、60 秒重发间隔；未配置邮件服务时所有邮箱相关 UI 自动隐藏，注册流程不变

* **邮箱·仅站长模式**：邮件页新增「仅站长使用」开关——无域名邮件服务（如 Resend onboarding 只能发给自己）场景下，普通用户注册不要求邮箱、看不到邮箱卡，只有站长邮箱（后台单独填写）能收验证码/绑定/开 2FA/找回密码

* **邮箱·管理员**：后台「我的」页新增管理员邮箱绑定/解绑（验证码验证，存 site\_settings）；后台登录页新增「忘记密码？」（邮箱验证码重置管理员密码，重置后踢掉全部管理员会话）；未启用邮件服务时入口自动隐藏

## 2026-08-30

* **AI token 用量统计**：概览页新增「AI 用量」卡片（今日/近 14/近 30 天 tokens 与调用次数 + 按模型排行）；每次对话结束回复气泡下显示 ↑输入/↓输出 tokens 小字

* **AI 模型类型标签**：后台可为每个模型标注类型（文本/视觉/推理），前台切换菜单里模型名后显示小徽标（与桌面客户端同款样式）

* **首页歌词横条**：播放 music/ 曲库的歌时，首页横条上打字机逐字同步显示当前句；把 `.lrc` 歌词文件和歌曲一起放进 music/（同名）即可生效，没有歌词则显示歌名占位

* **外观**：新增"背景模糊"滑杆（0\~30），给自定义背景加上毛玻璃质感；管理员可在后台设置**全站默认模糊度**，访客没自己调过时自动采用

* **后台「我的」页面**：侧边栏左上角的 YH 徽标可换成管理员头像（点击直达），支持更换/移除

* **后台下拉动效化**：所有下拉选择换为统一动效组件（展开/收起动画、点空白或 Esc 关闭）；AI 管理界面顶部说明精简、全局开关移至供应商列表下方

* **前台修复**：修复从 AI 界面切不到工具/文档/相册等其他界面的问题

* **后台整体精致化**：统一线性图标体系（行内操作全部换 SVG，替换掉 ✏️▶👁★☆＋⋯ 等文本字符），新增状态色变量并适配深浅主题；AI 供应商页细节拉齐（图标底块、绿点光圈、虚线添加钮、API Key 眼睛内嵌输入框并可切换显隐图标、模型行/默认徽标配色）；顺带修复音乐/视频列表被相册侧栏容器误隐藏的回归

* **AI 助手**：对话功能正式实装！支持 OpenAI 兼容与 Anthropic 两种协议（GLM/DeepSeek/Kimi/Claude 等主流服务商均可接入），流式打字机回复、多轮对话、Markdown 渲染；后台「AI」页升级为**供应商管理**（左侧供应商列表+右侧详情，一个供应商挂多个模型，✏️改名/启停/删除/★设默认，模型名支持自动获取），前台输入条底部可随时切换模型（按供应商分组的弹出菜单，仿桌面客户端样式）。仅登录用户可对话，服务商 Key 只存后台数据库

* **首页**：新增天气胶囊（Open-Meteo 免费接口，点击换城市）；顶栏新增「工具」与「文档」入口

* **工具合集**：计算器、倒计时、密码生成器、颜色转换四件套上线

* **文档**：文档区上线，当前收录《关于本站》《更新日志》

* **个人中心**：头像下拉菜单全新改版；新增「我的主页」（身份卡、自助修改密码、收藏聚合）

* **收藏**：相册灯箱和播放列表支持 ♥ 收藏，个人主页聚合展示

* **管理员**：登录后首页弹出欢迎浮窗（含进入后台入口）

* **后台**：图片页相册交互重做——左侧相册栏 + 拖拽归类，替代原来的下拉工具行

* **全屏界面**：工具、文档改为与 AI/相册一致的独立全屏界面，五个入口互斥切换、顶栏高亮跟随；图片画廊留在首页

## 2026-08-29

* 站点上线：时钟首页、相册、音乐播放器、访客/管理员登录、访问统计

* 管理后台上线：媒体上传管理、用户管理、外观设置、访问趋势

