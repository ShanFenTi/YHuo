# 更新日志

## 2026-09-09

* **前台+后端：全站代码审计第二轮（用户要求「审计全部代码看看有没有问题，必须看完」）+ 本地全链路 UI 实测，两处功能性 bug 修复 + 一处文案修正 + 一处死代码清理**——本轮通读全部代码（前台八页 HTML、site.css/common.js/blog-player.js 三资产全文、functions 后端 lib 七个库 + api 全部 52 个端点 + admin 单文件、schema/_headers/_redirects/杂项），修复：①**AI 历史抽屉「N 条」计数每轮保存后回落为 2 永不增长**（common.js `aiSaveHistory` 在 `filter` 回调里把消息数累加到**随即被移除**的对象上，之后新建的侧栏条目只取本次增量 `d.added`，累计值随丢弃对象一起丢——修为把累计后的条数带进新对象，见坑 32）；②**AI 用量上报限流键所有前台用户共享**（api/ai/usage.js 误写 `user.id`，`getUserSession` 返回的字段是 `userId`，取出来是 undefined，拼出的限流键恒为 `'uundefined'`——全员共用 120 次/小时的上报配额，一个活跃用户能把其他人的配额烧光；chat.js 同位置用的是正确的 `user.userId`，此处属笔误，已改，见坑 33）；③个人主页管理员身份卡文案把「课表」误列入「仅前台注册账号可用」（管理员课表实际已开放，改为「课表可用」）；④site.css 删 `.icon-*` 蒙版死规则约 42 行（引用不存在的 `assets/icons/dl_builtin_apple/*.svg`，全站零使用、不发请求，纯历史残留）。**审计确认干净的面**：上轮 27 处修复成果完好（admin 门卫/双会话隔离/SQL 参数化/后台转义面/登录限速/验证码 2FA 闭环/Cookie 属性/时间盲区对冲），pjax 竞态守卫、伪 200 兜底（音乐/歌词/清单三处）、预览 iframe 续播丢弃、歌词按歌名拉取等复杂机制守卫全在位，无新引入的安全问题。**本地 wrangler 全链路 UI 实测**（`pages dev` + 本地 D1/KV，API 铺测试数据后浏览器实点）：四档视口 1920×1080/1280×800/768×1024/390×844（四种屏比）前台八页全过——含坑 25 断点夹缝（1280 时钟秒数不被视频盒遮挡）、≤900 汉堡抽屉（768/390，图标/高亮/级联/选中收起）、AI 页手机端贴边+锁视口+隐藏播放条与外观圆钮、课表页未登录弹卡→登录→启用→周视图全链路、留言板等级徽标与 UTF-8 渲染、天气胶囊实拉成功；后台宽窄两态全过——登录门、概览（统计卡/趋势图/AI 用量/邮件统计/静态媒体自动同步）、音乐/图片（相册侧栏+缩略图）、随笔/外观/AI（宽屏左右栏、窄屏上下堆叠）/我的（安全中心）/状态/用户（触屏常显操作钮）、顶栏悬停实时缩略预览卡。check-code 全过。已推送上线（0e160b8）

## 2026-09-08

* **前台+后端：留言板显示留言用户真实头像（用户问「为什么不显示留言用户的头像」后拍板实现）**——原实现只渲染「首字字母头像」（用户名首字色块，管理员「站」），留言接口也从不返回头像数据。现：①后端 `GET /api/messages` 查询带出 `users.avatar_key`（返回 avatar 字段）；页面含站长留言时才查一次 `site_settings.admin_avatar`（站长头像用后台「我的」页设置的那个）；②前端 `boardItem`：有头像渲染 `<img class="board-avatar-img" src="/media/{键}">` 绝对定位盖在首字块上（object-fit cover 圆形裁切、lazy 加载），**加载失败移除 img 回落首字块**（坏键/已注销用户无 avatar 自然回落），无头像保持原首字；③CSS `.board-avatar` 加 relative + `.board-avatar-img` 覆盖规则。avatar_key 为服务端生成键（avatars/u{id}-{hex}.ext）非用户输入，src 直接拼接安全。实测（本地桩 /api/messages + 临时 /media 文件）：成功路径真实图片渲染盖住首字、坏键回落首字、无头像回落首字、站长无后台头像回落「站」四条路径全过；校验全过。已推送上线

* **前台：AI 页不显示外观圆钮（用户要求）**——全屏聊天布局自足，外观圆钮浮在上面容易遮挡输入区。一条规则：`html[data-page="ai"] .appear-tab { display:none !important; }`（面板本体无需同规则：只能由圆钮打开，pjax 进 AI 页时 closeAllTransientOverlays 已收起）。实测 AI 页隐藏/其他页正常；校验全过。已推送上线

* **前台：修外观圆钮「点击展开抽屉后被带出屏外」（用户实报：拖到左侧后点击展开，按钮直接左移出屏）**——圆钮此前挂在 `.appear-dock` 内，dock 收起时 `translateX(100%)` 右移出屏、展开时左移 300px 回来，圆钮跟着停靠架一起走：拖到屏幕左侧的钮一开抽屉就被带出左屏。修：**圆钮挪出 dock、挂到 body 下用真正的 `position: fixed; right:12px; bottom:84px; z 165` 视口固定定位**（transformed 祖先会劫持 fixed，dock 内 fixed 不可用——坑 28 同机制；z 165 高于外观面板 160，开着时浮在面板上就是关闭把手，低于搜索 350/登录 400）。拖动/持久化逻辑随之大幅简化：拖动偏移与存取全部变成纯视口坐标（无需再做面板 rect 换算），clamp 保证钮永远整颗在屏内；dock 的外点关闭判断不受影响（钮自身 click 有 stopPropagation）。实测（本地 8001）：拖到 (100,240) → 点开抽屉钮原地不动浮于面板上（z165）→ 再点收起 → 刷新位置保持 (100,240)；开合功能正常；校验全过。已推送上线

* **前台：外观圆钮支持自由拖动（用户要求「这个按钮可以自由拖动」）**——按住圆钮拖到任意位置松手即停，位置按视口坐标存 localStorage（yhuoAppearKnob），刷新/换页/重开浏览器都保持；窗口缩放时按视口右/下距重新回贴并 clamp 在屏内。实现要点：①位移 ≥5px 判为拖动、否则是点击开关抽屉（拖动结束的收尾 click 用 _dragMoved 标志跳过）；②move/up 监听挂 **document** 而非钮上——setPointerCapture 对合成输入不可靠，指针拖出钮范围会半路断听；③`position:fixed` 不可用（面板 transform 劫持包含块，坑 28 同机制），拖动改写的是相对包容块 .appear-dock 的 right/bottom 内联偏移，回贴时用 dock 实时 rect 从视口坐标换算；④坐标纪律：全程 clientWidth/Height（不含滚动条）——innerWidth 含滚动条与 rect 系混用会出现 15px 级漂移（首版踩过：恢复位偏 15px + 二次拖动失效）；⑤touch-action:none 让触屏拖动不吃页面滚动。实测（本地 390/8001，cua 真实拖动）：拖到 (100,240) 精确落位+存档+刷新精确恢复、纯点击开合正常、拖动不误触抽屉、1280 宽位置按视口保持、缩回 390 clamp 在屏内；校验全过。已推送上线

* **前台：外观抽屉入口从「白色耳朵」改成圆形毛玻璃小钮（用户反馈「外观箭头不好」，三方案中选定圆钮）**——原入口是 34×60 不透明白色圆角耳朵贴在屏缘（‹ 箭头），与下方迷你播放器收起钮挤在一起观感突兀。现：①形态改 40×40 圆钮、调音推子线性图标（sliders，比 ‹ 更达意「外观设置」），毛玻璃配方与顶栏胶囊同款（半透明+saturate blur+内侧高光），深浅主题各一档（.dark 补了旧耳朵时代没有的深色变体）；②仍挂在面板左缘随面板滑动：关着露出 34px 成独立圆钮、开着贴回面板左缘当圆形把手（点它收起，svg 转 90°+主题色提示激活态）；③**定位踩坑**：圆钮 position:absolute 挂在面板内，而面板收起位在不同宽度不齐屏缘——≤900 包容块内收 15px（搭接 11px 恰全在屏内）、桌面则与屏缘齐平（搭接 11px 会悬出屏外切掉图标，曾实测 1280/1600 各悬出 11px），故桌面档改外离 6px（`right: calc(100% + 6px)`）留缝隙；`position:fixed` 不可用（面板 transform 劫持包含块，坑 28 同机制，fixed 会变相随面板飞出屏）；④bottom 84px 避让回顶钮（bottom≤62）与音乐球（≤60）。图标 chevron→sliders 八页同步（坑 23，外壳比对 539 行过）。实测（本地 390/800/1280/1600 + 深浅主题 + 开合）：四档圆钮全在屏内（390/800 距缘 4px、1280/1600 留 6px 缝）、开合功能与把手形态正常、深色毛玻璃正常；校验全过。已推送上线

* **全站代码审计（用户要求「审计一下看看还有没有其他暗病」）：三路并行深读全部此前未逐行覆盖的代码（common.js 全量 7046 行 / functions 后端 38 端点 + lib + admin 单文件 / blog-player.js + site.css 全文 + 八页 main），确认 27 处暗病全部修复**——【前端 common.js】①页脚访问计数裸 sessionStorage 无 try/catch，存储被禁的环境抛异常会杀死整个主 IIFE（登录/播放器/路由全灭）→ 包 try/catch；②一言池当日缓存是死代码（内层回调 resolve 不 return，缓存 then 拿到 undefined 抛 TypeError）→ 改 return texts，省请求机制恢复；③AI 流式被打断（切对话/离页 abort）后 aiBusy 永不复位，重进 /ai/ 无法发消息 → AbortError 分支补 aiStopBusy；④scroll-spy 持有 pjax 前的过期 #home 引用，首页→子页一滚动就把子页导航高亮抹掉点亮「首页」→ 加 isConnected 守卫；⑤寄语打字机：settings/一言池请求在离页后才 resolve 会启动僵尸轮播链写已分离 DOM 且永不可清 → hqDead 标志全入口拦截；⑥mdToHtml 转义不含双引号+链接 URL 段允许引号，`[a](x"onmouseover="…)` 可逃逸属性注入（AI 回复/随笔/文档共用此渲染器）→ esc 补 &quot;/&#39;、URL 段排除引号；⑦天气城市搜索竞态（请求在飞时改词/清空，旧响应覆盖新状态）→ 回调校验当前关键词；⑧七处浮层关闭 setTimeout 无守卫，260ms 内重开会被旧定时器打回 hidden「打开即消失」→ 全部加 show 态守卫（登录卡×2/课表编辑/个人主页/天气/背景/阅读层）；⑨曲库清单指向的文件全 404 时 finishMusicLoad 不执行，播放器永远「曲库加载中…」→ 空结果也走统一收口；⑩home destroy 漏清歌词打字 interval → 补清；⑪歌词条引用 pjax 后 detached 不为 null，悬浮款在子页切歌白白 fetch 歌词+写脱离节点 → 四处守卫加 isConnected；⑫播放器款式热切换不暂停另一款 audio（「看不见的歌」/双声）→ applyPlayerMode 停另一款。【blog-player.js】⑬引用全项目不存在的 __blogOverlayCoordinator（demo 残留），互斥机制从未生效 → 换成 __blogPlayerClosePanel 钩子并挂进 closeAllTransientOverlays（pjax 换页收起播放器面板）；⑭封面加载失败只 hidden 不清 src，下次切回复现空破图 → error 时连 src 清掉。【site.css/HTML】⑮`.to-top` 是死选择器（实际是 .back-top），移动端播放器 sheet 打开时回顶钮隔面板透出 → 改选择器；⑯≤520px 悬浮球右半边被外观抽屉箭头（z160>z40）压住点不到 → 博客款球上移 68px 避让；⑰课表编辑弹窗 radiogroup 下是普通按钮（错误语义）→ 改 group；⑱13 个只有 placeholder 的输入框补 aria-label（工具页/课表编辑/个人主页/天气搜索/AI 输入框；外壳元素八页同步，曾触发外壳比对报错后补齐）；⑲重复的抽屉图标悬停规则去重。【后端 functions】⑳**用户登录完全无限速**（管理员分支有限速、普通用户分支没有，可在线爆破密码）→ 套同套 login_throttle（scope=userlogin，5 次锁 10 分钟）；㉑**解绑邮箱不同步关 2FA**（登录端点因查不到邮箱跳过验证码直接发正式会话，2FA fail-open 形同虚设）→ email-remove 时同步删 admin_2fa；㉒**验证码签发无 IP 级上限**（匿名批量请求可烧穿邮件日配额，耗尽后 2FA 发码失败把管理员锁在门外）→ 单 IP 每小时 8 封上限（login_throttle 按小时键计数）；㉓后台「设为默认模型」永久 404（models 已是对象数组还用 includes 查字符串）→ 改按 m.id 比对；㉔chat.js 引用未定义变量 s.protocol 被 catch 吞掉，上游错误详情永远为空 → 改 pick.protocol；㉕health 公开端点泄露 KV 键名/管理员数/DB 错误详情 → 只回布尔可用性；㉖tick 错误密钥每次写库可被刷 D1 写配额 → 60 秒节流；㉗AI 用量上报无频控可灌水 → 每身份每小时 120 次上限；上传 title 回退未截 200。**审计确认扎实的面**：admin 门卫全覆盖、会话双轨隔离、SQL 全参数化、admin 模板零插值+全 textContent/escapeHtml、API Key 只回尾 4 位、验证码/2FA 票据一次性闭环、Cookie HttpOnly+SameSite=Strict、上传类型/大小白名单、tick 幂等、曲库合并与歌词时间源逻辑闭合。校验全过。已推送上线

* **前台：代码审计两处暗病修复（用户要求「审计一下看看还有没有其他暗病」）**——①**登录卡闪现（慢线首访暗病）**：登录卡 markup 默认可见（无 hidden/hide），全靠 body 末尾的 common.js（约 200KB）执行时才隐藏——慢线上匿名访客每次整页加载都会先看到几秒半透明遮罩+登录卡才消失（「首访不弹卡」改造后全员受影响）；其他浮层（docViewer/cmdk/weatherPicker/bgPicker/profileView/schedEditor/miniPlaylist）默认都正确 hidden，唯独登录卡漏了。修：八页 markup 初始即 `class="login-gate hide" hidden`（openGate 本就会摘 hide+hidden，与「关闭后再打开」路径完全一致；.hide 的 visibility:hidden 不依赖 [hidden] 的 UA 样式，不会被 .login-gate 的 display:flex 盖掉；无 JS 用户本来无法登录，无副作用），实测头像点击开合/反复开合正常。②**Esc 分层混乱**：抽屉开着按 Esc 会把同帧把其下所有 document 级监听层（阅读层/个人主页等）一起关掉，而比抽屉高的搜索面板（350）/登录卡（400）开着时又会错误先关抽屉。修：抽屉 Esc 守卫——高层浮层开着时让位不关抽屉；处理完 `stopImmediatePropagation()`（同节点 document 上的后续监听只有它能拦住，抽屉监听注册早于阅读层等故可拦），Esc 从此一层一层关。**审计过无问题的**：八页无重复 ID；功能开关/预览卡与抽屉互不影响；transform fill 全部 backwards 无坑 22 残留；z-index 表一致；reduced-motion 降级齐备；宽窄两档八项回归（抽屉/搜索/揭示/pjax 前进后退/登录卡开合/深色变量）+ 错误收集器全零异常。校验全过。已推送上线

* **前台：修窄屏（480-767px）首页问候语被顶栏遮住上半截（用户手机截图实报，190963.xyz）**——用户手机视口约 509 CSS px 正落在 480-767 断点，该断点 `.hero-section` 顶部留白还是汉堡模式改造前的 40px，而顶栏（汉堡+logo+动作钮）高约 51px，问候语「夜深了 🌙」被盖 11px；≤480 与 768-899 两档均为 64/68px 一直正常，唯独这一档漏改。修：该断点 hero 顶部留白 40→68px（与相邻断点一致），实测 360/412/500/600/760/800/880/1280 八档间隙全部 15-25px 为正；顺带把该断点注释里过时的「导航可横向滑动」口径改为抽屉模式。校验全过。已推送上线

* **前台：全站搜索面板弹出/收回动画（用户要求「顶栏的搜索按钮增加弹出收回动画」）**：原面板只有 0.18s 的轻微过渡（scale .98→1，几乎看不出）。现——①**弹出**：scale(0.94)+下移 12px 起步，弹簧曲线 cubic-bezier(0.34,1.56,0.64,1) 320ms 带**轻微过冲**（scale 冲过 1 再落回），配合背景模糊淡入，放大镜/Ctrl+K 唤起都有"蹦出来"的感觉；②**收回（二次修正：用户反馈"还是很突然像瞬间消失"）**——首版 ease-in 起步慢、150ms 淡出先掉完，半透明玻璃面板没动几下就没了，观感即瞬间消失；改 ease-out（--ease-out 260ms）让位移从第一帧就明显，行程放大 scale(0.9)+下沉 22px，淡出拉长 240ms 与位移同步，遮罩淡出同步放缓 240ms，closePanel hidden 延时 280ms（>260ms 过渡）；③搜索按钮本体加 :active 按压缩放（scale 0.86，transition 原本就带 transform）；④prefers-reduced-motion 下两个方向的过渡都瞬时化。过渡方向归属：弹出过渡写在 .show 态（after-change style 决定用哪条 transition），收回写在基准态。实测（本地 1280/390）：弹出中帧 scale 0.9988→终态 1（弹簧生效）、Esc 后 100ms 收回中帧可见（scale 0.991 缩小中、未 hidden）、210ms 后 hidden=true；校验全过。已推送上线
* **前台：未登录首次访问不再自动弹登录卡（用户要求）**：

* **前台：顶栏窄屏改「汉堡+抽屉」模式（对齐后台，两种导航自由适配）+ 阅读层接入滚动模糊揭示（用户要求）**：①**窄屏导航抽屉**——网站顶栏的窄屏适配从「胶囊拉满行宽 + 导航横滑」改成与后台 /admin 完全同款的模式：**≤900px（后台同断点）**胶囊收窄为 汉堡+logo+动作钮（搜索/登录/深色），八个导航项进左侧毛玻璃抽屉（汉堡唤出、点遮罩/✕/选中任一项/Esc 收起、开抽屉锁背景滚动），>900px 维持原悬浮胶囊导航、汉堡与抽屉纯 CSS 藏起不可用，窗口跨断点时 matchMedia 自动复位抽屉状态——两种导航自由适配。实现：汉堡按钮（#navToggle）与抽屉 markup（#navDrawerMask/#navDrawer，八项 .nav-drawer-link 带 data-target）入外壳**八页同步**（坑 23，外壳比对 538 行过）；抽屉项高亮由 applyNavActive 顺手同步（active + aria-current，与胶囊同款反色 chip；并在启动按 `<html data-page>` 补调一次——原先 applyNavActive 只在 pjaxSwap 里调，整页加载/刷新时抽屉项没有高亮），功能开关按 `.nav-drawer-link[data-target]` 同口径隐藏（ff-tools/docs/ai-off 与 ff-*-boot-hide 全覆盖），pjax 换页经 closeAllTransientOverlays 收起；预览卡不受影响（resolveLink 只认 .site-nav 内的链接，抽屉项不弹）；原 ≤480px「藏 logo」规则删除（胶囊里只剩汉堡+logo+三钮，logo 放得下）；≤900px 下搜索/头像/主题动作组 `margin-left:auto` 推到胶囊最右（胶囊占满行宽后 `.site-nav` 的 flex:1 撑开元素没了，动作组原先紧贴 logo）；**抽屉视觉重做（用户反馈"不是很好看"）**：每项加 18px 线性图标（nav-drawer-ico，默认次级色、悬停/选中随文字变色），当前项从反色黑块 chip 改为主题色柔和底（`color-mix(primary 12%)` + 主题色文字/图标，深浅主题自动跟随），条目加高到 46px/间距 6px、抽屉加宽到 min(80vw,320px)，开抽屉时条目从左 36ms 级联滑入（keyframes from 隐藏 + fill backwards 盖延迟期；⚠️ 不能设 opacity:0 基准态——backwards 动画结束回基准样式，基准隐藏条目会永远隐形，踩过已修）；reduced-motion 下级联整体关闭；条目悬停按压感（hover scale(0.98) 下压+背景变深、:active scale(0.96) 更深，transform 150/80ms 过渡；.active 主题色底不被悬停底盖）；**抽屉右侧留白收整（用户反馈"右边留白太多"）**：每项行尾加导航箭头（.nav-drawer-chev，margin-left:auto 锚住行右缘、悬停右移 2px 提示），抽屉宽度 min(80vw,320px)→min(75vw,292px)，底部加「© YHuo」落款（margin-top:auto 沉底+上分隔线）填充列表下方竖向空白。②**阅读层（docViewer）接入滚动模糊揭示**——此前只有关于页的文档卡片有揭示，《更新日志》/《关于本站》点开后的正文没有：现 mdToHtml 顶层块（h1~h6/p/列表/pre/blockquote/table/img/hr）与更新日志时间轴**按天（.cl-day）**逐块揭示，阅读层内滚动（.doc-scroll 内部容器滚动，单独挂监听）与 openDoc 注入正文（MutationObserver 监听 #docViewer）都会触发重扫；阅读层关着时不采集（display:none 量到 0 会全部被误判已入视口）。实测（本地 8000，浏览器）：390×844 抽屉开合/点项 pjax 导航+自动收起/「关于」高亮 active/Esc 关闭/跨回 1280 胶囊导航回归全过，浅深两主题抽屉视觉正常；更新日志 9 个 .cl-day 首屏揭示+下方 blur10px 待揭示+层内滚动到底全部揭示、关于本站 10 个顶层块揭示正常；首页/blog/ai/board 四页 pjax 回归无异常，抽屉与阅读层随换页正确收起；校验全过。已推送上线并线上验证

* **前台：接入滚动模糊揭示（参考博客站 04 号特效，用户要求"给我的网页加入这种效果"）**：栏目卡片等内容进入视口时从「模糊 10px + 下移 20px」0.6s 渐变清晰，同批进入的元素按 60ms 逐个级联（封顶 6 档，`--reveal-delay`），已揭示不重播；位置判断法（`top < 94% 视口高`）对快速滚动/锚点跳转/滚动恢复稳健——被跳过的内容直接出现不补播。**覆盖范围**：顶栏标题条 `.album-bar`、内容卡 `.apple-card`、工具卡 `.tool-card`、折叠区 `.tool-more`、文档卡 `.doc-card`（异步渲染）、随笔条目 `article.note`/`.notes-year`/`.notes-lead`，**全部限定 `.page-main` 内**——外观抽屉/个人主页/登录卡在外壳里也是 `.apple-card`，绝不能被藏；首页 hero 三件套（有自己的入场+布局恒定修复）与 AI 流式消息不参与。**实现要点**：①采集+揭示在 common.js **顶部独立 IIFE**（放最前：后面模块就算抛错揭示监听也已就位，不会内容常隐）；②pjax 换页替换 main、docs/notes/board 接口返回插图，都靠 **main 上的 MutationObserver（80ms 防抖）重扫**，无需侵入各模块；③**门控**：head 内联脚本在 JS 可用且未开启 prefers-reduced-motion 时给 `<html>` 打 `fx-reveal` 类（与 ff-* 预隐藏同思路，防首帧闪现；无 JS 内容常显），common.js 启动打 `__fxRevealBoot` 标记、head 3 秒兜底摘类（防脚本加载失败把内容永远藏住）——八页 head 同步改（坑 23，外壳比对 514 行过）；④CSS 用 **animation + fill backwards** 不用 transition：过渡规则会常驻压过卡片自身 hover 过渡，backwards 动画结束零残留（不残留 transform 改 fixed 后代包含块——坑 22 同源），规则在 site.css 末尾「滚动模糊揭示」段。实测（本地 8000，浏览器）：工具页首屏外 `opacity:0+blur(10px)` 待揭示/首屏级联/滚动揭示全过；docs 异步卡片经 MO 重扫正常揭示（不卡隐藏）；notes/board/blog/ai/schedule 八页过一遍全正常；pjax 换页级联正常；外壳外观抽屉 opacity 1 不受影响；首页 0 个揭示目标；`display:none` 的 `#schedCard` 被立即预揭示（启用课表后直接可见）；旧缓存 HTML + 新 JS = 门控类不存在优雅降级全显。校验全过。已推送上线并线上验证
* **前台：未登录首次访问不再自动弹登录卡（用户要求）**：原首访逻辑是 sessionStorage 无登录记录就先给 body 挂 `login-lock` 锁滚动、再问 `/api/user/me`，查无有效会话 300ms 后浮出登录卡片——访客落地第一眼是暗色遮罩 + 登录卡，想先逛逛都得先点掉。现改为**一律不弹**：无记录时登录卡立即置 hide + hidden（与"已通过门"分支同处理），`/api/user/me` 只做**静默会话恢复**（Cookie 有效则 passGate 恢复登录态/头像并触发课表页钩子），无会话或接口失败都保持匿名浏览、不锁滚动；登录卡入口不变——点顶栏头像或做需登录的动作（留言发送/收藏/AI 发送/进课表页）时照常弹出。附带收益：顶栏导航悬停预览的实时 iframe 缩略图不会再在帧内弹卡/锁滚动（坑 29 场景）。实测（本地 8000）：首访不弹 + 可正常浏览、点头像可开可关、关闭后匿名刷新不弹、游客进入后刷新不弹（原 gatePassed 行为保持）、课表页未登录门照常弹；校验全过。已推送上线并线上验证
* **修：一言每次刷新都让歌词条上下抽搐（用户实报）**：根因是 hero 三件套（天气胶囊/一言/歌词条）都从 `display:none`（markup hidden）在各自异步数据到达时才弹进文档流——每弹一个，下面的内容（歌词条最直观）就被顶一截，慢线上十分显眼；窄屏一言还是换行样式，打字机逐字增长时行数变化会把歌词条顶得上下抖。修法（**首帧占位**）：①三件套 markup 摘掉 hidden，从首帧起常驻文档流、靠已有的 opacity/translateY 渐现入场（`.loaded`/`.show`），异步到达**零布局位移**；一言胶囊加 `min-height: calc(1.6em + 16px)` 让空胶囊与单行有字同高（否则首个一言到达时仍会长高 21px）；②flag 关闭/失败路径**显式补 hidden**（原"flag 关 = 永不给摘 hidden"的隐式隐藏随占位策略失效）：歌词三入口（lyricShowPlaceholder/lyricStartTyping/lyricRebind）与天气 renderWeather 改为显式收起，天气拉取失败也收起（保住"接口失败静默隐藏"原行为，不留幻影空隙）；③一言打字机（hqTypeText）打字前先用完整文本量出这条的最终高度锁进 min-height（CSS 260ms 过渡承接轮播切换的高度差），打字期间行数增长不再动布局。实测（本地 8804）：桌面 1280 全程 62 个采样歌词条 offsetTop 恒定 463（覆盖天气/一言到达与首轮播）；390 宽轮播全程恒定 569；点击触发打字机路径，打字首帧（0 字）min-height 已锁 37px、打字全程胶囊高度不变、歌词条位置恒定；校验全过。已推送上线并线上验证
* **前台：换页入场淡入（用户反馈"加载好了每个界面切换太生硬、像突然出现"）**：内容替换后给 `main.page-main` 挂 `.pjax-enter` 播 **300ms opacity 0→1**（pjaxEnter，--ease-out 令牌），八页统一入场——原 pageFade 只盖 tools/docs/board/ai 四页且与上一条的变暗恢复叠加后观感生硬；首页/课表/随笔/预览从此也有淡入。**只动 opacity 不动 transform**：transform 配 fill 会把 main 里 fixed 后代（AI 历史抽屉）的包含块改到 main 上（坑 22/28 同源雷，fill:both 残留 opacity:1 无副作用）；挂法：先摘类 + 强制 reflow 再挂回，保证每次换页都重播；仅 pjax 换页触发（整页加载不吃这个动画，不与 pageFade 叠加）；reduced-motion 下关闭。实测：动画中段采样 opacity 0.9（播放中）、结束归 1、二次换页重播正常；校验全过。已推送上线并线上验证
* **前台：pjax 换页加载反馈（用户提议"点击界面有个加载动画"）**：慢线上点击导航后到内容就位之间零反馈、像"点了没反应"，现 fetch 发起 **160ms 后还没返回**就亮反馈——复用顶部阅读进度条变**滑动加载段**（`.progress-bar.pjax-busy`：width 30% !important 压过滚动处理器写的内联宽，滑动动画走 translateX 与 width 互不打架、无限循环）+ 内容与页脚**轻降透明度 0.55**（`body.pjax-loading`，180ms 过渡不突兀）；**内容就位即全部撤除**，160ms 内返回（本地/快线）完全不出现不闪；fetch 失败整页加载兜底时保留加载态让旧页带着进度条直到卸载；`prefers-reduced-motion` 下降级为静态 40% 段无动画。样式在 site.css「Reading Progress Bar」后的「pjax 换页加载态」段，挂钩在 common.js pjaxSwap（pjaxLoadingOn/Off，撤除放 postVisit 前防上报慢时残留半截反馈）。实测（本地 8802）：快换零闪现、拦 fetch 延迟 900ms 后 160ms 准时出反馈（截图确认滑动条+内容变暗）、就位后类全撤/透明度回 1/新页渲染正常；校验全过。已推送上线并线上验证
* **修：后台随笔表单去掉日期项（用户反馈"都写随笔了还设时间多余"）**——随笔就是当日随手记，日期不该手填：①服务端 `create` 自动取**北京时间当天**（`Date.now()+8h` 转 ISO，与 visit\_daily/checkins 同口径），前端不再传 date；②`update` 日期不参与更新（保持创建当天的值），只改天气/时段与正文；③后台表单移除日期输入框，标题改「新增随笔（日期自动取当天）」、编辑标题标注「（日期不变）」；④`import` 动作保留清单原日期（搬存量场景不受影响）。校验全过。已推送上线并线上验证
* **修：随笔「心情」字段实为天气/时段，后台与文档文案全改**（用户实报"晴/夜是天气情况、时间，怎么搞成心情了"）——「心情」是接入参考博客站时沿用的原站叫法（mood 标记），但本站实际用法是天气（晴/雨）与时段（夜）；后台随笔表单占位文案改「天气 / 时段（可空，如 晴 / 雨 / 夜）」，交接文档/changelog/site.css 注释同步改口径。**字段名 mood（DB 列/接口/JSON 键）不改**——纯内部键名访客不可见，改名要动 D1 列与前后端三处、纯增量成本无收益；前台胶囊只显示值本身（晴/夜），本就不带"心情"字样，访客侧零影响。校验全过。已推送上线并线上验证
* **后台+前台：随笔接入后台管理**：随笔从"手改静态 JSON"升级为后台可管理——①**D1 新建 `notes` 表**（migrate.js 自动建，date/mood/text；schema.sql 同步留档）；②**公开接口 `GET /api/notes`**（日期倒序）+ **后台接口 `GET/POST /api/admin/notes`**（动作式：create/update/delete/import，date YYYY-MM-DD、mood ≤12 字、text 1~2000 字超限 400；import 按 date+text 去重，返回 {imported, skipped}）；③**后台新增第十一项「随笔」标签页**（导航在「图片」后，胶囊/抽屉双 DOM + 悬停预览卡 PANEL_INFO 同步）：上方表单新增/编辑（日期预填今天、天气/时段可空、正文 textarea），下方全部随笔列表（日期倒序、行尾编辑/删除图标钮、删除走 ask 两段式确认），「从静态清单导入」把 notes/notes.json 存量一键搬进 D1（date+text 全同自动跳过，重复导入零副作用）；进页即重置表单防草稿串场。④**前台数据源链改为 /api/notes → 失败或空库回落静态 notes/notes.json**（后台录入后静态清单不再参与显示）。实现注意：admin/index.js 是模板字符串（坑 18），注释里连 `${` 字面量都不能出现——本轮注释写"不许出现反引号和 ${"直接把模板字符串炸出语法错误，check-code 当场拦住；正则反斜杠一律双写。实测（本地 wrangler + D1/KV 模拟， fresh 建号登录）：建表/空清单/增改删/导入 5 条→再导全跳过/非法日期·空正文·超长各 400/未登录 401 全链路 curl 过；后台 UI 新增→编辑→删除（含确认弹窗文案）浏览器过；悬停「随笔」导航项预览卡实时切到随笔面板；前台 /notes/ 渲染 D1 数据 6 条、UTF-8 完整、同日多条并存；校验全过。已推送上线并线上验证
* **前台：新增随笔页 /notes/（站点七页收拢为八页）**：对齐参考博客站 `/notes/` 的随笔时间线——单卡片内按**年份分组**（年份标签 + 分隔线），每条 = 日期（"2026 年 9 月 7 日"格式）+ 天气/时段胶囊（可空）+ 正文，正文走 mdToHtml（先整体转义再解析防注入，支持粗斜体/行内码/链接/列表/引用）。**数据在 `notes/notes.json` 手工维护**（与 docs.json 同思路：加一条随笔 = 数组加一个 `{date, mood, text}` 对象，渲染时统一日期倒序 + 年份分组，同日多条锚点 id 顺延去重）。单条锚点 id=日期，`/notes/#2026-09-07` 可直达/分享——内容异步渲染原生锚点会扑空，渲染完成后 locateNote 补定位 + cmdk-flash 高亮。接入点：PAGE_ROUTE/PAGE_TITLES/PAGE_MODULES(notes)/预览卡 NAV_INFO('/notes')/搜索界面快跳「随笔」（紧跟首页）全部同步；顶栏导航八项 **首页|随笔|工具|AI|留言|课表|预览|关于**（随笔排在首页后，与参考博客站一致），外壳八页同步（坑 23），无功能开关（始终显示）；check-code.mjs 纳入第八页（八页外壳一致性比对 507 行全过）。实测（本地 8801 端口）：页面渲染/天气时段胶囊/markdown/pjax 首页↔随笔往返（标题 URL 高亮都正确）/搜索条目跳转/锚点直达定位/顶栏悬停预览卡（实时缩略图 + "YHuo · 随笔"说明）全部通过；390×844 无横向溢出，深浅主题排印正常。已推送上线并线上验证
* **修：窄屏流星左下角仍然偏少（二次修正）**：用户再报"左下角太少了"。上一版把出生带按视口自适应后，采样虽"九区全覆盖"，但**整条出生带约 3/4 在屏幕左侧外**——路径穿过左下角的迹线不仅占比小、在屏时间也短（从左缘进入到落出屏底只有约 0.1 屏高），观感仍稀。二次修正（common.js 流星段）：①**分配方式改为"屏幕目标点反推"**——每颗流星先在屏幕上均匀取一个路径必经点（x 0~100%、y 12~92%），按 35° 斜落几何反推出生横坐标 = 目标x − 1.427×(目标y + 64px)，resize 重排按存下的目标点重算；②但实测发现按目标点均匀取线时，**长对角线（穿屏幕中央）被选中概率正比于在屏长度**，左下角只被短迹线路过、频率反而被摊薄（390×844 采样左下 5/177）；③最终方案：**主群全屏均匀 + 补充群（数量≈主群 70%）目标点收紧到穿过左下角的几何带**（tx -0.05~0.35 / ty 0.5~0.95，带内约八成迹线扫过左下角九宫格），用更高出现频率补密度；④触屏窄屏档流星数 15→18。实测（390×844 十秒采样）：左下角 5 → **29**，与中左/上左同量级，底部一行（123）反超顶部（44）；宽屏 1280×800 回归：九区全覆盖、左下角 90 为最密区无退化；校验全过。已推送上线并线上验证（线上 /assets/common.js 含 birthLeftFor 目标点反推逻辑）
  - 排查注：浏览器对 /assets/common.js 有 1h 缓存（_headers），改完代码本地验证需换端口或强刷，否则跑的是旧脚本（本轮曾在 8798/8797 端口被缓存坑过两次）

## 2026-09-06

* **修：后台顶栏导航悬停预览卡始终钉在左上角**（用户实报"怎么始终在左上角预览"）——`.fx-admin-preview .ap-card` 漏写了 `position:absolute`：卡片的 left/top 由 JS 按鼠标位置写入，但 left/top 对**静态定位元素无效**，卡片于是永远渲染在 fixed 容器原点（左上角）。补 `position:absolute; left:0; top:0`（与前台 `.fx-link-preview__card` 同款写法）后，卡片正确出现在鼠标右下 18px 处并跟随移动。本地实测：悬停靠右的「状态」项（鼠标 x834,y33），卡片落在 (852,51) 紧贴鼠标；校验全过。已推送上线并线上验证（线上 /admin 含修复样式 position:absolute; left:0; top:0）

* **后台（顶栏导航悬停预览卡 + 视频行悬停播放卡，两处"首页同款"悬停预览）**：①**顶栏导航项悬停出栏目实时缩略卡**——后台是单页应用、十个栏目共用 /admin 一个地址，与前台多页面不同：视觉区放**一个常驻 iframe**（加载 /admin 自身，1280 宽 scale(0.25) 缩进 320×112），悬停不同栏目时 **postMessage（type=adminPreviewPanel）让 iframe 里的 admin 实例 switchPage 切到对应面板**——悬停哪个栏目就实时预览哪个，一个 iframe 全搞定；iframe 在登录门/加载态收到的消息先存 pending、enterMain 末尾补应用，iframe 就绪（enterMain）后回发 adminPreviewReady 通知父页补发；栏目描述表 PANEL_INFO 十项；触屏不启用。实测：悬停 音乐/外观 缩略实时切到对应管理面板、主界面自身不动。②**视频列表行悬停出播放卡**：renderList 给视频行挂 _vkey/_vtitle（JS 属性），悬停 150ms 弹 320 宽悬浮卡内 `<video muted loop>` 自动静音循环播放 + 底部标题条，移出/滚动即收起并卸载 src 停止下载；行内 ▶ 预览弹窗行为不变。实测：悬停不同行出对应视频且真实播放推进、移开即停即卸载。两卡样式走后台 --card/--border/--shadow 变量、z-index 400（压过抽屉让过弹窗/吐司）、仅悬停设备启用；校验全过。已推送上线并线上验证（线上 /admin 含 fx-admin-preview/video-hover-preview/adminPreviewPanel 全部新标记）

* **后台（顶栏复刻前台悬浮胶囊：宽屏胶囊即导航，窄屏保留侧边栏抽屉）**：按用户要求把网站顶栏复刻到后台管理界面，并按用户确认采用**响应式分工**——宽屏（>900px）胶囊即导航：`.admin-header`/`.header-pill` 毛玻璃胶囊（样式移植前台：rgba 硬编码+saturate(1.8) blur(22px)+内高光+投影，变量映射到后台 --fg/--bg/--hover），内容为 头像圆（原 brandMark，点击进「我的」）+ 十项纯文字导航（active=前台同款反色 chip）+ header-actions（回前台链接/黑白主题切换/退出登录）；胶囊外点击穿透、滚动 >10px 加深投影（.scrolled 监听）。窄屏（≤900px）胶囊收窄为 汉堡+头像+动作钮，**导航回原侧边栏抽屉**（aside.sidenav 原样保留为抽屉，汉堡唤出/遮罩收起/选完页自动收起）。退役：左侧常驻侧边栏、#sideInd 竖排滑动指示器与 moveSideIndicator()、侧栏收起按钮（nav-collapsed）、topbar 页标题（pageTitle/PAGE_TITLES）、themeLabel 文字。实现要点：①胶囊(#sideNav 纯文字)与抽屉(#drawerNav 图标)是**两套导航按钮 DOM**，navBtns 选择器扩为双容器覆盖，active 同走 switchPage；②applyTheme 对 themeLabel 本就有空值守卫，元素删除零改动；③**踩坑**：后台有全局 `button { padding:9px 18px }`，34px 圆钮被内边距吃光导致 svg 宽 0（图标"消失"）——.action-btn 显式 `padding:0` 抵消；④窄屏藏胶囊导航的选择器要用 `.site-nav.pill-nav`（两档类名），单 `.pill-nav` 会被基础规则 `nav.site-nav` 的 display:flex 压过。实测（1280 宽/390 宽双态）：十页切换 active 双导航同步、主题浅/深切换、头像进我的、回前台、滚动投影、抽屉开合与选页收起、退出登录回登录门，全部通过；校验全过。已推送上线并线上验证（线上 /admin 含 admin-header/header-pill/drawerNav 新标记、旧 topbar/pageTitle 零残留）

* **前台（课表课程块配色：8 色扩到 20 色 + 互不撞色分配）**：用户反馈"课程表界面的课颜色就几种"——旧实现 `SCHED_COLORS` 只有 8 个颜色且按课程名哈希取模，课一多（典型一学期 10~20 门）大量撞色。①色板扩为 **20 色白字友好色板**（原 8 色保留在前，新增 红/草绿/姜黄/湖蓝/靛蓝/兰紫/品红/棕/松绿/酒红/中蓝/墨蓝灰，全部按白字可读的明度挑选，深浅主题通用）；②**分配逻辑升级**：renderSchedGrid 每轮渲染先 `schedRebuildColors()` 给当前课表出现过的课程名分配颜色——从名字哈希位开始线性探测，**不同名的课在 20 色内互不重复、同名课恒同色**（哈希保证跨渲染稳定），课程超过 20 门才回落哈希复用；编辑器实时预览的未入库名字走哈希兜底。实测（本地导入 14 节 WakeUp 测试课表，8 门不同课程）：8 色零撞色、同名课同色、白字全部清晰；校验全过。已推送上线并线上验证（线上 /assets/common.js 含新分配逻辑）

* **前台（移除杂项界面 /misc/ 及其全部相关代码，站点八页收拢为七页）**：按用户要求整体退役图片画廊页。①**删除 `misc/` 目录**，其余七页外壳各摘三处：顶栏「杂项」导航项（miscBtn）、head 预隐藏脚本的 `ff-misc-off` 行、外观抽屉「动效」段两个开关（大卡片跟随鼠标/小图片倾斜——唯一消费者就是画廊，连 common.js 2082-2110 的读写接线一起删）。②**common.js 摘除画廊主体**（约 240 行：initMiscGallery/destroyMiscGallery/牌堆轮播/resetCardMouse/状态变量），PAGE_ROUTE/PAGE_TITLES/applyFeatureFlags/搜索界面清单/PAGE_MODULES/旧 hash 归一/预览卡 NAV_INFO 的 misc 项全部摘掉。③**搜索模块清掉 images 死代码**（data.images 一直在加载但 buildItems 从未消费）。④**背景选择器功能保全**：「从站内图片挑选」原来直接读画廊的已加载缓存，改在 openBgPicker 现场 fetch /api/playlist（失败回落 /images/manifest.json）——实测 8 张站内图正常出网格。⑤**顺手修一个相册时代残留雷**：renderFavorites 里 `if (f.type === 'image') photos.push(f)` 的 photos 从未声明（'use strict' 下存有旧图片收藏必然 ReferenceError），已删。⑥后台配套：FLAG_DEFS/FLAG_KEYS/settings flags 白名单移除 misc（旧库存 misc:false 自动忽略）；/api/playlist images 字段保留（背景选择器在用）。⑦**新增仓库根 `_redirects`**：/misc(/) 301 回首页——没有它 Cloudflare SPA 回退会把 /misc/ 以 200 返回首页内容（坑 2）。check-code.mjs 收拢为七页。实测：七页导航无杂项、pjax 往返正常、外观抽屉无动效段、搜索无杂项条目、直达 /misc/ 被弹回首页、零控制台报错；校验全过（含步骤[4] 六个子页外壳比对 506 行）。已推送上线并线上验证：/misc 与 /misc/ 均 301 弹回首页、首页 HTML 无 miscBtn/ff-misc 残留、六个导航项齐全、settings flags 无 misc 键、_headers 首次入库即生效（assets 缓存头 max-age=3600 在线确认）

* **后台+工具（坑 5 / 坑 23 根治：相册落库 + 外壳一致性校验）**：①**建 albums 表根治「空相册不落库」**——此前相册列表完全由 media.album 派生，新建的空相册只存在后台页面的会话内存里，刷新即消失；现在 migrate.js 建 albums 表（name 主键 + sort_order 预留相册排序）并把存量 media.album 回填进去，`POST /api/admin/albums` 新增 create 动作、rename/delete 改为 albums 表与 media.album 的 batch 双写（rename 三语句防主键冲突：先改行名 → INSERT OR IGNORE 兜底 → 再改 media；目标名已存在返回 400），upload 与单图改相册（`PUT /api/admin/media/:id`）凡写相册名都同步 upsert albums 行，`GET /api/admin/media` 响应新增 albums 名单（顺带补上该接口一直缺的 ensureSchema），后台前端删掉 extraAlbums 会话补丁改为 serverAlbums 与派生名取并集展示。实测：界面新建空相册 → F5 刷新仍在 → 重命名 → 刷新仍在 → 解散消失；API 层 建/重名 400/移入/改名/撞名 400/解散/双写 全链路 curl 全过。②**check-code.mjs 新增步骤 [4] 外壳一致性比对**（坑 23 的「后续可加比对脚本」落地）——剔除 `<main>` 区间取外壳，归一化三处合法差异（data-page / `<title>` / 导航高亮的 active+aria-current）后多页逐行比对，漏同步当场报出错页与首个差异行号；实测注入一行漂移即报 ✗、还原后恢复全过。校验全过。已推送上线并线上验证

* **前台（AI 页头部上移 + 移除右侧新对话图标）**：接上条头部精简，用户继续要求"向上移动不要太多空，右边的图标移除"。①**移除头部右侧「新对话」图标**（aiNewChatBtn）——JS 绑定处本就有 `if (aiNewChatBtn)` 守卫可直接删；「新对话」入口保留在历史抽屉顶部（aiHistNewBtn），功能不丢。②**顶部留白 96→68px**：子页统一 96px 顶部留白在 AI 页过大（悬浮胶囊下沿 ~58px，之下近 40px 空档），AI 页单独收到 68px，`#aiView` 全高公式同步 `calc(100dvh - 68px - 24px)`；③**标题行上下内边距 14→8px**。②③的选择器都要越过子页通用规则 `html[data-page]:not([data-page=home]) …` 的 0-3-2 特异性——用 :not() 补平（同分靠源码顺序）或多一档类名。④**顺带揪出坑 28 的一个隐藏后遗症**：坑 28 把 #aiView 从 static 改 relative 后，老全屏层样式残留的 `top:52px; bottom:0` 开始生效（static 时被忽略），整页内容下坠 52px——relative 化必须把 top/bottom/left/right 一并归零 auto（已入坑 28 条目）。实测（390×844）：头部行顶到 68px、首条消息 197→145px、输入条正常贴底、无横向溢出、抽屉开合与新对话正常、桌面 1280px 布局一致；校验全过

* **前台（AI 页头部移除「AI 助手」标题）**：按用户要求去掉头顶的"AI 助手"四个字——album-bar 是 space-between 弹性布局，直接删掉中间的 `<p class="album-title">` 后历史抽屉按钮与新对话按钮仍各贴两端，分隔线与行高不变（行高由 36px 按钮撑起）；页面 `aria-label`、浏览器标签页标题「AI 助手 - YHuo」与搜索面板条目名保持不变（只去可见的头部文字）。实测：头部仅剩左右两个图标、无横向溢出；校验全过

* **前台（AI 页手机端对话气泡/输入条贴边）**：用户实报"需要对话框贴边"并附手机截图——AI 页气泡外侧留白是**双重叠加**：`.page-main`（手机端 4vw）+ `.ai-chat`（6vw）+ 消息区 2px，390px 宽下气泡离屏幕边 ~41px，长回复右侧还留一大块空底，观感"悬在半空"。修复（site.css 末尾 AI 页专属段，**仅 ≤767px 生效**）：`html[data-page="ai"] .page-main` 侧留白 4vw→8px、`.ai-chat` 6vw→2px，总留白收到 **~12px**，机器人/用户气泡与输入条都贴向屏幕两侧（顶栏 album-bar 分隔线同步变宽，整页一致）；桌面与平板（≥768px）的居中栏式布局不变。实测（390×844）：气泡左缘 41→12px、输入条 10px、顶栏 8px，`scrollWidth−innerWidth=0` 无横向溢出；校验全过

* **前台（悬浮播放器歌词联动：点歌后首页歌词条不再一直是待机文案）**：用户反馈"使用悬浮播放器，不显示歌词"——原设计歌词横条只联动迷你播放条，悬浮款（博客款）播放时歌词条永远停在待机（文档原注"属预期"，按用户反馈改为联动）。实现（common.js + blog-player.js 双侧）：①blog-player.js 曲目映射保留**原始文件名**（rawName，同名 .lrc 拉取要用）与**后台自带歌词文本**（lrc，原映射直接丢掉）；②common.js 暴露 `window.__lyricBarApi`（loadTrack/tick/setPlaying/setSource），悬浮款经它驱动同一根歌词条：切歌（含播完自动切下一首）载词、timeupdate 驱动打字机、playing/pause 切律动并补完当前句；③**歌词时间源抽象**（lyricTimeSource）：默认读迷你条 audio，悬浮款 playing 时注入自己的 {now,dur}、暂停后**保留**（暂停拖进度条歌词仍跟悬浮款位置），迷你条 play 事件收回——谁最后开播谁驱动歌词条，两款的 tick 事件天然错开不打架；④pjax 回首页时 lyricRebind 经新增的 `window.__blogPlayerTrack()` 优先按悬浮款当前曲目补载歌词（原来只会取迷你条 tracks[current]，悬浮款在播时会挂错歌）；⑤初始被动恢复（载入上次曲目但未开播）**不上报**歌词，歌词条保持待机；待机文案在悬浮款款式下改指"右下角悬浮播放器"（原"打开底部播放器"在迷你条隐藏时是死路）。实测（本地 blog 模式）：点歌开播歌词打字机跟随悬浮款进度（0 秒行→"词：周杰伦"推进）、暂停律动熄灭且当前句补完、切歌换词、pjax 去留言页再回首页歌词条按悬浮款 72→75 秒处恢复并继续滚动；mini 模式回归正常（迷你条驱动照旧）；校验全过

* **前台（修：窄屏下流星只出现在屏幕一小块区域）**：用户实报"窄屏状态下流星效果只有一小部分，需要全屏都有"。几何根因：流星沿 `rotate(215deg) translate(-180vh)` 向右下 35° 斜落，屏幕空间水平行程**恒为 1.474×视口高、与屏宽无关**（竖直行程 1.033×视口高，出生点 top:-4rem）——而出生横坐标 `--left` 是**相对屏宽**的百分比。宽屏（1280×720）下水平行程 ≈0.83 屏宽，固定出生带 -80%~+100% 恰好让路径扫遍全屏（当日早间"左下角修复"正是在这个前提下成立）；**窄屏（390×844）下水平行程高达 3.2 倍屏宽**，同一个出生带只能命中上半段对角走廊：390×844 十秒采样（每 500ms 读 computed transform 矩阵换算屏幕位置、按九宫格计数）显示上排 20/40/40、中排 0/0/20、**下排 0/0/0**——底部三分之一整块从没有流星，中部大半也没有。修复（common.js 流星段）：①**出生带按视口几何自适应**——birthLeft() 反推最左出生 `minPct = -((0.95×innerHeight+64)×(1.474/1.033)/innerWidth)×100`（让路径恰好从左缘 95% 高度处进入屏幕；64=top:-4rem 偏移），右端 +100% 保持不变，宽屏下算出 ≈-83%~+100% 与旧值几乎一致（桌面观感不变），窄屏自动延展到 ≈-317%；②**resize/旋转屏 200ms 防抖重排**全部出生坐标（横行程÷屏宽比例随窗口变化）；③触屏窄屏档流星数 10→15（修复后每颗的路径都真正进屏，适度补密度）。修后实测：390×844 九宫格九区全覆盖（19/10/26 | 14/10/18 | 29/17/12），总可见采样次数 120→155；宽屏 1280×720 九区同样覆盖无退化；resize 后出生带按新视口重算（390→1100 宽时 -315%→-107% 起）。校验全过。注：ⅠAB 的 setViewportSize 不派发 resize 事件（真实设备旋转/窗口拖拽会触发），重排逻辑用手动 dispatch 验证

* **前台（修：播放音乐后切换界面，越切越多路音乐同时播）**：用户实报"首页播放音乐后切换界面，原来的音乐不停止（预期内），但还会重新播放音乐，两个音频同时响；再多进一个界面又多一路"。排查先排除了 pjax 本身——注入 play() 监听后逐页切换（工具/AI/杂项/留言/返回首页）全程零新建音频、零新播放调用，外壳的 `new Audio()` 只有一份。真正根因在**当天刚上的顶栏悬停预览**：预览卡的实时缩略图是按路径常驻缓存的本站整页 iframe（同源，与主页面**共享 localStorage**），每个 iframe 都会完整执行 common.js 的"恢复上次播放"流程——主页面音乐播放中时 `musicMeta.playing=true`，于是**悬停过的每个导航项都会让自己的预览帧自动续播一整份曲库**，且帧常驻不销毁，音频逐路累积。本地实测复现：悬停 5 个导航项 = 5 个 iframe 全部在放 + 主页面共 6 路音频。修复：common.js 读 `musicMeta` 初始化 pendingMeta 时检测 `window.self !== window.top`（预览帧场景）**整份丢弃恢复数据**——预览帧不再自动续播，顺带避免缩略图偷跑下载整首歌；主页面自身的续播行为不受影响。浏览器实测：主页播放中依次悬停 工具/AI/留言/课表/杂项，5 个预览帧全部静音且缩略图照常渲染（is-ready 正常）；pjax 切页音乐继续、返回首页歌词条实时走句；主页整页加载自动续播照旧；校验全过（坑 29 入文档）。线上验证（推送后）：线上曲库播放中悬停 AI/留言/课表/预览，4 个预览帧全部静音、缩略图正常渲染

* **前台（修：手机访问 AI 界面的三处 UI 问题）**：用户实报手机端 AI 界面 UI 异常，手机视口（390/320px）实测定位三处并修复：①**整页可滚、输入条被滚走**——页脚跟在全高聊天区下面把页面撑出 100+px，与消息区内部滚动形成双滚混乱；修复：AI 页锁视口（`html[data-page=ai] body` overflow:hidden + 100dvh）、页脚不参与，应用式全高布局桌面手机统一，pjax 离开即恢复。②**历史抽屉被顶栏胶囊压住**——抽屉顶部「新对话」按钮点不到；根因是**坑 28**：pageFade 入场动画（fill:both）让 #aiView 在 Chromium 里持续构成层叠上下文，抽屉的 z60 被关进容器的 z:auto 里，整体画在顶栏 z50 之下；修复：AI 页把 #aiView 自身抬到 z60（容器盒从 y96 起与顶栏不重叠，无副作用），抽屉/遮罩模态化罩住含顶栏的全屏。③**iOS 聚焦缩放**——输入框 14px 触发 Safari 强制放大视口，触屏设备提到 16px。浏览器实测（390/320/900px 三档）：滚动锁定、抽屉按钮可命中、遮罩点击关闭、pjax 往返、窄屏无横向溢出；校验全过

* **前台（修：流星雨左下角整块没有流星）**：用户实报"流星雨不是全屏都有，左下角就没有"。几何原因：流星是出生在顶边、向右下方 35° 斜落的（rotate(215deg) + translate(-180vh)），经过某点的前提是"出生横坐标 = 该点横坐标 − 下落水平分量"，左下角对应的出生位置要在 -60% 以远——而出生范围 `--left` 只取 -35%~+90%（示例站原样），左下角天然整块空白。修复：出生范围左移扩为 **-80%~+100%**。浏览器定量实测（10 秒每 500ms 采样可见流星所在象限）：左下从 0 → 87 个采样点（四象限最高之一），其余象限 48~91 覆盖均匀；校验全过

* **前台（AI 页隐藏播放条，音乐后台续播）**：按用户要求，AI 页不再显示底部播放条——迷你播放条（#miniPlayer）与悬浮播放器款式（.music-player）在 html[data-page="ai"] 下都 display:none，聊天输入条独占底部；**音乐本身不打断**：pjax 只换 `<main>`、外壳的 audio 不参与换页（本来就不会停），隐藏纯视觉——切到 AI 页歌继续放（迷你条 playing 状态类仍在），切回首页播放条与歌词原样恢复。顺带修一个同族小尾巴：lyricRebind 重绑出的新歌词条没有旧的 playing 律动态（播放中回首页律动条静止），现在按 audio.paused 实况补上。浏览器实测：播放中切 AI 页（条隐藏、音乐续播）→ 切回首页（条恢复、律动/歌词实时走句）；校验全过

* **前台（修：子页强刷后 pjax 回首页歌词不显示 + 顺手修静态曲目 .lrc 拉取失效）**：用户实报"非主页强刷后再点主页，主页歌词不显示"。根因两层：①子页整页加载时 `#lyricBar` 不存在，播放器恢复上次播放（applyResume/restoreCachedTracks）调 musicLyricsLoad 会**重置歌词状态后早退**——歌词数据从此是空的；pjax 回首页时 lyricRebind 见 audio.src 已恢复走了"播放中"分支，musicLyricsTick 又因没歌词数据静默早退 → 歌词条可见但**完全空白**。修复：lyricRebind 发现"有播放源但没歌词数据"时**补载当前曲目歌词**（tracks[current] 的 media.lrc / 同名 .lrc / 歌名占位三档照旧）。②顺带发现更老的静默回归：musicLyricsLoad 里 `!/music\//.test(src)` 的守卫在曲目 src 改为 blob URL（曲库缓存重构）后永远命中，**静态 music/ 曲目的同名 .lrc 拉取一直没真正执行过**（后台 KV 曲库的 media.lrc 不走这条所以平时正常）——守卫改为按歌名一律尝试同名 .lrc，404 自然回落歌名占位（与文档记载的歌词来源优先级一致）。排查利器：runPageHook 的吞错 catch 临时加 `window.__pageErrors` 错误面包屑（坑 24 教训的落地，已保留）。浏览器实测：子页强刷→回首页，歌词条正确恢复「晴天」30 秒处的当前句「故事的小黄花」（打字机态）；清掉播放历史后待机文案正常；pjax 往返状态延续；校验全过

* **前台（顶栏预览卡升级：站内页面显示实时缩略图）**：接用户要求，顶栏导航项的预览卡从纯文字说明升级为**实际界面内容**——视觉区放一个 1280 宽渲染、CSS `scale(0.25)` 缩进 320×112 的**实时 iframe 缩略图**，悬停即见目标页面真实渲染（导航、卡片、时钟都在 miniature 里）；加载完成前显示主题色兜底当 loading 态。iframe 按路径缓存常驻（首次悬停加载 1~2 秒，之后秒出），显隐用 `is-current` 类切 display——**不动 DOM 就不触发 iframe 重载**；切到站外链接（GitHub/B站演示卡）时全部藏掉防叠影，`pointer-events:none` 纯展示不可交互。**踩坑**：初版所有缩略帧常驻叠放且不管理层级，切回旧页时新页的帧压在上面、外链卡上也残留站内缩略图——用 is-current 显隐管理根治。浏览器实测：首悬停出真实缩略图、切页回切立即秒开且内容正确、外链卡零叠影；校验全过

* **前台（顶栏导航项悬停也弹预览卡）**：接用户要求，链接预览模块扩展到顶栏导航——悬停 首页/工具/AI/杂项/留言/课表/预览/关于 任一项 150ms 后弹出同款卡片，内容换成**页面说明**（视觉区「站内页面」+ 主题色底，meta 区「YHuo · 页面名」/ 导航文字 / 各页功能简述）；页面说明表 `NAV_INFO` 在 common.js 预览模块内（键 = 去尾斜杠 pathname，加页面改那里）。其余站内链接（页脚、正文等）照旧不弹，外链白名单与 /blog/ 演示页不受影响。**踩坑**：NAV_INFO 键初版带尾斜杠、与 resolveLink 归一化后的 pathname（`/tools/`→`/tools`）对不上，除「首页」外全部静默不弹——事件序列正常但查表落空，用捕获阶段事件计数器定位后统一键格式。浏览器实测：八项全部出卡、页脚站内链接不弹；校验全过

* **前台（顶栏「博客」外链改为「预览」演示页 /blog/，第八页）**：接上条，按用户要求改成独立界面演示——顶栏项从外链改回**站内真链接** `/blog/`（id blogBtn，导航文字改名「预览」，八页外壳同步，坑 23），页面内容对齐 `博客动画源码/demo.html` 的链接预览演示区：一张卡片里放**两个示例外链**（GitHub 上的 clay-blog 仓库 / 哔哩哔哩），悬停即出预览卡——GitHub 链接带仓库 opengraph 预览图与「laogou717 / clay-blog」上下文，B站走粉色视觉+台标，与示例站效果一致；复用全站卡片样式（album-bar / apple-card / card-eyebrow）。中途曾按"独立界面"反馈把 /blog/ 做成 iframe 内嵌参考博客站（对方站无 X-Frame-Options 禁嵌头、实测可嵌），最终确认要的是演示链接，iframe 方案退役、路由与第八页外壳保留。common.js：PAGE_ROUTE/PAGE_TITLES/搜索快跳加 blog 项（搜索词「链接预览演示」）；check-code.mjs 页面清单扩为八个。浏览器实测：两个示例链接悬停出卡、pjax 往返/返回键/直达刷新高亮正确；校验全过

* **前台（顶栏新增「博客」外链 + 链接预览卡片，参考博客站）**：顶栏在「课表」与「关于」之间新增「博客」链接（指向特效参考的博客站 yyyyyolo7a79-blog.pages.dev，**新标签打开**，带 ↗ 外链标识；七个页面同步，坑 23；插在「关于」前保持其最右位置）。同时把参考博客站的**链接预览卡片**特效接入本站（源码 博客动画源码/06，样式/脚本精简改写）：悬停白名单站点的外链 150ms 后弹出小卡片（站点名 + 链接文字 + 描述 + 上下文标识，GitHub 链接另取 opengraph 预览图、失败回落图标），跟随鼠标、贴边自动钳制，滚动/滚轮/触屏滑动/失焦即隐藏；白名单外（含全部站内链接）一律不弹，卡片不出现原始 URL。白名单：博客站 / GitHub / 哔哩哔哩（common.js 尾部 SITE_INFO，加站点改那里）。**实现要点**：卡片 DOM 由 common.js 尾部 IIFE 注入一次（外壳跨 pjax 不换，免七页同步），document 级监听整站常驻、pjax 换页后照常工作；颜色/阴影/缓动全映射站点语义变量（--card/--border/--primary/--spring 等），深浅主题与主题色自动跟随；仅「可悬停+精确指针」设备启用，`prefers-reduced-motion` 时去掉入场动画。**顺手修了原示例就存在的贴边缺陷**：钳制计算改用 `offsetWidth/offsetHeight`——原 `getBoundingClientRect` 在入场动画 scale(0.92) 期间量到缩小尺寸，动画放完卡片右/下缘会溢出屏幕十几 px。本地浏览器实测：悬停顶栏「博客」出卡（陶土色视觉+「博客首页」上下文）、临时 GitHub 外链出 opengraph 预览图（深浅主题各验一遍）、右下角贴边钳制进视口、pjax 换页后悬停仍出卡、滚动即隐藏；校验全过

* **前台（「文档」界面更名「关于」）**：顶栏与页面身份 文档→关于（导航文字、页面标题「关于 - YHuo」、界面标题「关于 · ABOUT」、搜索入口与结果分组标签同步），**位置移到导航最右**（首页 | 工具 | AI | 杂项 | 留言 | 课表 | 关于），路由 /docs/、id docsBtn、功能开关、docs.json 内容机制全部不动——页内仍是《关于本站》《更新日志》两张卡，点开阅读层不变（阅读层标题栏保留「文档」文案，属通用阅读器）。七页外壳同步（坑 23）

* **前台（课表新增「清空课程」）**：用户反馈"加入可以移除导入的课程"。课表页工具栏新增危险色「清空课程」按钮——移除全部课程（含 WakeUp 导入的）但**保留课表本体与作息、提醒设置**（区别于「移除课表」的连设置一起删），导完课表想重排/删掉部分导入课程时不必删掉整个课表重来。确认交互沿用 AI 对话删除同款**两段式确认**（首点变红显示「确认清空 N 门？」，2.5 秒内再点才执行，点别处/超时自动解除）——期间发现前台根本没有后台那套 ask() 组件，「移除课表」一直走的是原生 confirm，文档描述有误已更正。本地实测：添加 4 门课程→清空→网格归零、提示「已清空全部课程（4 门）」、服务端 courses=0 且作息（12 节）/每日早报/提前量全保留、课表 exists 不变；校验全过

* **前台（课表从毛玻璃浮层改回真实页面 /schedule/，第七页）**：接上条——用户进一步反馈"界面能不能改成其他界面一样不要那个白色浮层了"。照办：顶栏「课表」从 button 改回**真链接**（指向新建的第七个真实页面 `/schedule/`，七页外壳同步，坑 23 扩为七处），`#schedView` 浮层与全部浮层期配套（流星抬层/顶缝磨砂带对课表的部分保留给个人主页）退役；课表卡与课程编辑弹窗 markup 移入课表页 `<main>`（随 pjax 装卸）。**common.js 手术**：约 700 行课表代码（元素查询/绑定/渲染/保存）整体封装进 `initSchedPage()` 注册为 PAGE_MODULES.schedule（pjax 每次进出重查重绑）；document 级监听（编辑器 Esc 捕获处理器、周视图拖选 mouseup——后者原本每次渲染就叠一个）改为具名 + `schedPageCleanup` 离页摘除；新增 `onGatePassedPageHook` 机制（passGate 末尾触发）：未登录进课表页弹登录卡、页面本体留白，登录/过门后自动拉取课表，不必刷新。路由表/标题表/全站搜索快跳加课表项；check-code.mjs 页面清单扩为七个。**本地测试踩坑记录**：验证游客路径时先后被 两层会话骗过——`yhuo_user` cookie 未清（UI 登出点击没真正落到按钮）导致 /api/user/me 静默恢复登录态，后又发现浏览器还带着早前后台登录的 `yhuo_session` 管理员 cookie（me 接口识别管理员也回登录态，且 /api/schedule 本就接受管理员课表）——两个 cookie 都登出才是真游客。wrangler 全场景实测：游客直达/导航进课表页弹登录卡、登录后钩子自动加载、完整视图/启用视图正确、编辑弹窗 Esc 分层、pjax 往返+返回键+反复装卸无监听叠加、导航高亮随页切换；校验全过

* **前台（修：自定义背景下课表浮层顶部与下方判若两界）**：用户反馈"设置自定义背景的情况下，课表界面的最上方和下面的显示差异很大"。本地注入高对比背景复现实锤：浮层从 52px 起铺毛玻璃，顶部 0~52px 那条（导航后面）露出的是**原图原样**（饱和橙红、清晰的图形），52px 以下全被磨砂洗成淡色——一条贯穿全宽的生硬分界线。修复：`body:has(.profile-view.show) .site-header::before` 给头部透明壳补一条**与浮层同参数的磨砂带**（高 52px、65% 底色+blur14+saturate1.2），与浮层连成整面；头部 z50 < 浮层 150，带子只在顶条露出、正好补齐缺口；胶囊/阅读进度条在头部内绘制于带子之上不受影响，壳本身 pointer-events:none 不挡点击；浮层关闭随 `:has(.profile-view.show)` 失效自动消失。与上一条"流星抬层"同一触发条件，两者共组"浮层打开时的整套环境融合"。本地注入背景实测：接缝消失、顶条与下方连续磨砂、关闭后带子消失顶条恢复原图。（注：该浮层套修复现仅个人主页在用；课表已改真实页面，不存在此问题）

* **前台（修：课表浮层盖住流星，观感成"独立实心背景"）**：用户反馈课表浮层打开后看不到流星。两处修复：①`.profile-view` 外壳（个人主页/课表共用）毛玻璃从 88% 不透明+blur20 降到 **65%+blur14+saturate(1.2)**——原先太高把身后内容全部闷掉；②关键一手：流星层在 z 负层（内容后面），隔着玻璃只剩模糊影子，所以加 `body:has(.profile-view.show) .fx-meteors { z-index: 155 }`——**浮层打开时把流星抬到玻璃之上**（150<155<外观抽屉/头像下拉 160，头部胶囊 z50 与浮层不重叠），关闭自动回落 z:-1，日常"流星在内容后面"的设计不变，个人主页同外壳自动获得同款效果。本地实测：浮层打开时流星层 z=155、流星清晰划过课表界面、卡片可读性完好；关闭后 z=-1 回落；个人主页打开同样生效

* **前台（课表界面搬到顶栏，直达）**：顶栏导航新增「课表」按钮（六页外壳同步，坑 23）——点击直接打开课表界面浮层 `#schedView`，不用再绕 头像 → 我的主页 → 课表卡。实现：原个人主页里的课表卡（`#schedCard`，含周视图/WakeUp 导入导出/提醒设置/作息时间）**整体抽出**为独立全屏浮层（复用 `.profile-view` 外壳样式与开合动效，个人主页里不再有课表卡）；`openSchedView()` 未登录先弹登录卡（与收藏 ♥ 同口径），与个人主页/阅读层互斥，pjax 换页经 `closeAllTransientOverlays` 自动收起，Esc 分层（课程编辑弹窗开着时其捕获阶段处理器 stopImmediatePropagation，先只关编辑弹窗再关浮层）；顶栏「课表」是唯一的 button 型导航项（`button.nav-link` 在 site.css 加复位补丁，外观与链接项完全一致）。wrangler 本地全场景实测：游客点课表弹登录卡、登录后直达浮层并正确显示启用入口/完整视图、启用课表、课程编辑弹窗 Esc 分层、pjax 换页自动收起、个人主页无课表卡残留；校验全过

* **后台（移除访问 IP 记录 + UI 动效升级，参考 yyyyyolo7a79 博客站）**：①**「最近访问」IP 明细功能整体移除**（用户要求）——`/api/visit` 不再记录 IP/UA/访问明细（访问总量与按天计数保留，后台趋势图不受影响），`/api/admin/visit-logs`、`/api/admin/geoip` 接口连同文件删除，概览页「最近访问」卡片/样式/JS 摘除（fmtLogTime 保留给邮件发送明细、geo-toggle 滑动开关样式保留给外观页功能开关复用），migrate.js 不再建 visit_logs 表——线上残留旧表如需清数据可在 D1 控制台执行 `DROP TABLE IF EXISTS visit_logs;`。②**后台动效对齐参考站缓动体系**——动效令牌入 `:root`（`--ease-soft`/`--ease-outc`/`--ease-spring`/`--t-fast|med|reveal`）；页面切换改「模糊揭示」入场（上浮+blur 收焦，面板内卡片按 switchPage 写入的 `--stagger-i` 级联 55ms；fill 只用默认/backwards，防坑 22 的 fixed 包含块问题）；侧栏新增**滑动胶囊指示器**（参考站导航胶囊指示器竖排移植，moveSideIndicator 量 active 按钮对齐，`:has(#sideInd)` 兜底防选中项无底色）；主题切换加**圆形揭示**（View Transitions + element.animate 的 clip-path circle，半径按归一化对角线换算百分比，Firefox/减弱动态/无 VT 回退即时切换，themeVTBusy 锁防连点）；概览统计数字滚动（countUp，仅首次从 0 填充时播）；卡片悬浮轻抬+边框加深（hover:hover 才启用）、按钮 ：active 弹簧缩放、弹窗弹簧入场+blur、列表/吐司换弹簧曲线、细滚动条随主题。wrangler 本地全场景实测：IP 卡消失、指示器在展开/收起/收起态切页/窄屏抽屉全对齐、圆形揭示真实执行（埋点 startViewTransition 验证）、预览弹窗开关正常；校验全过

* **前台（修：进入其他界面后点「首页」回不去）**：用户实报"进入其他界面后点击首页好像回不去首页"，线上复现实锤。根因有两层：①pjax 只换 `<main>` 不换外壳，**导航的"形状"永远停留在最初加载的那个页面**——从首页出发 pjax 到其他页后，「首页」仍是首页外壳的 `#home` 锚点形态（直达子页时它是真链接 `href="/"`，反而正常，所以只有"从首页走进其他界面"才复现，隐蔽性强）；②锚点平滑滚动处理器**先无条件 `preventDefault()` 再找 `#home` 元素**——其他页面内容里没有 `#home`，点击被吞成毫无反应，pjax 拦截器见 `e.defaultPrevented` 也跳过（logo 原先同为 `#home`，在子页面一样点不动）。修复：①六页标记统一（坑 23 六处同步）——「首页」一律 `<a href="/" class="nav-link" data-target="home">`（仅首页那份带 active/aria-current），logo 一律 `href="/"`；②common.js 的 sectionLinks 处理器改为**先找目标、找不到不吞事件**（放行给 pjax）。场景矩阵实测全过：首页→pjax 留言→点首页回首页（高亮正确、时钟模块重绑后正常走动）、直达子页点首页回首页、logo 从文档页回家、首页内点首页平滑滚顶且 URL 不变、返回键正常、直达子页静态高亮不受影响。教训进交接文档坑 26：跨页共用的外壳里，凡"假设自己在某个页面"的锚点形态链接都是 pjax 定时炸弹——导航一律写真链接，行为差异交给 JS 按当前页面判定；pjax 验证必须覆盖"从首页出发"与"直达子页"两种外壳形态

* **前台（修：900~1199px 窗口宽度下时钟秒数被视频遮挡）**：用户实报"一定窗口比例下视频会盖住时间的秒数"。根因是响应式断点夹缝——900~1199px 段 `.page-main` 有专属规则收窄到 860px，但该段里首页 hero 行的视频盒仍固定 `flex: 0 0 320px` 占右侧、时钟字号仍是 `clamp(3rem, 9vw, 6rem)`（该段最大 96px），时钟容器 `.clock-hud` 又是 `overflow:hidden`，整段必溢出且溢出方向正对视频盒：线上逐宽度实测 901~1199px 全段复现（901px 起秒位开始缺角、1040px 起秒位整位消失；≤900px 视频换行到时钟下方、≥1200px 主容器加宽，两段边界外都正常）。修复：该段媒体查询内视频盒收窄 320→260px、`.clock-hud` 左右 padding 34→24px、时钟字号降一档 `clamp(3rem, 7.5vw, 68px)`、毫秒降为 `clamp(1rem, 2vw, 1.25rem)` + `min-width:3em`；实测该段全宽溢出转负（−31~−48px，秒位完整可见），≥1200px 恢复原字号不受影响。教训进交接文档坑 25：给某段窗口宽度单独收窄容器时，段内所有"固定宽度 + vw 大字号"的并排元素必须一起复核，验证要逐宽度扫描量 `scrollWidth − clientWidth`，不能只看首尾两端

* **前台（线上热修：首页时钟/视频/寄语/天气全部失效）**：多页面改造手术时把 `updateClock` 函数定义连同旧启动代码一起删掉，但新的 `startHomeClock` 仍在调用——ReferenceError 被 `runPageHook` 的 try/catch 静默吞掉，首页四大模块全部静默失效（线上实报"时钟不显示、视频不播放"；毫秒停在 .000、问候语停在标记初始值）。补回原函数体后线上复验：时钟秒位真实走动、视频拿到播放源、问候语按时段更新、天气胶囊正常、零报错。教训进交接文档坑 24：吞错模块改动后逐函数核对定义存在；验证页面行为用真实状态采样（秒位对比），别用"元素存在"当判据。本轮推送直连被重置，走系统代理端口推送（推完 unset）

* **前台（架构改造：拆多页面站点 + pjax 跨页不断播音乐）**：参考多页博客站形态，把单文件 index.html（13832 行全内联）拆成**六个真实 HTML 页面**——`/`（首页）、`/tools/`（工具）、`/docs/`（文档）、`/ai/`（AI 助手）、`/misc/`（杂项画廊）、`/board/`（留言板），各栏目内容原为全屏毛玻璃层（fixed + .show 显隐），现改为常规文档流页面（去关闭按钮/去全屏壳，内容排在悬浮胶囊导航下方随页滚动）。**共享外壳**：样式提取 `assets/site.css`、全站脚本提取 `assets/common.js`（流星特效并入）、悬浮播放器提取 `assets/blog-player.js`；头部胶囊导航改**真链接**（当前页静态高亮，保留 #toolsBtn 等 id 使功能开关 ff-* 隐藏规则继续生效），登录卡/个人主页/全站搜索/外观抽屉/docViewer/两个播放器等浮层**六个页面共用**（改外壳要六处同步，check-code.mjs 已扩展为校验全部六页）。**pjax 无缝换页**：站内导航被拦截 → fetch 目标页 → DOMParser 只替换 `<main>`（头部/播放器/浮层/页脚在外壳里不动）→ **音乐跨页不断播**（参考站同款机制）；后退/前进走 popstate 同套替换并恢复滚动位置；fetch 失败/禁 JS 整页加载兜底；外链//admin//api//media/新窗口不拦截；`/tools` 无斜杠由 Cloudflare Pages 与本地服务器自动 308 到 `/tools/`；旧 `#/tools` hash 链接启动时自动归一。**页面模块化**：原主 IIFE 里按页初始化的模块改为 init/teardown 注册制（首页时钟/寄语/视频/天气/歌词条重绑、工具七件套、画廊、AI、留言板按页装卸，pjax 换页旧页 teardown 新页 init，AI 离页断流 + document 监听防叠加）；全屏层管理器（FS_TOP/fsOpen/fsClose/垫底覆盖）与路径路由器（FS_ROUTE/popstate 深链接）整体移除；功能开关改**按页守卫**（后台关掉某栏目后访问/停留该页整页回首页）；pjax 换页按真实路径补报 /api/visit。**顺修三个移除相册时引入的现存 bug**：画廊加载调用（startGalleryIfAllowed/fetch/imgDone）被整体误删导致画廊完全不出图（恢复加载链，实测 8 张图入牌堆）；三处 Esc 处理器仍解引用已不存在的 lightbox（个人主页/docViewer 的 Esc 按下即静默 TypeError）；非杂项页外观开关触发 resetCardMouse 踩 undefined（画廊状态变量提升到 IIFE 作用域）。**改造中修的新坑**：①`data-page` 标在 `<html>` 上，页面化 CSS 选择器必须写 `html[data-page=…]` 而非 body；②页面容器入场动画不能带 transform（fadeInUp 配 fill:both 持续占位会把 position:fixed 的 AI 历史抽屉包含块改成视图自身，抽屉被顶出屏幕左缘），改只做透明度的 pageFade；③`.ai-chat` 入场态原由 .ai-view.show 门控，独立页直接放行；④**子页面相对路径坑**：`images/…`/`music/…`/`video/…` 等相对路径在 `/misc/` 等深度会解析成子路径 404，全部改根相对（/images/…）。wrangler 全链路实测：六页 200/308、跨页导航音乐持续播放（audio 206 流式不断）、返回键首页重建、刷新直达、#/tools 归一、搜索跨页跳留言板与 /tools/#toolCalc 定位、文档原地阅读、留言板身份/等级徽标/管理员删除、AI 页布局、深浅主题、头像→我的主页浮层；校验脚本全过

## 2026-09-05

* **前台（修：界面里点顶栏「首页」无反应）**：路径路由上线时漏掉的真 bug——顶栏「首页」锚点的平滑滚动处理器一律 `preventDefault`，URL hash 永不变动，路由器的"锚点回首页"分支（popstate）从不触发；于是全屏界面里点「首页」只是去滚动被界面盖住的底层页面，肉眼毫无反应（点其他界面正常，观感即"切界面和回主页不一样"）。修复：全屏界面开着或路径带路由时放行默认锚点行为（URL 变 /docs#home → popstate → 关所有层 + 路径清回 /），仅首页正常浏览时才接管为平滑滚动（preventDefault 阻止 hash 污染）。实测：/docs 里真实点击「首页」→ 层关闭/URL 回 / /标题与高亮恢复；首页正常浏览点「首页」仍平滑滚回顶部且 URL 不带 #home

* **前台（移除相册界面，代码完整备份）**：顶栏"相册"入口、相册界面（牌堆封面/照片墙/灯箱三层结构）及全部 CSS/JS 从 index.html 摘除，个人主页收藏卡的照片区一并移除（音乐收藏保留）——灯箱与收藏照片依赖相册，属于同一功能闭环。同步清理：FS_TOP/FS_ROUTE/FS_TITLES/FULLSCREEN_CLOSE_IDS 的 albumView 条目、功能开关的 album 键（后台 FLAG_DEFS、服务端 FLAG_KEYS、/api/settings flags）、全站搜索的相册快跳与图片/相册组、画廊照片点击进相册的跳转（画廊改纯展示）、图片加载门控改为只看杂项开关。**代码完整备份在 `相册功能备份/`**（CSS/DOM/JS 分文件 + 《恢复说明.md》列出 11 处需同步还原的零散修改点），本地目录不入库。**保留未动**：后台「图片」页相册分组侧栏、media.album 数据、user_favorites 照片收藏数据、/api/admin/albums 接口。验证：校验全过、无 albumView 等功能残留引用；导航（首页/工具/文档/AI/杂项/留言）、杂项画廊、全站搜索、收藏音乐均正常

* **前台+后台（双播放器：悬浮播放器可选用）**：把参考博客站的音乐播放器（悬浮球 + 点击 morph 展开面板）完整接入本站，**原迷你播放条保留**，后台「外观」页新增「底部音乐播放器样式」二选一（缺省迷你播放条，行为不变）；**播放列表与迷你播放条同一份站内曲库**（后台 KV 曲库 + music/ 静态目录合并去重，经主脚本暴露的 `window.__siteMusicTracks` 快照读取，不接网易云等外部 API；歌名按「歌名 - 歌手」自动拆分展示；本地文件夹 blob 曲目同样可播）。实现：播放器 CSS/DOM/JS 从 `博客动画源码/`（08/09 + fx CSS 音乐段 ~33KB）内联进 index.html，CSS 的原站 token（--brand/--surface/--nav-pill-bg 等）整体映射到本站语义变量并限定在播放器作用域（主按钮自动变陶土色）；JS 去掉 00 设置核心与 Astro 路由依赖，改为 `window.__initBlogPlayer()` 由设置回调按需拉起（DOM 常驻但 display:none，不切到它就不请求网易云）。切换链路：site_settings `player_config`（{mode,neteaseId,apiBase}）→ /api/settings 下发 playerMode/playerPlaylistId/playerApiBase → applyPlayerMode 挂 `using-blog-player` 类反向显隐（`html.using-blog-player #miniPlayer{display:none}`）+ 初始化；localStorage `yhuoPlayerMode` 供 head 内联脚本首屏预切防闪现，接口失败 5 秒兜底拉起防两个播放器都不出现。改造中修了两个真 bug：loadPlaylist 改造时漏建 AbortController（超时逻辑空指针直接进失败分支）、曲目字段映射错（迷你条的曲目字段是 src 不是 url，全被过滤成空歌单）。wrangler 全链路实测：API 往返（默认 mini→PUT blog→settings 下发→前台球显示/迷你条隐藏→面板列出站内曲库并正确拆分歌名歌手→播放站内歌曲 currentTime 走动→PUT mini→刷新恢复迷你条）、后台 UI（选中态切换）；校验全过。**补丁：切换后立刻刷新不生效的观感 bug**——/api/settings 带 `Cache-Control: max-age=60`，保存后 1 分钟内的刷新会拿到浏览器缓存的旧配置（播放器款式、视频模式等全部受影响，实测复现）；前台 4 处 /api/settings 请求统一加时间戳参数穿透，保存后下次进页面立即生效（接口缓存头保留，其他调用方不受影响）

* **前台（接入参考博客站流星特效）**：首页新增全屏流星层（源码取自用户提供的 `博客动画源码/03-流星-meteors.js` + fx CSS，本地参考目录不入库）——桌面 30 颗、触屏窄屏自动降 10 颗、`prefers-reduced-motion` 不渲染（JS 计 0 + CSS media 双保险），每颗随机位置/时长/负 delay（首帧即不同相位，不会集体从起点等起）+ 渐隐尾巴；浅色主题也显示（原站仅暗色，按其 demo 的双主题可见方案），主题切换 View Transitions 快照期间隐藏防半截流星入快照。**配套层级改造**：底色+顶部光晕从 body 挪到 html（画布层）、body 常透明——body 的不透明盒背景会盖住 z-index:-1 的流星层（上次光晕坑的同款机制）；`.bg-layer`（自定义背景）加 `display:none` 门控、`body.has-custom-bg` 才显示——body 透明后默认态 25% 压暗层（bgDim）会漏出来把整页压灰，此前靠 body 不透明底色无意间盖住。三场景实测：浅色（暖白底+光晕+流星）、暗色（流星最出彩）、自定义背景（图+压暗+流星共存）；校验脚本全过

* **前台（默认白色主题对齐参考博客站配色）**：默认浅色主题从纯白 + iOS 蓝改为参考站（yyyyolo7a79 的博客）同款**暖白纸感配色**——页面底色 `#f8f7f4`（暖白）、卡片/弹层纯白、正文 `#1b1c1e`（炭黑）、次级面 `#f0ede6`、边框 `#e6e3da`、次要文字 `#5f6166`；**默认主题色（强调色）从蓝色换成参考站品牌陶土色 `#b0532b`**（brand-50~900 全套色阶重做，链接/主按钮/选中态/播放器圆盘等全站强调色随之变为陶土色）。外观抽屉主题色五选一的首项同步改为「陶土」，后台「外观」页默认主题色列表同步（服务端 accent 白名单 blue→terracotta）；旧存档兼容：localStorage 或后台存过 `blue` 的自动归一到陶土色（applyAccent 统一归一，无残留蓝色）。暗色主题不受影响（浅色语义变量只在 :root 覆盖，.dark 全量重定义）。**补：顶部暖橙光晕**——对比参考站截图发现光看底色数值一致仍"偏冷"，参考站页面顶部有一层暖橙 radial 光晕（body::before fixed 540px 高，亮色 #cc7442/16% 起、暗色 #e7a37c/12% 起往下渐隐）才是其"暖白纸感"的另一来源；本站不能照抄 body::before z-index:-1（html 自带背景，body 的不透明底色属于普通盒背景绘制在负 z 伪元素之上会把光晕盖没——首版即踩此坑，像素采样验证光晕几乎不可见），改为**光晕做成 body 背景的第一层**（radial-gradient + `background-attachment: fixed` + size 100% 540px），底色第二层，暗色 .dark body 同构覆盖，`body.has-custom-bg` 的 background:transparent 整体覆盖天然去光晕。修后与参考站同点位像素采样逐位一致（±2 以内）。本地浏览器实测：校验脚本全过

* **前台（路径路由：每个界面一个真实地址）**：参考用户的多页博客站形态，把原 hash 路由（`#/tools`）升级为**真实路径路由**——六个顶栏界面各有一个干净地址（`/tools`、`/docs`、`/ai`、`/album`、`/misc`、`/board`），地址栏直接体现所在界面，与多页站点观感一致；**页面标题联动**（如 `/tools` 显示"工具合集 - YHuo"，关闭/返回恢复）。行为全部保留：刷新/分享直达、浏览器前进/后退、✕/Esc 静默清路由（replaceState 不产生历史）、`#home` 等锚点视为回首页（`/tools` 下点首页变成 `/tools#home`，路由器识别为锚点回首页：关层 + 路径清回 `/`，修了这个边界）；旧链接 `#/tools` 自动归一化成 `/tools`（boot 与路由器双入口兼容）；全站搜索等旁路入口统一走 `fsNavigate`。实现：依赖 Cloudflare Pages 的 SPA 回退（坑 2）——未知路径返回首页 200，脚本按 `location.pathname` 拉起界面；导航点击由 fsOpen `history.pushState` 写地址（不触发 popstate 不重复处理），路由器挂 `popstate` 统一处理后退/前进/锚点/手改地址。**注意**：本地 `python http.server` 没有 SPA 回退，直接刷新 `/tools` 会 404（导航点击不受影响），测直达/刷新用 wrangler 或线上。wrangler 实测：六档行为全过（/tools↔/docs 前进后退互斥切换、/docs 刷新直达、/album /board 直达、#/board 归一化、Esc 清回 /+标题恢复、/tools#home 锚点回首页、搜索跳转 /album）；校验脚本全过

* **前台（主题切换圆形揭示动效）**：参考用户提供的博客站，深色/浅色切换加 **View Transitions API 圆形揭示**——点击切换按钮，新主题从按钮位置以圆形扫满全屏（0.52s），旧主题定格垫底；切换按钮本身独立成像，配出场旋转缩小/入场反转进入的小翻转。实现：切换时算按钮中心与覆盖全屏所需半径，**全部换算成百分比**注入 `@keyframes theme-reveal`（坑：Chrome 快照按设备像素密度光栅化，px 裁剪坐标在 2x 屏只画一半；circle() 百分比半径按归一化对角线 hypot(W,H)/√2 解析）+ `html.theme-vt` 类接 `::view-transition-old/new(root)` 静态规则（旧帧 animation:none 垫底、新帧跑揭示、去默认混合、快照期间冻结元素自身过渡）；`themeSwitching` 锁防连点。**降级链**：Firefox 等不支持 VT、系统 prefers-reduced-motion、后台标签被浏览器跳过过渡——三种情况都直接落回即时切换（主题照常生效不坏）。本地实测：注入关键帧圆心=按钮真实坐标、`::view-transition-new(root)` 动画运行中 clip-path 圆形半径随时长推进（180ms 时 33%，与 520ms 线性吻合）、按钮翻转动画并行、结束后 theme-vt 类清理与 localStorage 持久化正确

* **前台（顶栏改居中悬浮胶囊导航）**：参考用户给的博客站点（yyyyyolo7a79-blog.pages.dev）顶栏形态重做——原通栏毛玻璃横条改为**居中悬浮胶囊**：logo + 七个导航项 + 搜索/登录/深色三钮全部装进一个 999px 圆角胶囊（半透明白底 + `saturate(1.8) blur(22px)` 毛玻璃 + 内侧 1px 高光 + 柔和投影，深色主题各一档），胶囊悬浮在页面上方、**胶囊外区域点击穿透**（头部 pointer-events:none）；**当前项高亮改反色 chip**（前景色底 + 背景色文字，参考站同款选中态），滚动后投影加深（沿用 .scrolled 钩子）；头部从 sticky 改 fixed（不再占文档流，hero 顶部留白相应加到 72/80px 容胶囊）。**顺带修了窄屏顶栏溢出的老问题**：390px 下导航原被挤成竖排文字且「留言」截断——现 ≤767px 胶囊占满行宽、导航区横向滑动（隐藏滚动条），≤480px 隐藏 logo 省空间；登录按钮登录态由描边变色改淡色底。全屏层（z-151，top 52px 起）与胶囊衔接自然：胶囊悬浮在层上方缝隙。实测 390/640/768/900/1024/1280 六档宽度：胶囊与右侧按钮完整可见、页面零横向溢出、首屏内容与胶囊间距 13~25px；暗色主题、Ctrl+K、头像下拉、全屏层开关全冒烟通过

* **前台（播放器审计补漏）**：全面复查本次上线功能时补的两处——①个人主页「收藏音乐」列表也剥格式后缀显示（musicDisplayName 只走展示层，收藏匹配仍用原名）；②迷你播放器封面图加加载失败容错：封面 KV 被手删/备份恢复后键悬空时回落音符图标，不露破图（src 每次切歌重新赋值，同地址走缓存，保证失败后再次选中仍能触发 onerror）。浏览器实测封面旋转链路：后台传封面的曲目播放时圆盘显示封面并旋转（transform 持续变化）、暂停保持角度，中心白点与律动条正常

* **前台（AI 历史对话抽屉重做）**：针对「不美观 + 使用有问题」整体翻新——**美观**：列表条目改两行式（标题 + 「N 条 · 相对时间」，aiConvTime 此前是死代码现已用上），当前对话主题色淡底高亮，空状态改图标+主副文案居中，「新对话」改实心主色 CTA，抽屉加右侧圆角，顶栏「新对话」图标从 ↻（像刷新）换成笔（新建语义）；**使用**：①删除确认弃用原生 window.confirm（前台根本没有 ask 组件，一直走的原生弹窗），改留言板同款**两段式确认**（首点删除钮变红武装，2.5 秒内再点才真删，点别处/关抽屉解除）；②**触屏可删**：删除钮原来纯 hover 浮现，触屏没有 hover 永远显示不出来，现 `@media (hover: none)` 常显（后台同款约定）；③加**半透明遮罩**（点遮罩关抽屉，原来只有点聊天区能关、点头栏无反应）；④**Esc 分层**：抽屉开着先只关抽屉，再按才关整个 AI 界面（原来一键盘 Esc 连界面一起关）；⑤唤出按钮加 aria-expanded 与高亮态；⑥头栏与抽屉两个「新对话」行为统一（都关抽屉）。本地 wrangler + 浏览器全实测：列表两行条目/高亮/空状态、切换对话自动关抽屉、武装态红钮与超时解除、两段式删除后列表即时更新、遮罩点击关闭、Esc 两层顺序、aria-expanded 翻转

* **前台（播放器细节打磨）**：①歌名展示全面去掉音频格式后缀——迷你播放条标题、播放列表面板、全站搜索的音乐项统一显示「晴天 - 周杰伦」而非「晴天 - 周杰伦.mp3」（新增 musicDisplayName 展示名处理，只剥一层音频扩展名；tracks[].name 数据保持原名，收藏定位与搜索匹配不受影响）；②律动条暂停不再瞬间弹回原始状态——迷你播放条与歌词条两处律动条改为**动画常驻、播放状态只切 animation-play-state**：暂停冻结在当前姿态、恢复原地接续（原来移除 animation 会直接跳回基础高度，观感生硬）；延迟从负值改正值让未播放的待机态停在基础高度；灰色↔主题色加 300ms 过渡淡入淡出；顺带把两处律动条纳入 prefers-reduced-motion（系统减弱动态时静止在基础高度）。本地浏览器实测：播放标题/播放列表/搜索均无后缀、暂停 800ms 后 transform 逐位一致（真冻结）、恢复 running 接续、颜色过渡生效

* **前台+后台（音乐专辑封面）**：后台音乐页每行新增「封面」按钮——上传/替换专辑封面（jpg/png/gif/webp/avif ≤2MB，已设显示 ✓ 和移除 ✕），存 media.cover 列（KV covers/，换图删旧，删除歌曲连封面一并清理），新接口 `POST/DELETE /api/admin/media/:id/cover`、/api/admin/media 清单带 has\_cover；前台迷你播放器左侧圆盘升级为**专辑封面显示**：当前曲目带封面时黑胶式旋转（播放转、暂停保持角度、`prefers-reduced-motion` 不转、中心白点固定不随图转），无封面回落原音符图标；封面随 /api/playlist 下发（静态 music/ 曲库与本地文件夹歌曲无封面），曲目合并/缓存恢复时后台清单后到会自动补齐并即时刷新。**顺手修了一个真 bug**：autoLoadMusicFolder 把 /api/playlist 的 music 项映射成 {name,url} 时丢了 lrc 字段——后台曲库配的歌词从未真正到达播放器（一直只有静态 music/ 同名 .lrc 在生效），现一并携带并经 wrangler 全链路实测确认下发。本地实测：登录→传歌→传封面→清单 has\_cover/cover 地址→封面 200 image/png→替换后旧 KV 404 新图 200→配歌词 playlist 带 lrc→移除封面后回落、lrc 保留→删歌曲封面 KV 连带清理，校验脚本全过

* **前台（课表导出）**：个人主页课表卡工具栏在「导入 WakeUp」旁新增「导出课表」按钮——纯前端把当前课表转成 **WakeUp 兼容 JSON** 下载（文件名 `WakeUp课表导出-日期.json`），本站「导入 WakeUp」可直接导回（服务端解析时保留提醒设置/作息；termStart 随文件携带，WakeUp App 忽略未知字段不受影响），WakeUp App 也能识别导入；weeks 转布尔数组（下标 0 = 第 1 周），内部空 weeks（= 每周都上）导出为全 true 保语义无损；无课程时提示不生成文件。本地实测：导出 → 服务端真实 parseWakeUp 解析往返测试，课程名/地点/教师/星期/节次跨度/周次/每周语义全部无损；校验脚本全过

* **前台（账号昵称，展示名与登录名分离）**：登录账号不再直接当昵称用——个人主页身份卡用户名旁新增「改昵称」入口（1~20 字，留空保存即恢复显示用户名），改完**即时同步**顶栏字母徽章/下拉菜单/欢迎语（sessionStorage demoLoginName 改存展示名），登录账号名保持不变（登录仍用账号名）。留言板历史留言的署名取**实时昵称**（改昵称会同步旧留言的显示，署名按 user_id 实时解析）；login/register/me/profile 接口响应均带 nickname；新接口 `POST /api/user/nickname`（仅前台用户会话；users 表补 nickname 列，ensureSchema 自动补列）。本地实测：改昵称后身份卡/顶栏字母/存储名三处同步、重登后昵称保持、留言板署名显示昵称 + 等级徽标

* **前台（留言板）**：顶栏新增「留言」全屏界面（hash 路由 #/board，Ctrl+K 快跳清单同步收录）——前台用户登录后可发布留言（≤500 字，同一用户 60 秒一条防刷），每条留言显示**用户名 + 签到等级徽标（Lv.0~6，与签到共用 lib/levels.js 规则）**；管理员可发布（金色「站长」徽标）并删除任意留言（两段式确认：首点变「确认删除」再点才删）；游客打开可见留言，发布时弹登录卡。实现：新表 messages（is_admin=1 为站长留言），新接口 GET/POST/DELETE /api/messages（双会话鉴权与 AI 同口径；/api/user/me 补 alsoAdmin 字段——同一浏览器同时有用户登录与管理员会话时，留言板据此显示删除入口）；全屏层走统一管理器注册（FS_ROUTE #/board、Esc 逐层退、垫底覆盖机制全部自动生效）。本地实测：游客拦截、登录发布带 Lv.2 徽标、60 秒限速、站长发布、两段式删除后其余留言保留（修了删除后分页偏移 bug）、搜索快跳直达

* **前台（更新日志时间轴）**：文档区《更新日志》改为时间轴渲染——`## 日期` 自动分组为时间轴节点（主题色圆点 + 竖线），每条更新变独立卡片（标题取条目开头的 **粗体** 段，正文支持行内代码/加粗/链接）；超 220 字的长条目默认折叠出「展开全部/收起」按钮，阅读体验不再是一整屏密文字。其余文档仍走原 Markdown 渲染，写日志的格式完全不变（部署说明照常 `## 日期` + `* 条目` 即可）。本地实测：7 个日期分组 64 条条目正确分组、折叠展开切换、Esc 逐层退出

* **后台（管理员安全中心，「我的」页四件套）**：「我的」页在头像/邮箱之外新增**安全卡**——①**修改管理员密码**：补上此前完全缺失的改密入口（原唯一途径是邮箱验证码重置，邮件服务故障即无解），旧密码验证 + 新密码 6-100 位，旧密码试错复用登录限速（scope=pwd，5 次锁 10 分钟），成功后自动踢掉其他设备（当前登录保持）；②**登录设备管理**：列出全部有效管理员会话（IP/UA/登录时间，标记「本设备」，不回传 token），「退出其他设备」一键吊销（sessions 表补 ip/ua 列，登录时记录来源）；③**最近登录记录**：最近 10 条成功/失败登录（✓/✕ + 原因 + IP + 时间），新表 admin_login_logs 自动裁剪只留 100 条；④**管理员登录二次验证（2FA）**：开关存 site_settings `admin_2fa`，开启前提是已绑管理员邮箱且邮件服务已启用——开启后**后台登录页与前台主页管理员直登两个入口**密码通过后都向管理员邮箱发 6 位验证码（purpose=admin2fa），凭一次性票据换会话（票据复用 email_login_pending 表，user_id 取负数=管理员，与 schedule_sent 约定同源；验证码错一次票据即作废防爆破）。**⚠️ 已知风险**：2FA 开启后若邮件服务故障，管理员会被锁在门外（两个入口都止步于发信失败），届时删 site_settings 的 `admin_2fa` 键即可恢复密码登录——开启门槛已因此要求邮件服务必须在线。后台登录卡相应增加验证码输入行（needCode 时出现、自动聚焦、按钮变「验证并登录」）。本地实测：改密错误旧密码 401 / 正确后旧会话失效 / 新旧密码登录边界、双会话踢单、登录日志全类型记录、2FA 未绑邮箱 400 / 两入口 needCode 拦截（不下发会话）/ 错码票据作废 / 对码发会话、UI 安全卡渲染与改密刷新、登录卡验证码行出现逻辑（fetch 桩模拟 needCode）。

* **前台（全站搜索 Ctrl+K）**：新增命令面板式搜索——**Ctrl/Cmd+K** 或顶栏放大镜（`#searchToggle`）唤起，任何界面里可用：搜索**界面**（首页/工具/文档/AI/相册/杂项，走 hash 路由返回键可用）、**工具卡**（打开工具界面后滚动定位到对应卡并高亮脉冲一下，密码生成会自动展开折叠区）、**文档**（docs.json 清单，直达阅读层）、**音乐**（按歌名回曲库定位播放，与收藏同机制，曲库中已删除的置灰）、**图片**（派发 yhuo:open-photo 直开大图）与**相册名**、**视频**（回首页）。实现要点：独立浮层 z-index 350（高于灯箱低于登录卡），**不参与全屏层互斥**——任何界面里唤起、关闭回到原界面；数据源 /api/playlist + docs.json（60 秒缓存，接口失败回退 music|video|images 三个静态清单，坑 2 的伪 200 也走兜底）；键盘 ↑↓ 选择 / Enter 打开 / Esc 关闭，**捕获阶段接管按键**（面板开着时按键不再漏给其他全屏层，stopPropagation 隔离）；匹配词高亮 `<mark>`（DOM textNode+mark 构建防注入，命中位置 0 的 falsy 坑已修）；分组渲染保持组整体不交错（组内按命中位置排序、组间按各组最佳命中排序）；被功能开关关闭的界面/模块（含 AI 全局开关）**不出现在结果里**；`prefers-reduced-motion` 关过渡。本地实测：唤起/关闭/搜歌播放（迷你播放器载入晴天）/搜"更新日志"直达阅读层/搜图开灯箱/快跳清单/方向键导航/AI 未配置时自动隐藏/后台关 docs+album+video 后搜索结果同步只剩音乐组，恢复开关即回。修了一个真 bug：文档项若立即 openDoc 会被 hash 路由拉起 docsView 时的互斥收掉（docViewer 会被其他层关闭），改为等界面拉起后再开阅读层

## 2026-09-04

* **前台（每日签到 + 等级 0~6）**：个人主页新增签到卡（身份卡下方，仅前台注册账号可见，管理员隐藏）——每日点「签到」记一天（北京时间计天，同日重复请求幂等返回 already 不报错），卡片显示**等级徽标 + 等级名 + 距下一级进度条 + 累计/连续天数 + 近 7 日签到圆点**；等级按**累计签到天数**实时计算（不入库）：0/5/15/40/80/150/300 天对应 Lv.0 初来乍到 → Lv.1 渐入佳境 → Lv.2 常客 → Lv.3 老朋友 → Lv.4 中坚力量 → Lv.5 镇站之宝 → Lv.6 传奇（改阈值只需改服务端 LEVELS 数组，存量自动重算；满级徽标金色、进度恒 100%）；连续天数断一天清零（今天未签时按昨天起算显示历史连续，签上即接回）。实现：新表 checkins（(user_id, day) 主键防同日重复，ensureSchema 自动建）；新接口 GET/POST /api/user/checkin（仅前台用户会话，管理员 401 与收藏同口径）；前端纯渲染，进度条百分比由服务端 prev/next 阈值算出。本地实测：未登录 401、注册→首签（total/streak/近 7 日圆点即时更新）→重复签到幂等→回填 16 天历史后自动升 Lv.2 常客（连签 17 天、进度 8%、「再签 23 天升到 Lv.3」）、管理员打开个人主页卡片隐藏，校验脚本全过

* **后台（按钮与动效体检 + 无障碍合规）**：排查全后台不合规代码并优化按钮/动画——①**状态色合规**：删除类按钮此前是"中性灰样式、毫无危险感"（`button.danger` 悬停不变色），且两处硬编码危险色（删除确认弹窗的「确定」钮 `#d64545`、相册 ⋯ 菜单的危险项 `#e0342b`）违反"状态色走 --ok/--warn/--danger 变量（深浅主题各一档）"约定——现 `button.danger` 悬停转危险色（文字/边框/浅红底全走 `--danger` 的 color-mix），两处硬编码改 `var(--danger)`（深色主题自动换亮红档，确认钮文字取 `--bg` 保证两主题对比度）；②**动画切换增强**：询问/预览弹窗此前瞬间弹出无过渡——现背景淡入 + 卡片缩放上滑入场（靠 hidden 切换 display 自动重播 CSS 动画，零 JS 参与）；登录门四张卡（初始化/登录/找回密码/网络错误）互切与进入主界面同样加了入场动效；③**无障碍合规**：`prefers-reduced-motion` 原来只覆盖 3 处动画（列表/下拉/侧边栏/弹窗等全漏），改为全局压缩规则（全部动画/过渡时长瞬时化，全后台无 infinite 动画所以安全）；新增全局 `:focus-visible` 焦点环（Tab 键盘导航可见，鼠标点击不触发）；④**全量排查结论**：模板字符串内 8 处正则反斜杠全部规范双写（坑 18 无回归）、无原生 confirm/prompt/alert（自定义 ask 全覆盖）、行内操作钮无文本字符（仅 toast/日志的状态 ✓✕ 标记属正当使用）、innerHTML 注入点 textContent/escapeHtml 到位。本地 wrangler 实测：13 个页面切换动画全覆盖零报错、删除确认/图片预览弹窗动画与深浅主题危险钮配色正确、danger 悬停变色与键盘焦点环生效

* **前台（工具合集六卡重做）**：工具界面整体换血——砍掉"手机上有更好替代品"的旧倒计时和颜色转换，换成六张更实用的卡：**🎯 重要日子**（考试/纪念日倒计时，多条记录按日期排序，显示"今天/明天/还有 N 天/已过 N 天"，存 localStorage）；**🍅 番茄钟**（原倒计时升级：专注 25 / 短休 5 / 长休 15 分段切换，响铃沿用原倒计时的 Web Audio 三声铃，专注完成自动计入"今日完成 N 个"并切到短休，跨天自动归零，存 localStorage）；**🔄 换算器**（单位 + 进制合一：长度/重量/温度/数据大小四类单位互转，温度走公式、其余按系数；进制二/八/十/十六互转用 BigInt 逐位解析杜绝超精度失真，四进制同行显示、点击行复制，非法输入显示 —）；**📝 文本工具**（字数统计实时四项：字符/不含空格/词数（中文按字、英文数字串按词）/行数；按行整理：去重/排序/去空行/去首尾空格/大小写；时间戳转日期（10 位以内按秒、更长按毫秒）与当前时间戳；JSON 格式化/压缩带报错提示）；**🎲 随机决策**（每行一个选项滚灯式抽取约 1 秒落定，选项自动保存）；**🧮 计算器**保留并新增历史记录（最近 20 条持久化，点击一条把结果填回屏幕，可一键清空；连续相同算式不重复入历史）。密码生成器为低频工具收进底部「更多工具 ▾」折叠区（功能不变）。全部纯本地无外部依赖、随机决策/重要日子/番茄钟计数/计算器历史分别存 localStorage（yhuoDice / yhuoDays / yhuoPomo / yhuoCalcHist），网格仍为 2 列自适应（窄屏单列）。本地实测：六卡添加/换算/进制/统计/整理/时间戳/JSON/抽签/计算/历史/折叠区全链路通过，刷新后持久化正常，hash 路由直达正常

* **前台（切换界面不再闪现主页）**：五个全屏界面本就是半透明毛玻璃（背景 88% 不透明 + blur），此前切换时旧层 250ms 淡出、新层 250ms 淡入，中途两层同时半透明、主页从缝隙里闪出来。现改为"垫底覆盖"：切换时旧层不淡出，原样压在新层下面（新层 z-index 提到 151 垫在最上、旧层加 fs-covered 禁点击），等新层过渡完（320ms）再在遮盖下走正常关闭流程——主页全程被挡住；快速切回被覆盖层会撤销覆盖原样留住（FS_COVER_TIMER 定时器管理）；fsClose 的滚动锁解锁改为"还有别的层开着就保持锁定"（排除自身 .show 未摘的时序），修复覆盖层延迟关闭会提前解锁主页滚动的问题。浏览器逐帧采样实测（189 帧）：切换全程 0 闪现帧，✕/Esc 关闭后滚动锁正确解除

* **前台（hash 路由）**：顶栏五个全屏界面与地址栏双向同步——点工具/文档/AI/相册/杂项，地址变为 #/tools、#/docs、#/ai、#/album、#/misc；**浏览器返回/前进键可用**（相册返回回工具、再返回回首页，修复此前"进了界面按返回直接退出网站"的缺陷）；**刷新/分享直达**：以 #/xxx 打开页面直接拉起对应界面；✕/Esc 手动关闭时路由静默清除（replaceState，不产生历史记录，返回键不会"重开"已手关的界面）；顶栏"首页"/logo/回到顶部等 #home 锚点视为回首页路由，自动关闭打开的界面。实现：FS_ROUTE 路由表挂在统一全屏管理器上——fsOpen 先写路由再互斥关闭其他层（防 fsClose 误清新路由）、fsClose 校验后静默清路由、hashchange 路由器与深链接都经注册表新增的 open 回调拉起界面（界面的实际显示在各自 open 函数里，直接调 fsOpen 不会显示，注册表原本只存 close 不存 open）；个人主页与文档阅读层不参与路由；与功能开关联动：指向已关闭界面的路由静默清掉（缓存已知关闭直接回首页，首访无缓存先拉起、配置到达后就地关闭并清路由），正开着的界面被后台新配置关闭时同样处理

* **前台（功能开关）**：后台「外观」页新增「功能开关」卡片——顶栏四个界面（工具/文档/相册/杂项）与首页三个模块（天气胶囊/歌词横条/首页视频）可逐个关闭，保存后前台**直接隐藏**（顶栏项消失导航自动补位、全屏层彻底打不开、首页模块不渲染，访客感知不到功能存在过）；AI 不在此列、跟随「AI」页的全局启用开关（且全局开关关闭时前台顶栏 AI 入口现在也直接消失，而不是打开后才看到"接入中"）。实现：site\_settings 新增 feature\_flags 键（白名单 tools/docs/album/misc/weather/lyric/video，缺省全开，全部为开时自动清键），/api/settings 下发 flags 字段（接口失败一律全开、不裁功能），前台 fsOpen 管理器与各界面 open 函数入口双重守卫（画廊点照片跳相册等旁路入口一并拦住），/api/admin/appearance PUT 支持 {flags}；开关改完对访客下次进页面生效（settings 60 秒缓存）。**首屏无闪现**：flags 每次成功拉取后存 localStorage（yhuoFlags / yhuoAiOff），head 末尾内联脚本在 body 渲染前读缓存给 html 打 ff-*-off 类（CSS display:none!important 隐藏导航项与视频框），applyFeatureFlags/fetchAiConfig 拿到最新配置后增删同样的类校正并刷新缓存——访客下一次进页面，被关功能从第一帧就不出现；**首次到访（无缓存）改为先藏后放**：head 内联脚本先打 ff-boot-hide / ff-ai-boot-hide 把顶栏可选项与视频框整体藏住，settings/AI 配置到达后放行真实状态，接口失败或 3 秒超时兜底强制放行（缺省全开不裁功能）——被关功能任何情况下都不会闪现；**配套省流量：杂项和相册都关闭时，前台启动不再下载任何图片**（画廊与相册照片墙共用同一批图，只关其一时仍属正当展示照常加载；判断用 head 内联脚本同步存的 window.\_\_FF\_CACHE 加实时 flags 双查，配置到达后经 FF\_APPLY\_HOOKS 钩子校正——管理员重新开启时自动补加载；首次到访无缓存时无从得知开关仍会加载，与首屏"先藏后放"同一取舍）；浏览器实测验证：模拟首访（清缓存 + settings 人为延迟 5 秒）——首帧可选功能全部隐藏、3 秒超时放行、5 秒配置到达后按真实 flags 校正，回访（有缓存）首帧即最终状态；杂项相册双关时画廊 0 张图 0 个图片请求（仅站点背景图正常加载），重新开启后配置到达自动补加载 8 张

* **修复管理员绑定邮箱 / 忘记密码重置（2026-08-31 遗留 Bug 根治）**：后台「我的」页绑定管理员邮箱、登录页「忘记密码」重置后台密码，验证码校验从来通不过——根因：后台整页 HTML+CSS+JS 包在 `const PAGE = \`...\`` 模板字符串里，内联正则 `/^\d{6}$/` 的 `\d` 被模板转义吃成 `d`，浏览器实际执行 `/^d{6}$/`（匹配 6 个字母 d），任何数字验证码都被前端拦截（报"请填写 6 位验证码"/"请填写邮箱和 6 位验证码"），请求根本没发到服务端；服务端/D1/邮件发送全程正常（验证码收得到、curl 直调接口一次通过），所以此前排查走了弯路。现把模板里全部 8 处被吞的反斜杠双写修复（2 处 `\d{6}` 验证码校验 + 6 处 `\.` 歌词后缀/去扩展名正则），`const PAGE` 上方留防回归注释——以后后台页面内联代码写正则必须 `\\d`、`\\.` 双写（校验脚本查不出这类错，它不解释模板转义）。已实测绑定/解绑/错误验证码提示/忘记密码放行全链路正常；前台用户绑定邮箱走 index.html 真实文件、不受影响

* **后台概览（访问趋势）**：访问趋势图支持柱状图 / 环形图切换（右上角图标按钮）——环形图按日期顺时针排段、多色调色板取色（纯色无灰底、细缝分隔；零流量天不画段，占比按有访问的天计）；活动日 ≤8 个全画，更多时只画占比 Top 7、其余合并成灰色「其他」段（颜色恒不复用、图例收敛 ≤8 行，点击可列出所含逐日明细），今天的段默认径向外移 5px 作锚点、图例带「今」徽标；中心显示近 N 天总量与日均、大段环外标注占比；环在左、图例在右双列布局（窄卡片自动换成上下）；两种视图统一悬停交互：柱子抬起 / 扇区径向外移并放大、其余淡出、跟随鼠标的数值气泡（日期·次数·占比）、点击弹出当日访问明细（含星期/占比/峰值标记）——顺带修了两个悬停 bug：扇区 --dx/--dy 漏写 px 单位导致 transform 整条失效（径向外移从未生效过），柱/扇区缩放未设 transform-box 导致绕 SVG 左上原点而非自身中心（右边的柱悬停会横漂）；悬停弹出改"进即弹、离缓回"——鼠标落到柱/扇区瞬间到位（悬停规则内关过渡），移开后 0.2s 平滑弹回，气泡仍跟随鼠标；**命中层与视觉层分离**根治悬停抽搐——此前弹出动的是被悬停元素自己，鼠标贴环内缘（或柱底）时「弹出→鼠标悬空→回落→再弹出」无限循环抽搐，现每个扇区/柱子配一个静止的透明命中层接管指针事件，视觉层 pointer-events:none 纯展示随便弹；切 14/30 天与切视图互不影响

* **后台概览（最近访问·IP 归属地）**：新增"最近访问"卡片——每次访问（每浏览器会话一条）记录 IP / 时间 / 页面路径 / UA，列表展示最近 20 条（悬停看完整 UA、点击无明细弹窗不变）；IP 归属地逐条懒查询（国内走 whois.pconline 返回中文省/市，国外兜底 ipwho.is 经内置中文国家表转中文，接口侧 24 小时内存缓存去重、查询失败显示"—"）；归属地可随时关闭——卡片头部滑动开关（仅站长模式化独立设置，不删数据重开即恢复）

* **后台概览（邮件统计）**：邮件统计卡全面细化——新增发送明细列表（最近 20 条：时间/类型徽标/收件人打码/主题/✓ 成功 ✕ 失败，失败的悬停可看原因；验证码邮件只显示"验证码邮件"不泄露验证码）；新增近 14 天发送趋势迷你柱状图；按类型筛选 chips（全部/验证码/测试/自定义/早报/课前提醒/提醒测试）；卡内新块加载淡入、明细行悬停高亮、趋势柱悬停抬起；每次发送（成功与失败）都记明细，失败带原因（email\_logs 表）

* **后台概览（统计卡）**：新增"网站运行"统计卡（时钟样式、与前台页脚同源基准时间，每秒刷新　X 天 X 时 X 分 X 秒，内容居中展示）；访问量卡改名为"总访问量（历史累计）"，访问趋势卡汇总行新增"历史累计 X 次"前缀；统计卡统一去图标居中

* **后台（上传）**：音乐/视频/图片上传行弃用浏览器原生文件控件——改为自定义"选择文件"按钮（ghost 风格 + 上传图标，保留多选/文件夹/类型限制全部能力），选择后按钮下方淡入"已选 N 个文件：…"提示，上传完成后同步清空

* **后台（动效）**：功能界面切换加入场动效（新面板淡入 + 轻微上移）；图片页相册切换与整表刷新（重命名/解散/批量移入）时列表淡入 + 上移 5px，搜索输入不触发避免击键闪烁

* **后台外观（侧边栏）**：左上角头像（含默认 YH 徽标）改为正圆形；标题改为"管理界面"并移除副标题小字；侧边栏展开/收起动画重做——文字改为"宽度从 0 平滑展开 + 淡入"（与侧边栏宽度过渡严格同步，0.2s 级），按钮间距与图标对齐过渡平滑，彻底消除展开时文字/图标"闪一下"的跳变

* **后台（访问统计口径）**：新增 visit\_logs / email\_logs 两张明细表与对应接口（/api/admin/visit-logs、/api/admin/geoip、/api/admin/email/logs），前端访问带上页面路径上报；均为新数据从上线后开始积累

* **前台（管理员体验）**：管理员用账号密在前台登录后，顶栏直接显示后台「我的」页设置的管理头像（刷新页面静默恢复时同样识别管理员会话）；点击个人主页不再显示"未登录"——改为显示管理员身份卡（用户名/头像/管理员标识与创建时间），并隐藏仅前台账号可用的功能卡（收藏/邮箱/改密/更换头像，头像需在后台改）

* **前台（管理员课表）**：管理员也能使用课表功能——前台个人主页课表卡对管理员开放（启用/导入 WakeUp/手动编辑/提醒设置全部可用），课表数据存 site\_settings.admin\_schedule（独立于用户课表）；每日早报与重点课课前提醒对管理员生效，提醒邮件发到后台「我的」页绑定的管理员邮箱（未绑定则发站长邮箱，仅站长模式下限定站长邮箱）；后台「邮件」页「发送测试提醒」在邮箱命中管理员邮箱时也可测管理员课表；管理员检测诊断与普通账号同列

* **首页（视频播放模式）**：后台视频页顶部新增播放模式设置（顺序循环 / 单视频循环 / 随机播放三个胶囊按钮）——单视频模式可在视频列表任一条点「设为独播」指定循环哪一条（星标高亮，选中项显示在设置栏）——设置存 site\_settings.video\_mode；前台首页右侧视频轮播按模式播放：单视频循环 = 当前视频播完自动重播（指定视频被删除自动回落顺序）；随机 = 每次播完随机换一条（不重复上一个）；顺序 = 原行为依次循环

* **首页（歌词律动条）**：歌词横条右侧的音乐图标改为与底部迷你播放器同款的律动条（三根竖条）——播放时主题色错峰跳动（0.9s 循环，与 .mini-eq 同款动画与延迟），暂停/待机时灰色静止（半透明）；播放状态经 updatePlayIcon 与播放器同步切换，窄屏下与其他条内装饰一样隐藏。窄屏居中修复：左右装饰（待机点/律动条）改为同时隐藏——此前只藏右侧律动条，左侧待机点 opacity:0 仍占约 25px 布局空间，居中歌词被顶向右偏约 12px

## 2026-09-02

* **后台曲库歌词（新功能）**：管理后台音乐页支持歌词——①上传歌曲时同选同名 .lrc 自动配对成附件随歌曲入库（.lrc 不单独成条目，未配对会提示）；②每首歌曲行新增「歌词」按钮，可补传/替换 .lrc（≤200KB），已配显示 ✓ 并附移除按钮；③歌词存数据库随歌单接口直接下发，前台歌词横条播放后台曲库的歌时同步逐句显示（此前后台曲库只能显示歌名占位）；静态 music/ 同名 .lrc 方案不变，后台歌词优先

## 2026-09-01

* **课表提醒**：tick 接口增加访问留痕——每次被访问（无论密钥对错）都记北京时间时间戳与结果到 site\_settings（成功/密钥错误分开记），后台「邮件」页 tick URL 下方显示"最近一次 cron 访问"，外部 cron 没在跑或用过期 URL 敲 401 一眼可见，不用去 cron-job.org 后台查

* **课表提醒**：「立即执行一次」升级为带诊断——逐账号显示"为什么没发/发了什么"（今天没课、邮箱未绑定未验证、仅站长模式不符、早报未开/未到点/今天已发过、重点课是否在提醒窗口、发送错误原因，邮箱打码显示），不再是干巴巴的"发送 0 封"；排查"真实提醒没到点发"从此一眼定位

* **课表提醒**：后台「邮件」页新增「发送测试提醒」——不用等真实到点，按真实课表算出"今天该发什么"并立即发送早报 / 课前提醒样式的【测试】邮件，主动验证 解析→计算→拼邮件→发送 全链路；收件邮箱可指定（留空=站长邮箱，仅站长模式下只能发站长邮箱），需是绑定了已验证邮箱且启用过课表的账号；测试发送不写防重发记录，不影响真实提醒；发送结果回显教学周/星期/今日课程数，测试邮件计入邮件统计（"课表提醒测试"）

## 2026-08-31

* **课表**：新增"移除课表"——提醒设置区底部红色按钮，自定义确认框二次确认后删除服务端的全部课程/作息/提醒设置（提醒邮件随之停止）；没启用过课表的账号个人主页只显示"＋ 启用课表"入口行，不再直接铺开空表格

* **后台**：邮件设置支持移除站长邮箱——清空"站长邮箱"输入框后点"保存配置"即移除（原先清空保存仍保留旧值，无法移除）；若"仅站长使用"处于开启状态会自动关闭，输入框下方提示已说明

* **课表修复**：修复 WakeUp JSON 导入报"没有解析到有效课程"——部分版本导出的字段名不同（name/teacher/room/startSection/endSection、weeks 为文本如"1-16"、"2-16双"），解析器已全部兼容（已用真实导出文件验证 14 门课全部正确解析，双周课周次正确）

* **课表**：导入支持 WakeUp 的 CSV 格式（此前仅 JSON）——按表头名匹配列（课程名/星期/讲师/节数/教室/周次，兼容列序变化和别称），支持"第1,2节"、"周一"等中文写法、带引号转义的字段和 BOM；前端按扩展名+文件内容自动分流两种格式

* **课表**：修复个人主页课表卡「作息时间」展开后布局错乱——格子里"第N节"文字和时间输入框横向溢出互相挤压，改为标签在上、输入框占满整格的纵向排列

* **课表提醒（新功能）**：个人主页新增「课表」卡——周视图展示本学期课程（按教学周过滤、可切周），支持导入 WakeUp 课程表导出的 JSON（解析在服务端，保留提醒设置）+ 手动添加/编辑/删除课程；提醒设置支持每日早报（每天固定时间一封列出当天所有课）和重点课课前提醒（勾选的课程提前 N 分钟单独发邮件），作息时间（各节上课时间）可自定义；后台「邮件」页新增定时任务配置——显示 tick URL、复制/重新生成密钥、手动执行一次，需配合外部 cron（推荐 cron-job.org 每 5 分钟）触发。提醒发给用户绑定的已验证邮箱

* **首页/杂项**：顶栏新增「杂项」全屏界面；首页的图片画廊（折叠牌堆轮播）迁入杂项界面，首页不再直接展示图片（相册界面不受影响）

* **首页**：普通用户登录成功后也弹顶部欢迎浮窗（👋 欢迎回来，用户名；不显示"进入后台"按钮，6 秒自动消失）；管理员浮窗行为不变

* **首页**：退出账号后弹顶部浮窗提示「👋 已退出登录，期待下次再见」；关闭登录卡片改为上飘滑出退场动效（点击空白处/登录成功关闭时卡片向上浮起淡出）

* **首页**：登录卡片的登录/注册/找回密码/验证码模式切换改为平滑过渡——邮箱、验证码、密码等输入行高度渐展开收起，注册按钮宽度渐变，副标题与提交按钮文案淡出淡入（尊重系统"减少动态效果"设置）

* **首页修复**：修复天气胶囊被拉伸占满整行、右侧大片留白的问题，现在按内容自适应宽度

* **后台**：用户列表显示注册用户的头像（没设头像的仍显示用户名首字徽章）

* **首页**：未播放音乐时，歌词横条改为显示一言（管理员寄语 > 每日一言当日缓存 > 待机提示）

* **首页**：一言从歌词横条移出，改为天气胶囊下方的独立胶囊（与天气同款样式，打字机轮播）；点击一言胶囊可立即切换下一句

* **后台**：主页寄语支持保存多条（最多 20 条）；前台寄语/一言打字机轮播（没配寄语或只配一条时拉 6 条一言混合轮播，3 秒切一句，主域名失败自动换国际备用域名）；一言改为天气胶囊同款样式；修复刷新后自动接续播放导致歌词条待机一言被歌名覆盖的问题

* **AI 助手**：对话记录服务端保存（按账号隔离，每人最多 200 条，刷新/换界面后自动恢复）；关闭界面不再清空对话，点「新对话」才清（本地+服务端一起清）；退出登录清空内存对话防止串号

* **AI 助手**：多对话管理——历史对话改为抽屉式（默认收起，顶栏气泡按钮唤出，点聊天区/选完对话自动关闭，全屏宽一致）；新对话/切换历史对话继续聊/删除带确认框，对话自动以第一句命名，最多 50 个；旧的单条历史自动归入"历史对话"

* **邮箱体系**：后台新增「邮件」设置页（Resend/Brevo 双协议，API Key 存 D1 不回显，测试发送）；启用后前台开放——注册需邮箱验证码、登录卡片新增「忘记密码」（邮箱验证码重置，重置后踢全部设备）、个人主页可绑定/换绑邮箱并开启**登录二次验证**（2FA）；验证码 6 位、10 分钟有效、限 5 次尝试、60 秒重发间隔；未配置邮件服务时所有邮箱相关 UI 自动隐藏，注册流程不变

* **邮箱·仅站长模式**：邮件页新增「仅站长使用」开关——无域名邮件服务（如 Resend onboarding 只能发给自己）场景下，普通用户注册不要求邮箱、看不到邮箱卡，只有站长邮箱（后台单独填写）能收验证码/绑定/开 2FA/找回密码

* **邮箱·管理员**：后台「我的」页新增管理员邮箱绑定/解绑（验证码验证，存 site\_settings）；后台登录页新增「忘记密码？」（邮箱验证码重置管理员密码，重置后踢掉全部管理员会话）；未启用邮件服务时入口自动隐藏

## 2026-08-30

* **AI token 用量统计**：概览页新增「AI 用量」卡片（今日/近 14/近 30 天 tokens 与调用次数 + 按模型排行）；每次对话结束回复气泡下显示 ↑输入/↓输出 tokens 小字

* **AI 模型类型标签**：后台可为每个模型标注类型（文本/视觉/推理），前台切换菜单里模型名后显示小徽标（与桌面客户端同款样式）

* **首页歌词横条**：播放 music/ 曲库的歌时，首页横条上打字机逐字同步显示当前句；把 `.lrc` 歌词文件和歌曲一起放进 music/（同名）即可生效，没有歌词则显示歌名占位

* **外观**：新增"背景模糊"滑杆（0\~30），给自定义背景加上毛玻璃质感；管理员可在后台设置**全站默认模糊度**，访客没自己调过时自动采用

* **后台「我的」页面**：侧边栏左上角的 YH 徽标可换成管理员头像（点击直达），支持更换/移除

* **后台下拉动效化**：所有下拉选择换为统一动效组件（展开/收起动画、点空白或 Esc 关闭）；AI 管理界面顶部说明精简、全局开关移至供应商列表下方

* **前台修复**：修复从 AI 界面切不到工具/文档/相册等其他界面的问题

* **后台整体精致化**：统一线性图标体系（行内操作全部换 SVG，替换掉 ✏️▶👁★☆＋⋯ 等文本字符），新增状态色变量并适配深浅主题；AI 供应商页细节拉齐（图标底块、绿点光圈、虚线添加钮、API Key 眼睛内嵌输入框并可切换显隐图标、模型行/默认徽标配色）；顺带修复音乐/视频列表被相册侧栏容器误隐藏的回归

* **AI 助手**：对话功能正式实装！支持 OpenAI 兼容与 Anthropic 两种协议（GLM/DeepSeek/Kimi/Claude 等主流服务商均可接入），流式打字机回复、多轮对话、Markdown 渲染；后台「AI」页升级为**供应商管理**（左侧供应商列表+右侧详情，一个供应商挂多个模型，✏️改名/启停/删除/★设默认，模型名支持自动获取），前台输入条底部可随时切换模型（按供应商分组的弹出菜单，仿桌面客户端样式）。仅登录用户可对话，服务商 Key 只存后台数据库

* **首页**：新增天气胶囊（Open-Meteo 免费接口，点击换城市）；顶栏新增「工具」与「文档」入口

* **工具合集**：计算器、倒计时、密码生成器、颜色转换四件套上线

* **文档**：文档区上线，当前收录《关于本站》《更新日志》

* **个人中心**：头像下拉菜单全新改版；新增「我的主页」（身份卡、自助修改密码、收藏聚合）

* **收藏**：相册灯箱和播放列表支持 ♥ 收藏，个人主页聚合展示

* **管理员**：登录后首页弹出欢迎浮窗（含进入后台入口）

* **后台**：图片页相册交互重做——左侧相册栏 + 拖拽归类，替代原来的下拉工具行

* **全屏界面**：工具、文档改为与 AI/相册一致的独立全屏界面，五个入口互斥切换、顶栏高亮跟随；图片画廊留在首页

## 2026-08-29

* 站点上线：时钟首页、相册、音乐播放器、访客/管理员登录、访问统计

* 管理后台上线：媒体上传管理、用户管理、外观设置、访问趋势

