# YHuo 创意规划与灵感池

> 生成时间：2026-09-09 23:10（深夜自主执行批次）
> 性质：站点的**创意库 + 路线图**。每次不知道"下一步做什么"时，先翻这份文档。
> 维护约定：做完一条把状态改成 ✅ 并注明批次；新想法随手往对应分组里追加；放弃的写明原因，别删（避免重复想一遍）。

---

## 0. 先回答一个问题：这个站"到头了吗"？

没有。它现在的形态是「**静态个人主页 + 轻后端（Cloudflare Functions + D1 + KV + 邮件）**」，已经做完的是"展示与基础互动"（首页/工具/AI/留言/课表/随笔/预览/关于 + 播放器 + 登录体系）。真正还没挖掘的空间在三条轴上：

1. **动态能力轴**：站点其实有真后端（D1/KV/邮件/cron），但大部分页面仍是纯静态思路。可以做出"越用越活"的东西：每日壁纸、数据看板、RSS、短链、投票墙——这些是"静态托管做不到、而你已经能做到"的事，是站点的差异化。
2. **留住人轴**：现有功能都是"来一眼走"型。游戏页、签到成就、每日变化的内容（壁纸/一言/统计）给访客一个"明天再来"的理由。
3. **作者工具轴**：这个站也是你自己的作品集。写作辅助、AI 深度整合、数据回显，让"运营这个站"本身变好玩。

下面所有创意都按这三条轴分组。

---

## 1. 创意库（全量）

标注：价值 ★☆☆☆☆~★★★★★；工作量 XS（<1h）/ S（1~2h）/ M（2~4h）/ L（半天+）；
状态：⬜ 未做｜🔧 做了一半｜✅ 已做（批次）｜❌ 放弃（原因）。

### 1.1 动态能力轴（静态托管做不到的事——最推荐优先做）

| # | 创意 | 说明与实现要点 | 价值 | 工作量 | 状态 |
|---|------|----------------|------|--------|------|
| D1 | **每日壁纸** | `functions/api/wallpaper.js` 代理 Bing 每日壁纸接口（`cn.bing.com/HPImageArchive.aspx?format=js&idx=0&n=1`），返回 `{url, copyright}`；再配 `/api/wallpaper/image` 流式代理图片字节（CF 侧缓存 1 天），避免国内访客直连 Bing 图床不稳定。前台外观抽屉背景选择器里加「今日壁纸」按钮（JS 注入 DOM，免九页外壳同步），点击即把背景设为当日壁纸并显示版权说明。 | ★★★★ | S | ✅（批次 C） |
| D2 | **RSS/Atom 订阅** | `functions/feed.xml.js` 动态生成：数据源 = D1 notes（随笔）+ 解析 docs/changelog.md 的 `## 日期` 段（走 `env.ASSETS.fetch` 读静态 md）。八页 head 加 `<link rel="alternate" type="application/rss+xml">`。博客圈刚需，订阅器（Follow/NetNewsWire）可自动收你的更新。 | ★★★★ | S | ✅（批次 B） |
| D3 | **站点数据看板（公开透明页）** | `functions/api/summary.js` 公开接口：总访问、今日访问（visit_daily 聚合）、随笔数、留言数、曲库数、注册用户数、建站天数——**只给聚合并集，绝不给任何个体记录**（与 2026-09-06 移除 IP 记录的隐私立场一致）。关于页加一张「站点数据」卡实时渲染。 | ★★★★ | S | ✅（批次 D） |
| D4 | **短链服务 /s/** | D1 表 `short_links(code PRIMARY KEY, url, created_at, clicks)`；`functions/s/[code].js` 302 跳转 + 计数；后台管理页（新标签页）增删查。个人域名当短链服务用，社交分享体面。 | ★★★★ | M | ✅（批次 G/H） |
| D5 | **功能投票墙（灵感池上站）** | 把本档「创意库」精简版做成 `docs/roadmap.md` 展示在关于页；访客（登录用户）可对条目 👍+1，D1 表 `idea_votes(idea_key, user_id)` 唯一约束防刷；后台看票数。让访客参与决定站点走向——站点本身变成产品。 | ★★★★ | M | 🔧（批次 D：精简版 roadmap 已上站，投票功能未做） |
| D6 | **公开 JSON API + 徽章** | `/api/summary` 已是开放数据；再加 `GET /api/quotes/random`（随机寄语）之类小接口， README/他人站点可引用「YHuo 今日一言」；配 SVG 徽章（ shields.io 动态端点或自绘 endpoint 返回 SVG）。 | ★★☆ | S | ⬜ |
| D7 | **网站运行状态页** | 已有 `/api/health`；做一个公开 `/status/` 页：D1/KV/邮件/外部 cron（tick 最近访问时间戳已有记录）四项健康灯 + 历史可用率（可以懒：只显示当前状态）。 | ★★☆ | S | ⬜ |
| D8 | **Webhook 通知矩阵** | 站点事件（新留言/新注册/课表提醒失败）推送到 Bark/Server酱/Telegram Bot。后台加一个通知配置卡，`lib/notify.js` 统一封装 fetch。站长不在电脑前也能知道站上动静。 | ★★★ | S | ⬜ |

### 1.2 留住人轴（给访客一个"明天再来"的理由）

| # | 创意 | 说明与实现要点 | 价值 | 工作量 | 状态 |
|---|------|----------------|------|--------|------|
| K1 | **游戏页 /games/（第九页）** | 2048 + 贪吃蛇 + 记忆翻牌三款，纯 vanilla JS，零依赖零素材（CSS 画棋盘）；最高分存 localStorage（登录用户以后可同步 D1）。按「随笔页接入点清单」套路加第九个页面（8 处外壳 + PAGE_ROUTE/PAGE_TITLES/PAGE_MODULES + NAV_INFO + 搜索快跳 + check-code PAGES）。移动端触屏要能玩（贪吃蛇滑动手势、2048 滑动）。 | ★★★★★ | M | ❌ 放弃（2026-09-10 用户要求移除，代码在 8c131d4 可找回） |
| K2 | **每日签到成就扩展** | 现有 Lv0~6 之外加"成就徽章"（连续 7 天/30 天、累计 100 天、深夜签到、生日当天等），徽章墙放个人主页。D1 已有 checkins 全量数据，成就可实时算不入库。 | ★★★ | S | ✅（批次 E/F） |
| K3 | **"历史上的今天"** | 随笔满一年后可做：首页/随笔页顶部显示"去年今天你写过什么"。数据就在 notes 表，一个 WHERE date LIKE '%09-10' 的事。现在做太早，记下来等数据。 | ★★★ | XS | ⬜ |
| K4 | **每日一题/每日壁纸打卡** | 每天换一道趣味题（本地题库或 API），答对点亮当日徽章；与签到系统联动。 | ★★☆ | M | ⬜ |
| K5 | **番茄钟统计报表** | 番茄钟数据现在只存"今日完成数"，改存全量历史（localStorage 或登录后 D1），画周/月热力图（GitHub contribution 风格）。 | ★★☆ | S | ✅（批次 E/F） |
| K6 | **留言板匿名投递** | 未登录也能留言（限速+内容审核字数），显示为「路人」；降低互动门槛。注意与现有垃圾内容风控平衡。 | ★★☆ | XS | ✅（批次 I） |
| K7 | **彩蛋系统** | Konami 秘技（↑↑↓↓←→←→BA）触发流星爆发+彩带；离开标签页时 `document.title` 变「:( 回来看看」；连点 logo 10 次弹隐藏寄语。零成本高记忆点。 | ★★★ | XS | ✅（批次 D） |

### 1.3 体验轴（视听与交互的"哇"）

| # | 创意 | 说明与实现要点 | 价值 | 工作量 | 状态 |
|---|------|----------------|------|--------|------|
| E1 | **音乐频谱可视化** | Web Audio `AnalyserNode` 接迷你条与悬浮播放器两路 audio；播放器上方浮出 canvas 频谱条/环形；面板自带开关（JS 注入按钮，免外壳同步）。**雷区**：`createMediaElementSource` 一旦调用音频就永久走 AudioContext——必须 source→destination 直连且 Context 常驻；`window.self!==window.top` 守卫（坑 29）；失败时禁用功能绝不静音。 | ★★★★ | M | ✅（批次 C） |
| E2 | **PWA（可安装 + 离线）** | manifest + Service Worker + 图标（node zlib 手写 PNG 生成器，零依赖）；SW 策略：HTML network-first（绝不供陈旧页面）+ assets SWR + /api /admin /media 全穿透；专属离线兜底页。对国内这种"网络抽风"场景是真实用价值——断网时站点壳还能打开。 | ★★★★ | M | ❌ 放弃（2026-09-10 上午用户要求移除，完整代码在 22a03bf 可找回） |
| E3 | **天气背景联动** | 下雨时流星层换雨丝、雪天飘雪（canvas 粒子替换 meteor 主题）；晴夜星星闪烁。天气数据已有（Open-Meteo），只差粒子层变体。 | ★★★ | S | ✅（批次 I） |
| E4 | **首页视差/鼠标跟随光斑** | hero 区鼠标视差（背景光斑跟随，幅度小、有阻尼）；reduced-motion 直接关。参考站动效体系补最后一块。 | ★★☆ | XS | ✅ 批次 K |
| E5 | **打字机命令面板升级** | Ctrl+K 里支持 `> 计算式` 直接算数、`/ 笔记关键词` 跳随笔锚点、`# 歌名` 直接播放——把命令面板做成真正的“站内终端”。 | ★★★ | S | ✅（批次 E/F：= 算数模式已上；`/` 跳锚点、`#` 播歌未做） |
| E6 | **分享卡片生成器** | 随笔/留言一键生成精美分享图（canvas 绘制：标题+摘要+站点水印+日期），下载 PNG。传播利器。 | ★★★ | M | ✅ 批次 J（随笔版） |

### 1.4 AI 深度整合轴（站里已经有最完整的 AI 基建，别浪费）

| # | 创意 | 说明与实现要点 | 价值 | 工作量 | 状态 |
|---|------|----------------|------|--------|------|
| A1 | **AI 对话导出** | 历史抽屉每条对话加「导出 .md」（对话结构化成 Markdown 下载）；配合现有 D1 存储即可，零后端改动。 | ★★★ | XS | ✅（批次 E/F） |
| A2 | **语音输入** | Web Speech API（Chrome/Edge 中文识别免费）在 AI 输入条加麦克风按钮，识别结果填入输入框（不自动发送）；不支持的浏览器隐藏按钮。 | ★★★ | S | ✅（批次 G/H） |
| A3 | **AI 随笔润色** | 随笔编辑区（后台）加「润色」按钮：把正文发给当前默认模型，返回改写建议插入编辑器。后端复用 /api/ai/chat 的供应商逻辑。 | ★★☆ | S | ⬜ |
| A4 | **AI 摘要留言板** | 留言超过 N 条后，后台留言页加"AI 总结今日留言"按钮（拉留言→拼 prompt→返回摘要）。 | ★★☆ | S | ⬜ |
| A5 | **AI 画图接入** | 供应商协议扩展 `image` 类型（OpenAI images / 其他兼容端点），AI 页加画图模式。工作量最大，等有便宜可用的画图 key 再做。 | ★★☆ | L | ⬜ |
| A6 | **AI 角色预设卡** | AI 页模型菜单旁加"人格"快选（标准/翻译官/程序员/猫娘…），本质是拼系统提示词，前台实现即可。 | ★★★ | S | ✅（批次 G/H） |

### 1.5 基础设施与工程轴（不炫但保命）

| # | 创意 | 说明与实现要点 | 价值 | 工作量 | 状态 |
|---|------|----------------|------|--------|------|
| I1 | **SEO 基线** | 八页（加 games 九页）head 补 `description` + Open Graph + `theme-color`；`robots.txt` + `sitemap.xml`（静态文件）。搜索引擎能正确收录是“存在感”的地基。 | ★★★★ | XS | ✅（批次 B） |
| I2 | **访客地理聚合（隐私安全版）** | 不记 IP，但可以只记"省级归属"一个字节？——**❌ 放弃**：与移除 IP 的隐私立场冲突，聚合计数已有 visit_daily 够用。 | - | - | ❌ |
| I3 | **自动备份** | cron 每日把 D1 关键表（notes/messages/checkins）导出 JSON 存 KV 轮换 7 份。D1 免费档无自动备份，这是数据安全的最大空洞。 | ★★★★★ | S | ✅（批次 E/F） |
| I4 | **图片压缩上传** | 后台上传头像/封面时前端 canvas 压缩（长边 1280 + WebP）再传，省 KV 配额（KV 单文件 24MB 上限，坑 6）。 | ★★★ | S | ⬜ |
| I5 | **CI 校验** | GitHub Actions：push 时自动跑 check-code.mjs + `node --check` 全部 functions。防手滑推坏代码。 | ★★☆ | XS | ✅ 批次 K |

### 1.6 内容轴（站点是作品集，内容也要养）

| # | 创意 | 说明 | 价值 | 工作量 | 状态 |
|---|------|------|------|--------|------|
| C1 | **关于页作品展示区** | 把 YHuo 的技术亮点（pjax 音乐不断播、AI 多协议流式、课表提醒链路）做成卡片展示——面试/社交场景直接甩链接。 | ★★★ | S | ✅ 批次 K（docs/highlights.md 上站） |
| C2 | **友链页** | 博客圈传统：/links/ 页互挂友链（数据 D1，后台管理，前端卡片墙+悬停预览卡复用）。 | ★★★ | M | ⬜ |
| C3 | **随笔写作规范** | notes.json/后台随笔已双轨；补一份《怎么写随笔》进文档区（含 md 语法支持范围），降低未来自己的维护成本。 | ★☆☆ | XS | ⬜ |

---

## 2. 今晚执行计划（2026-09-09 23:10 → 2026-09-10 05:04+，硬性满 6 小时）

### 2.0 选取原则

- 一晚上要**全部落地并验证**，所以选「骨架价值高 + 互相独立 + 出问题不互相牵连」的组合；
- 四个批次全部符合站点既有审美与工程约定（语义变量/vanilla 零依赖/reduced-motion/坑 20~33 规避）；
- **按站内约定：本地提交，不主动推送**（交接文档 §10：用户明说才推）。

### 2.1 批次 A：游戏页 /games/（第九页）—— K1

范围：2048 / 贪吃蛇 / 记忆翻牌 三款 + 第九页接入全流程。
关键集成点（勘察已确认）：
1. 新建 `games/index.html`：拷任意外壳，`<html data-page="games">`、标题「游戏 - YHuo」、nav 该项 active；
2. 八个 HTML 各加顶栏项 `<a href="/games/" class="nav-link" id="gamesBtn">游戏</a>` + 窄屏抽屉一项（**九处同步，坑 23**，check-code 步骤[4]兜底）；
3. `assets/common.js`：PAGE_ROUTE（:239）/PAGE_TITLES（:240）/PAGE_MODULES（:6663）/NAV_INFO（:7073）/搜索快跳（:6463）五处；
4. `check-code.mjs:20` PAGES 数组加 games；
5. 游戏逻辑**内联在 games/index.html 的 main 里**（随页面装卸，像课表页 initSchedPage 模式）或 PAGE_MODULES.games.init 内——实现者按现有代码风格二选一，必须 pjax 进出可重入（init/destroy 成对，定时器/监听摘除，坑 24/27）。
游戏细节要求：键盘 + 触屏双操作；最高分 localStorage（键 `yhuoGame2048`/`yhuoSnake`/`yhuoCard`）；暂停/重开；窄屏单列三卡；颜色全走语义变量；`prefers-reduced-motion` 关过渡动画。

### 2.2 批次 B：PWA + SEO + RSS —— E2 + I1 + D2

范围：
1. **图标**：`tools/gen-icons.mjs`（node 内置 zlib 手写 PNG 编码器，零依赖）生成 `assets/icons/icon-192.png / icon-512.png / icon-maskable-512.png / apple-touch-icon.png / favicon.png` + `favicon.svg`；设计：陶土色（#b0532b）圆角方块 + 白色「Y」几何构成；
2. **manifest.webmanifest**（根目录）：name/short_name/theme_color(#b0532b)/background_color(#f8f7f4)/display:standalone/icons（含 maskable）；
3. **sw.js**（根目录，手动注册）：版本常量 `YHUO_SW_V`；HTML 导航请求 **network-first**（超时 3s 回缓存，缓存回源更新）——绝不供陈旧页面，保住"热修 max-age:0"工作流；`/assets/*` SWR（stale-while-revalidate）；`/api/ /admin/ /media/` 一律不拦（passthrough）；`/music/ /video/ /images/` cache-first + 上限 60 条 LRU；专属 `/offline.html` 兜底页（自足样式，站不了外壳）；`activate` 清旧版本缓存；**九页 head 不加注册脚本**——注册放 common.js 启动段（IIFE 尾部，`window.self===window.top` 才注册，坑 29）；
4. **SEO**：九页 head 补 `<meta name="description">` + Open Graph 三件套（og:title/og:description/og:type）+ `<meta name="theme-color">`（深浅两枚 media）+ favicon/apple-touch-icon 链接 + manifest 链接 + RSS alternate；这批 head 改动与批次 A 的 nav 改动**合并在同一批页面同步里做**（避免九页跑两遍）——执行顺序：先 A 后 B，B 只补 head；
5. **robots.txt + sitemap.xml**（静态，列出九页 + 优先级）；
6. **RSS**：`functions/feed.xml.js` GET 输出 RSS 2.0 XML：`ensureSchema` → D1 notes 取 50 条（title=日期+天气、description=正文转纯文本）→ 空库回落解析 `notes/notes.json`；channel 信息挂站点名 + 自定义域名 `https://190963.xyz/`。

风险与对策：SW 是今晚最大风险源——本地必须实测「有 SW 时强刷拿到新 HTML」「断网打开已访问页回退 offline.html」「/api 不被缓存」三条；出问题即降级（SW 只留 offline 兜底，砍 SWR）。

### 2.3 批次 C：音乐频谱可视化 + 每日壁纸 —— E1 + D1

范围：
1. **可视化**：common.js 尾部新 IIFE（对齐链接预览卡模式，免外壳同步）：
   - common.js 迷你条 audio 是闭包变量（:715 `var audio = new Audio()`）→ 暴露 `window.__miniAudioEl = audio`；
   - blog-player 的 audio 用 `[data-music-audio]` 查询；
   - 点击播放器上的音符/频谱小按钮（JS 注入）弹出频谱面板（canvas，主题色渐变条 ×48，requestAnimationFrame 绘制）；
   - **铁律**：`AudioContext` 在用户点击面板时才创建；`createMediaElementSource` 后立刻 `connect(analyser)` 并 `analyser.connect(destination)`，Context 全程不 close；两路 audio 各建 source，谁最后播放画谁（对齐 lyricTimeSource 思路）；任何一步抛错 → 面板显示「此浏览器不支持」且**不影响播放**；
   - `window.self!==window.top` 直接不初始化（坑 29）；`visibilitychange` 停 rAF；reduced-motion 默认不开且按钮隐藏。
2. **每日壁纸**：`functions/api/wallpaper.js`（GET，公开）：fetch Bing 接口拿当日图 URL+版权 → `json({ok,url,copyright})`，服务端 `caches.default` 缓存 6h；`functions/api/wallpaper/image.js`：`?u=` 只允许 `^https://.*\.bing\.(net|com)/` 白名单，流式回传图片字节 + `Cache-Control: public, max-age=86400`；前台外观抽屉背景选择器 openBgPicker（common.js:1862）内 JS 注入「🏠 今日壁纸」首格按钮 → 点击 fetch 接口 → 把背景设为 `/api/wallpaper/image?u=...`（走本地存档机制，与自定义背景同路径）+ toast 显示版权。

### 2.4 批次 D：站点数据卡 + 彩蛋 + 路线图上站 —— D3 + K7 + D5(精简版)

范围：
1. `functions/api/summary.js`（GET，公开）：`ensureSchema` 后并发查 visits 总数、visit_daily 当日、notes/messages/media/users/checkins 计数，返回 `{ok, data:{totalVisits, todayVisits, notes, messages, tracks, users, checkins, days}}`（days=自 2026-08-29 建站天数，服务端算）；
2. `docs/index.html` main 顶部加「站点数据」卡（骨架占位，fetch 后填充数字；失败整卡隐藏）；数字滚动动画复用后台既有风格、reduced-motion 直接显示；
3. **彩蛋**（common.js 尾部 IIFE）：Konami 秘技触发流星爆发（临时把流星密度拉满 6 秒）+ 彩带 canvas + toast「🎮 你发现了彩蛋！」；`blur` 时 `document.title = ':( 回来看看嘛…'`、`focus` 恢复原标题；两处都加 `window.self===window.top` 守卫（坑 29，预览 iframe 不许抢戏）；
4. **路线图上站**：本档创意库精简为 `docs/roadmap.md`（《灵感池与路线图》，公开展示未做想法的标题+一句话描述，勾掉已做），登记进 `docs/docs.json`——访客能在关于页看到站点未来。

### 2.5 收尾（03:30 后）

1. 全量 `node check-code.mjs`；`node node_modules/wrangler/bin/wrangler.js pages dev . --d1 DB --kv MEDIA --port 8799` 起本地全功能环境，curl 验：/api/wallpaper、/api/wallpaper/image、/api/summary、/feed.xml、/api/notes 常规回归；
2. 浏览器实测：九页导航/pjax/音乐跨页不断播回归 + 游戏页三款可玩 + 外观抽屉今日壁纸 + 频谱面板 + 关于页数据卡 + PWA DevTools 面板无报错 + 断网 offline 回退；
3. 更新《部署说明.md》（§3 目录/§4 新页与可视化/壁纸段/§6 接口表/§9 新坑若出现）+ `docs/changelog.md` + 本档状态列；
4. `git add` 精确列出文件（**绝不带** `0`、`访问量`、`WakeUp*`、`优选IP工具/`、`博客动画源码/`、`相册功能备份/` 等本地不入库项），按站内 commit 风格中文提交；
5. **不推送**（§10 约定），明早用户确认后 `推送更新.bat` 一键上线；
6. 满 6 小时（≥05:04）后关机（用户明示要求）。

### 2.6 备用创意池（批次提前完成时按序取用）

A1 对话导出（✅ 已取用，批次 E/F）→ K2 成就徽章（✅ 已取用，批次 E/F）→ E5 命令面板算数（✅ 已取用，批次 E/F，算数模式）→ I3 自动备份（✅ 已取用，批次 E/F）→ C1 作品展示区（✅ 已取用，批次 K，以 docs/highlights.md 形式上站）。
（每项独立、可随时中断，优先选不碰外壳的。）

### 2.7 执行结果（2026-09-10 凌晨补记）

- **落地四个 commit（全部本地提交，按 §10 约定未推送）**：8c131d4（批次 A~D：游戏页 / PWA+SEO+RSS+壁纸+频谱 / 数据卡+彩蛋+roadmap 上站）→ 34d4493（批次 E/F：导出 / 算数 / 番茄钟热力图 / 每日自动备份 / 成就徽章）→ 639dd50（批次 G/H：语音输入 / 人格预设 / 短链服务）→ c515e5d（批次 I：天气粒子 / 路人留言）→ 3b8533c（批次 J：随笔分享卡片，E6 ✅）→ 34148bd（批次 K：CI / 视差光斑 / 工程亮点文档上站，I5/E4/C1 ✅）→ 667c2d1（全量代码审查修复：SW 导航缓存失效等六处，详见 changelog）。
- **验证结论**：每批 check-code 全过（外壳比对扩到九页）；本地 wrangler（D1/KV 模拟）实测——14 条路由冒烟、壁纸全链路出网（元信息 → 白名单 400 → 图片代理 334KB）、/api/summary 聚合、/feed.xml 空库回落、备份全链路（未登录 401 / 管理员 POST 备份 594B / 清单 counts / 下载 attachment 头 / health 新字段）、短链全链路（自动码 / 非法 url 400 / 黑名单 400 / 302 两次 clicks=2 / 未命中 404 / 未登录 401）、路人留言（首条 ok → 429 → 列表 isGuest:true）。
- **浏览器实测要点**：九页导航与 pjax 回归、三款游戏可玩（2048 键盘+最高分、贪吃蛇运行）、频谱面板待机态、关于页数据卡渲染、今日壁纸设背景全过；Chrome 已出 PWA 安装提示。
- **未推送**：等用户明说后走 §2/§10 推送流程（推送更新.bat）。
- **后续（2026-09-10 上午）**：游戏页已按用户要求整体移除（K1 状态改 ❌ 放弃），前台九页回八页；批次 A 代码仍在提交 8c131d4 可找回。

---

## 3. 长期路线图建议（做完今晚之后的下一批）

- ~~下一批（内容+动态）：短链服务 D4 + 投票墙 D5 + 自动备份 I3~~ —— **D4/I3 已提前在今晚批次做完（✅），D5 做了一半（🔧，投票功能未做）**；
- ~~再下批（AI）：A1+A2+A6~~ —— **已提前在今晚批次做完（批次 E/F + G/H 全 ✅）**；
- **数据攒够后**：K3 历史上的今天（K5 番茄钟热力图已提前做完 ✅）；
- **有画图 key 后**：A5 AI 画图；
- **随时可插**：C2 友链页、D5 投票墙补全（E6 分享卡片 ✅、E3 天气联动粒子 ✅、I5 CI ✅ 均已做完）。

## 4. 红线（本档所有创意的公共约束）

1. 不引入任何构建步骤/npm 运行时依赖——纯 vanilla + CDN，保持"无构建直部署"特性；
2. 不碰隐私底线：不记 IP/UA/个体轨迹，公开数据只有聚合计数；
3. 所有新动效尊重 `prefers-reduced-motion`；
4. 颜色一律走语义变量（--apple-*），深浅主题自动跟随；
5. 外壳改动必须九页同步 + check-code 过；
6. 后台 admin/index.js 是模板字符串：禁反引号/`${`，正则反斜杠双写（坑 18）；
7. 每批完成必须：check-code 全过 + 本地实测真实变化（坑 24：不能只看元素存在）。


---

## 5. 2026-09-10 上午追加：疯狂档与技术档创意池（头脑风暴归档）

> 与用户三轮头脑风暴的完整记录，条目按「疯度」归档。做掉的标 ✅，其余随取随做。

### 技术档（部分已做）
- ✅ **资源指纹 + immutable 缓存**（批次 L：边缘注入方案，零构建）｜✅ **悬停预取**（Speculation Rules）｜✅ **首屏引导样式**（零漂移版，完整 critical CSS 评估后放弃）｜✅ **前端错误自动上报 RUM**（批次 L）
- （注：I1 SEO 基线中的 favicon 图标引用已于同日应用户要求移除，图标文件与生成器已删）⬜ View Transitions 全站 pjax morph；⬜ 多标签同步总线（SharedWorker：一处切歌处处切歌）；⬜ SQLite WASM 本地全文搜索（Ctrl+K 断网可搜）；⬜ WebLLM 离线 AI 助手；⬜ 语义搜索+相似阅读（CF Vectorize）；⬜ Whisper 语音随笔；⬜ 工具页 OCR（Tesseract WASM）；⬜ AI 自动 TL;DR+标签；⬜ CRDT 多端同步（Yjs：收藏/课表/设置跨设备实时）；⬜ 离线草稿队列（IndexedDB+自动提交）；⬜ 文章时光机（历史版本 diff/回滚）；⬜ 邮件即 CMS（post@域名 发邮件=发随笔）；⬜ 定时发布队列；⬜ 端到端加密留言（WebCrypto）；⬜ Passkey 登录；⬜ 公开 API 文档页；⬜ 网站心电图（SSE 实时日志）；⬜ 缓存 purge 按钮；⬜ 审计日志

### 疯狂档（体验/艺术/荒诞）
- 温柔系：站宠（像素生物+心情值）｜网站会长大（访问浇灌像素树+年轮）｜时间胶囊（一年后投递的信）
- AI 系：AI 数字分身（RAG 假我值班）｜网站每晚做梦（当日事件生成荒诞小故事）｜AI 台长电台（口播串烧）｜千人合唱团｜万物访谈录｜不存在的美术馆｜平行世界新闻台
- 游戏系：全站 RPG 化｜全站寻宝（藏金币换手写明信片）｜同屋檐光点（WebSocket 在线互见）｜AI 耻辱柱｜图灵测试时段｜预测市场｜剧本杀月常
- 艺术系：数据生成艺术每日一画｜千人一笔画｜网站博物馆（年度快照）｜YHuo 镇手绘地图｜访客星图
- 经济系：YH 币（签到互动赚，买皮肤/帽子/现实奖励）｜拍卖行｜打工系统（员工墙）｜功能股市（人气低的功能自动退市）｜站民议会（公投定案）
- 感官系：麦克风气流（吹散流星）｜触觉摩斯密码｜打字音效包｜每篇随笔专属旋律｜环境音分层
- 现实渗透：树莓派本体+桌面屏｜电子墨水屏分身｜NFC 碰一碰｜热敏打印机留言纸条｜网站陪你旅行（定位打卡勋章）｜日出晨语｜4 月 1 日复古日｜失踪嘲讽模式（AI 代管）
- 荒诞系：角落的老鼠（全站唯一成就）｜cookie 真的是饼干｜网站有重力（matter.js 抛掷卡片）｜俯瞰小镇视角｜互换孵蛋友链｜电梯入口与负一层｜404 收集册｜站史神话连载｜NPC 酒保
- 时间/温情：慢速蜗牛模式｜网站日晷（配色随太阳）｜每日一词｜倒着活的一天｜未来访问预约｜树洞回声（陌生人强制互回）｜无人知晓的书｜凌晨限定皮肤｜网站握手（两站同时在线互亮灯）｜时间锁留言（100 天后可见）｜站长心情气象站｜声音门牌｜年度报告（Wrapped 长图）｜每日自我挑战｜数字遗产页｜纪念模式｜擦肩而过（同一秒的上一个访客）｜网站会老（glitch 老化+修复按钮）｜报错变成诗｜千手裁缝（访客 CSS 涂鸦）｜传话游戏｜YHuo 词典｜每日拆字｜命运转盘｜随机传送门

> 状态说明：以上条目均为头脑风暴原始记录，未排期；挑中即做。游戏页（原批次 A）已于 2026-09-10 上午应用户要求移除（K1 翻 ❌）。
