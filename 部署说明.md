# YHuo 个人主页 · 交接文档

> 更新时间：2026-08-30（晚，含工具合集/文档区/AI 对话实装/个人主页/收藏/相册后台改版/管理员欢迎浮窗）。供新会话/新人快速接手项目的完整说明。
> 快速上手：改前台 → 只动 `index.html`；改后台 → 动 `functions/`；推送到 GitHub 后两个线上地址自动更新。
> 当前进度：本日上线的功能全部完成（见 §4/§5），AI 对话已实装（后台配置服务商 Key 即可用，见 §4 AI 助手段）。

## 1. 项目是什么

个人主页站点，两部分组成：

- **前台**：单个 `index.html`（全部 HTML/CSS/JS 内联，无构建、无运行时依赖），纯静态托管可直接跑
- **管理后台 `/admin`**：Cloudflare Pages Functions（`functions/` 目录即服务端代码，push 后随站点自动部署）+ D1 数据库 + KV 存储

功能总览：时钟首页（寄语/一言/天气胶囊）、图片画廊、工具合集（计算器/倒计时/密码生成/颜色转换）、文档区（Markdown 阅读）、AI 助手（多协议流式对话，后台配置服务商）、相册界面（分组的照片墙 + 收藏）、音乐播放器、视频轮播、访客/管理员登录、个人主页（资料/改密/收藏）、外观自定义、访问统计、媒体管理后台。

## 2. 线上地址与部署

| 平台 | 地址 | 说明 |
|---|---|---|
| GitHub Pages | https://shanfenti.github.io/YHuo/ | 关联 GitHub 仓库 main 分支，推送后约 1 分钟更新（无后台功能，前台自动回退静态模式） |
| Cloudflare Pages | https://yhuo.pages.dev/ | 关联同一仓库，推送后自动部署；后台/D1/KV 只在这边生效 |

- 仓库：https://github.com/ShanFenTi/YHuo（main 分支）
- git 身份（仓库级）：user.name=ShanFenTi，user.email=ShanFenTi@users.noreply.github.com
- 本网络 push 偶发失败（连接被重置/超时）：**重试几次即可**（本日多次实测 3~7 次内成功），实在不行开代理
- GitHub Pages 缓存约 10 分钟，部署后旧样子先 Ctrl+F5 强刷
- ⚠️ 不要在 GitHub 网页上直接上传/编辑文件（本地也在改会冲突；曾发生过，靠 rebase 化解）。已在网页改过的话先 `git pull --rebase origin main`

### 后台首次启用（新环境才需要，线上已配好）

1. Cloudflare 控制台 → Storage & Databases → D1 → Create（名字随意，如 `yhuo-db`）
2. KV → Create namespace（如 `yhuo-media`）。**不需要 R2、不需要绑卡**
3. Workers & Pages → yhuo → Settings → Bindings → Add：D1 绑定 Variable name 必须填 `DB`；KV 必须填 `MEDIA`
4. Deployments 里对最新部署点 Retry deployment（绑定只对新部署生效）

建表已自动化（首次访问接口时 `ensureSchema` 自动建表 + 老库补列），无需手动执行 `db/schema.sql`。首次访问 `/admin` 会要求创建超级管理员（仅一次，PBKDF2 哈希存 D1）。

## 3. 目录结构

```
index.html              前台全部代码（内联）
functions/              后台服务端（Cloudflare Pages Functions）
  admin/index.js        /admin 页面（单文件 HTML+CSS+JS）
  api/                  全部 REST 接口（见 §6）
  lib/                  auth.js 认证+限速 / migrate.js 自动建表 / util.js 杂项 / ai.js AI 上游代理
docs/                   前台文档区内容
  docs.json             文档清单（手工维护：加文档 = 放 md + 在此加一条）
  about.md              《关于本站》
  changelog.md          《更新日志》（每次上线建议补一条）
db/schema.sql           建表 SQL（仅参考，实际自动建）
music/ video/ images/   静态媒体兜底（前台与后台内容合并显示）
music/playlist.json     静态音乐清单（生成歌单.bat 维护）
video/playlist.json     静态视频清单（同上）
images/manifest.json    静态图片清单（生成图片清单.bat 维护）
node_modules/           wrangler（本地调试用，已 gitignore）
*.bat                   双击工具：启动网页/停止服务器/推送更新/生成歌单/生成图片清单
```

## 4. 前台功能明细

### 顶栏导航（重要机制）

顶栏 = **首页 | 工具 | 文档 | AI | 相册**。只有「首页」是滚动锚点；**工具/文档/AI/相册四个是互斥的全屏界面**：

- 打开一个会自动收起其他已打开的全屏层（含个人主页下拉），并接管顶栏高亮 + 锁背景滚动（`body.overflow=hidden`）
- 关闭（✕ 或 Esc）后恢复按滚动位置的高亮（`onScroll()`），滚动解锁
- `onScroll` 的滚动高亮有保护条件：任一全屏层打开期间不抢高亮——**新增全屏层必须同步改四处**：各 open 函数的互斥 id 列表、onScroll 保护条件、Esc 处理、`navLinks`（有 .nav-link class 即自动生效）
- 文档阅读层（docViewer，z=175）叠在文档界面（docsView，z=150）之上：Esc 只关阅读层；点「首页」收起所有全屏层

### 首页

- **时钟**：本地时间（毫秒级）+ 时段问候语 + 视频（静音自动播放）+ 寄语
  - 寄语优先级：管理员后台设置的"主页寄语" > 每日一言（hitokoto，当日缓存 localStorage）
  - **天气胶囊**：日期下方显示 ☁️ 城市 温度·天气 最高/最低温；Open-Meteo 免费接口（无需 key，缓存 30 分钟）；位置优先级 = localStorage 手动选择 > IP 定位（ipwho.is → geojs 备用 + bigdatacloud 反查中文城市名）> 默认北京；点击胶囊换城市（弹窗：内置 50 城市坐标表本地匹配 + Open-Meteo 在线搜索合并，在线接口连不上时本地表兜底）；浏览器定位按钮；接口失败静默隐藏（纯前端实现，GitHub Pages 也可用）
- **图片画廊**（#gallery section，hero 下方）：折叠牌堆式轮播（4 秒切换、悬停暂停、左右按钮），照片带确定性随机倾斜；点击任意照片进入相册界面
- **页脚**：运行时长（自 2026-08-29 起）+ 访问计数（每浏览器会话计一次，sessionStorage 去重，POST /api/visit）

### 工具界面（toolsView 全屏层）

图片画廊不在这里（在首页）。工具合集为 2×2 卡片，纯本地无依赖：

- **计算器**：四则+括号，求值只放行 `[0-9+\-*/().%\s]`（Function 求值前白名单过滤，杜绝代码注入）
- **倒计时**：1/5/25 分钟预设、开始/暂停/重置，到点 Web Audio 响铃三声 + 数字闪烁
- **密码生成器**：crypto 随机 8~32 位、可含符号、排除易混淆字符（0O1lI）、一键复制
- **颜色转换**：取色器 + HEX/RGB 实时显示，点值复制

### 文档界面（docsView 全屏层）+ 阅读层（docViewer）

- 文档卡片列表读 `/docs/docs.json`（手工维护清单——静态托管无目录列表，同坑 1；加文档 = 放 md 进 docs/ + 在 docs.json 加一条 {file,title,desc,date}）
- 点卡片进全屏阅读层（叠在文档界面上），自写迷你 Markdown 渲染：标题/列表/引用/代码块/粗斜体/链接/图片/分隔线；**先整体 HTML 转义再解析，防 XSS**
- 预置《关于本站》《更新日志》；每次上线建议给 changelog.md 补一条

### AI 助手（aiView 全屏层，已实装真模型对话，供应商+多模型）

- **架构**：前端永不接触服务商 Key。后台"AI"标签页管理**模型供应商**（每个供应商独立：名称/协议/Base URL/Key/系统提示词/启用状态，可挂**多个模型**；存 site_settings 表 `ai_models` 供应商数组 + `ai_default` 默认模型键"供应商/模型" + `ai_enabled` 全局开关），对话统一走服务端代理 `POST /api/ai/chat`（流式）
- **协议**：每供应商 `openai`（OpenAI 兼容格式，GLM/DeepSeek/Kimi/Gemini 兼容端点等都是这个格式）或 `anthropic`（Claude 原生）；Base URL 留空用协议官方默认；OpenAI 兼容 = base + `/chat/completions`，Anthropic = base + `/messages`
- **流式**：后端把上游 SSE 归一化成统一格式（`data: {"delta":"…"}` 行 / `{"error":"…"}` / `[DONE]`）再转发，前端只解析这一种，加新协议不用动前端
- **前端行为**：打字机效果、多轮历史（内存，**关闭界面或点"新对话"（＋按钮）即清空**）、回复用文档区 mdToHtml 渲染 Markdown（先转义防 XSS）；**模型切换在输入条底部工具行**（纯文字+⌄，点击向上弹菜单：按供应商分组、当前项 ✓、底部"管理模型"跳后台；只影响之后的回复、历史延续）；未配置/未启用时显示"接入中"文案；未登录点发送弹登录卡片
- **鉴权与限速**：/api/ai/chat 要求前台用户会话**或**管理员会话（管理员不注册前台账号也能聊）；每用户每分钟 20 条（内存滑动窗口，跨 Worker 隔离实例为近似值）；服务端截断单条 4000 字、最多 20 条
- **模型键回落链**：指定"供应商/模型"失效 → 后台默认模型 → 第一个可用（pickModel 找不到不静默回落，由 chat.js 逐级尝试）
- **历史兼容**：更早的单配置键、再到单模型档案，读取时自动迁移为供应商格式；首次保存后落新格式并清老键

### 相册界面（顶栏"相册"或点画廊照片进入）三层结构

1. 牌堆封面：每个相册一张卡，前三张照片微旋转叠放，悬停扇形展开，最前面的照片每 4 秒交叉淡入淡出轮播（悬停/展开/后台标签页时暂停）
2. 点击展开：相册卡占满整行，照片墙 `max-height:0` 平滑撑开
3. 照片墙：拍立得白框 + 随机倾斜 + 错落入场（间隔 70ms）+ 悬停 spring 回正微放大 + 卡片 3D 倾斜/聚光灯；竖图自动 4:5
4. 灯箱：计数、左右切换、键盘 ←→/Esc（Esc 逐层退出：先灯箱后相册）；登录后右上角 ♥ 收藏照片
- 分组：按后台 `media.album` 分成多个牌堆，未分组排最前；没建相册时整站一组"全部照片"

### 音乐迷你播放器

底部悬浮，播放列表、顺序/随机/单曲循环、进度条；列表每行尾 ♥ 收藏音乐（本地文件夹 blob 曲目不可收藏）。

### 外观设置抽屉（右侧）

主题色 5 选 1、自定义背景图（本地 localStorage）、背景变暗、两个动效开关（大卡片 3D 跟随 / 小图片倾斜）——开关真实控制所有相关动效，且都受 `prefers-reduced-motion` 尊重。

### 登录与管理员

- 登录卡片：开放注册（用户名 2-30 字符 + 密码 ≥6 位），服务端 PBKDF2
- **管理员账密登录后留在首页并弹出顶部欢迎浮窗**（👑 欢迎回来，管理员 XXX + "进入后台"一键跳转，6 秒自动消失；仅依据登录响应里的 admin:true 判断并在 sessionStorage 打 adminWelcome 标记，刷新不重复弹，普通账号登录/退出时清标记；**绝不能用 /api/auth/status 判断"是不是管理员"**——它只反映浏览器有无管理员会话 Cookie，分不清当前登录的是谁，见 §9 坑 11）
- 未登录/游客点头像 → 弹登录卡片；首次到访先查 /api/user/me 静默恢复会话

### 头像与个人中心下拉菜单

- 顶栏人像按钮三态：未登录人形图标 / 已登录用户名首字徽章 / 有头像显示圆形头像图
- 已登录点头像 → 下拉菜单在头像下方缩放淡出（右缘/顶缘 JS 对齐头像按钮留 8px，窄屏 12px；再点/点空白/Esc 收起）
- 菜单项：我的主页 / 更换头像 / 移除头像（没头像时隐藏）/ 退出登录（红色危险项）
- 登录态三键存 sessionStorage（demoLogin / demoLoginName / demoLoginAvatar），退出/游客清空；头像存 KV `avatars/u{userId}-{随机hex}.{ext}`，JPG/PNG/GIF/WebP ≤2MB 前后端双校验，换图删旧

### 个人主页（profileView 全屏层）与收藏

- 下拉菜单"我的主页"进入；身份卡（88px 头像可点更换、用户名、已加入天数/注册日期/上次活跃）+ 修改密码（旧密码验证+新密码≥6位+两次确认，服务端限速同登录）+ 更换头像/退出登录
- **收藏**：相册灯箱 ♥ 收藏照片、播放列表 ♥ 收藏音乐；未登录点 ♥ 弹登录卡片
- 收藏以**站点内路径（pathname）为标识**，静态文件与后台媒体统一处理，不与 media 表关联（媒体删除后收藏自然失效，主页渲染时静默过滤）
- 个人主页聚合展示：收藏照片（点击→关主页→回相册开大图，走 yhuo:open-photo 自定义事件）、收藏音乐（按歌名回曲库定位播放，找不到禁用）
- 收藏状态源是 IIFE 级 `favSet`，登录/注册/退出时经 `favLoad()` 同步
- 已知边界：管理员账号不在 users 表，个人主页显示"未登录"且收藏/改密不可用（想收藏就注册个前台账号）

## 5. 管理后台功能明细（/admin）

**布局**：左侧图标侧边栏（概览/音乐/视频/图片/用户/外观/AI，可收起，窄屏折叠成菜单按钮）+ 顶栏页标题；黑白主题（侧边栏底部切换，本地记住）；提示用 toast、确认/输入用自定义弹窗（ask，不依赖原生 confirm/prompt）。单文件实现。**图标体系**：JS 顶部 `ICO` 常量集统一提供线性 SVG（16px/stroke2/currentColor），行内操作钮一律用图标（✏️▶👁↑↓⋯★ 等文本字符禁用——不同系统渲染不一致且发虚）；状态色走 `--ok/--warn/--danger` 变量（深浅主题各一档）；列表行内操作桌面悬停浮现、触屏常显。注意 `image-shell` 里包着媒体列表（第 362 行有全局 `[hidden]{display:none!important}`），switchPage 只收起 `.album-side`、外壳必须常显，否则音乐/视频列表会消失（踩过已修）。

**概览标签页**：统计区（6 张卡：音乐/视频/图片/注册用户/总访问量/今日访问）+ 访问趋势柱状图（近 14/30 天切换，悬停看单日，SVG 无依赖；按天明细从上线起积累）。

**音乐/视频/图片标签页**：
- 上传：多选文件 + 拖拽文件/整个文件夹，队列逐个传、总进度条、当前文件名；同名（带/不带扩展名都比对）、超 24MB、格式不符自动过滤并在结果里说明
- 列表：搜索（搜索/筛选视图下隐藏排序控件）、批量勾选删除、✏️ 改名、大小+上传时间、音乐 ▶ 试听 / 视频 ▶ 预览 / 图片 👁 查看（弹窗，Esc/遮罩关闭）
- 存储用量条：全部媒体合计 / 1GB，超 80% 提示清理
- 拖拽或 ↑↓ 排序
- **相册侧栏（仅图片页）**：左侧相册栏（全部图片/未分组/各相册，带张数徽标）；点击筛选；**拖图片到相册名上即归类**（勾选状态下拖任意已选行=整批移动；与列表内拖拽排序共存：落在侧栏=归类、落在行上=排序）；勾选后点相册名=批量移入（弹确认，移完自动清勾选）；每相册 ⋯ 菜单：重命名/**解散相册（不删图，图片回未分组）**；新建相册在侧栏底部（≤50 字）；每行仍有归属下拉；在某相册视图上传自动归入该相册（上传提示行会说明）
- **静态媒体自动同步**：打开后台自动对比三个清单（images/manifest.json、music/playlist.json、video/playlist.json），把静态文件夹里还没进后台的文件批量搬进 KV（每批 12 个），同名跳过；往静态文件夹加删文件后重跑对应 bat 并推送

**用户标签页**：列表（状态/注册时间/最后活跃）、搜索用户名、注册时间新→旧/旧→新切换、禁用（踢下线且无法登录）/解封/删除（不可恢复）。最后活跃 = 登录时 + 进主页时（me 接口 1 小时节流防频繁写库）。

**外观标签页**：全站默认主题色（5 色）、默认背景图（上传存 KV，换图自动删旧）、**主页寄语**（≤100 字，留空恢复每日一言）、**备份导出**（一键下载 JSON：全部媒体条目+访问地址+站点设置+访问统计；KV 文件本体不含，重要文件另行下载）。

**AI 标签页**：供应商管理界面（仿客户端模型设置页）——左侧供应商列表（立方体图标+绿点=启用/空心点=停用，选中高亮，底部"＋添加供应商"，上限 10 个）+ 右侧详情表单（名称✏️重命名、已启用绿徽标、禁用/启用按钮、右上🗑删除；Base URL；API 格式下拉 = 协议；API Key 带👁显示切换；系统提示词；**模型列表**每行 ☆设默认/✏️改名/🗑删除，＋添加模型内联输入，↻自动获取经服务端代理拉 `GET {base}/models`）+ 保存供应商/测试连接 + 全局启用开关。API Key 保存在 D1（明文），界面永不回显完整 Key（只显示尾 4 位），编辑时留空 = 保留原 Key。

**安全**：管理员登录限速——同一 IP+用户名连续失败 5 次锁 10 分钟（后台登录和主页管理员直登两个入口都拦），成功登录清零；用户自助改密的旧密码试错复用同套限速（scope=pwd）。

## 6. API 一览

前台（公开）：
- `GET /api/playlist` — 媒体清单（music/video/images，images 带 album）；失败返回 ok:false 前台自动走静态兜底
- `GET /api/settings` — 站点默认外观（accent/background/quote），缓存 60s
- `POST /api/visit` — 访问计数（总量 + 按天）
- `POST /api/user/{register,login,logout}`、`GET /api/user/me` — 前台用户（管理员账号从 /api/user/login 登入会发管理员会话并返回 admin:true；login/me 响应带 avatar）
- `POST/DELETE /api/user/avatar` — 用户头像（multipart file，JPG/PNG/GIF/WebP ≤2MB，存 KV avatars/，换图删旧）
- `GET /api/user/profile` — 登录用户资料（username/avatar/created_at/last_seen_at）
- `PUT /api/user/password` — 自助改密 {oldPassword,newPassword}，验证旧密码 + 登录限速复用（scope=pwd）
- `GET/POST/DELETE /api/user/favorites` — 收藏列表 / 添加 {type:'image'|'music',url,title} / 移除 {url}；url 必须是站点内路径，同 url 唯一（INSERT OR IGNORE 幂等）
- `GET /api/ai/config` — AI 可用状态 {enabled, model(默认键), models:[{provider,name,key}]}（不含任何敏感信息，key="供应商/模型"供切换回传）
- `POST /api/ai/chat` — 流式对话代理（见 §4 AI 助手段；需登录，入参 {messages, model?}，model=key、缺省/失效回落默认；响应 text/event-stream）
- `GET /api/health`

后台（`/api/admin/*` 有 `_middleware.js` 会话门卫，未登录一律 401）：
- `GET/POST /api/admin/media` — 清单 / 重排 {type, ids}
- `PUT/DELETE /api/admin/media/:id` — 改 title 或 album / 删除（连 KV 文件）
- `POST /api/admin/upload` — multipart 上传（type/file/title?/album?），单文件 ≤24MB
- `POST /api/admin/import` — 静态媒体批量导入 {type, files:[{title,url}]}（≤12/批）
- `POST /api/admin/albums` — {action:'rename'|'delete', from/to/name}
- `GET /api/admin/users`、`PUT/DELETE /api/admin/users/:id` — 用户管理
- `GET/PUT /api/admin/appearance` — accent/quote；`POST/DELETE /api/admin/appearance/background` — 默认背景图
- `GET /api/admin/ai` — 供应商列表（Key 不回传）；`PUT /api/admin/ai` — {enabled} 全局开关（**仅无 action 时生效**）/ {action:'save',provider} 新增更新（含 models 数组；api_key 留空=保留原 Key）/ {action:'rename',from,to} / {action:'toggle',name,enabled} 单供应商启停 / {action:'delete',name} / {action:'default',key}；`POST /api/admin/ai/test` — 测试连接 {key 或 name}（真发一条消息）；`POST /api/admin/ai/models` — 拉取服务商模型列表（{name?,protocol?,base_url?,api_key?} 草稿值优先，api_key 留空回落指定供应商已存 Key；不落库）
- `GET /api/admin/visits` — 总量/今日/昨日/近 30 天明细
- `GET /api/auth/{status,logout,setup}`、`POST /api/auth/login`（限速生效）

会话体系（互不影响）：管理员 Cookie `yhuo_session`（7 天，HttpOnly/Secure/SameSite=Strict）；前台用户 Cookie `yhuo_user`（30 天）。

## 7. 数据库表（D1，自动创建）

| 表 | 用途 |
|---|---|
| admin_users | 管理员（PBKDF2 + salt） |
| sessions | 管理员会话 |
| users | 前台用户（banned / last_seen_at / avatar_key） |
| user_sessions | 前台用户会话 |
| media | 媒体条目：type(music/video/image)、title、r2_key(KV键)、mime、size、sort_order、**album**(相册名，''=未分组)、created_at |
| site_settings | key-value：visits(总访问)、accent、bg、quote、ai_models(AI 供应商数组 [{name,protocol,base_url,api_key,enabled,system_prompt,models[]}])、ai_default(默认模型键"供应商/模型")、ai_enabled（AI 助手配置） |
| visit_daily | 按天访问计数（北京时间 YYYY-MM-DD） |
| login_throttle | 登录限速（key=scope:ip:username，fails/locked_until） |
| user_favorites | 前台用户收藏：type(image/music)、url(站点内路径，UNIQUE user_id+url)、title |

## 8. 本地开发与调试

- **只看前台**：双击 `启动网页.bat` → http://localhost:8000（python http.server；`停止服务器.bat` 停止）
- **带后台完整调试**（推荐）：
  ```
  node node_modules/wrangler/bin/wrangler.js pages dev . --d1 DB --kv MEDIA --port 8799
  ```
  - 本地 D1/KV 状态在 `.wrangler/`（已 gitignore，可随时删掉重置本地数据；当前本地库有测试账号 admin/admin12345 与 testuser）
  - wrangler 装在本地 node_modules（已 gitignore）；重装：`npm install --no-save wrangler`
  - 本地测试时管理员账号在 .wrangler 重置后要重新 setup
  - 停 wrangler 注意杀干净：TaskStop 后常残留 node/workerd 进程锁着 `.wrangler/` 的 sqlite 文件，删库前先 `taskkill` 掉（按 8799 端口或进程名找）
- **改 `index.html` / `functions/admin/index.js` 后**：内联 `<script>` 不受 node --check 检查，需提取后 `new Function()` 校验（或直接浏览器打开看控制台）
- **改 `functions/` 的 ESM 文件后**：本地 package.json 没有 "type":"module"，直接 `node --check` 会报 import 错误——拷成 `.mjs` 临时文件再查
- **改了音乐/视频/图片静态文件后**：重跑 `生成歌单.bat` / `生成图片清单.bat` 再推送
- Windows 控制台 curl 发中文 JSON 会乱码（GBK 终端问题）：用浏览器或 python 写 UTF-8 文件再 `--data-binary @file` 测

## 9. 已知坑（历史修复记录，改代码时别踩回去）

1. **静态托管无目录列表**：music/、video/、docs/ 目录在 GitHub Pages 返回 404，必须有清单文件兜底（docs/ 用 docs.json，加文档记得同步清单）
2. **Cloudflare SPA 回退**：对不存在路径返回 200 + 首页内容（不是 404）。解析目录列表"没有音频/视频"时必须抛错转清单兜底，不能静默 return
3. **`/media/*` 的两个坑**：KV 读取必须用 getWithMetadata（拿 mime）；catch-all 路由的 params.key 是数组
4. **import.js 相对路径坑（2026-08-30 修复）**：清单里的相对路径（`images/1.jpg`）必须按**站点根**解析；直接拿 `request.url` 当 base 会落到 `/api/admin/*` 被会话门卫 401，静态同步"全部跳过"且无报错
5. **空相册不落库**：相册列表由 media.album 派生，新建的空相册靠前端会话级 `extraAlbums` 数组保持可见，刷新后空相册消失属预期（移入图片后才真正存在）。根治需建 albums 表（见 §11）
6. **KV 限制**：单文件 24MB（接口已拦），总量 1GB；GitHub 单文件 <100MB、仓库建议 <1GB
7. **浏览器自动播放**：只允许静音自动播放，首页视频 muted+controls
8. **换行符**：仓库在 Windows 检出为 CRLF，写正则匹配 `<script>` 块时要兼容 `\r?\n`
9. **严格模式块级函数不外提（2026-08-30 踩过）**：前台主 IIFE 是 `'use strict'`，个人中心代码一度插在 `if (loginGate) { ... }` 块内，导致顶栏监听里调块内的 `setProfileOpen` 直接 ReferenceError（无任何可见报错，面板点不开）。此类跨块调用的函数必须放在块外；文件里已留注释
10. **authPost 回调签名**：`authPost(path, u, p, done)` 的 done 收到的是对象 `{ok, error}`，不是 `(ok, msg)` 布尔（注册回调曾因此把失败当成功，已修）
11. **判断"是不是管理员"只能用登录响应的 admin:true（2026-08-30 两次踩坑）**：/api/auth/status 的 authenticated 只看浏览器有无有效管理员会话 Cookie（7 天），分不清当前登录的是谁——曾导致普通账号登录被误跳后台，后来欢迎浮窗初版又因此对普通用户误弹。铁律：欢迎标记（sessionStorage adminWelcome/adminWelcomeShown）只由登录响应 admin:true 写入，页面加载只认标记，**任何身份判断都不要查 /api/auth/status**；前台退出登录只清用户会话，不清 yhuo_session
12. **外部接口的网络坑（2026-08-30）**：geocoding-api.open-meteo.com 在部分网络环境 curl 通但浏览器连不上（天气城市搜索曾因此空结果），已用内置 50 城市坐标表兜底；ipapi.co 返回 403，IP 定位改用 ipwho.is（备用 get.geojs.io）。改天气相关代码时别把本地城市表和备用源删掉
13. **全屏界面的互斥与高亮接管（2026-08-30）**：相册/工具/文档/AI 四个全屏层互斥靠各 open 函数收起其他层；顶栏高亮由 open 接管、close 里 `onScroll()` 恢复；onScroll 有保护条件防抢回。**新增全屏层漏改任何一处就会出现"高亮不切换/被抢回/两层叠放"**（AI 界面上线时漏了接管，文档界面上线时 onScroll 保护没加全，都踩过）。检查清单：互斥 id 列表、onScroll 保护、Esc 处理、关闭时恢复
14. **[hidden] 全局 CSS 等于永久隐藏**：动态显隐一律用 class 切换（.show 等），不要用 hidden 属性做闪烁类动画的开关
15. **AI 对话的几个注意点（2026-08-30）**：服务商 Key 只存 D1（site_settings `ai_models` 内），任何接口不得回传完整 Key；`/api/ai/chat` 的鉴权同时接受 yhuo_user 和 yhuo_session 两种 Cookie（管理员也能聊）；限速是内存 Map（跨 Worker 隔离实例为近似值，别指望它做精确计费）；前端解析流式响应前先关掉输入（aiBusy），失败时要把用户消息从 aiHistory 弹回去否则重试会重复；AI 界面关闭即清空历史（aiResetChat 会 abort 进行中的请求）
16. **AI 配置接口的两个坑（2026-08-30）**：① `functions/api/` **嵌套子目录**（如 `api/ai/`、`api/admin/ai/`）里的相对导入比顶层多一层——`../../lib/`、`../../../lib/`，config.js 因此重写时三次写浅被 wrangler 构建拦下（`node --check` 不解析 import 查不出来），**改完必须本地 wrangler 起一遍**；② admin/ai.js 的 PUT 里 `enabled` 字段是多义的（全局开关 vs toggle 动作参数），带 `action` 的请求必须忽略顶层 `enabled`，否则停用单个供应商会把整个 AI 关掉（踩过已修）

## 10. 行为约定

- **前台内容合并显示**：后台清单排前面，静态文件夹（music/、video/、images/）内容跟在后面，同名去重；后台某类完全没内容时该类只显示静态内容；任一来源不可用时自动只用另一来源
- **上传同名查重**：标题带扩展名和不带扩展名都算同名（静态同步进来的标题带扩展名）
- **推送**：优先由用户双击 `推送更新.bat`（git add + commit + push 一条龙）；助手会话改完代码要明确说"要推送哪些文件"。2026-08-30 起经用户同意助手也可直接推送
- **删库不可恢复**：后台删除同时删 KV 文件；解散相册只解除归属

## 11. 待办 / 扩展点

- 备份的**导入恢复**（配合已有导出，做恢复接口 + 后台入口）
- 用户密码重置（后台强制改密；前台自助改密已上线）
- 建 albums 表根治空相册不落库（顺带支持相册排序；当前靠 extraAlbums 会话内存兜底）
- 外观偏好跟随账号存服务端（涉及与 localStorage 的合并优先级，想清楚再做）
- 前台用户个性化：评论等（收藏已上线）
- 访问趋势：按小时粒度、来源统计（需记录更多维度）
- 相册封面自定义（当前用前 3 张）
- AI 扩展：本地持久化聊天记录（当前关闭即清）、多模型切换、对话长度/用量统计（限速目前是内存近似值）

## 12. 常用操作速查

| 想做什么 | 怎么做 |
|---|---|
| 改前台样式/功能 | 编辑 `index.html`，本地 8000/8799 端口验证 |
| 改后台界面 | 编辑 `functions/admin/index.js`，wrangler 8799 端口验证 |
| 加/改接口 | `functions/api/` 下按路径放文件，导出 `onRequestGet/Post/Put/Delete` |
| **给文档区加一篇文档** | md 放进 `docs/`，在 `docs/docs.json` 加一条 {file,title,desc,date}，推送 |
| 给更新日志补条目 | 编辑 `docs/changelog.md` |
| 上线 | `推送更新.bat`（或 git push），等 1-2 分钟，Ctrl+F5 |
| 重置本地后台数据 | 关 wrangler 进程后删掉 `.wrangler/` 目录重启（进程不杀干净文件会锁住） |
| 看线上数据库 | Cloudflare 控制台 → D1 → yhuo-db → Console |
| 校验前台内联 JS 语法 | 提取 `<script>` 内容后 `new Function()` 校验 |
