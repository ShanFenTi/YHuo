# 网站部署与维护速查（交接文档）

> 更新时间：2026-08-29。此文件供新会话快速了解项目与部署状态。

## 项目概况
- 项目路径：`C:\Users\31788\Desktop\3`
- 纯静态站点：所有 HTML/CSS/JS 都在 `index.html` 内联，无后端
- **新增（2026-08-29）**：管理后台 `/admin`（Cloudflare Pages Functions + D1 + KV），可登录后上传/管理音乐、视频、图片；`functions/` 目录是后端代码，`db/schema.sql` 是建表 SQL
- 功能：时钟首页、图片画廊（images/）、相册界面、音乐播放器（music/）、首页视频轮播（video/）、演示版登录弹窗、外观设置抽屉（主题色/自定义背景/动效开关）、管理后台

## 两个线上地址
| 平台 | 地址 | 部署方式 |
|---|---|---|
| GitHub Pages | https://shanfenti.github.io/YHuo/ | 关联仓库 ShanFenTi/YHuo 的 main 分支，推送后约 1 分钟自动更新 |
| Cloudflare Pages | https://yhuo.pages.dev/ | 关联同一 GitHub 仓库，推送后自动部署 |

## Git 信息
- 仓库：https://github.com/ShanFenTi/YHuo（main 分支）
- git 身份已配置（仓库级）：user.name=ShanFenTi，user.email=ShanFenTi@users.noreply.github.com
- 认证：首次 push 弹浏览器授权（Git Credential Manager）
- 本网络环境下 push 偶发失败（连接被重置/超时）：重试几次即可，或开代理

## 本地工具脚本（双击运行）
- `启动网页.bat`：启动本地服务器 http://localhost:8000（python http.server）
- `停止服务器.bat`：停止本地服务器
- `推送更新.bat`：一键 git add + commit + push（可带提交信息），推完两边线上自动更新
- `生成歌单.bat`：扫描 music/ 和 video/ 文件夹，重新生成 `music/playlist.json` 和 `video/playlist.json`

## 更新内容的标准流程
1. 本地改文件（或加图片/音乐/视频）
2. 加了音乐或视频 → 双击 `生成歌单.bat`
3. 双击 `推送更新.bat`
4. 约 1 分钟后两个线上站点生效；看不到变化就 Ctrl+F5 强刷

> ⚠️ **2026-08-29 起的新约定：推送一律由用户手动执行**（双击 `推送更新.bat`）。
> 助手（ZCode 会话）只负责改代码 + 本地验证，改完告诉用户"要推送哪些文件"，**不要自己 `git push`**。

⚠️ 不要在 GitHub 网页上直接上传/编辑 index.html（本地也在改会起冲突）。已在网页上改过的话，本地先 `git pull --rebase origin main`。

## 静态托管的关键坑（代码已适配）
1. **无目录列表**：GitHub Pages 对 `music/`、`video/` 目录返回 404，所以歌单/视频列表必须有 `playlist.json` 清单文件兜底（代码逻辑：目录列表优先 → 失败读清单）。
2. **Cloudflare Pages SPA 回退**：对不存在的路径返回 200 + 首页内容（不是 404）。代码在解析目录列表"没有音频/视频"时会抛错转走清单兜底，不能静默 return。
3. **缓存**：GitHub Pages 缓存约 10 分钟，部署后旧样子先 Ctrl+F5 强刷。
4. **自动播放**：浏览器只允许静音自动播放，首页视频是 muted+controls，访客可手动取消静音。
5. **文件限制**：GitHub 单文件 <100MB，仓库建议 <1GB。音乐/视频较大时注意。

## 管理后台（2026-08-29 新增，首次上线需手动配置 Cloudflare）

### 架构
- `functions/` 目录 = Cloudflare Pages Functions（服务端代码，push 后随站点自动部署）
- D1 数据库存账号、会话、媒体清单；**KV 存上传的音乐/视频/图片文件**（免费版无需绑卡；单文件上限 24MB，总量 1GB）
- `/media/<key>` 从 KV 读文件（手动实现 Range，播放可拖进度条；KV 自带边缘缓存）
- `/api/playlist` 给前台供清单；**拿不到时前端自动回退**到静态文件夹/playlist.json，所以 GitHub Pages 版和本地版不受影响
- 后端接口：`/api/auth/{status,login,logout,setup}`、`/api/admin/{media,media/[id],upload}`（有 `_middleware.js` 会话门卫）、`/api/playlist`

### 首次启用步骤（在 Cloudflare 控制台操作，共 4 步）
1. **建数据库**：控制台 → Storage & Databases → D1 SQL Database → Create，名字随意（如 `yhuo-db`）。**建表已自动化**：后台接口首次访问时自动建表，`db/schema.sql` 仅作参考，无需手动执行
2. **建 KV 命名空间**：Storage & Databases → KV → Create namespace，名字随意（如 `yhuo-media`）。**不需要 R2，也不需要绑卡**
3. **绑定到 Pages 项目**：Workers & Pages → yhuo → Settings → Bindings → Add：
   - D1：Variable name 必须填 `DB`，选上面的 `yhuo-db`
   - KV namespace：Variable name 必须填 `MEDIA`，选上面的 `yhuo-media`
   - （旧版控制台在 Settings → Functions → D1 database bindings / KV namespace bindings）
4. **重新部署**：Deployments 里对最新一次部署点 Retry deployment（绑定只对新部署生效）

### 启用后
- 访问 `https://yhuo.pages.dev/admin` → 首次会要求"创建超级管理员"（只有一次机会，账号存 D1，密码 PBKDF2 哈希）；**主页用管理员账密登录也会直接进后台**
- 界面：**黑白主题**（右上角可切换浅色/深色，选择存浏览器本地）；**靠左布局**；顶部统计卡片（音乐/视频/图片数量、注册用户数，单色线条图标）+ 四个标签页（音乐/视频/图片/用户）
- 音乐/视频/图片：上传（带进度条）、点击名称改名、**拖拽或 ↑↓ 排序**、删除；**图片列表带缩略图**
- **静态媒体全自动同步（无需任何手动导入）**：打开后台时自动对比三个清单（`images/manifest.json`、`music/playlist.json`、`video/playlist.json`），静态文件夹里还没进后台的音乐/视频/图片自动批量搬进 KV（每批 12 个），同步完直接可见可管理；同名跳过。清单由 `生成歌单.bat` / `生成图片清单.bat` 维护（往文件夹加删文件后重跑并推送）。
- 主页管理员登录跳转是**双保险**：登录成功后先查 `/api/auth/status` 确认会话身份，是管理员才 `location.href='/admin'`
- **外观标签页**：设置**全站默认主题色**（5 色）和**默认背景图**（上传存 KV，换图自动删旧文件）。存 D1 `site_settings` 表；前台通过 `/api/settings` 读取，**访客本地没自己改过外观时才采用**（改过以访客本地为准）
- 用户页：注册用户列表（用户名/状态/注册时间）、**禁用**（踢下线且无法登录）、解封、**删除**（不可恢复）
- 后台删的是 KV 里的文件，彻底删除不可恢复
- 本地 `music/`、`video/`、`images/` 静态文件保留作兜底；前台为**合并显示**：后台内容在前、静态内容在后、同名去重

## 当前待办
- 管理后台已上线可用：D1/KV 已绑定、表已自动创建、管理员账号已初始化、上传→前台显示链路已验证。
- ⚠️ 媒体服务 `/media/*` 曾有两个已修复的坑（KV 要用 getWithMetadata；catch-all 路由 params.key 是数组），记录在此避免以后改挂。
- 行为约定：前台内容为**合并显示**——后台清单排在前面，静态文件夹（music/、video/、images/）内容跟在后面，同名去重；后台某类完全没内容时该类只显示静态内容。任一来源不可用时自动只用另一来源。

## 登录功能（2026-08-29 起为真实账号体系）
- 主页登录卡片：**开放注册**（用户名 2-30 字符 + 密码 ≥6 位），注册/登录都走服务端 PBKDF2 验证，用户会话 30 天（Cookie `yhuo_user`）
- 用户账号存 D1 `users` 表，与管理员 `admin_users` 完全分开；管理员用户名不可被前台注册占用
- **主页输入管理员账密登录 → 自动跳转 `/admin`**（`/api/user/login` 对管理员发 `yhuo_session` 管理员会话并返回 admin:true）
- 接口：`/api/user/{register,login,me,logout}`；`me` 用于回访时凭 Cookie 静默放行，不再重复弹卡片
- 右上角人像按钮悬停显示当前用户名；游客进入仍然保留
- 管理后台（/admin）自己的登录是 `/api/auth/*`（Cookie `yhuo_session`，7 天），两条会话体系互不影响
- **用户还没有个性化功能**（收藏/评论等），属于后续扩展点；主页也没有登出按钮（需要时可清 Cookie 或以后加）
