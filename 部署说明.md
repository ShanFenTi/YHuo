# YHuo 个人主页 · 交接文档

> 更新时间：2026-09-11 晚间四（本行仅列大项：**后台：回退晚间三的窄屏顶栏布局，头像恢复原位靠左排列（用户要求），撤掉 margin-left:auto 一行，详见 changelog 2026-09-11（晚间四）**）；2026-09-11 晚间三（本行仅列大项：**后台：窄屏（≤900px）顶栏改「汉堡靠左、头像+动作钮靠右」——.header-pill .mark 加 margin-left:auto（仅窄屏媒体查询内，宽屏居中布局不动），胶囊 gap:6px 兜底最小间距，详见 changelog 2026-09-11（晚间三）**）；2026-09-11 晚间二（本行仅列大项：**后台：图片网格悬停操作钮仍 4+1 两行——b881dcf「只留图标+允许换行」治标不治本（5 钮一行需 ~186px > 158px 最小列可用 ~142px，必然换行），紧凑化（图标 14/padding 4×5/gap 3 ≈132px）+ 最小列 164px 让单行数学上必成立，四档视口逐卡量测全单行，详见 changelog 2026-09-11（晚间二）与 §5「媒体页上传区与图片网格」段**）；2026-09-11 晚（本行仅列大项：**前台：首页 hero 下新增「内容区」——最新随笔卡（/api/notes → notes.json 回落，条目 /notes/#日期 pjax 直达定位）+ 快捷入口磁贴（随笔/工具/AI/留言，ff-* 同口径隐藏）+ 站点数据行（/api/summary，失败整行隐藏）；顺带根治坑 36：毫秒时钟每 50ms textContent 替换文本节点把「滚动模糊揭示」的 80ms 防抖重扫饿死（换页回首页卡片永不显现、只能整页刷新），毫秒位改保留文本节点 nodeValue 写入，详见 changelog 2026-09-11（晚间）与 §4 首页段/§9 坑 36**）；2026-09-11（本行仅列大项：**后台 UI 七连改——①管理界面现代化：配色对齐前台暖白纸感/纯黑 + 陶土主题色（--brand），12 个功能页统一重排（页面标题区/分组卡/字段栅格/统一表格与列表行/骨架屏+图标空状态/自绘开关）；②输入细节打磨：控件主题化/字数计数器/搜索框升级/邮箱卡重排/Enter 提交/AI 表单统一字段体系；③上传区改居中式大拖放区；④选中文件绿色确认条 + 胶囊单独移除；⑤图片列表改缩略图网格 + 悬停操作层溢出修复（详见 changelog 2026-09-11 与 §5「UI 组件层」「媒体页上传区与图片网格」段）**）；2026-09-09（全天批次见 changelog；本行仅列大项：**全站代码审计第二轮 + 本地 wrangler 四档视口前后台 UI 实测**（通读全部代码后修 2 处功能性 bug——AI 历史抽屉「N 条」每轮保存后回落为 2 永不增长、AI 用量上报限流键误写 `user.id` 致所有前台用户共享 `'uundefined'` 一份配额；管理员课表文案修正；`.icon-*` 死规则清理约 42 行；本地 pages dev + D1/KV 铺测试数据后，四档视口 1920/1280/768/390 前台八页与后台宽窄两态 UI 实测全过，见 changelog 2026-09-09 与 §9 坑 32/33）；深夜自主批次（2026-09-09 23 时 → 09-10 凌晨，本地已提交 8c131d4/34d4493/639dd50/c515e5d/3b8533c/34148bd/667c2d1 尚未推送，见 changelog 2026-09-10 与 §3/§4/§5/§6 各新段）：**第九页 /games/ 游戏页 + PWA 可安装/离线 + RSS/SEO + 音乐频谱可视化 + 每日壁纸 + 站点数据卡/公开聚合统计 + 短链 /s/ + D1 每日自动备份 + AI 语音输入/人格预设 + 番茄钟热力图 + 签到成就徽章 + 天气雨雪粒子 + 路人留言 + 彩蛋两枚 + 《创意规划.md》与《灵感池与路线图》建档** → 2026-09-08 批次：**前台：外观入口改圆形毛玻璃小钮 + 自由拖动 + 修「点击展开后被带出屏外」+ AI 页隐藏**（40×40 圆形毛玻璃+调音推子图标替代 34×60 白色耳朵；拖到任意位置松手即停，位置按视口右/下距存 localStorage 跨刷新保持，窗口缩放回贴并 clamp 在屏内；位移 ≥5px 判拖动否则点击开关抽屉；曾挂在 .appear-dock 内被 translateX(100%) 带出屏外（用户实报「按钮靠左点击后左移出屏」），已挪到 body 下做真正视口固定定位——dock transform 劫持 fixed，坑 28 同机制；AI 页按 data-page 隐藏圆钮；实测多宽度+深浅+开合全过，见 §4 外观段）、**前台+后端：留言板显示留言用户真实头像**（GET /api/messages 带出 users.avatar_key，站长用后台设置的 admin_avatar；前端有头像渲染圆图、坏键/无头像/已注销回落首字块，img 绝对定位盖首字+error 移除回落；本地桩接口四条路径全过，见 §6 messages 行）、**全站代码审计：27 处暗病全部修复**（三路深读全部代码：前端 12 处——页脚裸 sessionStorage 杀主脚本/一言池缓存死代码/AI abort 后 aiBusy 永锁/scroll-spy 过期引用抹子页高亮/寄语僵尸轮播链/mdToHtml 引号逃逸注入/天气搜索竞态/七处浮层关闭定时器重开竞态/曲库全 404 不收口/home destroy 漏清歌词定时器/歌词 detached 引用白拉词/播放器切换双声；blog-player 2 处——不存在的协调器互斥失效换 __blogPlayerClosePanel/封面破图复现清 src；后端 9 处——用户登录无限速可爆破/解绑邮箱不关 2FA fail-open/验证码无 IP 上限可烧邮件配额锁死 2FA/设默认模型 includes 类型错误永久 404/chat.js 未定义变量吞错误详情/health 泄露 KV 键名管理员数/tick 错误密钥可刷写配额/AI 用量上报无频控/上传 title 未截断；另有 .to-top 死选择器/radiogroup 错误语义/13 输入框补 aria-label/重复规则清理；审计确认扎实面：admin 门卫/会话隔离/SQL 参数化/后台转义面/Key 尾 4 位/验证码 2FA 票据闭环/Cookie SameSite；见 changelog 2026-09-08 全清单）、**前台：顶栏窄屏改「汉堡+抽屉」（对齐后台）+ 阅读层接入滚动模糊揭示**（≤900px 后台同断点：胶囊收窄为 汉堡+logo+动作钮、八项导航进左侧毛玻璃抽屉（点遮罩/✕/选中/Esc 收起、锁滚动），>900px 胶囊导航不变，跨断点 matchMedia 复位；抽屉项 applyNavActive 同步高亮、功能开关 data-target 同口径隐藏、八页外壳同步；docViewer 阅读层正文（md 顶层块+更新日志按天）接入滚动模糊揭示——.doc-scroll 内滚单独挂监听、openDoc 注入经 #docViewer MO 重扫、关层不采集；实测宽窄两视口全过，见 §4 顶栏导航/外观段）、**前台：接入滚动模糊揭示（参考博客站 04 号特效）**（栏目卡进视口从模糊+下移逐个渐清晰，60ms 级联、不重播；目标限定 .page-main 内八类内容元素，外壳浮层 .apple-card 不参与；common.js 顶部独立 IIFE + main 上 MutationObserver 防抖重扫盖 pjax/异步插图；head 内联脚本打 fx-reveal 门控类（八页同步，坑 23），无 JS/reduced-motion/脚本加载失败 3 秒兜底全走「内容常显」；CSS 用 animation fill backwards 防残留 transform（坑 22 同源）+不压 hover 过渡；测试注意：本地无 cache-control 资源走启发式缓存，改完必须 Ctrl+F5，见 §4 外观段）、**前台：修未登录首次访问不再自动弹登录卡（用户要求）**（原首访无 sessionStorage 登录记录就锁滚动弹登录卡、/api/user/me 查无会话 300ms 后浮出；改为一律不弹——登录卡立即 hide+hidden，/api/user/me 只做静默会话恢复（Cookie 有效照样恢复登录态+头像+课表页钩子），无会话/失败保持匿名浏览不锁滚动；点顶栏头像与需登录动作（留言/收藏/AI 发送/课表页）照常弹卡；预览 iframe 缩略图从此不在帧内弹卡；校验全过+本地浏览器五场景实测，见 §4 登录段）、**后台+前台：随笔接入后台管理**（D1 notes 表 + 后台第十一项「随笔」标签页（新增/编辑/删除/从静态清单导入，两段式删除确认），公开 GET /api/notes + 后台动作式 POST /api/admin/notes（create/update/delete/import），前台数据源链改 /api/notes → 失败或空库回落 notes/notes.json；本地 wrangler 全链路 curl+浏览器实测（导入去重/校验 400/401 门卫/UI 三操作/悬停预览卡/前台联动），已推送上线并线上验证，见 §4 随笔段/§5）、**前台：新增随笔页 /notes/（第八页，对齐参考博客站 /notes/ 的随笔时间线）**（notes.json 时间线按年份分组，单条 = 日期 + 天气/时段胶囊 + 正文（mdToHtml 渲染、先转义防注入），锚点 #日期 直达/分享；顶栏八项 首页|随笔|工具|AI|留言|课表|预览|关于，外壳八页同步（坑 23），NAV_INFO 预览卡/搜索快跳/check-code 八页校验同步纳入；已推送上线并线上验证，见 §4 随笔段）、**前台：修窄屏流星左下角偏少（二次修正）**（出生带均匀分布下路径穿过左下角的迹线占比小且在屏时间短——改「屏幕目标点反推 + 左下角几何带补充群（≈主群 70%）」，触屏窄屏 15→18 颗；实测 390×844 左下角采样 5→29；已推送上线并线上验证，见 §4 流星段）、**前台：移除杂项界面**（/misc/ 图片画廊页整体退役，八页收拢为七页；画廊代码/外观抽屉动效开关/搜索与功能开关的 misc 项全清，背景选择器改自拉清单，新增 _redirects 301 兜底；已推送上线并线上验证，见 §4 首页段）、**后台+工具：坑 5/坑 23 根治**（建 albums 表让空相册刷新不消失 + check-code 步骤[4] 外壳一致性比对漏同步即报错；已推送上线并线上验证，见 §9 坑 5/坑 23）、**前台：AI 页头部精简**（移除「AI 助手」标题与右侧新对话图标，只留左侧历史按钮，新对话入口保留在历史抽屉；顶部留白 96→68px、标题行内边距 14→8px；顺带修坑 28 relative 激活老层残留 top:52px 偏移导致的整页下坠；已推送上线并线上验证）、**前台：AI 页手机端对话气泡/输入条贴边**（page-main(4vw)+ai-chat(6vw) 双重侧留白叠加 ~40px、气泡远离屏幕边——≤767px 收窄到 ~12px 总留白贴向两侧，见 §4 AI 页手机布局段；已推送上线并线上验证）、**前台：悬浮播放器歌词联动**（原设计歌词条只联动迷你条、悬浮款播放时一直待机；改经 __lyricBarApi 驱动同一根歌词条：切歌载词/进度驱动打字机/律动与补完，歌词时间源跟随最后开播的播放器，pjax 回首页按悬浮款当前曲目补载，见 §4 悬浮播放器段；已推送上线并线上验证）、**修：窄屏下流星只出现在屏幕一小块**（坑：流星水平行程恒为 1.474×视口高、与屏宽无关，固定出生带 -80%~+100%（相对屏宽）在窄屏（横行程可达 3.2 倍屏宽）只命中上半对角走廊，390×844 采样底部三区全 0；出生带改按视口几何自适应反推 + resize 防抖重排 + 触屏档 10→15 颗，见 §4 流星段；已推送上线并线上验证）、**修：播放音乐后切换界面越切越多路音频同时播**（坑 29：顶栏悬停预览的实时缩略 iframe 是同源整页、共享 localStorage，主页播放中时悬停过的每个页面各自自动续播一份曲库且帧常驻——实测悬停 5 项叠 6 路音频；修复=iframe 场景整份丢弃播放恢复数据，预览帧不出声也不偷跑下载；已推送上线并线上验证）、**修：手机访问 AI 界面三处 UI 问题**（页脚撑出双滚→AI 页锁视口页脚不显示；历史抽屉被顶栏胶囊压住→坑 28 pageFade fill:both 层叠上下文，#aiView 抬 z60 模态化；触屏输入 16px 防 iOS 聚焦缩放；已推送上线并线上验证）、**修：流星雨左下角空白**（出生范围 -35%~+90% → -80%~+100%，几何：向右下 35° 斜落要经过左下角须从 -60% 以远出生；已推送上线）、**前台：接入参考博客站链接预览卡片 + 顶栏「预览」演示页 /blog/（第八页，站内放两个示例外链演示悬停预览；顶栏导航项悬停出实时缩略图预览卡）**（已推送上线并线上验证）、**前台：课表改真实页面 /schedule/（第七页）+ 新增「清空课程」+「文档」更名「关于」移至导航最右**（均已推送上线并线上验证）、**前台：浮层环境融合两连修**（.profile-view 毛玻璃 65%+blur14 + 流星抬层 z155 + 顶缝磨砂带——现仅个人主页浮层在用）、**后台：移除访问 IP 记录 + UI 动效升级**（「最近访问」IP 明细卡/归属地查询整体摘除——/api/visit 不再记 IP/UA、visit-logs 与 geoip 接口删除；后台动效对齐参考博客站缓动体系：模糊揭示入场+卡片级联、侧栏滑动胶囊指示器、主题切换圆形揭示、统计数字滚动等，见 §5 动效约定；已推送上线，线上验证新标记生效/功能标识清零）、**修：子页强刷后回首页歌词不显示**（坑 27：子页整页加载时歌词条元素不存在，播放恢复流程重置歌词状态后早退，回首页补载当前曲目；顺带修静态曲目 .lrc 拉取被 blob 前缀守卫架空）+ **AI 页隐藏播放条音乐续播**（data-page=ai 下迷你条/悬浮款 display:none，audio 不参与换页照常放）+ **修：进入其他界面后点「首页」/logo 回不去首页**（pjax 只换 `<main>`，外壳导航形状停留在最初加载的页面 + 锚点处理器先吞事件——六页标记统一真链接 + 处理器改为找不到目标不吞事件，见 §9 坑 26；已推送上线并线上复验）、**修：900~1199px 窗口宽度下首页时钟秒数被视频遮挡**（断点夹缝：该段主容器收窄 860px 但视频盒仍固定 320px、时钟仍 9vw 字号，时钟溢出正对视频盒——段内视频盒收窄+时钟降档根治，见 §9 坑 25；已推送上线并线上复验）、**拆多页面站点**——首页+工具/文档/AI/杂项/留言六个真实 HTML 页面，共享 assets/ 外壳，pjax 无缝换页让音乐跨页不断播；全屏层管理器与路径路由器退役，栏目页常规文档流化（详见 §3/§4/§9 坑 20~23，已推送上线）、**修：首页时钟/视频/寄语/天气全失效**（多页面改造误删 updateClock 定义被 try/catch 静默吞错，见 §9 坑 24，已推送上线并线上复验）、**修：全屏界面里点顶栏「首页」无反应**（锚点 preventDefault 挡住 hash 变动，路由器"锚点回首页"收不到——界面开着时放行默认行为，详见 §4 路由段）、**移除相册界面**（前台顶栏"相册"入口/界面/灯箱/收藏照片卡整体摘除，代码完整备份在 `相册功能备份/`（本地目录不入库），恢复步骤见其中《恢复说明》；后台图片页的相册分组与数据库数据保留不动），**课表导出**（课表卡一键下载 WakeUp 兼容 JSON，可再导入/WakeUp App 识别，见 §4 课表段）、**音乐专辑封面**（后台音乐行「封面」按钮上传/替换/移除，前台迷你播放器圆盘黑胶式旋转，media.cover 列 + /api/admin/media/:id/cover，见 §4 播放器/§5 音乐页/§6）、**账号昵称**（个人主页改昵称，展示名与登录账号分离；留言板署名/顶栏/欢迎语全用展示名，POST /api/user/nickname，users.nickname 列，见 §4 昵称段）、**留言板**（顶栏「留言」：前台用户 60 秒一条、站长标识、签到等级徽标、管理员可删；/api/messages + messages 表 + lib/levels.js 共享等级规则）、**更新日志时间轴**（文档区《更新日志》按日期分组渲染成时间轴，长条目折叠）、**后台管理员安全中心**（我的页新增：改密+踢其他设备、登录设备列表与一键吊销、最近登录记录、管理员邮箱 2FA（两登录入口同口径；⚠️ 开 2FA 后邮件服务故障会锁死，删 admin_2fa 键解锁），见 §5 安全卡）、**全站搜索 Ctrl+K**（命令面板：界面/工具/文档/音乐/图片/相册/视频，动作复用现有机制、被关功能不出现，见 §4 全站搜索）、**前台签到功能（等级 0~6）**（个人主页签到卡：每日签到/累计·连续天数/近 7 日圆点，按累计天数定级 0~6 级，接口 /api/user/checkin，表 checkins，仅前台注册账号，见 §4 签到段）、**后台按钮与动效体检 + 无障碍合规**（删除类按钮悬停转危险色、两处硬编码危险色改回 --danger 变量、弹窗/登录门入场动画、全局 prefers-reduced-motion 压缩、:focus-visible 焦点环；全量排查坑 18 正则/原生弹窗/文本字符按钮/innerHTML 转义全合规，见 §5 动效约定）、**工具合集六卡重做**（重要日子/番茄钟/换算器/文本工具/随机决策/计算器带历史，旧倒计时与颜色转换移除、密码生成收进「更多工具」折叠区，见 §4）、**修复管理员绑定邮箱/忘记密码重置**（模板字符串吞正则，原 §9.5 遗留 Bug，见 §9 坑 18）、**后台概览增强**（访问趋势柱状图/环形图切换：Top 7 + 「其他」聚合、今天段标记、悬停进即弹 + 命中层/视觉层分离根治抽搐（见 §9 坑 19）；最近访问·IP 归属地、邮件统计细化、网站运行卡）、**管理员前台体验**（头像/身份卡/课表开放）、**首页视频播放模式**（顺序/单曲/随机，后台可设独播）、**歌词律动条**（横条右侧图标改与底部播放器同款）、**后台曲库歌词**（上传配对 .lrc + 「歌词」按钮维护）、侧边栏动画重做 + 上传选择文件按钮、**前台功能开关**（后台可整体隐藏界面/首页模块，前台直接消失）、**前台 hash 路由**（#/tools 等可直达/分享，返回键可用）、**切换界面不闪主页**（垫底覆盖，见 §4 顶栏导航）；**文档同步：站点仅托管 Cloudflare Pages，移除 GitHub Pages 托管记录**）。供新会话/新人快速接手项目的完整说明。
> 快速上手：改前台页面内容 → 动对应栏目页（index.html / tools|docs|ai|board|schedule|blog|notes 的 index.html）；改全站样式 → `assets/site.css`；改全站脚本 → `assets/common.js`；**改外壳（头部/浮层/页脚/播放器 markup）要八个页面同步**（坑 23，漏同步 校验代码.bat 步骤[4] 会报）；改后台 → 动 `functions/`；改完跑 `校验代码.bat`（或 `node check-code.mjs`，会校验全部八页）再验证；推送到 GitHub 后线上地址自动更新。
> 当前进度：**2026-09-11 晚间四（回退窄屏顶栏布局、头像恢复原位，用户要求）已推送上线并线上验证**；**2026-09-11 晚间三（后台窄屏顶栏「汉堡靠左、头像+动作钮靠右」）已推送上线并线上验证**——500px 视口量测贴边对称后推送；详见 changelog 2026-09-11（晚间三）。**2026-09-11 晚间二（后台图片网格操作钮单行根治）已推送上线并线上验证**——本地 wrangler 登录后台真实图片数据四档视口（1440/900/500/390）逐卡量测 rows 全 1 后推送；详见 changelog 2026-09-11（晚间二）与 §5。**2026-09-11 晚间批次（前台首页内容区 + 坑 36 根治）已推送上线并线上验证**——check-code 全过 + 本地 wrangler 深浅/宽窄实测 + 摘高频源定位根因 + 换页矩阵回归（基线/留言/工具/随笔回首页卡片全揭示、毫秒位照常跳）后才推送；详见 changelog 2026-09-11（晚间）与 §4 首页段/§9 坑 36。**2026-09-11 全天后台 UI 批次（七个提交 98eeea6/caaebcd/1f74ebb/2703ddc/7c97d13/7c2e2aa/b881dcf：①管理界面现代化 ②输入细节打磨 ③上传区大拖放区 ④选中文件确认条与胶囊移除 ⑤图片列表缩略图网格 ⑥⑦悬停操作层溢出修复）已全部推送上线并线上验证**——每轮都是 check-code 全过 + 本地 wrangler 浏览器实测后才推送；详见 changelog 2026-09-11 与 §5「UI 组件层」「媒体页上传区与图片网格」段。2026-09-09 深夜自主批次（8c131d4/34d4493/639dd50/c515e5d/3b8533c/34148bd/667c2d1，末位为全量代码审查修复）已随后续推送上线。该批次**：第九页 /games/ 游戏页（2048/贪吃蛇/记忆翻牌）+ PWA 可安装/离线兜底 + RSS /feed.xml + SEO 基线 + 音乐频谱可视化 + 每日壁纸 + 公开站点数据 /api/summary + 短链 /s/ + D1 每日自动备份 + AI 语音输入/人格预设 + 番茄钟热力图 + 签到成就徽章 + 天气雨雪粒子 + 路人留言 + 彩蛋两枚 +《创意规划.md》与《灵感池与路线图》建档（详见 changelog 2026-09-10 与 §3/§4/§5/§6 新段）。** 2026-09-09 白天批次已推送上线（0e160b8）并本地实测：全站代码审计第二轮——通读全部代码后修 2 处功能性 bug（AI 历史侧栏「N 条」计数丢失累计 / AI 用量上报限流键全员共享）+ 管理员课表文案修正 + `.icon-*` 死规则清理；本地 wrangler（D1/KV 模拟）四档视口 1920/1280/768/390 前台八页与后台宽窄两态 UI 实测全过（详见 changelog 2026-09-09）。** 2026-09-08 批次全部已推送上线并线上验证：外观入口圆钮化（毛玻璃小钮+自由拖动+AI 页隐藏+出屏修复）、留言板真实头像、全站代码审计修复 27 处暗病、修 480-767 顶栏遮问候语、搜索面板弹出收回动画、窄屏抽屉视觉迭代（图标/主题色高亮/级联/按压/动作组贴右）。**2026-09-07：前台新增随笔页 /notes/（第八页）已推送上线并线上验证**。2026-09-06 全天批次均已推送上线并线上验证（⑰~㉑已推送上线并线上验证；⑮⑯已推送上线并线上验证；①~⑭均已上线）：①时钟秒数遮挡修复（坑 25）②首页/关于导航回不去修复（坑 26）③后台 IP 记录移除 + 动效升级④课表 /schedule/ 第七页（含「清空课程」）⑤文档更名关于+移至导航最右⑥浮层环境融合（个人主页）⑦链接预览卡片接入（参考博客站，见 §4 链接预览段）⑧顶栏「博客」外链按用户三轮反馈最终改为「预览」演示页 /blog/（第八页：站内放 GitHub clay-blog 仓库与 B站两个示例外链演示预览卡，中途的 iframe 内嵌方案已弃）⑨顶栏导航项悬停出预览卡（实时缩略图，见 §4 链接预览段）⑩修子页强刷回首页歌词不显示（坑 27）+ 静态曲目 .lrc 拉取失效⑪AI 页隐藏播放条音乐续播⑫修流星雨左下角空白（出生范围扩 -80%~+100%）⑬修手机访问 AI 界面三处 UI 问题（锁视口/坑 28 抽屉层级/iOS 聚焦缩放）⑭修播放音乐后切界面越切越多路音频（坑 29 预览 iframe 自动续播；线上验证：播放中悬停 AI/留言/课表/预览，4 个预览帧全部静音、缩略图正常渲染）⑮修窄屏流星只出一小块（出生带按视口自适应 + resize 重排 + 触屏档 15 颗，见 §4 流星段；线上 390×844 采样九区全覆盖 14/15/22|23/20/20|17/8/10；已推送上线并线上验证）⑯悬浮播放器歌词联动（__lyricBarApi + 歌词时间源跟随最后开播的播放器 + __blogPlayerTrack 供 lyricRebind 补载，见 §4 悬浮播放器段；线上 blog 模式点歌后歌词条打字机跟随悬浮款进度、迷你条回归正常；已推送上线并线上验证）⑰AI 页手机端气泡/输入条贴边（≤767px 双重侧留白 ~40px 收窄到 ~12px，见 §4 AI 页手机布局段；390px 实测气泡左缘 41→12px 无横向溢出；已推送上线并线上验证）⑱AI 页头部移除「AI 助手」标题（album-bar 只留历史/新对话两个图标；已推送上线并线上验证）⑲AI 页头部上移+移除右侧新对话图标（顶留白 96→68px、标题行内边距 14→8px；顺带修坑 28 relative 激活老全屏层残留 top:52px 偏移——静态时被忽略、relative 下生效导致整页下坠 52px，relative 时必须 top/bottom/left/right 归零 auto；新对话入口保留在历史抽屉；线上验证：390 宽下标题/右图标消失、头部行顶 68px、气泡左缘 12px；已推送上线并线上验证）⑳后台+工具：坑 5 建 albums 表根治空相册不落库（create 接口 + upload/PUT 写相册名 upsert + 清单响应带 albums 名单 + 前端 serverAlbums 并集展示）、坑 23 check-code 新增步骤[4] 外壳一致性比对（归一化 data-page/标题/导航高亮后逐行比，漏同步报页名+行号）（已推送上线并线上验证）㉑前台：移除杂项界面 /misc/（图片画廊页整体退役，八页收拢为七页；画廊代码/外观抽屉「动效」两开关/搜索与功能开关的 misc 项全清，背景选择器站内图改自拉清单，新增 _redirects 301 防伪 200；线上验证：/misc 与 /misc/ 均 301 弹回首页、首页 HTML 无 miscBtn/ff-misc 残留、六个导航项齐全、settings flags 无 misc 键、_headers 缓存头首次入库即生效）。**站点现为七个真实页面**：/ 与 /tools/ /docs/(关于) /ai/ /board/ /schedule/（课表）/blog/（预览），顶栏顺序 首页|工具|AI|留言|课表|预览|关于，外壳七处同步（坑 23）㉒前台：课表课程块配色 8 色→20 色白字友好色板 + 互不撞色分配（渲染时按课程名哈希位线性探测去重，同名课恒同色，见 §4 课表段；已推送上线并线上验证）。㉓后台：顶栏复刻前台悬浮胶囊（宽屏>900px 胶囊即导航：头像+十项反色 chip+回前台/主题/退出，侧边栏退役、内容区全宽；窄屏≤900px 保留原侧边栏抽屉 + 胶囊收窄为汉堡/头像/动作钮；#sideInd 指示器退役改反色 chip；已推送上线并线上验证：线上 /admin 含 admin-header/header-pill/drawerNav 新标记、旧 topbar/pageTitle 零残留）+ 顶栏导航悬停预览卡（postMessage 切 iframe 面板，悬停哪个栏目实时预览哪个）+ 视频行悬停播放卡（muted loop 自动静音播放、移开即卸载）（均已推送上线并线上验证：线上 /admin 含 fx-admin-preview/video-hover-preview/adminPreviewPanel 全部新标记）。㉔修：窄屏流星左下角仍偏少（二次修正：主群屏幕目标点全屏均匀反推 + 左下角几何带补充群 70%、触屏窄屏 15→18 颗；线上 /assets/common.js 含 birthLeftFor；已推送上线并线上验证）。推送按约定**默认走系统代理**：注册表现查端口 → curl 探测 → 配 git → unset（§10 已固化；当日端口 55734 全天可用）。已知坑全部根治（见 §9，坑 20~29 为多页面改造期及其后新增）。2026-09-06 推送批次：多页面改造 3 连发（提取 assets → 六页+pjax+模块化+顺修 3 个现存 bug → 实测新坑修复）→ 线上实报时钟/视频失效 → updateClock 热修（本轮 push 直连被重置，走用户提供的系统代理端口推送，推完已 unset——流程见 §2）。2026-09-05 推送批次：签到/留言板/全站搜索/时间轴/安全中心 → 账号昵称 → 路径路由 → 主题切换动效 → 胶囊导航 → 播放器细节 → 默认暖白主题（对齐参考博客站，含顶部光晕）→ 流星特效 → 悬浮音乐播放器（后台可切，站内曲库）→ settings 缓存穿透 → 移除相册界面 → 修界面内点「首页」无反应；每次推送前先 `git fetch` 确认远端分叉（今日无分叉）；git 连不上时查系统代理（§2，端口每次现查、推完必须 unset）

## 1. 项目是什么

个人主页站点，两部分组成：

* **前台**：八个真实 HTML 页面（`index.html` + `tools/ docs/ ai/ board/ schedule/ blog/ notes/` 各含 index.html），全站样式与脚本抽到 `assets/`（site.css / common.js / blog-player.js），头部/浮层/播放器是八页共用的外壳；站内跳转走 pjax 只换内容区（音乐跨页不断播），无构建、无运行时依赖，纯静态托管可直接跑（每个页面也都能独立直达/刷新）

* **管理后台** **`/admin`**：Cloudflare Pages Functions（`functions/` 目录即服务端代码，push 后随站点自动部署）+ D1 数据库 + KV 存储

功能总览：时钟首页（寄语/一言/天气胶囊/歌词横条）、留言板（真实头像/等级徽标/站长标识/删除钮）、工具合集（重要日子/番茄钟/换算器/文本工具/随机决策/计算器，折叠区含密码生成）、文档区（Markdown 阅读）、随笔页（/notes/ 年份分组时间线，notes.json 手工维护）、AI 助手（多协议流式对话 + 多对话管理，后台配置服务商）、双款式音乐播放器（迷你播放条/悬浮播放器后台可切，共用站内曲库）、视频轮播、全站搜索（Ctrl+K 命令面板）、访客/管理员登录（邮箱验证/找回密码/2FA）、个人主页（资料/昵称/改密/收藏音乐/**课表提醒**/签到等级）、外观自定义、访问统计、媒体管理后台、邮件服务（验证码/自定义邮件/课表提醒）；RSS 订阅 /feed.xml、每日壁纸、音乐频谱可视化、关于页公开站点数据卡、短链服务 /s/、D1 数据每日自动备份、AI 语音输入+人格预设、天气雨雪粒子、留言板路人投递、彩蛋两枚（以上均为 2026-09-09 深夜批次，见 §4/§5/§6 各段）。相册界面已移除（备份见 `相册功能备份/`）；杂项页/图片画廊已随 /misc/ 整体移除（2026-09-06）。

## 2. 线上地址与部署

| 平台               | 地址                        | 说明                                              |
| ---------------- | ------------------------- | ----------------------------------------------- |
| Cloudflare Pages | <https://yhuo.pages.dev/> | 关联 GitHub 仓库 main 分支，推送后自动部署；前台与后台/D1/KV 都在这边生效 |
| 自定义域名 | <https://190963.xyz/> | 2026-09-06 绑定为第二域名；曾试灰云优选 IP 提速，当晚晚高峰翻车（整站打不开）已回退，现为灰云（仅 DNS）CNAME 直指 yhuo.pages.dev（慢但稳），见下方「国内访问优化（已回退）」 |

* 仓库：<https://github.com/ShanFenTi/YHuo（main> 分支）

* git 身份（仓库级）：user.name=ShanFenTi，user.email=<ShanFenTi@users.noreply.github.com>

* 本网络 push 偶发失败（连接被重置/超时）：**重试几次即可**（本日多次实测 3\~7 次内成功），实在不行开代理。**代理实践经验（2026-09-05，两次实战）**：开代理后 git 不一定走代理——若 fetch/push 仍报 Connection reset / Could not connect，**先重试**（代理 TUN 全局接管时 git 直连即可生效，实测逐端口 curl 探测全部探不到代理、但推送一次成功，端口探测不是可靠信号；首次连接超时可能是 TUN 冷启动，curl 预热后再试 git）。重试仍不通按下面顺序排查（2026-09-05 第二次实战：代理软件这次走的是 **Windows 系统代理**而非 TUN，git 不读系统代理所以一直连不上）：
	1. **查 Windows 系统代理**（最直接）：`reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings" //v ProxyServer`（ProxyEnable=1 表示开启）——拿到地址如 `127.0.0.1:55068` 后 `git config --global http.proxy http://127.0.0.1:55068`，一次就通
	2. 注册表没有再看代理软件界面里的 HTTP/SOCKS 端口（Clash 系常见 7890；SOCKS 用 socks5:// 前缀）
	3. **端口不固定**：同一代理软件重启后端口会变（实测 7890→55068），旧配置端口不通不代表代理没开，每次现查
	4. 用完 **必须 `git config --global --unset http.proxy` 清掉**——不清的话代理一关 git 会全挂；症状速记：**"curl 通 / 浏览器能开但 git 连不上" = 十有八九是系统代理模式，git 没跟上**
	（诊断时可 `netstat -ano | grep LISTENING` 扫本地监听端口逐个用 `curl -x http://127.0.0.1:端口 https://github.com` 试探，但先查注册表更快）

* 部署后旧样子先 Ctrl+F5 强刷（浏览器/CDN 缓存）

* **国内访问优化（2026-09-06 尝试后回退）**：域名 DNS 托管 Cloudflare，曾把根记录改**灰云 A 记录**指「优选」CF 边缘 IP（下午实测 96ms / 2~6MB/s，香港节点，HTML 首字节 0.5s）——**当晚晚高峰即翻车**：优选 IP 整颗拉黑（先传输卡 2 分钟+，后 TCP 直接黑洞数分钟），用户侧整站打不开，**已回退为灰云（仅 DNS）CNAME**（190963.xyz CNAME→yhuo.pages.dev，直指 pages.dev 官方线路；灰云/橙云对纯 Pages 站无实质差异，关键是不再指优选 IP），慢（首字节 ~1s、下载几十 KB/s）但稳。**教训：单条静态 A 记录是单点，测速通过≠晚高峰可用，可用性优先于速度**。若将来重试：至少双 A 记录（不同 IP 轮询兜底）+ 工作时段与 19~23 点都实测 + 接受偶发波动，且必须让用户知情同意；工具留档在仓库本地 `优选IP工具/`（cfst.exe）。**同日保留的优化（与优选无关、纯增益）**：仓库根新增 `_headers`（push 即生效）——/assets/* 浏览器缓存 1 小时、music/video/images 缓存 1 天，回访少拉资源；代价是改代码推送后老访客最多延迟 1 小时拿到新 assets，急发热修可临时把 /assets/* 的 max-age 改 0 推一版

* ⚠️ 不要在 GitHub 网页上直接上传/编辑文件（本地也在改会冲突；曾发生过，靠 rebase 化解）。已在网页改过的话先 `git pull --rebase origin main`

* ⚠️ **推送被拒且远端是一串 "Add files via upload" 的处理（2026-09-04 实战）**：① 逐文件比对 `git diff origin/main <本地提交> -- 文件` 并加 `--ignore-cr-at-eol`——网页上传常把 CRLF 转成 LF，纯换行差异可忽略；② 确认远端没有本地缺少的内容后 `git pull --rebase -X ours`；③ **坑：rebase 时 `-X ours` 的 ours 指远端侧**（方向与 merge 相反），关键文件若被取成远端旧版，用 `git checkout <本地提交> -- 文件` 恢复再 `git commit --amend`，最后 `git diff --ignore-cr-at-eol <本地提交> HEAD` 核验零差异后推送

### 后台首次启用（新环境才需要，线上已配好）

1. Cloudflare 控制台 → Storage & Databases → D1 → Create（名字随意，如 `yhuo-db`）
2. KV → Create namespace（如 `yhuo-media`）。**不需要 R2、不需要绑卡**
3. Workers & Pages → yhuo → Settings → Bindings → Add：D1 绑定 Variable name 必须填 `DB`；KV 必须填 `MEDIA`
4. Deployments 里对最新部署点 Retry deployment（绑定只对新部署生效）

建表已自动化（首次访问接口时 `ensureSchema` 自动建表 + 老库补列），无需手动执行 `db/schema.sql`。首次访问 `/admin` 会要求创建超级管理员（仅一次，PBKDF2 哈希存 D1）。

### 课表提醒的外部 cron（已配置在用，换环境才需要重做）

Pages Functions **不支持定时触发**，课表提醒靠外部 cron 每 5 分钟敲门：

1. 后台 `/admin` →「邮件」页 → 最下方「课表提醒定时任务」→ 复制 tick URL（首次查看自动生成密钥；形如 `https://yhuo.pages.dev/api/schedule/tick?key=…`）
2. 注册 <https://cron-job.org>（免费）→ CREATE CRONJOB：URL 粘贴 tick 地址，Method 保持 GET，Schedule 选 Every 5 minutes
3. 验证：任务列表 Last execution 显示 200 即通；后台「立即执行一次」可手动触发（幂等，不会重发）
4. **重新生成密钥后旧 URL 立即失效**，必须去 cron-job.org 编辑任务换新地址，否则提醒静默停发

替代方案：单独部署一个带 Cron Triggers 的 Worker 去 fetch 同一个 tick URL（逻辑零改动），嫌第三方依赖可切换。

## 3. 目录结构

```
index.html              首页（时钟/寄语/天气/歌词条/视频）
tools/index.html        工具页（/tools/）
docs/index.html         关于页（/docs/，2026-09-06 由「文档」更名；与下面 docs.json/*.md 同目录共存互不影响）
ai/index.html           AI 助手页（/ai/）
board/index.html        留言板页（/board/）
schedule/index.html     课表页（/schedule/，2026-09-06 从浮层改回真实页面）
blog/index.html         预览页（/blog/，2026-09-06 起为链接预览卡片演示页：两个示例外链）
notes/index.html        随笔页（/notes/，2026-09-07 起：按年份分组的随笔时间线）
notes/notes.json        随笔数据（手工维护：加一条随笔 = 加一个 {date, mood, text} 对象）
manifest.webmanifest    PWA 清单（名称/主题色 #b0532b/standalone/图标含 maskable，可安装入口）
sw.js                   SW 卸载器（PWA 已于 2026-09-10 移除：仅对装过旧版的访客自注销+清缓存；no-cache 头保留，见 §4 PWA 段）
offline.html            SW 离线兜底页（自足样式不依赖外壳；注意 CF Pages Pretty URLs 会把它 308 到 /offline，坑 34）
robots.txt              SEO 基线：允许全部爬虫（2026-09-09 深夜批次）
sitemap.xml             SEO 基线：八页站点地图（2026-09-09 深夜批次）
assets/                 八页共享外壳资产（2026-09-06 多页面改造从 index.html 迁出）
  site.css              全站样式（原内联 CSS 整体 + 末尾「多页面常规化」段）
  common.js             全站脚本（原主 IIFE + 页面模块 init/teardown + pjax 路由 + 流星特效）
  blog-player.js        悬浮音乐播放器（博客款）
_headers                Cloudflare Pages 静态资源缓存策略（assets 1h、媒体 1 天；2026-09-09 深夜批次起加 sw.js no-cache；见 §2 国内访问优化）
_redirects              Cloudflare Pages 重定向（/misc(/) 301 回首页——杂项页移除后防 SPA 回退以 200 返回首页内容，坑 2）
build-pages.py          多页面改造的一次性页面生成脚本（使命已完成，重跑会断言退出；留档）
surgery.py              多页面改造的一次性 common.js 手术脚本（同上，留档）
functions/              后台服务端（Cloudflare Pages Functions）
  admin/index.js        /admin 页面（单文件 HTML+CSS+JS）
  api/                  全部 REST 接口（见 §6）
  api/summary.js        /api/summary 站点公开聚合统计（八项计数，绝不含个体数据；2026-09-09 深夜批次）
  api/wallpaper.js      /api/wallpaper 每日壁纸元信息（Bing 双源，边缘缓存 6h；2026-09-09 深夜批次）
  api/wallpaper/image.js /api/wallpaper/image 壁纸图片白名单反代（只放 *.bing.net 等官方域，缓存 1 天）
  api/admin/backup.js   /api/admin/backup 备份清单/立即备份/按日期下载（后台；2026-09-09 深夜批次）
  api/admin/links.js    /api/admin/links 短链增删改查（24 项站内保留字黑名单；2026-09-09 深夜批次）
  s/[code].js           /s/{code} 短链 302 跳转+计次（公开；no-store 防边缘缓存吞计次）
  lib/                  auth.js 认证+限速 / migrate.js 自动建表 / util.js 杂项 / ai.js AI 上游代理
                        email.js 邮件发送+验证码（sendMail 带 kind 计账）/ schedule.js 课表核心（解析/周次/tick）
                        backup.js 每日自动备份（四表全量 JSON 进 KV 轮换 7 份，搭课表 tick 便车）
docs/                   前台文档区内容（docs/ 目录同时承载文档页 /docs/ 的 index.html）
  docs.json             文档清单（手工维护：加文档 = 放 md + 在此加一条）
  about.md              《关于本站》
  changelog.md          《更新日志》（每次上线建议补一条）
  roadmap.md            《灵感池与路线图》（2026-09-09 深夜批次上站：访客可见的创意清单，做掉一条勾一条）
创意规划.md             站点创意库与路线图（根目录，2026-09-09 深夜批次建档入库存档：26 条创意六轴分组 + 今晚执行方案与执行结果；本地维护文档，不进前台）
db/schema.sql           建表 SQL（仅参考，实际自动建）
check-code.mjs          一键代码校验（八个前台页面的内联脚本语法与本地引用 + 八页外壳一致性比对 + ESM import 路径；推送前跑，双击 校验代码.bat）
music/ video/ images/   静态媒体兜底（前台与后台内容合并显示）；music/ 里可放与歌曲同名的 .lrc 歌词
music/playlist.json     静态音乐清单（生成歌单.bat 维护）
video/playlist.json     静态视频清单（同上）
images/manifest.json    静态图片清单（生成图片清单.bat 维护）
node_modules/           wrangler（本地调试用，已 gitignore）
*.bat                   双击工具：启动网页/停止服务器/推送更新/生成歌单/生成图片清单/校验代码（bat 均只留本地，不入库）
优选IP工具/             CloudflareSpeedTest（cfst.exe）+ 当日测速结果（本地不入库；优选 IP 方案 2026-09-06 已回退，工具留档备将来重试，见 §2）
WakeUp课表导入.json/.csv 本地测试样例（课表导入用，不入库）
```

## 4. 前台功能明细

### 顶栏导航与多页面架构（2026-09-06 起，重要机制）

顶栏 = **首页 | 随笔 | 工具 | AI | 留言 | 课表 | 预览 | 关于**（八项；「随笔」= /notes/（id notesBtn），2026-09-07 起的随笔时间线页，见 §4 随笔段；「预览」= /blog/（id blogBtn），是链接预览卡片的站内演示页，2026-09-06 起——第一版曾是外链直跳、二版曾 iframe 内嵌参考博客站，最终按用户要求改为演示页；**杂项页 /misc/（图片画廊）2026-09-06 整体移除**，见 §4 首页段画廊条目；「文档」界面 2026-09-06 更名「关于」，路由 /docs/ 与内部机制不变）。「首页」八页统一写成真链接 `<a href="/" class="nav-link" data-target="home">`（仅首页那份带 active/aria-current；首页内点击由滚动处理器接管为平滑滚顶，其他页面放行给 pjax——**别改回 #home 锚点形态**，见 §9 坑 26）；logo 同理一律 `href="/"`；「课表」与工具等同为真链接（`/schedule/`，id scheduleBtn）；**工具/文档/AI/留言各是真实 HTML 页面**（`/tools/` `/docs/` `/ai/` `/board/`，目录+index.html；相册界面已移除，代码备份在 `相册功能备份/`——恢复思路已因架构改造过时，见其《恢复说明.md》顶部警示）：

* **形态（2026-09-05 起，居中悬浮胶囊）**：头部 `#siteHeader` fixed 透明壳（pointer-events:none 胶囊外点击穿透），内里 `.header-pill` 一颗胶囊装下 logo + 八个导航项 + 搜索/登录/深色三钮——半透明底 + saturate blur 毛玻璃 + 内侧高光 + 投影（深色主题各一档；onScroll 的 `.scrolled` 只加深胶囊投影）；**当前页高亮 = 反色 chip**（各页面静态写死 active + aria-current；pjax 换页由 applyNavActive 按导航项 id 重算——**保留 #toolsBtn 等 id 是为了让功能开关的 ff-* 隐藏规则继续生效**）；头部 fixed 不占文档流，首页 hero 各断点 padding-top（72/80/68/64px）与子页 main 的 padding-top（96px）都是为容胶囊留的。窄屏：**≤900px（2026-09-07 起，与后台窄屏同断点）改「汉堡+抽屉」模式**——胶囊收窄为 汉堡(#navToggle)+logo+搜索/登录/深色三钮，八个导航项进左侧毛玻璃抽屉（#navDrawerMask 遮罩 + #navDrawer 抽屉，点遮罩/✕/选中任一项/Esc 收起、`body.nav-drawer-open` 锁背景滚动，pjax 换页经 closeAllTransientOverlays 收起）；抽屉项 .nav-drawer-link 用 data-target 标识，applyNavActive 同步高亮（同款反色 chip），功能开关按 `[data-target]` 同口径隐藏（site.css「窄屏导航抽屉」段）；>900px 汉堡与抽屉纯 CSS 藏起不可用，跨断点 matchMedia 复位状态；原「≤767px 胶囊占满行宽导航横滑 / ≤480px 藏 logo」规则已随本模式作废（横滑规则自然失效、logo 不再藏）。抽屉视觉迭代（09-08）：每项 18px 线性图标+行尾箭头、当前项主题色柔和高亮（整页加载由启动补调 applyNavActive 保证）、开抽屉条目级联滑入、悬停按压感（scale .98/按下 .96）、动作组贴右（margin-left:auto）、底部「© YHuo」落款；旧横滑/藏 logo 死规则已删除。**深色切换圆形揭示（2026-09-05 起）**：View Transitions API——点击时算按钮中心/覆盖半径并全换算成百分比注入 `@keyframes theme-reveal` + `html.theme-vt` 类接 `::view-transition-*` 规则（细节搜 theme-vt）；Firefox/reduced-motion/后台标签自动落回即时切换。改切换逻辑时保留 `themeSwitching` 锁与 `theme-vt` 类的同步清理（`vt.finished.finally`）

* **页面 = 外壳 + 内容区（改外壳八处同步，坑 23）**：八个页面除 `<html data-page="…">`、`<title>`、导航高亮和 `<main>` 内容外**完全一致**——外壳 = head（flags 预隐藏内联脚本 + tailwind + `assets/site.css`）+ 背景层/流星 + 头部胶囊 + **全部共用浮层**（登录卡/个人主页 profileView/课表编辑/外观抽屉/头像下拉/Ctrl+K/docViewer/欢迎 toast/背景与天气选择器）+ 页脚/回顶 + 两个播放器 + `assets/common.js` / `blog-player.js`。栏目内容 = 原 views 的 DOM 常规页化：去 ✕ 关闭按钮、去全屏 fixed/毛玻璃壳（site.css 末尾「多页面常规化」段，选择器是 `html[data-page=…]`——**data-page 标在 `<html>` 上不在 body 上**，坑 21），AI 页保持全高聊天布局

* **pjax 无缝换页（common.js 尾部 PJAX 段；跨页音乐不断播的关键）**：document 级 click 拦截站内链接（同源、非外链//admin//api//media/非 _blank/非 download/非纯锚点）→ `pjaxSwap`：fetch 目标页 → DOMParser → **只替换 `<main class="page-main">` 的 innerHTML** 并同步 data-page/`<title>`/导航高亮 → pushState。播放器/浮层/头部在外壳里不参与替换，**音乐跨页不断播**。后退/前进挂 popstate 同套替换并按 `scrollMem` 恢复滚动位置；同页 hash 变动（如 /tools/#toolCalc）只定位不换内容；fetch 失败 `location.href` 整页加载兜底（页面本来就是真文件，**刷新/直达/分享/禁 JS 全部正常**——这也是相对旧 SPA 架构的最大收益，本地 python http.server 现在能直接刷新 /tools/）；**换页加载反馈（2026-09-07，用户实报慢线上"点击没反应过一会才进"）**：fetch 发起 160ms 后还没返回就亮反馈——顶部阅读进度条变滑动加载段（`.pjax-busy`，width !important 压过滚动内联宽、滑动走 transform 互不打架）+ 内容/页脚轻降透明度（`body.pjax-loading`，180ms 过渡），内容就位即撤（秒回完全不出现不闪）；reduced-motion 下降级为静态 40% 段；详情搜 site.css「pjax 换页加载态」段；**换页入场淡入（同日用户反馈"加载好了切换太生硬像突然出现"）**：内容替换后给 main 挂 `.pjax-enter` 播 300ms opacity 0→1（pjaxEnter），统一覆盖八页（原 pageFade 只盖 tools/docs/board/ai 四页）；⚠️ 只动 opacity 不动 transform——transform 配 fill 会把 main 里 fixed 后代（AI 历史抽屉）的包含块改到 main 上（坑 22/28 同源）；挂法先摘类强制 reflow 再挂回保证每次重播；`/tools` 无斜杠由 Cloudflare Pages/本地服务器自动 308 到 `/tools/`；旧 `#/tools` hash 链接启动时 `location.replace('/tools/')` 归一。换页时 `closeAllTransientOverlays()` 收起全部浮层、`postVisit()` 按真实路径补报 /api/visit

* **页面模块注册制（PAGE_MODULES）**：按页初始化的模块都改成 `{init, destroy, onHash}` 注册——home（时钟/寄语/视频/天气 startHomeXxx 系列按当前 DOM 重查重绑 + 歌词条 lyricRebind）、tools（七件套 + pomoStopHook 离页停表 + onHash 锚点定位工具卡 locateToolCard）、docs（文档卡列表重拉）、ai（重建绑定；内存 aiHistory 重建消息视图；离页 abort 流式请求 + 摘 document 监听防叠加）、board（重拉身份与留言）。外壳模块（主题/flags/登录/个人主页/搜索/外观抽屉/页脚/播放器/滚动高亮）只在整页加载时初始化一次。**原「全屏层统一管理器」（FS_TOP/fsOpen/fsClose/垫底覆盖，坑 13/17）与「路径路由器」（FS_ROUTE/popstate 深链接）已整体移除**；个人主页（profileView）与 docViewer 仍是外壳浮层，开合各自为政（openProfileView 顺手 closeDocViewer 互斥；Esc 处理各自保留，lightbox 相关条件已修）

* **功能开关（按页守卫）**：后台「外观」页逐个关闭顶栏界面（工具/文档）与首页模块（天气/歌词/视频）——导航项照旧 ff-* 类隐藏（CSS `display:none!important`）；**被关栏目的页面**由 applyFeatureFlags 与 pjaxSwap 双重守卫 `location.replace('/')` 整页回首页；AI 跟随「AI」页全局开关（关闭时顶栏 AI 入口隐藏；/ai/ 直达仍能打开但对话按钮提示接入中，与旧行为一致）。首屏预隐藏机制不变（head 内联脚本读 localStorage 缓存打 ff-* 类，配置到达后校正）。注意这是展示开关不是安全边界（AI 除外，接口侧本就硬关）

### PWA 与离线兜底（已移除，2026-09-10 上午）

* PWA（manifest + sw.js + offline.html + 注册 IIFE）曾于 2026-09-09 深夜批次加入，**2026-09-10 上午应用户要求整体移除**（完整代码在 git 22a03bf 可找回）。现根目录 `sw.js` 只保留一个**自卸载器**（yhuo-sw-v4-uninstall：对装过旧 SW 的访客自动注销 + 清光历史缓存，新访客不注册任何 SW；no-cache 头保证卸载脚本即时生效）；theme-color 等 SEO 标签保留；favicon/apple-touch-icon 引用亦于同日应用户要求移除（见 changelog）。恢复：找回 22a03bf 的 sw.js/manifest.webmanifest/offline.html + common.js 注册 IIFE + 九页 head manifest link，并删除现卸载器。

### 链接预览卡片（外链悬停，2026-09-06 起）

**演示页 /blog/**：顶栏「预览」就是本特效的站内演示（对齐 `博客动画源码/demo.html` 的演示区）——一张卡片放两个示例外链（GitHub clay-blog 仓库 / 哔哩哔哩），悬停出卡给访客看效果。**顶栏导航项也弹（实时缩略图）**：悬停 首页/工具/AI/留言/课表/预览/关于 出同款卡片，视觉区是目标页的**实时 iframe 缩略图**（1280 宽渲染 scale(0.25) 缩进卡片，加载前主题色兜底；按路径缓存常驻，is-current 类切 display 显隐——不动 DOM 不触发 iframe 重载；切外链卡全藏防叠影；pointer-events:none 纯展示；**预览帧不自动续播音乐**——iframe 场景 common.js 丢弃播放恢复数据，坑 29），meta 区仍是页面说明。页面说明表 NAV_INFO 在 common.js 预览模块内（键=去尾斜杠 pathname）；其余站内链接照旧不弹。

参考博客站 06 号特效接入（源码 `博客动画源码/06-链接预览卡片.js`）：悬停**白名单站点的外链** 150ms 后弹出小卡片（站点名/链接文字/描述/上下文标识；GitHub 链接另取 opengraph 预览图，加载失败回落图标），跟随鼠标、贴边自动钳制，滚动/滚轮/触屏滑动/失焦即隐藏；白名单外（含全部站内链接）一律不弹，卡片不出现原始 URL。**白名单与卡片 DOM 都在 common.js 尾部的独立 IIFE**（SITE_INFO 常量，现为博客站/GitHub/B站，加站点改那里；DOM 由 JS 注入一次——外壳跨 pjax 不换，免七页同步，坑 23），document 级监听整站常驻、pjax 换页后照常工作。样式在 site.css 末尾「链接预览卡片」段，颜色/阴影/缓动全走站点语义变量（深浅主题/主题色自动跟随）；仅「可悬停+精确指针」设备启用，`prefers-reduced-motion` 时去入场动画。**贴边钳制量的是 offsetWidth/offsetHeight**——getBoundingClientRect 在入场动画 scale(0.92) 期间会量到缩小尺寸，动画放完卡片右/下缘溢出屏幕十几 px（原示例自带的坑，接入时已修）。

### 全站搜索（Ctrl+K，2026-09-05 起）

* **唤起**：`Ctrl/Cmd+K` 或顶栏放大镜（`#searchToggle`）；独立浮层（z-index 350，低于登录卡 400），**任何页面可用**；键盘 ↑↓ 选择 / Enter 打开 / Esc 关闭；按键监听挂**捕获阶段**并用 stopPropagation 隔离（面板开着时按键不会漏给其他层的 Esc 处理）；**弹出/收回动画（09-08）**：弹出弹簧曲线 320ms 带轻微过冲、收回 ease-out 260ms 缩回+淡出（遮罩同步放缓），搜索钮 ：active 按压缩放，reduced-motion 瞬时化——过渡方向归属：弹出 transition 写在 .show 态（CSS 过渡取 after-change style）

* **搜索范围与动作**（全部复用现有机制）：界面（pjax 跳真实页面）、工具卡（`/tools/#卡片id` 跨页跳转，页内 onHash 定位 scrollIntoView + `cmdk-flash` 高亮脉冲，密码生成自动展开折叠区）、文档（**原地开 docViewer 阅读层**，任何页面可用）、音乐（按歌名回 `tracks` 定位 `playIndex`，与收藏同机制，曲库没有则置灰）、视频（回首页）。**被功能开关关闭的界面/模块与全局关闭的 AI 不出现在结果里**（读 FLAGS_OFF + aiConfig）；界面快跳清单含随笔/留言板/课表/链接预览演示

* **数据源**：/api/playlist + docs/docs.json（60 秒缓存，每次打开面板时过期即刷）；接口失败回退 music|video 两个静态清单（**清单路径已改根相对 `/music/…` 等，坑 20**；坑 2 的 SPA 伪 200 也走兜底；原 images 分支为死代码，已随杂项页移除清掉）。实现注意（踩过）：①匹配高亮 `<mark>` 用 textNode+mark 构建（防注入），命中位置 0 时 `!hl` 是 falsy 要用 `hl < 0` 判；②结果按组渲染，组内按命中位置排序、组间按各组最佳命中排序（全局排序会让组头交错重复）

### 首页

* **时钟**：本地时间（毫秒级）+ 时段问候语 + 视频（静音自动播放）+ 寄语

  * **视频播放模式（2026-09-04 起）**：首页右侧视频轮播（#homeVideo，video 文件夹/后台曲库依次循环播放）支持三种模式，后台「视频」页顶部设置（存 site_settings.video_mode：{mode:'seq'|'single'|'random', url}，/api/settings 下发）——**顺序循环**（默认，原行为：依次循环）、**单视频循环**（只循环播放指定的一条，后台视频列表点行尾星标「设为独播」，url 以播放清单口径标识，被删自动回落顺序）、**随机播放**（每次播完随机换一条，不重复上一个）

  * 寄语优先级：管理员后台设置的"主页寄语"（**多条，打字机逐字打出并轮播切换**，每条停留约 6 秒循环，2026-08-31 起） > 每日一言（**一次拉 6 条去重、当日缓存 texts 数组，3 秒切一句轮播**，同样打字机效果）；显示在天气胶囊下方的独立胶囊里（与天气同款样式；打字机打字前先用完整文本量出这条的最终高度锁进 min-height（CSS 260ms 过渡承接轮播切换的高度差），窄屏换行样式下打字行数增长不再把下面的歌词条顶得上下抽搐——2026-09-07 用户实报修复）

  * **天气胶囊**：日期下方显示 ☁️ 城市 温度·天气 最高/最低温；Open-Meteo 免费接口（无需 key，缓存 30 分钟）；位置优先级 = localStorage 手动选择 > IP 定位（ipwho.is → geojs 备用 + bigdatacloud 反查中文城市名）> 默认北京；点击胶囊换城市（弹窗：内置 50 城市坐标表本地匹配 + Open-Meteo 在线搜索合并，在线接口连不上时本地表兜底）；浏览器定位按钮；接口失败静默隐藏（纯前端实现；2026-09-07 起天气/一言/歌词条三件套改**首帧占位**——markup 不带 hidden、常驻文档流靠 opacity 渐现，异步到达零布局位移，flag 关闭/拉取失败才显式 hidden 收起）

  * **歌词横条**（#lyricBar，一言下方全宽深色圆角条）：播放时同步显示当前句，**打字机逐字打出**（左待机点 + 中间歌词+闪烁光标 + 右律动条——**2026-09-04 起与底部迷你播放器（.mini-eq）同款**：三根竖条，播放时主题色错峰跳动（0.9s 循环）、暂停/待机灰色静止半透明，播放状态经 `updatePlayIcon` 与播放器同步，窄屏下左右装饰（待机点/律动条）均隐藏——必须两边同时藏，只藏右边会让居中歌词被左侧仍占位的待机点顶向右偏）。歌词来源优先级 = **后台曲库自带的歌词（media.lrc，随 /api/playlist 直接下发文本，后台「歌词」按钮维护）** > **music/ 里与歌曲同名的 .lrc 文件**（如 `晴天 - 周杰伦.mp3` 配 `晴天 - 周杰伦.lrc`，注意歌名带" - 歌手"要完整一致，切歌时自动 fetch、URL 用去掉扩展名的歌名 encodeURIComponent；注意曲目 src 是 blob URL，fetch 按歌名发起、不看 src 前缀——旧版按 src 前缀判断的守卫在曲库 blob 化后曾把静态曲目 .lrc 拉取整个架空（2026-09-06 修复）；pjax 回首页时 lyricRebind 会对"有播放源但没歌词数据"的当前曲目补载（坑 27）；支持 `[mm:ss.xx]` 时间标签（含一行多标签、`,`/`.` 毫秒分隔）、`[ti:` 等元数据行忽略、**纯文本无时间戳的 lrc 按时长均分逐行降级**。防坑 2：响应以 `<` 开头（Cloudflare 伪 200 首页）视为无歌词。显示状态机：播放中→当前句（暂停时当前句立即补完不留半截）；无歌词 → ♪ 歌名占位；从未播放 → "♪ 打开底部播放器" 待机提示。打字速度按句时长自适应（15\~40ms/字），seek 回退天然正确（每次全量找句）。本地测试：改 localStorage `musicMeta` 的 time/current 后刷新可模拟任意恢复进度

* **首页内容区（2026-09-11 晚间起，hero 之下）**：把首页从一屏钟表屏保变成能滚的门面，三件套（只动首页 main + site.css 末尾「首页内容区」段 + common.js startHomeLower/homeNotesRender/homeLowerAbort，PAGE_MODULES.home init/destroy 挂钩、pjax 离页 abort；不碰八页外壳，坑 23 免同步）——①**最新随笔卡**（.home-notes-card）：最新 3 条（MM-DD + note-mood 胶囊 + noteStripMd 剥 md 单行省略摘要），数据链与随笔页同源（/api/notes → 空库/失败回落 notes/notes.json），加载期骨架条（min-height 锁高零位移）、两路全败显示提示行不收卡；点击 `/notes/#日期` pjax 直达并 locateNote 定位高亮；②**快捷入口卡**：随笔/工具/AI/留言磁贴（图标复用导航抽屉，悬停浮起+↗），ff-tools-off/ff-ai-off（含 boot-hide 档）同口径隐藏磁贴；③**站点数据行**（#homeStats）：/api/summary 聚合计数一行（数字主题色、textContent 组装），失败整行保持 hidden（与关于页数据卡同口径）。样式注意：≤900px 网格必须 `minmax(0,1fr)`——裸 1fr 的 min-width:auto 会被单行省略文本的 min-content 撑破视口；.apple-card 自动进滚动模糊揭示清单。**改这段前先读坑 36**：首页毫秒位高频写入会饿死揭示模块的防抖重扫

* **图片画廊（已随杂项页整体移除，2026-09-06）**：原折叠牌堆式轮播（#gallery，居杂项页 /misc/）退役——misc/ 目录、initMiscGallery 等画廊代码与样式、外观抽屉「动效」两个开关（大卡片跟随鼠标/小图片倾斜，唯一消费者是画廊）一并删除；**外观抽屉背景选择器的站内图来源**从画廊已加载缓存改为 openBgPicker 现场拉 /api/playlist（失败回落 /images/manifest.json）；后台图片管理与相册分组不受影响。历史注：画廊加载调用曾在移除相册时被整体误删，2026-09-06 早间修复（startGalleryIfAllowed/fetch/imgDone 缺一不可的教训随页面移除一并作废）

* **页脚**：运行时长（自 2026-08-29 起）+ 访问计数（每浏览器会话计一次，sessionStorage 去重，POST /api/visit）

### 工具页（/tools/，2026-09-04 六卡重做；2026-09-06 起为独立页面）

工具合集为 2×3 卡片（窄屏单列），纯本地无依赖，数据各自存 localStorage：

* **🎯 重要日子**：考试/纪念日倒计时，多条记录（名称 + 日期），按日期升序显示「今天/明天/昨天/还有 N 天/已过 N 天」徽标，行尾 ✕ 删除；存 `yhuoDays`。日期比较用本地时区零点（new Date(y, m-1, d)），不要改回 UTC 解析

* **🍅 番茄钟**：专注 25 / 短休 5 / 长休 15 分段切换，开始/暂停/继续/重置，到点 Web Audio 响铃三声 + 数字闪烁并**自动切到下一阶段（不自动开始）**；专注完成计入「今日完成 N」（按本地日期存 `yhuoPomo`，跨天自动归零）；**历史热力图（2026-09-09 深夜批次）**：全量历史按日存 `yhuoPomoHist`（`{"YYYY-MM-DD": 完成数}`；旧键 yhuoPomo 只存当天，读到时并入新表取大者，迁移幂等双写），GitHub contribution 风格最近 15 周 105 格三档色阶 + 累计/连续天数小字

* **🔄 换算器**：单位/进制两个主 tab。单位 = 长度/重量/温度/数据大小四类（温度走公式、其余乘系数，`Number(v.toPrecision(10))` 去浮点尾数）；进制 = 二/八/十/十六，**BigInt 逐位解析**（无精度上限），四行同行显示、点击行复制（剪贴板 API），非法输入显示 —。原生 select 沿用 `select.profilev-input` 先例

* **📝 文本工具**：字数/整理/时间戳/JSON 四个 tab——字数统计（字符/不含空格/词数（中文按字、英文数字连续串按词）/行数，实时）；按行整理（去重/排序/去空行/去首尾空格/转大写/转小写，结果写回输入框）；时间戳转日期（10 位以内按秒、更长按毫秒）+ 当前时间戳；JSON 格式化/压缩（解析失败显示原样报错信息，textContent 写入防注入）

* **🎲 随机决策**：每行一个选项，滚灯式滚动约 1 秒落定；选项即改即存 `yhuoDice`；只有一个选项时显示「（没得选）」

* **🧮 计算器**：四则+括号，求值只放行 `[0-9+\-*/().%\s]`（Function 求值前白名单过滤，杜绝代码注入）；**历史记录**（最近 20 条存 `yhuoCalcHist`，点击一条把结果填回屏幕，头部可清空；连续相同算式不重复入历史）

* **更多工具折叠区**：`<details class="tool-more">` 收纳低频工具——**密码生成器**（crypto 随机 8\~32 位、可含符号、排除易混淆字符 0O1lI、一键复制，ID 未变 JS 原样保留）

* 2026-09-04 起**移除**：旧倒计时（被番茄钟替代）、颜色转换（太垂直）；`.timer-display`/`.timer-controls` 样式保留给番茄钟用，别当死代码删

* 游戏页 /games/ 曾于 2026-09-10 深夜批次加入，同日应用户要求整体移除（相关提交 8c131d4 中 A 部分，移除见 git）。

### 彩蛋（2026-09-09 深夜批次）

两枚，都在 common.js 尾部独立 IIFE（`self===top` 守卫，预览 iframe 不许抢戏）：①**Konami 秘技**（↑↑↓↓←→←→BA）触发流星爆发 + 彩带 canvas + toast「🎮 你发现了彩蛋！」（《灵感池与路线图》页里还留了提示语）；②**离开标签页标题彩蛋**——`blur` 时 `document.title` 变「:( 回来看看嘛…」、`focus` 恢复原标题。零成本高记忆点，改标题相关逻辑时别把这两处冲掉。

### 关于页（/docs/，原「文档」界面）+ 阅读层（docViewer 外壳浮层，任何页面可用）

* 文档卡片列表读 `/docs/docs.json`（手工维护清单——静态托管无目录列表，同坑 1；加文档 = 放 md 进 docs/ + 在 docs.json 加一条 {file,title,desc,date}）

* 点卡片进全屏阅读层（阅读层标题栏文案仍为「文档」，属通用阅读器，未随界面更名）（叠在文档界面上），自写迷你 Markdown 渲染：标题/列表/引用/代码块/粗斜体/链接/图片/分隔线；**先整体 HTML 转义再解析，防 XSS**

* 预置《关于本站》《更新日志》；每次上线建议给 changelog.md 补一条。**《更新日志》走时间轴特例渲染（2026-09-05 起）**：openDoc 检测 file 为 changelog.md 时改用 clTimelineHtml——`## 日期` 变时间轴节点、`* ` 条目变卡片（标题取条目开头 `**粗体**：` 段），超 220 字的条目默认折叠出「展开全部/收起」；其余文档仍走 mdToHtml

### 站点数据卡与公开统计（/api/summary，2026-09-09 深夜批次）

* `GET /api/summary` 公开无鉴权：`ensureSchema` 后并发聚合八项——总访问（site_settings.visits）/今日访问（visit_daily 当日，北京时间口径同 visit.js）/随笔数/留言数/曲目数（media type=music）/注册用户数/累计签到数/建站天数（自 2026-08-29 服务端算，与页脚同起点）。**隐私口径：只给聚合计数，绝不含任何个体记录/IP/UA**（与 2026-09-06 移除 IP 记录的立场一致）；任一条 SQL 挂了整体 `ok:false`，不 500 不泄细节。

* 关于页 main 顶部「站点数据」卡消费：数字 600ms 计数上滚（复用后台 countUp 观感，reduced-motion 直接显示），**接口失败整卡隐藏**（不留空壳）；公告页也挂了《灵感池与路线图》（docs/roadmap.md，docs.json 第三条——做完一条勾一条，访客可见站点走向）。

### 随笔页（/notes/，2026-09-07 起）

对齐参考博客站 `/notes/` 的随笔时间线（单卡片内按年份分组的短内容流），数据静态维护、无后台依赖：

* **数据源链 = /api/notes（D1 notes 表，后台「随笔」页维护，2026-09-07 起）→ 接口失败或空库回落静态 `notes/notes.json`**（手工维护数组，与 docs.json 同思路：`{date:"2026-09-07", mood:"晴", text:"…"}`，`mood` 可空串）。`date` 形如 `2026-09-07`（顺序随意，渲染统一按日期倒序 + 年份分组，同日多条锚点 id 顺延 -2/-3 防重复）；`text` 走 common.js 的 `mdToHtml`（**先整体 HTML 转义再解析，防注入**），支持粗斜体/行内码/链接/列表/引用。两处都拿不到时显示 empty-hint（接口失败会改写提示文案）。**后台录入后静态清单即不再参与显示（空库才回落）**——存量迁移用后台「从静态清单导入」一键搬进 D1。

* **渲染与锚点**：initNotesPage（PAGE_MODULES.notes）fetch 渲染；单条 `<article class="note" id="日期">`——`/notes/#2026-09-07` 可直达/分享，**内容异步渲染原生锚点跳转会扑空**，渲染完成后 locateNote 补 scrollIntoView 居中 + `cmdk-flash` 高亮（pjax 同页/跨页 hash 都经 onHash 进同一入口）。

* **分享卡片（2026-09-10 深夜批次）**：每条随笔正文尾部「生成分享图」按钮——离屏 canvas 900×1200@DPR2 固定暖白纸感风格（不管当前深浅主题：#f8f7f4 底 + #cc7442 顶部光晕 + 陶土圆点 + 双域名水印），正文剥 md 后中文逐字测宽折行、14 行上限省略号收尾，toBlob 下载 `随笔-日期.png`；坑 29 预览 iframe 不渲染按钮。

* **接入点清单**（新增页面的固定套路）：PAGE_ROUTE/PAGE_TITLES 加 notes、PAGE_MODULES 注册 `{init, onHash}`、预览卡 NAV_INFO 加 `'/notes'` 条目、搜索界面快跳加「随笔」（紧跟「首页」）、导航项 `<a href="/notes/" class="nav-link" id="notesBtn">`（applyNavActive 按 id 去 Btn 后缀算高亮）；**无功能开关**（notes 不在 feature_flags 里，始终显示——后台关不掉，属正常）。样式在 site.css 末尾「随笔页」段（.notes-year 年份标签带分隔线、.note-mood 天气/时段胶囊（字段名 mood 沿用参考站，语义是天气/时段如 晴/雨/夜）、.note-body 正文排印），颜色全走语义变量、深浅主题自动跟随。

* check-code.mjs 的 PAGES 已纳入（**八页**外壳一致性比对）；改外壳仍要八处同步（坑 23）。

* **后台管理（2026-09-07 起）**：后台新增第十一项「随笔」标签页（导航位置在「图片」后，胶囊与窄屏抽屉两套 DOM 都有，悬停预览卡 PANEL_INFO 同步）——上方表单新增/编辑（**没有日期项**——随笔即当日随手记：create 服务端自动取北京时间当天（与 visit/checkins 同口径），update 保持原日期只改天气/时段与正文；表单只有天气/时段 ≤12 字 + 正文 textarea），下方全部随笔列表（日期倒序，行尾编辑/删除图标钮，删除走 ask 两段式确认）；「从静态清单导入」把 notes/notes.json 批量搬进 D1（date+text 全同的自动跳过，重复导入零副作用）。注意进页即重置表单（防编辑草稿串场）。接口见 §6。

### AI 助手（/ai/ 页，已实装真模型对话，供应商+多模型）

* **架构**：前端永不接触服务商 Key。后台"AI"标签页管理**模型供应商**（每个供应商独立：名称/协议/Base URL/Key/系统提示词/启用状态，可挂**多个模型**，每个模型可带**类型标签**（文本/视觉/推理，前台切换菜单里显示徽标）；存 site\_settings 表 `ai_models` 供应商数组（models 为 `[{id, tag}]`，旧字符串格式读取时自动迁移）+ `ai_default` 默认模型键"供应商/模型" + `ai_enabled` 全局开关），对话统一走服务端代理 `POST /api/ai/chat`（流式）

* **协议**：每供应商 `openai`（OpenAI 兼容格式，GLM/DeepSeek/Kimi/Gemini 兼容端点等都是这个格式）或 `anthropic`（Claude 原生）；Base URL 留空用协议官方默认；OpenAI 兼容 = base + `/chat/completions`，Anthropic = base + `/messages`

* **流式**：后端把上游 SSE 归一化成统一格式（`data: {"delta":"…"}` 行 / `{"error":"…"}` / `[DONE]`）再转发，前端只解析这一种，加新协议不用动前端

* **AI 页手机布局（2026-09-06 起）**：锁视口应用式布局——body overflow:hidden + 100dvh、页脚不显示（坑：页脚跟在全高聊天区下面把页面撑出 100+px 双滚混乱），仅消息区内部滚动；历史抽屉/遮罩经 #aiView z60 模态化罩住含顶栏的全屏（坑 28）；触屏设备输入框 16px 防 iOS 聚焦缩放；**气泡与输入条贴边**（≤767px：page-main(4vw)+ai-chat(6vw) 双重侧留白叠加出 ~40px 外边距、气泡远离屏幕边，收窄到 ~12px 总留白让气泡/输入条贴向屏幕两侧，顶栏分隔线同步变宽；桌面/平板布局不变，规则在 site.css 末尾 AI 页专属段）；**头部标题「AI 助手」四字与右侧新对话图标已按用户要求移除**（album-bar 只留左侧历史抽屉按钮，顶留白 96→68px、标题行内边距 14→8px——注意子页通用规则特异性 0-3-2，AI 页覆盖选择器必须带 :not() 或多一档类名才能取胜；「新对话」入口保留在历史抽屉顶部；页面 aria-label 与浏览器标签页标题仍是「AI 助手」，搜索面板条目名也未动）。

* **AI 页隐藏播放条（2026-09-06 起）**：html[data-page="ai"] 下迷你播放条与悬浮播放器款式都 display:none（site.css 末尾「AI 页隐藏播放条」段），聊天输入条独占底部；音乐不打断——pjax 只换 main，外壳 audio 照常续播，切回原页播放条/歌词原样恢复。

* **前端行为**：打字机效果、多轮历史；**多对话管理（2026-08-31 起）**——历史对话抽屉（**默认收起**，顶栏气泡按钮唤出（带 aria-expanded 与开启高亮）；**带半透明遮罩**，点遮罩/聊天区/选完对话/关界面自动收起；全屏宽行为一致；**Esc 分层：抽屉开着先只关抽屉，再按才关 AI 界面**）：「新对话」开新会话（旧的保留；头栏与抽屉内两个入口行为一致，都关抽屉；头栏图标是笔=新建语义）、点历史项切换继续聊（上下文延续）、**条目两行式：标题 + 「N 条 · 相对时间」**、当前对话主题色淡底高亮、**删除为留言板同款两段式确认**（删除钮首点变红武装，2.5 秒内再点才删；桌面 hover 浮现、**触屏 `@media (hover:none)` 常显**否则删不掉；删的是当前对话则回开场白）；空状态图标+主副文案；对话自动以第一句用户消息前 20 字命名，每人最多 50 个对话（超出删最旧）、每对话最多 200 条消息；关闭界面不清空，重开恢复**最近活跃**对话；每轮成功的一问一答写入 D1（多模态消息只存 text 部分、图片转「\[图片]」占位）；退出登录清内存对话防串号；恢复的 bot 消息照走 mdToHtml 渲染（先转义防 XSS）；**模型切换在输入条底部工具行**（纯文字+⌄，点击向上弹菜单：按供应商分组、类型标签徽标、当前项 ✓、底部"管理模型"跳后台；只影响之后的回复、历史延续）；**输入条底部「＋」上传附件**（图片≤4MB→base64 多模态发给视觉模型，文本类文件≤200KB→内容注入上下文；一次最多 4 个、chips 可删；附图片但当前模型未打「视觉」标签时提示建议切换；不存服务器）；未配置/未启用时显示"接入中"文案；**全局开关关闭时顶栏 AI 入口直接隐藏（2026-09-04 起，前台启动即拉 /api/ai/config 判定）**；未登录点发送弹登录卡片

* **鉴权与限速**：/api/ai/chat 要求前台用户会话**或**管理员会话（管理员不注册前台账号也能聊）；每用户每分钟 20 条（内存滑动窗口，跨 Worker 隔离实例为近似值）；服务端截断单条 4000 字、最多 20 条；/api/ai/history 同套鉴权

* **语音输入（2026-09-09 深夜批次）**：输入条麦克风按钮，走 Web Speech API——特性检测不过（Firefox 等）按钮根本不渲染；zh-CN 识别，interim 中间结果**实时写输入框末尾（不自动发送）**；识别中按钮主题色脉冲（reduced-motion 只变色不脉冲）；权限被拒 toast 提示；离开 AI 页 stop() 防僵尸识别。

* **人格预设六选一（2026-09-09 深夜批次）**：输入条模型菜单旁同款「人格」菜单（标准/翻译官/程序员/猫娘等六项），存 localStorage `yhuoAiPersona`（iframe 守卫照旧）；后端 `POST /api/ai/chat` 新增可选 `system` 字段（≤500 字截断）——经 lib/ai.js 的 `pick.systemPrompt` **单点并入**：OpenAI 兼容协议覆盖首条 role:"system" 消息、Anthropic 协议覆盖顶层 system 字段，两协议同时生效；只影响之后的回复、历史延续；后台配置的供应商系统提示词仍在前，人格追加在后。

* **模型键回落链**：指定"供应商/模型"失效 → 后台默认模型 → 第一个可用（pickModel 找不到不静默回落，由 chat.js 逐级尝试）

* **历史兼容**：更早的单配置键、再到单模型档案，读取时自动迁移为供应商格式；首次保存后落新格式并清老键

### 相册界面（已移除，2026-09-05）

前台相册界面（顶栏"相册"入口、牌堆封面、照片墙、灯箱、收藏照片卡）已整体摘除，全部代码备份在 `相册功能备份/`（CSS/DOM/JS 分文件 + 《恢复说明.md》列了需要同步还原的 11 处零散修改点）。后台「图片」页的相册分组侧栏与 `media.album` 数据、`user_favorites` 照片收藏数据**保留未动**，恢复界面即恢复功能。

### 音乐迷你播放器

底部悬浮，播放列表、顺序/随机/单曲循环、进度条；列表每行尾 ♥ 收藏音乐（本地文件夹 blob 曲目不可收藏）。**歌名展示名（2026-09-05 起）**：播放条标题/播放列表/搜索里的歌名统一去掉音频格式后缀（musicDisplayName 只剥显示层，tracks[].name 原名保留——收藏与搜索匹配靠原名）。**专辑封面（2026-09-05 起）**：播放条左侧圆盘——当前曲目带封面（后台曲库 media.cover 随 /api/playlist 下发）时显示封面图并**黑胶式旋转**（播放转、暂停保持角度；`prefers-reduced-motion` 不转；中心白点不随图转），无封面回落原音符图标；静态 music/ 曲库与本地文件夹歌曲无封面。封面数据在曲目合并/恢复（缓存接续、后台清单后到）时自动补齐并即时刷新。**律动条（.mini-eq，2026-09-05 起平滑化）**：动画常驻、播放状态只切 animation-play-state——暂停冻结当前姿态、恢复原地接续，不弹回基础高度；delay 用正值（负值会让待机态停在中间姿态）；灰色↔主题色 300ms 过渡；歌词条右侧 .lyric-eq 同款同机制，两处一起改。

### 音乐播放器（双款式，后台可切）

底部音乐播放器有两种款式，后台「外观」页「底部音乐播放器样式」二选一（缺省**迷你播放条**，行为不变；存 site_settings `player_config` {mode:'mini'|'blog', neteaseId, apiBase}，/api/settings 下发 playerMode/playerPlaylistId/playerApiBase，访客下次进页面生效）：

* **迷你播放条（原版）**：站内曲库（后台 KV 曲库 + music/ 静态目录合并清单），收藏、歌词横条联动、搜索定位都基于它
* **悬浮播放器（博客款，2026-09-05 起；2026-09-06 起歌词条联动）**：右下角悬浮球 + 点击 morph 展开面板；**播放列表与迷你条同一份站内曲库**（`window.__siteMusicTracks` 快照，未就绪时轮询等待；不接外部 API），歌名按「歌名 - 歌手」拆分展示、本地文件夹 blob 曲目可播；随机/单曲循环/收藏（本地，存档键 yhuoBlogPlayer）/面板可拖动/≤859px 底部 sheet。与迷你条互斥显示（`html.using-blog-player` 类反向显隐），本地存档 `yhuoPlayerMode` 供首屏预切防闪现。**歌词横条联动（2026-09-06 起，原先只联动迷你条、悬浮款播放时歌词条一直待机）**：blog-player.js 曲目映射保留原始文件名与 media.lrc 文本，经 common.js 暴露的 `__lyricBarApi`（loadTrack/tick/setPlaying/setSource）驱动同一根歌词条——切歌（含播完自动切）载词、timeupdate 驱动打字机、playing/pause 切律动与补完当前句；**歌词时间源跟随"最后开播的播放器"**（`lyricTimeSource`，悬浮款 playing 注入、迷你条 play 事件收回，悬浮款暂停后保留——拖进度条歌词仍跟悬浮款位置），迷你条与悬浮款谁在播谁驱动，互不打架；初始被动恢复（载入上次曲目未播放）不上报歌词，待机文案在悬浮款下指向"右下角悬浮播放器"；`window.__blogPlayerTrack()` 供 pjax 回首页时 lyricRebind 按悬浮款当前曲目补载歌词（优先于迷你条 tracks[current]）。**频谱面板入口（2026-09-09 深夜批次）**：迷你条与悬浮款各注入一个频谱开关（JS 注入 DOM 免外壳同步），详见下节。

### 音乐频谱可视化（2026-09-09 深夜批次）

* **两处入口、谁在播画谁**：迷你播放条与悬浮播放器各有一个频谱开关按钮（JS 注入，免外壳同步，坑 23 规避）；点开浮出 canvas 频谱面板（主题色竖条 ×48，requestAnimationFrame 绘制）。两路 audio（迷你条 + 悬浮款）各建一次 MediaElementSource，**最后开播的驱动绘制**（对齐歌词 lyricTimeSource 思路，互不打架）；迷你条的 audio 是 common.js 闭包变量，已暴露 `window.__miniAudioEl` 供本模块取用，悬浮款按 `[data-music-audio]` 查询。

* **AudioContext 接管铁律（坑 35，改这段前先读）**：`createMediaElementSource` 一旦对某个 audio 调用，该元素的**声音就永久走 AudioContext**——所以 source 建完必须立刻 `connect(analyser)` 且 `analyser.connect(destination)`（漏了就是静音）、**Context 建后绝不 close**（关了声音回不来）、面板开关只能停 rAF 绘制、**绝不能 disconnect**。Context 必须在用户手势（点击开关）内创建（自动播放策略）；`self!==top` 直接不初始化（坑 29）、visibilitychange 停帧、reduced-motion 默认不开启且藏按钮；初始化链路任一步抛错面板显示「此浏览器不支持」并整体停用，**绝不影响正常播放**。

### 外观设置抽屉（右侧）

主题色 5 选 1（**默认陶土色 #b0532b，2026-09-05 起对齐参考博客站配色，原蓝色退役——后台列表与前端 data-accent 键名同步改 terracotta，旧 `blue` 存档 applyAccent 统一归一**）、自定义背景图（本地 localStorage；**背景选择器首格是「🏠 今日壁纸」，2026-09-09 深夜批次加入，见下方每日壁纸段**）、背景变暗、背景模糊（0\~30px 滑杆，存 localStorage `bgBlur`；`filter: blur` 加在 `#bgLayer` 上并配 `scale(1.08)` 消除模糊边缘软边；对默认无背景图状态无视觉效果；访客没自己调过时采用管理员设的站点默认 `bg_blur`，调过则以本地为准，此时滑杆下方出现`跟随站点默认`按钮可清除本地存档切回默认）、两个动效开关（大卡片 3D 跟随 / 小图片倾斜）——开关真实控制所有相关动效，且都受 `prefers-reduced-motion` 尊重。抽屉 z-index 160（高于全屏界面 150，任何界面里箭头都可见可点；低于灯箱 200/登录卡 400）。**默认白色主题（2026-09-05 起）对齐参考博客站暖白纸感配色**：底 `#f8f7f4`/面 `#fff`/文字 `#1b1c1e`/次级面 `#f0ede6`/边框 `#e6e3da`/次要文字 `#5f6166`，直接写在 :root 浅色语义变量上（theme-vars 块内有注释），`.dark` 全量重定义不受影响；改浅色配色就改这一处，别去动 background-50 等原色调色板（暗色也在引用）。**顶部暖橙光晕（同日补）**是参考站观感的另一半：与底色一起放在 **html（画布层）** 的 background 第一层（radial-gradient fixed 顶 540px，亮 #cc7442 16%/暗 #e7a37c 12% 起）——**别放 body 上、也别用 body::before z-index:-1**：body 的不透明盒背景绘制在负 z 层之上会盖住光晕/流星（踩过两次）；`.bg-layer`（自定义背景）有 `display:none` 门控、`body.has-custom-bg` 才显示（body 透明后默认态 25% 压暗层会漏出来把整页压灰）。**流星特效（2026-09-05 起；2026-09-06 出生带自适应化；2026-09-07 二次修正：出生带均匀分布下路径穿过左下角的迹线在屏时间短、观感仍稀——改为「屏幕目标点反推 + 左下角几何带补充群（数量≈主群 70%）」，触屏窄屏 15→18 颗，实测 390×844 左下角采样 5→29 与其他区域同量级）**：`.fx-meteors` 全屏负 z 固定层（在 bgLayer 之后，自定义背景之上），30 颗（触屏窄屏 18 颗、reduced-motion 0 颗），JS 在 assets/common.js 流星段、CSS 在流星段（搜 fx-meteor）；**出生横坐标 --left 按视口几何自适应**（common.js birthLeft()）：流星沿 rotate(215deg) translate(-180vh) 向右下 35° 斜落，水平行程恒为 1.474×视口高、与屏宽无关——固定出生带（示例原值 -35%~+90%，曾让左下角整块没有；2026-09-06 早间改 -80%~+100%，宽屏够用）在窄屏（横行程可达 3 倍屏宽）会整体挤在上半对角区、下三分之一全无（用户实报"窄屏只有一小部分"，390×844 十秒采样底部三区全 0）。现为反推最左出生 = 路径恰好从左缘 95% 高度进入（minPct = -((0.95h+64)×1.427/w)×100），右端 +100% 不变；窗口 resize/旋转屏 200ms 防抖重排全部出生坐标。修后 390×844 九宫格采样九区全覆盖（29/17/12 等）、宽屏 1280×720 无退化；浅色也显示（原站仅暗色），theme-vt 快照期间隐藏；**个人主页/课表浮层（.profile-view）打开时流星层经 `body:has(.profile-view.show)` 抬到 z 155**（150<155<外观抽屉/头像下拉 160），否则毛玻璃浮层观感是"独立实心背景"、流星全被盖住（2026-09-06 用户反馈）；浮层关闭自动回落 z:-1，日常"流星在内容后面"的设计不变；**同触发条件的顶缝配套**：浮层从 52px 起铺玻璃，自定义背景下顶条(0~52px)露原图原样、与下方磨砂判若两界（用户反馈）——`body:has(.profile-view.show) .site-header::before` 给头部透明壳补一条与浮层同参数的磨砂带（高 52px、65%+blur14），头部 z50<浮层150 故带子只在顶条露出正好补齐缺口，胶囊/进度条绘制其上不受影响、壳 pointer-events:none 不挡点击，关闭随 :has 失效自动消失；源码参考 `博客动画源码/`（本地目录不入库）。

**外观圆钮（2026-09-08 起，替代原白色耳朵箭头；同日追加自由拖动）**：40×40 圆形毛玻璃小钮（调音推子图标），`position:fixed; right:12px; bottom:84px; z 165`——bottom 避让回顶钮(≤62)与音乐球(≤60)，z 高于外观面板 160（抽屉开着时浮在面板上就是关闭把手）、低于搜索 350/登录 400；毛玻璃配方同顶栏胶囊，深浅主题各一档，图标 chevron→sliders 八页同步（坑 23）。**自由拖动**：按住拖到任意位置松手即停，位置按「视口右距/下距」存 localStorage（yhuoAppearKnob）跨刷新/换页保持，resize 回贴并 clamp 在屏内；位移 ≥5px 判拖动否则点击开关抽屉（收尾 click 用 `_dragMoved` 跳过）；move/up 监听挂 **document**（setPointerCapture 对合成输入不可靠，挂钮上指针拖出范围会半路断听）。**坐标纪律（坑 30）**：全程用 clientWidth/Height（不含滚动条）——innerWidth 含滚动条与 rect 系混用会 15px 级漂移（恢复位偏移+二次拖动失效）。**必须挂在 body 下**：曾挂在 .appear-dock 内，dock 收起 translateX(100%) 会把拖到左侧的钮带出屏外（transform 劫持 fixed，坑 28 同机制），用户实报后已挪出。AI 页按 `html[data-page="ai"]` 隐藏圆钮。

**滚动模糊揭示（2026-09-07 起，接入参考博客站 04 号特效）**：栏目卡等内容进入视口时从「blur 10px + 下移 20px」0.6s 渐清晰，同批按 60ms 级联（`--reveal-delay` 封顶 6 档），已揭示不重播；位置判断 `top < 94% 视口` 对快速滚动/锚点/滚动恢复稳健（跳过的直接出现不补播）。**目标清单（与 site.css「滚动模糊揭示」段保持同步）**：`.album-bar/.apple-card/.tool-card/.tool-more/.doc-card/.note/.notes-year/.notes-lead`，**全部限定 `.page-main` 内**——外观抽屉/个人主页/登录卡在外壳也是 `.apple-card`，绝不能命中；首页 hero（有自己的入场与布局恒定修复）与 AI 流式消息不参与。**文档阅读层（docViewer，2026-09-07 补）**：mdToHtml 顶层块 + 更新日志时间轴按天（`.cl-day`）单独一档（`#docArticle` 作用域，`.cl-timeline` 本身不参与防嵌套双重动画）——阅读层关着不采集（display:none 量到 0 会被误判全部入视口），`.doc-scroll` 是内部容器滚动要单独挂 scroll 监听，openDoc 注入正文经 #docViewer 上的 MutationObserver 重扫。**实现**：采集+揭示在 common.js **顶部独立 IIFE**（放最前：后面模块抛错时揭示监听已就位，不会内容常隐）；pjax 换页与 docs/notes/board 异步插图靠 **main 上 MutationObserver 80ms 防抖重扫**，不侵入各模块；**门控**：head 内联脚本在 JS 可用且非 reduced-motion 时给 `<html>` 打 `fx-reveal` 类（与 ff-* 预隐藏同思路防首帧闪现；无 JS 内容常显），common.js 启动打 `__fxRevealBoot` 标记、head 3 秒兜底摘类（防脚本加载失败内容永隐）——改这段要**八页 head 同步**（坑 23）；CSS 在 site.css 末尾「滚动模糊揭示」段，用 **animation + fill backwards** 不用 transition（过渡常驻会压过卡片 hover 过渡；backwards 结束零残留，不踩坑 22 的 transform 包含块雷）。⚠️ 测试注意：本地 python 服务器资源无 cache-control，浏览器走启发式缓存——改完 HTML/CSS/JS 必须 Ctrl+F5，曾因旧缓存 HTML（无门控类）误判功能失效（旧 HTML + 新 JS 恰好走优雅降级全显，本身是设计内路径）。

### 每日壁纸（/api/wallpaper，2026-09-09 深夜批次）

* **两个公开接口**：`GET /api/wallpaper` 返回当日 Bing 壁纸 `{ok, url, copyright}`（Bing 官方接口双源，服务端 `caches.default` 边缘缓存 6 小时）；`GET /api/wallpaper/image?u=…` 图片反代——`u` 只放行 `.bing.net` 等官方域白名单（缺失/不合法/域不对一律 400），流式回传图片字节 + `Cache-Control: public, max-age=86400`（1 天），并统一带 Referer 出网（图床校验 Referer，不带会 403）。访客浏览器不直连 Bing 图床——国内直连不稳定，全走站内反代。

* **前台入口**：外观抽屉背景选择器**首格**「🏠 今日壁纸」（openBgPicker 内 JS 注入 DOM，免外壳同步）——点击即把背景设为当日壁纸（`/api/wallpaper/image?u=…` 走既有背景存档链，与自定义背景同路径、可清除），toast 小字显示版权说明（copyright）。

### 天气联动粒子（2026-09-09 深夜批次）

common.js 末尾独立 IIFE（不碰流星段）：每 **60 秒**轮询天气胶囊已缓存的 Open-Meteo 数据取 weathercode——**雨类（51-67/80-82）起雨丝 ~90 颗、雪类（71-77/85-86）起雪片 ~60 颗**（按视口宽高自适应），天气类别变化时清空重生成；canvas fixed z-1 与流星层同级（自定义背景之上、内容之下），`prefers-reduced-motion` 与预览 iframe 双守卫（坑 29），visibilitychange 停帧、resize 防抖重设。天气接口失败/未获取到天气 = 无粒子（静默，与天气胶囊「失败静默隐藏」同口径）。

### 登录与管理员

* 登录卡片：开放注册（用户名 2-30 字符 + 密码 ≥6 位），服务端 PBKDF2；**登录/注册/找回/二次验证模式切换带平滑动效**（输入行高度展开收起、注册按钮宽度渐变、文案淡出淡入；`prefers-reduced-motion` 时直接切换）；关闭卡片为上飘滑出退场

* **邮箱体系（2026-08-31 起，后台配好邮件服务才生效，否则相关 UI 全部隐藏）**：

  * 后台「邮件」标签页：服务商（resend/brevo，都是 HTTP API——Workers 无原始 TCP 不能 SMTP）+ API Key（存 site\_settings `email_config`，不回显，编辑留空=保留）+ 发件地址 + 站长邮箱（清空后点保存即**移除**，"仅站长"自动关闭）+ 「仅站长使用」开关 + 启停 + 测试发送 + **自定义邮件**（任意收件人+主题+纯文本正文 ≤5000 字，HTML 转义防注入）+ **课表提醒定时任务配置**（tick URL 复制/重新生成密钥/立即执行一次/**发送测试提醒**——不用等真实到点，按真实课表立即发早报/课前提醒样式的【测试】邮件，主动验证提醒全链路/**cron 访问留痕**——tick 接口每次被访问（无论密钥对错）都记时间戳到 site\_settings，页面显示最近一次访问时间与结果，直接看出外部 cron 有没有真的在跑、是不是在用过期 URL 敲 401）+ 概览页**邮件统计**卡片（见 §5）

  * **仅站长模式（无域名邮件服务的正确用法）**：Resend 无域名时 onboarding 地址只能发给自己——开关打开后普通用户注册**不要求邮箱**、个人主页不显示邮箱卡，只有站长邮箱（后台填的那个）能收验证码/绑定/开 2FA/找回密码；注册邮箱验证仅在全员模式下生效（/api/settings 的 `emailRegister` 字段控制前端显隐）

  * 注册（全员模式）：需邮箱 + 6 位验证码（60 秒重发间隔）

  * 忘记密码：登录卡片「忘记密码？」→ 邮箱验证码 + 新密码重置；重置后踢掉该账号全部会话

  * 绑定/换绑邮箱：个人主页「邮箱」卡（验证码验证；换绑后 2FA 自动关闭）

  * 登录二次验证（2FA）：个人主页开关（需已验证邮箱）；登录密码通过后服务器发码，凭中间票据 + 验证码换正式会话；验证码输错票据作废需重新登录

  * 验证码安全：SHA-256 哈希存储、10 分钟有效、限 5 次尝试、重发覆盖旧码

* **登录成功弹顶部欢迎浮窗**（管理员 👑 + "进入后台"按钮；普通用户 👋 欢迎回来，无按钮——**普通用户也有**，2026-08-31 起）；退出登录弹「👋 已退出登录，期待下次再见」；均 6 秒自动消失、刷新不重复弹（sessionStorage adminWelcome/userWelcome 标记，两套标记互清防换账号误弹）；**绝不能用 /api/auth/status 判断"是不是管理员"**——它只反映浏览器有无管理员会话 Cookie，分不清当前登录的是谁，见 §9 坑 11）

* 未登录/游客点头像 → 弹登录卡片；**首次到访不弹登录卡（2026-09-07 用户要求）**——仍查 /api/user/me 但只做静默会话恢复（Cookie 有效则恢复登录态与头像、触发课表页钩子），无会话/失败保持匿名浏览（不挂 login-lock）；需登录的动作（留言发送/收藏/AI 发送/进课表页）各自照常 openGate

### 头像与个人中心下拉菜单

* 顶栏人像按钮三态：未登录人形图标 / 已登录用户名首字徽章 / 有头像显示圆形头像图

* 已登录点头像 → 下拉菜单在头像下方缩放淡出（右缘/顶缘 JS 对齐头像按钮留 8px，窄屏 12px；再点/点空白/Esc 收起）

* 菜单项：我的主页 / 更换头像 / 移除头像（没头像时隐藏）/ 退出登录（红色危险项）

* 登录态三键存 sessionStorage（demoLogin / demoLoginName / demoLoginAvatar），退出/游客清空；头像存 KV `avatars/u{userId}-{随机hex}.{ext}`，JPG/PNG/GIF/WebP ≤2MB 前后端双校验，换图删旧

### 个人主页（profileView 全屏层）与收藏

* 下拉菜单"我的主页"进入；身份卡（88px 头像可点更换、用户名、已加入天数/注册日期/上次活跃）+ 修改密码（旧密码验证+新密码≥6位+两次确认，服务端限速同登录）+ 更换头像/退出登录

* **昵称（2026-09-05 起）**：身份卡用户名旁「改昵称」入口——展示名优先（昵称空则显示用户名），1~20 字、留空保存即恢复用户名；改完即时同步顶栏字母徽章/下拉菜单/欢迎语（sessionStorage `demoLoginName` 存展示名）。存 users.nickname 列；留言板历史留言的署名取实时昵称（改昵称同步旧留言显示）。接口 `POST /api/user/nickname`

* **收藏**：相册灯箱 ♥ 收藏照片、播放列表 ♥ 收藏音乐；未登录点 ♥ 弹登录卡片

* 收藏以**站点内路径（pathname）为标识**，静态文件与后台媒体统一处理，不与 media 表关联（媒体删除后收藏自然失效，主页渲染时静默过滤）

* 个人主页聚合展示：收藏照片（点击→关主页→回相册开大图，走 yhuo:open-photo 自定义事件）、收藏音乐（按歌名回曲库定位播放，找不到禁用）

* 收藏状态源是 IIFE 级 `favSet`，登录/注册/退出时经 `favLoad()` 同步

* **课表（2026-08-31 起；2026-09-06 起为真实页面 /schedule/）**：入口 = 顶栏「课表」真链接（七页外壳都有；曾先做成毛玻璃浮层，用户反馈"改成其他界面一样不要白色浮层"后改回常规页面——自定义背景与流星随页面自然呈现）。页面化要点：课表全部代码（元素查询/绑定/渲染/保存）封装在 `initSchedPage()` 页面模块（PAGE_MODULES.schedule，pjax 每次进出重查重绑；document 级监听——编辑器 Esc/拖选 mouseup——经 schedPageCleanup 离页摘除防叠加）；**未登录进页弹登录卡**（页面本体留白），登录/游客门通过后经 `onGatePassedPageHook`（passGate 末尾触发、init 注册 destroy 注销）自动拉取课表；编辑弹窗 #schedEditor 的 markup 放在课表页 main 内（随页面装卸，勿移回外壳）。原个人主页课表卡仍不存在（2026-09-06 早些时候已从个人主页抽出）。没启用过只显示「＋ 启用课表」入口，启用后才展开；「清空课程」按钮（2026-09-06，工具栏右侧危险色）移除全部课程但**保留课表本体与作息、提醒设置**——两段式确认（首点变红显示「确认清空 N 门？」，2.5 秒内再点才执行；注意前台没有后台的 ask() 组件，「移除课表」走的其实是原生 confirm）；「移除课表」删全部数据——周视图（按教学周过滤、‹›切周；点/拖空白格快速加课并预填星期节次；**课程块配色 2026-09-06 起为 20 色白字友好色板 + 互不撞色分配**：renderSchedGrid 每轮渲染给当前课表出现过的课程名从名字哈希位线性探测分配颜色，同名课恒同色、不同名的课在 20 色内互不重复，课程超过 20 门才回落哈希复用——旧实现仅 8 色纯哈希取模，课一多就撞色）、导入 WakeUp 课程表导出文件（JSON 和 CSV 都支持：JSON 兼容两套字段名 courseName/name、roomName/room、teacherName/teacher、startNode/startSection、endNode/endSection、weeks 支持布尔数组/数字数组/文本"1-16"、"2-16双"；CSV 按表头名匹配列（课程名称/开始节数/结束节数/老师/地点/周数等别名），支持"第1,2节"/"周一"/数字星期；前端按扩展名+内容自动分流，PUT {wakeUp} 或 {wakeUpCsv}，服务端解析、保留提醒设置）。**导出课表（2026-09-05 起）**：工具栏「导出课表」按钮纯前端把当前课表转成 WakeUp 兼容 JSON 下载（`WakeUp课表导出-日期.json`）——本站「导入 WakeUp」可直接导回（提醒设置/作息服务端保留），WakeUp App 也能识别；weeks 转布尔数组（下标 0=第 1 周），内部空 weeks（=每周都上）导为全 true 保语义无损；无课程时提示不生成。**课程编辑为居中弹窗**（缩放弹起+回弹动效；Esc/点遮罩关闭），控件全胶囊化：星期 7 颗、节次起止数字胶囊（选中显示开课时间）、周次快选（全学期/单周/双周/前半/后半）+ 自定义输入；改动时周视图实时半透明预览。提醒设置：每日早报 iOS 拨动开关 + 时间快选（6:30\~8:00）、重点课提前 15/30/60 分钟快选、作息时间逐节可改。数据整份存 schedules 表，每次改动全量 PUT。**提醒发送**：外部 cron（cron-job.org 每 5 分钟，已在用）GET `/api/schedule/tick?key=…`（密钥在后台「邮件」页管理，可重新生成——**重生成后必须同步更新 cron 任务里的 URL**，否则提醒静默停发），接口按北京时间计算当日课程并发邮件（每日早报到点未发则发；重点课进入 \[开课-提前量, 开课) 窗口发），schedule\_sent 表幂等防重发；发给用户绑定并已验证的邮箱，仅站长模式下跳过非站长邮箱。**管理员课表（2026-09-04 起）**：管理员身份同样可用课表（/api/schedule 鉴权同时接受用户会话与管理员会话，按会话分流）——课表数据存 site_settings.admin_schedule（JSON 同构），提醒发给后台绑定管理员邮箱（site_settings.admin_email），未绑定则发站长邮箱（email_config.owner_email），仅站长模式下限定站长邮箱；防重发记录键用 user_id=-1

* **签到（2026-09-04 起）**：个人主页签到卡（身份卡下方，仅前台注册账号，管理员隐藏）——每日签到一次（北京时间计天，同日重复 POST 幂等返回 already），显示**等级徽标（Lv.0~6，按累计签到天数）+ 等级名 + 距下一级进度条 + 累计/连续天数 + 近 7 日圆点**；等级规则在服务端 `functions/api/user/checkin.js` 的 LEVELS 常量（阈值 0/5/15/40/80/150/300 → 初来乍到/渐入佳境/常客/老朋友/中坚力量/镇站之宝/传奇），等级不入库实时计算，改阈值改数组即可（存量数据自动重算）；连续天数断一天清零（今天未签时按昨天起算显示历史连续，签上即接回）；满级（Lv.6 传奇）徽标金色、进度恒 100%。想调整显示（如把等级徽标挂到身份卡/顶栏）前端拿 GET 返回的 level 即可。**成就徽章墙（2026-09-09 深夜批次）**：8 枚徽章（累计 1/7/30/100/300 天 + 满级 + 连续 7/100 天），挂在近 7 日圆点下方——全部由签到 GET 已返回的字段**实时计算，零后端改动不入库**；未达成的徽章悬停 title 显示还差 N 天；容器动态挂在 #ckWeek 之后，跟随签到卡整体显隐（卡片隐藏/管理员模式天然不出现），签到成功即时刷新

* **已知边界（2026-09-04 起部分解除）**：管理员账号不在 users 表，个人主页现在能正常打开——显示管理员身份卡（用户名/后台设置的头像/管理员标识与创建时间），顶栏也直接显示管理头像（登录响应与 /api/user/me 均识别管理员会话并带回后台头像）；**收藏/改密/邮箱/签到仍仅限前台注册账号**（管理员个人主页隐藏这些卡）；**课表对管理员开放**（见下方课表段，管理员课表存 site_settings.admin_schedule，提醒发管理员绑定邮箱/站长邮箱）。想用收藏就注册个前台账号

## 5. 管理后台功能明细（/admin）

**布局（2026-09-06 起宽屏顶栏胶囊化，复刻前台顶栏）**：宽屏（>900px）用前台同款悬浮毛玻璃胶囊顶栏做导航（`.admin-header`/`.header-pill`：头像圆（原 brandMark，点击进「我的」）+ 概览/音乐/视频/图片/随笔/短链/用户/外观/AI/邮件/我的/状态 十二项纯文字导航，active=反色 chip；右侧 header-actions= 回前台链接/黑白主题切换/退出登录；胶囊外点击穿透、滚动 >10px 加深投影（.scrolled），毛玻璃样式移植前台（rgba 硬编码+saturate blur）并把前台变量映射到后台 --fg/--bg/--hover/--border）；窄屏（≤900px）胶囊收窄为 汉堡+头像+动作钮，导航回**原侧边栏抽屉**（aside.sidenav，汉堡唤出/遮罩点外部收起/选完页自动收起，图标列表样式沿用）；⚠️ 胶囊与抽屉是**两套导航按钮 DOM**（#sideNav 纯文字 + #drawerNav 图标版），navBtns 选择器同时覆盖两处，active 状态同走 switchPage；**顶栏导航项悬停预览卡（2026-09-06 起，复刻前台 fx-link-preview）**：悬停胶囊导航项 150ms 弹出栏目实时缩略卡（320×112 视觉区 + 栏目名/描述 meta）；后台是单页应用、十二栏目共用 /admin 一个地址，故视觉区放**一个常驻 iframe**（加载 /admin 自身，1280 宽 scale(0.25)），悬停不同栏目时 **postMessage（type=adminPreviewPanel）让 iframe 里的 admin 实例 switchPage 切面板**——悬停哪个栏目就实时预览哪个；iframe 在登录门/加载态收到的消息先存 pending、enterMain 末尾补应用，iframe 就绪后回发 adminPreviewReady 通知父页补发；触屏不启用（hover+fine 指针守卫），主题自动跟随（iframe 共享 localStorage adminTheme）；⚠️ 后台有全局 `button` 内边距（9px 18px），胶囊里的圆钮必须显式 `padding:0` 抵消，否则 svg 被挤成 0 宽；黑白主题（顶栏胶囊右侧切换，本地记住）；提示用 toast、确认/输入用自定义弹窗（ask，不依赖原生 confirm/prompt）。单文件实现。**图标体系**：JS 顶部 `ICO` 常量集统一提供线性 SVG（16px/stroke2/currentColor），行内操作钮一律用图标（✏️▶👁↑↓⋯★ 等文本字符禁用——不同系统渲染不一致且发虚）；状态色走 `--ok/--warn/--danger` 变量（深浅主题各一档，**别写死 hex**——删除确认钮/相册菜单危险项曾硬编码 #d64545/#e0342b，2026-09-04 改回变量）；**删除类按钮（button.danger）悬停转危险色**（文字/边框/浅红底全走 --danger 的 color-mix）；列表行内操作桌面悬停浮现、触屏常显。注意 `image-shell` 里包着媒体列表（第 362 行有全局 `[hidden]{display:none!important}`），switchPage 只收起 `.album-side`、外壳必须常显，否则音乐/视频列表会消失（踩过已修）。**动效约定（2026-09-06 对齐参考博客站缓动体系）**：**动效令牌**在 `:root`——`--ease-soft`（cubic-bezier(.16,1,.3,1) 长缓出）/ `--ease-outc`（.22,1,.36,1）/ `--ease-spring`（.34,1.56,.64,1 轻回弹）/ `--t-fast .22s`、`--t-med .4s`、`--t-reveal .6s`，新动效一律引用令牌别写死值。**页面切换「模糊揭示」**：switchPage 给目标面板挂 `.panel-enter`（同一面板不重播）→ 面板上浮+blur 收焦，面板内 `.card`/`.stat` 按 `--stagger-i`（switchPage 写入、封顶 8）级联 55ms——**入场动画 fill 只用默认/backwards，别用 both**：坑 22 前台同款，fill:both 会持续占 transform 把面板内 fixed 后代（相册菜单）的包含块改掉；**侧栏滑动胶囊指示器（已随顶栏胶囊化退役，2026-09-06）**：原 `#sideInd` 竖排滑动指示器与 moveSideIndicator() 已删除，选中态=导航按钮**反色 chip**（前台同款：background var(--fg) / color var(--bg)）；**主题切换圆形揭示**：View Transitions——startViewTransition + 对 `::view-transition-new(root)` 做 element.animate 的 clip-path circle（半径按归一化对角线换算百分比，px 在 2x 屏只画一半），themeVTBusy 锁防连点，Firefox/`prefers-reduced-motion`/无 VT 回退即时切换；**统计数字滚动**：countUp() 仅首次从 0 填充时播（后续刷新直接落值不反复播）；卡片悬浮轻抬（hover:hover 才启用）、按钮 ：active 弹簧缩放、弹窗/列表/吐司走弹簧与模糊曲线。弹窗（ask/预览）与登录门四卡互切、进入主界面的入场动画**全靠 CSS**——hidden 切换（display none→显示）会自动重播 `:not([hidden])` 规则里的 animation，加新弹窗直接套 `.modal` 结构即可，别用 JS 加类；`prefers-reduced-motion` 是**全局压缩规则**（所有动画/过渡瞬时化，不再逐个枚举——漏一个就还有动的；全后台无 infinite 动画所以 iteration-count 收 1 安全；countUp 是 JS 动画自己查 mediaQuery 跳过）；有全局 `:focus-visible` 焦点环（键盘导航可见、鼠标点击不触发），别再给单个控件手写 outline。**坑 18 提醒**：admin/index.js 是模板字符串，往里加 JS 不许出现反引号和 `${`，正则反斜杠双写。

**UI 组件层（2026-09-11 UI 现代化，改后台页面/样式前先读）**：语义变量已对齐前台（浅色暖白纸感 #f8f7f4 底/#e6e3da 边框/#f0ede6 chip，深色纯黑 #000 底/#1c1c1e 卡），新增 `--brand`（浅 #b0532b / 深 #c96a42，对齐前台 terracotta）与 `--on-brand`——主按钮、输入焦点环、进度/存储/用量条、访问趋势与邮件趋势柱状图强调色、开关 on 态（geo-toggle 与新 .switch）、滑杆与勾选框 accent-color 全走 brand，新加控件别写死色值。组件层 CSS 集中在「组件层（2026-09-11 UI 现代化）」注释段：`.page-head` 页面标题区（大标题+.ph-desc 说明+.ph-actions 右侧主操作；媒体页三档子页共用一个面板，标题/说明由 switchPage 动态写 #mediaPhTitle/#mediaPhDesc）/ `.section-card`+`.section-title`/`.section-sub` 分组卡 / `.field`+`.form-grid` 字段与两列栅格（窄屏自动单列）/ `.data-table` 统一表格 / `.list-row`+`.chip-tag`（变体 ok/bad/banned/mono）+`.lr-main/.lr-grow/.lr-side` 统一列表行 / `.empty-state` 图标空状态 / `.sk`/`.sk-row` 骨架屏 / `.switch` 自绘开关（包原生 checkbox，input 语义保留、JS 照常读写 checked/disabled）/ `.seg-btn` 分段钮 / `.meter-row` 用量条 / `.mini-bars` 迷你柱图 / `.search-box` 搜索框（放大镜+一键清空，清空派发 input 事件复用既有过滤；switchPage 清空搜索词时要同步摘 has-value 类）/ `.char-count` 字数计数器（`attachCounter(el, max)` 挂载，超 90% 转警示；随笔 2000/邮件正文 5000/AI 提示词 2000/寄语 100）。加载态一律用 JS 顶部助手 `skListHtml(n)`、空态/失败态用 `emptyStateHtml(icon, title, hint)`（配合 ICO 常量），别再写「加载中…」裸文本与裸段落空态。单行输入按 Enter 提交走 `enterToClick(inputIds, btnId)`（登录门/初始化/短链创建三处已接）。级联入场的 stagger 查询已扩到 `.section-card`/`.page-head`（CSS 的 .panel-enter 级联规则同步）。旧 `.st-row`/`.badge` 样式已删除，随笔/短链/设备/登录记录/备份清单/RUM 行都是 .list-row，行尾操作统一包 `.row-actions`（桌面悬停/聚焦浮现、触屏常显，.list-row 的 hover/focus-within 已并入同一条 CSS）。

**媒体页上传区与图片网格（2026-09-11 后续轮次，改这三块前先读）**：①**上传区**（音乐/视频/图片三页共用）是居中式大拖放区——`.upload-row`（min-height 130px、居中列布局、陶土 `.ur-ico` 大图标 + 「点击选择文件」钮），`#uploadHint` 在框内做副文案且**被 renderAlbumSide 按相册视图动态改写**（相册/未分组视图会追加「新上传自动归入」提示），别挪出框外；显示名称与上传按钮在框下 `.upload-actions` 行；**整块拖放区可点**（click 委托到 fileInput，`closest('button')/closest('input')` 直接返回防重复弹文件框），hover 陶土虚线浅陶土底、拖入 `.dragover` 实线加深。②**选中文件确认条** `.file-pick-info`：绿色（--ok）浅底横条 + 「✓ 已选择」前缀，位于拖放区正下方；`renderPickInfo()` 把每个文件渲染成胶囊 `.pick-chip`（名称+大小，textContent 防注入），胶囊上 `.pick-x` 点击经 **DataTransfer 重建 input.files** 实现单文件移除（纯前端，上传逻辑不变），头部「N 个文件，共 X」实时同步，全部移完自动收起；上传完成的既有清理流程（fileInput.value='' + 摘 .show）与其兼容。③**图片列表缩略图网格**：图片页给 `#list` 挂 `.img-grid`（**用 classList.toggle**，别整块重写 className——会碰掉 list-swap 动画类；音乐/视频不挂仍是行式），卡片结构 `.ic-thumbs`（4:3 大缩略图，点击预览）+ `.ic-ov` 悬停渐变操作层（白色玻璃底按钮，**只留图标且紧凑化保证单行**——`span` 被 CSS 隐藏、图标 14/padding 4×5/gap 3，5 钮一行 ~132px < 最小列 164px 去掉层内边距的 ~152px，单行数学上必成立；b881dcf 的「允许换行」治标不治本已废弃，flex-wrap 仅作兜底；说明走 title；触屏常显）+ `.ic-info`（名称=行内重命名入口/大小·日期/相册下拉 chip）；勾选框左上角悬停或选中时显示、选中卡片边框转陶土（`:has(.sel:checked)`）；拖卡片到左侧相册归类、↑↓ 排序、批量删、搜索过滤全部保留，改这块只动样式与容器结构别碰各 handler。

**概览标签页**：统计区（6 张卡：音乐/视频/图片/注册用户/总访问量/今日访问，**数字首次填充时有滚动动画**）+ 访问趋势柱状图/环形图切换（近 14/30 天切换，悬停联动看单日，SVG 无依赖；环形图活动日 ≤8 全画、更多只画 Top 7 + 灰色「其他」聚合段（点击列出逐日明细），今天段默认外移 + 图例「今」徽标，环左图例右双列；悬停交互为"进即弹、离缓回"（落下瞬间弹出到位、移开 0.2s 缓回），且命中层/视觉层分离防抽搐——见 §9 坑 19；按天明细从上线起积累）+ **AI 用量卡片**（今日/近 14/近 30 天 tokens 与调用次数 + 按模型排行表格；数据来自前台对话后上报，`ai_usage_daily` 表）+ **邮件统计卡片**（今日/近 14/近 30 天发送量 + 按用途分列：验证码/测试/自定义/课表早报/课表课前提醒；`sendMail` 成功后自动按天写入 `email_usage_daily`，记账失败不影响发送）。**注**：原「最近访问」IP 明细卡与归属地查询已于 2026-09-06 整体移除（用户要求）——`/api/visit` 不再记录 IP/UA/访问明细（总量与按天计数保留，趋势图不受影响），`/api/admin/visit-logs`、`/api/admin/geoip` 接口删除；线上残留的 `visit_logs` 旧表如需清数据可在 D1 控制台执行 `DROP TABLE IF EXISTS visit_logs;`（命令也记在 migrate.js 注释里）。

**音乐/视频/图片标签页**：

* 上传：多选文件 + 拖拽文件/整个文件夹，队列逐个传、总进度条、当前文件名；同名（带/不带扩展名都比对）、超 24MB、格式不符自动过滤并在结果里说明；**音乐页可同时选入 .lrc**——与同 basename 的歌曲自动配对成歌词附件随上传入库（.lrc 不单独成条目），未配对上的会在结果里提示改用行内「歌词」按钮

* **首页视频播放模式（视频页顶部，2026-09-04 起）**：顺序循环 / 单视频循环 / 随机播放三选一（存 site_settings.video_mode，/api/settings 下发前台首页视频轮播）；单视频模式时在视频列表点行尾星标「设为独播」指定循环哪一条（实心金星=当前独播，设置栏显示"独播：视频名"，默认第一个）；指定视频被删自动回落顺序模式

* 列表：搜索（搜索/筛选视图下隐藏排序控件）、批量勾选删除、✏️ 改名、大小+上传时间、音乐 ▶ 试听 / 视频 ▶ 预览 / 图片 👁 查看（弹窗，Esc/遮罩关闭）；**音乐行另有「歌词」按钮**（补传/替换 .lrc，≤200KB；已配歌词显示 ✓ 和移除 ✕；歌词存 media.lrc 列，保存后前台刷新生效）**和「封面」按钮**（上传/替换专辑封面 jpg/png/gif/webp/avif ≤2MB，存 media.cover（KV covers/），换图删旧、删除歌曲连封面一并清理；已设显示 ✓ 和移除 ✕；保存后前台刷新生效）

* 存储用量条：全部媒体合计 / 1GB，超 80% 提示清理

* 拖拽或 ↑↓ 排序

* **相册侧栏（仅图片页）**：左侧相册栏（全部图片/未分组/各相册，带张数徽标）；点击筛选；**拖图片到相册名上即归类**（勾选状态下拖任意已选行=整批移动；与列表内拖拽排序共存：落在侧栏=归类、落在行上=排序）；勾选后点相册名=批量移入（弹确认，移完自动清勾选）；每相册 ⋯ 菜单：重命名/**解散相册（不删图，图片回未分组）**；新建相册在侧栏底部（≤50 字，**落 albums 表刷新不消失**——2026-09-06 根治空相册不落库，见 §9 坑 5）；每行仍有归属下拉（自定义组件，与 AI 面板同款 `makeAiDrop` 带展开动画；列表频繁重绘，全局关闭监听里会对宿主已断连的实例自动清理注册表）；在某相册视图上传自动归入该相册（上传提示行会说明）

* **静态媒体自动同步**：打开后台自动对比三个清单（images/manifest.json、music/playlist.json、video/playlist.json），把静态文件夹里还没进后台的文件批量搬进 KV（每批 12 个），同名跳过；往静态文件夹加删文件后重跑对应 bat 并推送

**用户标签页**：列表（状态/注册时间/最后活跃）、搜索用户名、注册时间新→旧/旧→新切换、禁用（踢下线且无法登录）/解封/删除（不可恢复）。最后活跃 = 登录时 + 进主页时（me 接口 1 小时节流防频繁写库）。

**外观标签页**：全站默认主题色（5 色）、默认背景图（上传存 KV，换图自动删旧）、**默认背景模糊**（0\~30 滑杆 + 保存按钮，存 site\_settings `bg_blur`，0=清除；访客本地没调过模糊滑杆才采用，调过则以本地为准）、**主页寄语**（多条列表编辑，每条 ≤100 字、最多 20 条，前台随机显示其一；全部删除恢复每日一言；存 site\_settings `quotes` JSON 数组，旧单条 `quote` 读取时自动并入）、**功能开关**（顶栏界面 工具/文档 + 首页模块 天气胶囊/歌词横条/首页视频 逐个关闭，前台直接隐藏；AI 跟随「AI」页全局开关不在列；存 site\_settings `feature_flags`，全部为开自动清键；相册/杂项项已随界面移除）、**底部音乐播放器样式**（迷你播放条/悬浮播放器二选一，共用站内曲库，存 site\_settings `player_config`，见 §4 播放器段）、**备份导出**（一键下载 JSON：全部媒体条目+访问地址+站点设置+访问统计；KV 文件本体不含，重要文件另行下载；2026-09-09 深夜批次起另有 D1 四表每日自动备份，见「状态」页数据备份卡与 §6 /api/admin/backup）。

**AI 标签页**：供应商管理界面（仿客户端模型设置页）——左侧供应商列表（立方体图标+绿点=启用/空心点=停用，选中高亮，底部"＋添加供应商"，上限 10 个）+ 右侧详情表单（名称✏️重命名、已启用绿徽标、禁用/启用按钮、右上🗑删除；Base URL；API 格式下拉 = 协议；API Key 带👁显示切换；系统提示词；**模型列表**每行 模型名+标签下拉（无标签/文本/视觉/推理）+☆设默认/✏️改名/🗑删除，＋添加模型内联输入（带标签选择），↻自动获取经服务端代理拉 `GET {base}/models`）+ 保存供应商/测试连接 + 全局启用开关（位于供应商管理界面下方）。页面顶部无说明文字。API Key 保存在 D1（明文），界面永不回显完整 Key（只显示尾 4 位），编辑时留空 = 保留原 Key。**下拉均为自定义组件**（`makeAiDrop`，带展开/收起/翻转动画、点空白/Esc 关闭，替代原生 select——原生弹层是系统渲染的做不了动画）；供应商详情切换有淡入、"添加模型"内联表单有展开动画（该容器 overflow:hidden 会裁掉内里下拉弹层，靠 `open-ov` class 在展开完成后放开裁剪，别删）。

**短链标签页（2026-09-09 深夜批次）**：站内短链管理（导航位置在「随笔」后，胶囊与窄屏抽屉两套 DOM 都有，悬停预览卡 PANEL_INFO 同步）——上方表单创建：目标链接必须 http(s) 完整链接（跳转直接塞 Location，其它协议不收）；自定义短码 2~32 位字母数字_-，**留空自动生成 6 位**；撞站内保留字黑名单（admin/api/assets/media/九个页面路由/feed.xml/sw.js/robots.txt 等共 24 项，防短码冒充站内路径）或撞已有短码（UNIQUE）都给友好 400。下方全部短链列表：/s/码 点击一键复制完整短链（协议+域名现场拼，本地预览/线上都拿到可用地址）、目标地址、创建时间（北京时间）、点击次数，删除走 ask 两段式；码/url 均来自用户输入，行渲染一律 textContent/DOM API 防注入。访问侧 `GET /s/{code}`：校验码格式 → SELECT → UPDATE clicks+1 → **302 + no-store**（防边缘缓存把计次跳掉），未命中返回自足 404 页。短链数据落 short_links 表（进 migrate DDL 自动建）。

**我的标签页**：管理员资料卡——圆形头像（88px，无头像时显示"YH"字母徽标）+ 更换/移除头像（JPG/PNG/GIF/WebP ≤2MB，存 KV `avatars/admin-{hex}.{ext}`，键名存 site\_settings `admin_avatar`，换图删旧，读取走 /media/{key}）+ 管理员用户名与创建时间 + **管理员邮箱绑定/解绑**（邮件服务启用才显示；验证码验证后存 site\_settings `admin_email`，绑定后后台登录页可用「忘记密码」走邮箱验证码重置管理员密码，重置踢全部管理员会话）。**侧边栏左上角的 logo 也是头像**（`#brandMark`，有头像显示图、没有显示 YH 徽标），点击任意位置可直接跳"我的"页；头像在进后台时（enterMain）加载。

**我的标签页 · 安全卡（2026-09-05 起）**：
* **修改密码**——旧密码验证 + 新密码 6-100 位（限速复用登录限速 scope=pwd，错 5 次锁 10 分钟）；成功后**踢掉其他设备**（当前登录保持），这是此前完全没有的改密入口（原唯一途径是邮箱验证码重置）
* **登录二次验证（2FA）**——滑动开关存 site\_settings `admin_2fa`；开启前提：**已绑管理员邮箱且邮件服务已启用**（否则邮件链路故障时管理员会被锁在门外）。开启后**两个登录入口**（后台登录页 + 前台主页管理员直登）密码通过后都向管理员邮箱发 6 位验证码（purpose=`admin2fa`），凭一次性票据（`email_login_pending`，user\_id 取负数=管理员，与 schedule\_sent 的 -1 约定同源）+ 验证码换会话；验证码错一次票据即作废需重新登录。**⚠️ 若开启后邮件服务故障，管理员会被锁死**（登录止步于发信失败），届时只能直接改库：删 site\_settings 的 `admin_2fa` 键即可恢复密码登录
* **登录设备**——列出全部有效管理员会话（IP/UA/登录时间，标记"本设备"，不回传 token），「退出其他设备」一键吊销除当前外的全部会话
* **最近登录**——最近 10 条登录记录（成功 ✓/失败 ✕ + 原因 + IP + 时间），`admin_login_logs` 表只留最近 100 条；本次上线后开始积累

**安全**：管理员登录限速——同一 IP+用户名连续失败 5 次锁 10 分钟（后台登录和主页管理员直登两个入口都拦），成功登录清零；用户自助改密的旧密码试错复用同套限速（scope=pwd）。

**状态标签页 · 数据备份卡（2026-09-09 深夜批次）**：D1 每日自动备份——`functions/lib/backup.js` 把 notes/messages/checkins/users **四张业务表全量 JSON** 存 KV（键 `backup:YYYY-MM-DD`，北京时间日期；**轮换保留最近 7 份**；sessions/限速等会话类表不备）。触发方式是**搭课表 tick 的便车**：外部 cron（cron-job.org 每 5 分钟）每次敲门时 `maybeRunDailyBackup` 顺带检查当天备没备、没备就补——免注册新 cron（Pages Functions 本就不支持定时触发）；`waitUntil` 执行不拖慢 tick 响应、失败绝不影响提醒主流程、`backup:lastdate` 只在全查成功后写（防半途失败落了标明天却不补）。卡上显示最近 7 份清单（日期/大小/各表条数 counts）+「立即备份」（无视 lastdate 强制跑一次）+ 按日期「下载」（attachment 直下原始 JSON）；无备份时提示点「立即备份」生成第一份。`/api/health` 新增 `backupLastDate` 字段（见 §6），监控备份有没有断天。

## 6. API 一览

前台（公开）：

* `GET /api/playlist` — 媒体清单（music/video/images，images 带 album，**music 带 lrc 歌词文本（配了才有）与 cover 封面地址（设了才有）**）；失败返回 ok:false 前台自动走静态兜底

* `GET /api/settings` — 站点默认外观（accent/background/quotes/blur/emailEnabled/emailRegister/**videoMode**（首页视频播放模式 {mode:'seq'|'single'|'random', url}）/**playerMode**（底部播放器样式 mini|blog，缺省 mini，两种款式共用站内曲库）/**flags**（功能开关 {tools,docs,weather,lyric,video}，缺省全开；album/misc 已随界面移除，旧库存键忽略）/quote 兼容字段=第一条；emailRegister=注册需邮箱验证，仅站长模式下 false），缓存 60s——前台拉取统一带时间戳参数穿透浏览器缓存（后台改完设置下次进页面立即生效，2026-09-05 补）

* `POST /api/visit` — 访问计数（总量 + 按天）

* `POST /api/user/{register,login,logout}`、`GET /api/user/me` — 前台用户（管理员账号从 /api/user/login 登入会发管理员会话并返回 admin:true、avatar=后台管理头像；/api/user/me 同样识别管理员会话，返回 admin:true + 管理用户名与头像；login/me 响应带 avatar）；register 在邮件服务启用时需 {email, code}；login 支持二次验证（密码通过返回 {ok:false,needCode,ticket}，再发 {username,ticket,code} 换会话）

* `POST /api/email/code {email, purpose:'register'|'reset'}` — 公开发验证码（注册/找回密码；60 秒重发间隔；发送失败自动作废刚写的码）

* `GET /api/user/email` / `POST /api/user/email {action:'bind-send'|'bind-verify'|'toggle2fa'}` — 绑定邮箱状态与操作（需登录）

* `POST /api/user/password {email, code, newPassword}` — 邮箱验证码重置密码（免登录，踢全部会话）；PUT 仍是旧密码改密

* `POST/DELETE /api/user/avatar` — 用户头像（multipart file，JPG/PNG/GIF/WebP ≤2MB，存 KV avatars/，换图删旧）

* `GET /api/user/profile` — 当前身份资料：前台用户 {username,avatar,created\_at,last\_seen\_at}；管理员会话返回 {admin:true, username, avatar(后台管理头像), created\_at}（前端据此显示管理员身份卡并隐藏仅前台可用的功能卡）

* `PUT /api/user/password` — 自助改密 {oldPassword,newPassword}，验证旧密码 + 登录限速复用（scope=pwd）

* `GET/POST/DELETE /api/user/favorites` — 收藏列表 / 添加 {type:'image'|'music',url,title} / 移除 {url}；url 必须是站点内路径，同 url 唯一（INSERT OR IGNORE 幂等）

* `GET/POST /api/user/checkin` — 签到状态 / 今日签到（2026-09-04 起，仅前台用户会话）；GET 返回 {checkedToday,total,streak,week(近 7 天含 today 标记),level:{lv,name,total,prev,next,nextName}}；POST 重复签到返回 {ok:false,already:true,status}（幂等不报错）；等级按累计天数实时算（LEVELS 在 functions/lib/levels.js，与留言板共用），(user_id,day) 主键防重

* `GET/POST/DELETE /api/messages` — 留言板（2026-09-05 起）：GET 最近留言（?offset 分页每页 30，条目带用户名 + 签到等级徽标 + 真实头像（users.avatar_key，站长用 site_settings.admin_avatar；无头像/失效回落首字）数据；2026-09-09 深夜批次起路人条目带 isGuest:true、level:null，渲染 🎭 首字块 + 灰色路人徽章）；POST {content}（≤500 字；前台用户或管理员会话同一用户 60 秒一条；**未登录路人可发（2026-09-09 深夜批次）**——无会话按「路人」落库：复用 messages.is_admin 整型列加值 2（0=注册用户 1=站长 2=路人，零建表）、user_id=0，身份凭 `yhuo_guest` 随机 Cookie（16 位 hex，无则现发，180 天长效匿名标识），单人 60 秒一条滑动窗口 + 全局每分钟 10 条兜底**双限速**（超限 429；内存 Map 超 500 条自动清理））；DELETE ?id=N（仅管理员会话，留言板管理）

* `POST /api/user/nickname` — 设置昵称 {nickname}（2026-09-05 起，仅前台用户会话；1~20 字，空串=恢复用户名）。login/register/me/profile 响应均带 nickname；留言板署名 = COALESCE(昵称, 用户名)

* `GET/PUT/DELETE /api/schedule` — 课表读取（GET 带 exists 标记是否启用过）/ 整份保存（需登录）；**鉴权接受前台用户会话或管理员会话**：用户课表存 schedules(user\_id)，管理员课表存 site\_settings.admin\_schedule（JSON 同构）；PUT 另支持 {wakeUp:\<WakeUp导出JSON>} 或 {wakeUpCsv:'\<CSV文本>'} 服务端解析导入（替换课程、保留设置）；DELETE 移除整份课表（提醒停止）；数据结构见 functions/lib/schedule.js 头注释

* `GET /api/schedule/tick?key=…` — 课表提醒定时入口（外部 cron 每 5 分钟调；密钥存 site\_settings `schedule_tick_key`，后台「邮件」页管理）。幂等：schedule\_sent 表防重发。响应带 `users` 逐账号诊查明细（邮箱打码：今日课数/早报开未开/到点未/今天发过没/重点课提醒窗口命中数/跳过原因），后台「立即执行一次」会把这些原因直接显示出来

* `GET /api/ai/config` — AI 可用状态 {enabled, model(默认键), models:\[{provider,name,key,tag}]}（不含任何敏感信息，key="供应商/模型"供切换回传）

* `POST /api/ai/chat` — 流式对话代理（见 §4 AI 助手段；需登录，入参 {messages, model?}，model=key、缺省/失效回落默认；content 支持字符串或 OpenAI 风格多模态 parts（text + dataURL 图片，≤4 张、单 dataURL ≤9M 字符），Anthropic 协议由后端自动转换 parts 格式；响应 text/event-stream，**结束前会发** **`data: {"usage":{prompt,completion}}`** **事件**——OpenAI 兼容协议已自动带 `stream_options.include_usage`，Anthropic 从 message\_start/message\_delta 攒出；入参另有可选 `system` 字段（2026-09-09 深夜批次，AI 人格预设）：一句话人格提示词 ≤500 字截断，经 pick.systemPrompt 单点同时覆盖 OpenAI 兼容首条 system 消息与 Anthropic 顶层 system 两协议，只影响之后的回复）

* `POST /api/ai/usage` — 前台把流里拿到的 token 用量上报入账（需登录；入参 {key, prompt\_tokens, completion\_tokens}，按北京时间 day+供应商+模型 累加进 ai\_usage\_daily）

* `GET/POST/DELETE /api/ai/history` — 对话历史持久化（同 chat 鉴权：前台用户或管理员；GET 无参返回最近活跃对话+消息、?conv=N 返回指定对话；POST {messages,conv?} 批量追加并自动建对话/命名/挪到最前；DELETE ?conv=N 清指定对话消息、无参清全部）

* `GET/POST/DELETE /api/ai/conversations` — 对话列表（GET 含标题/时间/条数，按最近活跃排序）/ 新建空对话 / ?id=N 删除对话及消息（历史栏用）

* `GET /api/notes` — 随笔清单（2026-09-07 起，公开）：`{ok, list:[{id,date,mood,text}]}` 按日期倒序；前台 /notes/ 数据源，失败/空库回落 notes/notes.json 静态清单

* `GET /api/summary` — 站点公开聚合统计（2026-09-09 深夜批次）：`{ok, data:{totalVisits, todayVisits, notes, messages, tracks, users, checkins, days}}` 八项，**只计数不含任何个体数据**（隐私口径见 §4）；关于页站点数据卡消费，失败 ok:false 前台整卡隐藏

* `GET /api/wallpaper` / `GET /api/wallpaper/image?u=…` — 每日壁纸（2026-09-09 深夜批次，公开）：前者 `{ok, url, copyright}`（Bing 双源，边缘缓存 6h）；后者图片白名单反代（`u` 只放 *.bing.net 等官方域，其余 400；缓存 1 天、统一带 Referer 出网）

* `GET /feed.xml` — RSS 2.0 订阅（2026-09-09 深夜批次，公开；functions/feed.xml.js）：D1 notes 取 50 条（标题=日期+天气、正文转纯文本），**空库回落静态 notes/notes.json**；channel 挂自定义域名；八页 head 已挂 alternate 链接，订阅器（Follow/NetNewsWire）可直接收

* `GET /s/{code}` — 短链跳转（2026-09-09 深夜批次，公开）：校验码格式 → 查库 → clicks+1 → **302 + no-store**（防边缘缓存吞计次），未命中自足 404 页；管理在后台「短链」标签页（见 §5）

* `GET /api/health` — 可用性自检（只回布尔，不泄细节）；2026-09-09 深夜批次起带 `backupLastDate`（最近一次每日备份日期，读 KV backup:lastdate，读失败为 null——监控备份断天用）

后台（`/api/admin/*` 有 `_middleware.js` 会话门卫，未登录一律 401）：

* `GET/POST /api/admin/notes` — 随笔管理（2026-09-07 起）：GET 全部（含 created\_at）；POST 动作式（与 albums 同款）`{action:'create'|'update'|'delete'|'import', ...}`——create 收 {mood?,text}（**日期服务端自动取北京时间当天，前端不传**）、update 收 {id,mood?,text}（日期保持原值不参与更新）、delete 收 {id}、import 收 {list:[...]}（date YYYY-MM-DD 格式校验，保留原日期——用于搬存量）；mood ≤12 字、text 1~2000 字超限 400；import 按 date+text 全同跳过，返回 {imported, skipped}

* `GET /api/admin/backup` — 每日备份清单（2026-09-09 深夜批次）：最近 7 份 `{ok, list:[{date, size(字节), counts:{notes,messages,checkins,users}}], lastdate}`；`GET ?date=YYYY-MM-DD` — 下载该份备份原始 JSON（attachment 头，浏览器同源带 Cookie 直接下）；`POST` — 立即强制备份一次（无视 lastdate）。自动备份机制见 §5 状态页数据备份卡（搭课表 tick 便车、KV 轮换 7 份）

* `GET/POST/PUT/DELETE /api/admin/links` — 短链管理（2026-09-09 深夜批次）：GET 清单 / POST 创建（url 必须 http(s) 开头 ≤2048 字，码留空自动 6 位；自定义码 2~32 位字母数字_-，撞 24 项站内保留字黑名单或撞已有码（UNIQUE）友好 400）/ PUT 改目标地址 / DELETE 删除

* `GET/POST /api/admin/media` — 清单（**响应多带 `albums`：albums 表相册名单含空相册**，前端与 media.album 派生名取并集展示）/ 重排 {type, ids}

* `PUT/DELETE /api/admin/media/:id` — 改 title 或 album 或歌词 {lrc}（非空文本=保存 ≤200KB，''=移除；**album 非空时同步 upsert albums 行**）/ 删除（连 KV 文件与专辑封面）

* `POST/DELETE /api/admin/media/:id/cover` — 专辑封面（2026-09-05 起）：POST multipart {file} 上传/替换（jpg/png/gif/webp/avif ≤2MB，存 KV covers/m{id}-*.{ext}，换图删旧）；DELETE 移除（删 KV + 置 NULL）

* `POST /api/admin/upload` — multipart 上传（type/file/title?/album?/lrc?），单文件 ≤24MB；lrc 仅音乐类，.lrc 文本附件 ≤200KB 随歌曲入库；album 非空时同步 upsert albums 行

* `POST /api/admin/import` — 静态媒体批量导入 {type, files:\[{title,url}]}（≤12/批）

* `POST /api/admin/albums` — {action:'create'|'rename'|'delete'}：**create 新建空相册**（落 albums 表，重名 400）；rename 同步改 albums 行与 media.album（batch 三语句：先改行名 → INSERT OR IGNORE 兜底只存在于 media 的名 → 再改 media；目标名已存在 400）；delete 解散（图片回未分组 + 删 albums 行）

* `GET /api/admin/users`、`PUT/DELETE /api/admin/users/:id` — 用户管理

* `GET/PUT /api/admin/appearance` — accent/quotes/blur/flags/player（flags 白名单已无 album/misc；PUT {quotes:\[...]} 每条≤100 字最多 20 条、空数组=清除；{blur:N}，0 清除；{flags:{...}} 白名单键布尔值、未传的键视为开、全部为开=清除；{player:{mode:'mini'|'blog'}} 播放器样式，见 §4 播放器段）；`POST/DELETE /api/admin/appearance/background` — 默认背景图

* `GET /api/admin/me` — 管理员资料 {username, created\_at, avatar(KV 键或 null), email, emailEnabled, twoFa, sessions(有效会话列表：current/ip/ua/created\_at), logins(最近 10 条登录记录)}；`POST /api/admin/me` — multipart 上传管理员头像（JPG/PNG/GIF/WebP ≤2MB，存 KV avatars/admin-\*，键写 site\_settings `admin_avatar`，换图删旧）/ JSON {action:'email-send'|'email-verify'|'email-remove'} 管理员邮箱操作；`PUT /api/admin/me` — 同 JSON（邮箱操作 + {action:'password',oldPassword,newPassword} 改密踢其他设备 + {action:'sessions-revoke-others'} + {action:'2fa',enabled}）；`DELETE /api/admin/me` — 移除头像

* `GET /api/admin/ai` — 供应商列表（Key 不回传）；`PUT /api/admin/ai` — {enabled} 全局开关（**仅无 action 时生效**）/ {action:'save',provider} 新增更新（含 models 数组；api\_key 留空=保留原 Key）/ {action:'rename',from,to} / {action:'toggle',name,enabled} 单供应商启停 / {action:'delete',name} / {action:'default',key}；`POST /api/admin/ai/test` — 测试连接 {key 或 name}（真发一条消息）；`POST /api/admin/ai/models` — 拉取服务商模型列表（{name?,protocol?,base\_url?,api\_key?} 草稿值优先，api\_key 留空回落指定供应商已存 Key；不落库）；`GET /api/admin/ai/usage` — token 用量统计（总/今日/近14天/近30天 + 按模型排行，概览页"AI 用量"卡片用）

* `GET /api/admin/visits` — 总量/今日/昨日/近 30 天明细

* `GET/PUT/POST /api/admin/email` — 邮件服务配置（GET Key 只回尾 4 位；PUT {enabled,provider,from,owner\_email?,admin\_only?,api\_key?} 留空=保留 Key，开 admin\_only 必须有合法 owner\_email；POST {to} 测试发送）

* `GET/PUT /api/admin/video-mode` — 首页视频播放模式（GET 返回 {mode,url}；PUT {mode:'seq'|'single'|'random', url?}，只有 single 需要 url，存 site\_settings.video\_mode）

* `GET/POST /api/admin/schedule` — 课表定时任务密钥（GET 首次自动生成并回 tick URL；POST {action:'regenerate'} 重置密钥，旧 URL 失效；POST {action:'test', email?} **发送测试提醒**——按真实课表算"今天该发什么"并立即发【测试】邮件（早报样式必发 + 今天有重点课时加发课前提醒样式），不写 schedule_sent 防重发记录、真实提醒不受影响；收件人须是绑定了已验证邮箱且启用过课表的前台账号，留空=站长邮箱，仅站长模式下强制只发站长邮箱；响应含教学周/星期/今日课程明细，kind=sched-test 计入邮件统计）

* `GET /api/auth/{status,logout,setup}`、`POST /api/auth/login`（限速生效；{username,password} 或 2FA 第二步 {ticket,code}，开 2FA 时返回 needCode+ticket）；`POST /api/auth/reset {email,code,newPassword}` — 管理员邮箱验证码重置密码（免登录，只认 site\_settings `admin_email`，重置踢全部管理员会话）

会话体系（互不影响）：管理员 Cookie `yhuo_session`（7 天，HttpOnly/Secure/SameSite=Strict）；前台用户 Cookie `yhuo_user`（30 天）。

## 7. 数据库表（D1，自动创建）

| 表                     | 用途                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| admin\_users          | 管理员（PBKDF2 + salt）                                                                                                                                                                                                                                                                                                                                                                   |
| sessions              | 管理员会话                                                                                                                                                                                                                                                                                                                                                                                |
| users                 | 前台用户（banned / last\_seen\_at / avatar\_key / email / email\_verified / twofa\_enabled / nickname——展示名，空=用用户名，见 §4 昵称段）                                                                                                                                                                                                                                               |
| user\_sessions        | 前台用户会话                                                                                                                                                                                                                                                                                                                                                                               |
| media                 | 媒体条目：type(music/video/image)、title、r2\_key(KV键)、mime、size、sort\_order、**album**(相册名，''=未分组)、**lrc**(音乐歌词 .lrc 文本，NULL=无)、**cover**(专辑封面 KV 键 covers/，NULL=无，见 §4 播放器/§5 音乐页)、created\_at                                                                                                                                                                                                                                                                              |
| site\_settings        | key-value：visits(总访问)、accent、bg、quotes(主页寄语 JSON 数组，旧 quote 单条自动并入)、email\_config(邮件服务 {provider,api\_key,from,enabled,admin\_only,owner\_email})、admin\_email(管理员绑定邮箱，重置后台密码用)、ai\_models(AI 供应商数组 \[{name,protocol,base\_url,api\_key,enabled,system\_prompt,models:\[{id,tag]}]])、ai\_default(默认模型键"供应商/模型")、ai\_enabled（AI 助手配置）、admin\_avatar(管理员头像 KV 键)、admin\_schedule(管理员课表 JSON，用户课表在 schedules 表，见 §4 课表段)、video\_mode(首页视频播放模式 {mode:'seq'|'single'|'random', url}，见 §4 首页段)、**player\_config(底部播放器样式 {mode:'mini'|'blog'}，见 §4 播放器段)**、bg\_blur(站点默认背景模糊 px)、feature\_flags(前台功能开关 {tools,docs,weather,lyric,video}，缺省/缺失=开，全部为开自动清键，见 §4 顶栏导航；album/misc 已随界面移除，旧库存键忽略) |
| ai\_usage\_daily      | AI token 用量（北京时间 day + provider + model 聚合：calls/prompt\_tokens/completion\_tokens；前端对话后上报，概览页展示）                                                                                                                                                                                                                                                                                    |
| ai\_conversations     | AI 对话（会话）：owner 隔离、title（第一句前 20 字自动命名）、updated\_at；每人最多 50 个（超出删最旧）；左侧历史栏用                                                                                                                                                                                                                                                                                                          |
| ai\_chat\_history     | AI 对话历史（owner 隔离 + conv\_id 关联会话；role+content 纯文本；每对话最多 200 条自动裁剪；旧单条历史迁移时自动归入"历史对话"）                                                                                                                                                                                                                                                                                                |
| visit\_daily          | 按天访问计数（北京时间 YYYY-MM-DD）                                                                                                                                                                                                                                                                                                                                                              |
| login\_throttle       | 登录限速（key=scope:ip:username，fails/locked\_until）                                                                                                                                                                                                                                                                                                                                      |
| email\_codes          | 邮箱验证码（PK=email+purpose；code\_hash 哈希存储；attempts 限 5 次；10 分钟过期懒清理）                                                                                                                                                                                                                                                                                                                    |
| email\_login\_pending | 2FA 登录中间票据（ticket→user\_id，10 分钟有效，用后即焚）                                                                                                                                                                                                                                                                                                                                             |
| user\_favorites       | 前台用户收藏：type(image/music)、url(站点内路径，UNIQUE user\_id+url)、title                                                                                                                                                                                                                                                                                                                        |
| schedules             | 用户课表（每用户一份 JSON：termStart/nodeTimes/courses/daily/remindAhead，结构见 functions/lib/schedule.js 头注释）                                                                                                                                                                                                                                                                                     |
| admin_login_logs     | 管理员登录记录（成功/失败 + IP/UA/原因，只留最近 100 条，「我的」页安全卡展示）                                                                                                                                                                                                                                                                      |
| messages              | 留言板（is_admin=1 为站长留言 user_id=0；**is_admin=2 为路人留言（2026-09-09 深夜批次，user_id 同为 0 靠本列区分，身份凭 yhuo_guest Cookie）**；60 秒一条由接口侧限制；等级徽标按 checkins 实时算，见 lib/levels.js）                                                                                                                                                                                                                                |
| albums                | 相册本体（name 主键 + sort_order 预留相册排序；图片归属仍以 media.album 为准，凡写 media.album 的接口同步 upsert 本表，存量在 ensureSchema 回填；2026-09-06 根治空相册不落库，见 §9 坑 5）                                                                                                                                                             |
| checkins              | 每日签到（PK=user_id+day，day=北京时间 YYYY-MM-DD；等级按累计天数接口侧实时计算不入库，规则见 functions/api/user/checkin.js LEVELS）                                                                                                                                                                                                                                                                  |
| schedule\_sent        | 课表提醒发送记录（PK=user\_id+day+kind+ref，kind=daily/class，防重复发送）                                                                                                                                                                                                                                                                                                                            |
| email\_usage\_daily   | 邮件发送量按天计账（PK=day+kind；sendMail 成功后写入，概览页“邮件统计”卡片展示）                                                                                                                                                                                                                                                                                                                                  |
| short\_links          | 短链（code 主键、url、created\_at 北京时间、clicks 计次；2026-09-09 深夜批次，进 migrate DDL 自动建；访问走 /s/{code} 302+计次，管理走 /api/admin/links，见 §5/§6）                                                                                                                                                                                                                                  |

## 8. 本地开发与调试

* **只看前台**：双击 `启动网页.bat` → <http://localhost:8000（python> http.server；`停止服务器.bat` 停止）
* **注意：wrangler 本地同样吃 `_headers` 的缓存头**（/assets/* max-age=3600）——改完 common.js/site.css 起 wrangler 实测时浏览器可能拿 1 小时内的旧资源（症状：新按钮/新分支不出现但代码明明在），必须 Ctrl+F5 强刷再下结论，别误判功能失效（坑 31 的 wrangler 变体，2026-09-10 深夜实测踩过）

* **带后台完整调试**（推荐）：

  ```
  node node_modules/wrangler/bin/wrangler.js pages dev . --d1 DB --kv MEDIA --port 8799
  ```

  * 本地 D1/KV 状态在 `.wrangler/`（已 gitignore，可随时删掉重置本地数据；当前本地库有测试账号 admin/admin123 与 tester/test123456——2026-09-09 重置过本地库并重新 setup）

  * wrangler 装在本地 node\_modules（已 gitignore）；重装：`npm install --no-save wrangler`

  * 本地测试时管理员账号在 .wrangler 重置后要重新 setup

  * 停 wrangler 注意杀干净：TaskStop 后常残留 node/workerd 进程锁着 `.wrangler/` 的 sqlite 文件，删库前先 `taskkill` 掉（按 8799 端口或进程名找）。**只 kill workerd 不够——父 node（npx/wrangler.js）会把它重新拉起**，要用 PowerShell `Get-CimInstance Win32_Process` 按 CommandLine 含 wrangler 把 npx-cli.js → wrangler.js → workerd 整条链全杀；端口被僵尸占用时请求表现为「TCP 能连上但永远无响应」（netstat 见 CLOSE_WAIT 挂着），别误判成代理问题。另外删 `.wrangler/state` 必须在进程杀干净**之后**——文件被锁时 rm 会部分失败，留下「半清空」库（报表存在但查询 no such table）。测接口发中文 JSON 用 UTF-8 文件 `--data-binary @file`（下一条）+ curl 加 `--noproxy "*"`（系统代理开着时避免本机请求被代理截胡）

* **改** **`index.html`** **/** **`functions/admin/index.js`** **后**：直接跑 `校验代码.bat`（node check-code.mjs）——一键校验全部内联 `<script>` 语法 + functions/ 的 ESM 语法与 import 路径 + 本地静态引用（或老办法：提取后 `new Function()` / 浏览器开控制台）

* **改** **`functions/`** **的 ESM 文件后**：本地 package.json 没有 "type":"module"，直接 `node --check` 会报 import 错误——校验脚本内部已按 .mjs 临时拷贝处理，无需手工

* **改了音乐/视频/图片静态文件后**：重跑 `生成歌单.bat` / `生成图片清单.bat` 再推送

* Windows 控制台 curl 发中文 JSON 会乱码（GBK 终端问题）：用浏览器或 python 写 UTF-8 文件再 `--data-binary @file` 测

* 备份/短链等新接口（2026-09-09 深夜批次）本地实测命令参见 changelog 或直接 curl：管理员登录后 `POST /api/admin/backup` 立即备份、`GET /api/admin/backup` 看清单、`GET /s/某测试码` 看跳转与计次；sw.js 改动用 `node --check sw.js` 验语法

## 9. 已知坑（历史修复记录，改代码时别踩回去）

1. **静态托管无目录列表**：music/、video/、docs/ 目录没有目录列表（直接访问 404），必须有清单文件兜底（docs/ 用 docs.json，加文档记得同步清单）
2. **Cloudflare SPA 回退**：对不存在路径返回 200 + 首页内容（不是 404）。解析目录列表"没有音频/视频"时必须抛错转清单兜底，不能静默 return。**多页面改造（2026-09-06）后**：/tools/ 等是真实文件，静态资源命中优先于回退；回退只剩真正未知的路径会命中，pjax 的 popstate 对未知路径 `location.reload` 交给服务器处理
3. **`/media/*`** **的两个坑**：KV 读取必须用 getWithMetadata（拿 mime）；catch-all 路由的 params.key 是数组
4. **import.js 相对路径坑（2026-08-30 修复）**：清单里的相对路径（`images/1.jpg`）必须按**站点根**解析；直接拿 `request.url` 当 base 会落到 `/api/admin/*` 被会话门卫 401，静态同步"全部跳过"且无报错
5. **空相册不落库（2026-09-06 已根治）**：原状——相册列表由 media.album 派生，新建的空相册靠前端会话级 `extraAlbums` 数组保持可见，刷新后空相册消失。**现为 albums 表**（migrate.js DDL + 存量回填 `INSERT OR IGNORE … SELECT DISTINCT album FROM media`），`POST /api/admin/albums` 加 create 动作、rename/delete 改 batch 双写 albums 表与 media.album（rename 三语句防主键冲突：先改行名 → INSERT OR IGNORE 兜底只存在于 media 的名 → 再改 media；目标名已存在 400），upload 与 `PUT /api/admin/media/:id` 写相册名同步 upsert albums 行，`GET /api/admin/media` 响应带 albums 名单（顺带补上该接口一直缺的 ensureSchema），后台前端 extraAlbums 补丁删除、改 serverAlbums 与派生名取并集展示（派生并集天然兜底任何漏网数据）——空相册刷新不再消失。相册排序 UI 仍待做（sort_order 列已备好，见 §11）
6. **KV 限制**：单文件 24MB（接口已拦），总量 1GB；GitHub 单文件 <100MB、仓库建议 <1GB
7. **浏览器自动播放**：只允许静音自动播放，首页视频 muted+controls
8. **换行符**：仓库在 Windows 检出为 CRLF，写正则匹配 `<script>` 块时要兼容 `\r?\n`
9. **严格模式块级函数不外提（2026-08-30 踩过）**：前台主 IIFE 是 `'use strict'`，个人中心代码一度插在 `if (loginGate) { ... }` 块内，导致顶栏监听里调块内的 `setProfileOpen` 直接 ReferenceError（无任何可见报错，面板点不开）。此类跨块调用的函数必须放在块外；文件里已留注释
10. **authPost 回调签名**：`authPost(path, u, p, done)` 的 done 收到的是对象 `{ok, error}`，不是 `(ok, msg)` 布尔（注册回调曾因此把失败当成功，已修）
11. **判断"是不是管理员"只能用登录响应的 admin:true（2026-08-30 两次踩坑，当日根治）**：/api/auth/status 的旧字段 authenticated 只看浏览器有无有效管理员会话 Cookie（7 天），分不清当前登录的是谁——曾导致普通账号登录被误跳后台，后来欢迎浮窗初版又因此对普通用户误弹。**根治**：status 接口已改为自描述的会话分列（`adminSession`=管理员会话存在、`userSession`={userId,username} 或 null=前台登录人、`authenticated` 保留=adminSession 兼容旧缓存），语义不再有歧义；但前台欢迎标记机制不变（只由登录响应 admin:true 写入 sessionStorage，页面加载只认标记）——前台退出登录只清用户会话，不清 yhuo\_session
12. **外部接口的网络坑（2026-08-30）**：geocoding-api.open-meteo.com 在部分网络环境 curl 通但浏览器连不上（天气城市搜索曾因此空结果），已用内置 50 城市坐标表兜底；ipapi.co 返回 403，IP 定位改用 ipwho.is（备用 get.geojs.io）。改天气相关代码时别把本地城市表和备用源删掉
13. **（已退役）全屏界面的互斥与高亮接管（2026-08-30 根治为统一管理器；2026-09-06 多页面改造后管理器整体移除，栏目改真实页面 + 外壳浮层，此坑成历史）**：曾靠各 open 函数自己收其他层，互斥清单散落多处，AI 界面上线漏了接管、文档界面上线 onScroll 保护没加全，各踩过一次。**现为注册制管理器（index.html 搜** **`FS_TOP`）**：互斥/高亮接管/锁滚动集中在 `fsOpen/fsClose`，新增全屏层只需 `fsRegister` 一次 + open 开头 `fsOpen(id)`；Esc 处理仍是各层自己的监听（保持逐层退），新增时记得加。`FULLSCREEN_CLOSE_IDS`（关闭按钮 id 映射表）也要补一条（见坑 17）
14. **\[hidden] 全局 CSS 等于永久隐藏**：动态显隐一律用 class 切换（.show 等），不要用 hidden 属性做闪烁类动画的开关
15. **AI 对话的几个注意点（2026-08-30）**：服务商 Key 只存 D1（site\_settings `ai_models` 内），任何接口不得回传完整 Key；`/api/ai/chat` 的鉴权同时接受 yhuo\_user 和 yhuo\_session 两种 Cookie（管理员也能聊）；限速是内存 Map（跨 Worker 隔离实例为近似值，别指望它做精确计费）；前端解析流式响应前先关掉输入（aiBusy），失败时要把用户消息从 aiHistory 弹回去否则重试会重复；AI 界面关闭即清空历史（aiResetChat 会 abort 进行中的请求）
16. **AI 配置接口的两个坑（2026-08-30，②当日根治）**：① `functions/api/` **嵌套子目录**（如 `api/ai/`、`api/admin/ai/`）里的相对导入比顶层多一层——`../../lib/`、`../../../lib/`，config.js 因此重写时三次写浅被 wrangler 构建拦下（`node --check` 不解析 import 查不出来），**现在双击** **`校验代码.bat`（node check-code.mjs）一键校验全部内联脚本语法 + 所有相对导入路径存在性，推送前跑一遍**；② admin/ai.js 的 PUT 里 `enabled` 字段曾多义（全局开关 vs toggle 动作参数），带 `action` 的请求必须忽略顶层 `enabled`，否则停用单个供应商会把整个 AI 关掉（踩过）——**已根治**：全局开关改显式 `{action:'global', enabled}`（旧裸 enabled 写法仍兼容），后台界面已发新格式
17. **（已退役）互斥关闭别用 `.album-close` 类找关闭按钮（2026-08-30 根治；2026-09-06 多页面改造后随管理器一并退役，关闭按钮已从栏目页摘除，docViewer/个人主页等浮层仍用各自 id）**：AI 顶栏的"新对话"按钮（aiNewChatBtn）也用了 `album-close` 样式类且排在真正的关闭按钮（aiViewClose）前面，而全屏层互斥逻辑靠 `querySelector('.album-close')` 找关闭按钮——结果开其他界面时点到的是"新对话"，AI 层永远关不掉（AI→工具/相册/文档/首页全卡在 AI 界面）。**根治**：关闭按 `FULLSCREEN_CLOSE_IDS` 映射表按 id 点，且互斥/高亮/锁滚动整体收敛进注册制管理器（见坑 13）；以后新增全屏层在映射表补一条 id 即可
18. **后台整页是模板字符串，内联正则反斜杠必须双写（2026-09-04 根治原 §9.5 遗留 Bug）**：functions/admin/index.js 的 `const PAGE = \`...\`` 包着整页 HTML+CSS+JS，模板转义会把内联代码里的 `\d` 吃成 `d`、`\.` 吃成 `.`——验证码校验 `/^\d{6}$/` 到浏览器里成了 `/^d{6}$/`（匹配 6 个字母 d），管理员「绑定邮箱」和登录页「忘记密码」重置从此**任何数字验证码都过不了前端校验**（报"请填写 6 位验证码"/"请填写邮箱和 6 位验证码"），请求根本没发到服务端。**服务端/D1/邮件发送全程正常**（验证码收得到、curl 带会话直调 /api/admin/me 一次通过），所以 §9.5 排查走了弯路——前端拦截类 bug 先看浏览器实际执行的正则源码（`RegExp.prototype.source`）。**根治**：模板内 8 处反斜杠全部双写（2 处 `\d{6}` 验证码校验 + 6 处 `\.` 歌词后缀/去扩展名正则），`const PAGE` 上方已留注释。**以后往后台页面内联代码里写正则一律 `\\d`、`\\.` 双写**；注意校验脚本查不出这类错（它不解释模板转义，语法层面 `^d{6}$` 完全合法）
19. **悬停弹出不能动命中几何——命中层/视觉层分离（2026-09-04）**：访问趋势图的弹出效果若直接挂在被悬停元素自己的 `:hover` 上，鼠标贴环内缘/柱底时会「弹出→元素离开鼠标→hover 失效→回落→再弹出」无限抽搐。**现为每个扇区/柱子画两份**：透明命中层（`.donut-hit`/`.bar-hit`，静止几何、`pointer-events:all` 接管全部指针事件，data-* 都在它身上）+ 视觉层（`.donut-seg`/`.bar`，`pointer-events:none` 纯展示随便弹）。悬停选择器是**相邻兄弟** `.donut-hit:hover + .donut-seg`（hit 在前 visual 在后，一个循环里紧挨着 emit）；事件委托 `findSeg` 同时匹配四类（`.donut-hit, .donut-seg, .bar-hit, .bar`）；`setFocus` 按 NodeList 位置联动 dim/hl，命中层不带这两类所以索引不会错位。**别把透明命中层当冗余删掉、别给视觉层恢复 pointer-events、别把 hit 和 visual 的 DOM 顺序拆开**——抽搐就回来了。悬停节奏为"进即弹、离缓回"（悬停规则内 `transition:none`，回落走基础规则 .2s）；另一坑：transform 的 translate 变量必须带 px（无单位整条 transform 失效），缩放必须 `transform-box:fill-box` 否则绕 SVG 左上原点
20. **多页面下的相对路径坑（2026-09-06）**：页面不再都在站点根——`/tools/` 等子页面里 `images/…`、`music/…`、`video/…` 这类相对路径会解析成 `/tools/images/…` 直接 404（原杂项页画廊曾因此全空；该页 2026-09-06 已移除，坑对现存子页仍成立）。前台静态资源引用一律写**根相对**（`/images/…`），common.js 已全量改过，新代码别再写裸相对路径
21. **`data-page` 在 `<html>` 上（2026-09-06）**：各页的页标识在 `<html data-page="tools">`（documentElement），页面化 CSS 选择器必须写 `html[data-page="…"]`——写成 body 永远匹配不上，页面容器停在 opacity:0/visibility:hidden 的入场态，肉眼即"整页空白"（本日踩过：工具/文档页点不动元素即此因）
22. **页面入场动画别带 transform（2026-09-06）**：`animation-fill-mode: both` 的 transform 关键帧会**持续占用**，把 `position:fixed` 后代的包含块改成该元素——AI 历史抽屉（fixed left:0）因此被顶出屏幕左缘只露一条。页面容器入场用只改透明度的 `pageFade` 关键帧；同理 `.ai-chat` 等原 `.show` 门控的入场态在独立页要显式放行（`html[data-page="ai"] .ai-chat{transform:none;opacity:1}`）
23. **改外壳要八个文件同步（2026-09-06 起；后增 /schedule/、/blog/，移除 /misc/，2026-09-07 增 /notes/，2026-09-09 深夜增 /games/（同日应用户要求移除），现共八页）**：头部胶囊/浮层/页脚/播放器等外壳 markup 在八个页面各有一份拷贝，改任何一处七处同步。**【2026-09-06 已补比对脚本】**check-code.mjs 新增**步骤 [4] 七页外壳一致性**：每页剔除 `<main>…</main>` 区间取外壳（先断言 main 标记各恰好一次），归一化三处合法差异（`data-page` / `<title>` / 导航项的 ` active`+` aria-current="page"`，仅处理含 nav-link 的行、ai-entry 类不受影响）后八页逐行比对基准页（首页），不一致报出错页名 + 首个差异行号——实测注入一行漂移即报 ✗、还原恢复全过（比对 506 行）。「改外壳七处手动同步」的操作约定不变，但现在漏同步机器兜底
24. **带 try/catch 吞错的模块里，"引用了不存在的函数"会静默炸掉整组功能（2026-09-06 线上事故）**：多页面手术把 updateClock 的函数定义连同旧启动代码一起删了，新的 startHomeClock 仍调用它——ReferenceError 被 runPageHook 的 `try/catch(e){}` 吞掉，**首页时钟/视频/寄语/天气全部静默失效且无任何报错**（线上用户实报才现形）。两条教训：①改 PAGE_MODULES 这类吞错模块后，必须逐函数核对引用处都有定义（`grep 函数名` 看定义处存在）；②**验证页面行为绝不能用"元素存在"当判据**——元素是标记自带的永远"通过"，要采样真实变化（时钟秒位两次采样对比、视频 src 非空、问候语≠标记初始值"你好"）。另记：登录卡对纯游客首访自动弹出是**既有"游客门"设计**（gatePassed = sessionStorage 有 demoLogin；登录或点「游客进入」即过门），别当 bug 修
25. **响应式断点夹缝：给某段窗口宽度单独收窄容器时，段内"固定宽度 + vw 大字号"的并排元素会相撞（2026-09-06 用户实报）**：900~1199px 段 `.page-main` 有专属规则收窄到 860px（site.css 搜「平板横向」），但首页 hero 行的视频盒仍是固定 `flex: 0 0 320px` 占右侧、时钟字号仍是 `clamp(3rem, 9vw, 6rem)`（该段最大 96px），而时钟容器 `.clock-hud` 是 `overflow:hidden`——**901~1199px 全段时钟必溢出且溢出方向正对视频盒**，观感即"视频盖住秒数"（线上逐宽度实测：901px 起秒位缺角、1040px 起秒位整位消失、该段可用宽度基本恒定 400~430px 而字号随 vw 变大，越接近 1200px 盖得越狠；≤900px 视频换行到时钟下方、≥1200px 主容器加宽到 94vw，两段边界外都正常，所以只有夹缝里出事）。**修法**（都在该段媒体查询内）：视频盒 `flex-basis: 320→260px`、`.clock-hud` 左右 padding 34→24px、时钟降一档 `clamp(3rem, 7.5vw, 68px)`、毫秒 `clamp(1rem, 2vw, 1.25rem)` + `min-width:3em`；修后该段全宽溢出转负（−31~−48px，字体渲染差异也留足余量），≥1200px 恢复原字号。**教训**：新增/修改某段窗口的容器宽度规则时，必须把该段里所有并排的固定尺寸元素与大号 vw/clamp 字号一起复核；验证逐宽度扫描量 `display.scrollWidth − hud.clientWidth`（或等价溢出判据），别只测首尾两端。另记：媒体查询整数边界（`max-width:1199px`）遇视口含小数（Windows 125%/150% 显示缩放常见）会整段不命中、按 ≥1200 宽版渲染——那是另一套有空间的布局，不算回归
26. **pjax 只换 `<main>`，外壳导航的"形状"停留在最初加载的那个页面——锚点形态的导航链接会吞掉点击（2026-09-06 用户实报）**：首页外壳的「首页」链接原是 `href="#home" data-target="home"`（平滑滚动用），子页面外壳才是真链接 `href="/"`。从首页出发 pjax 到其他页后**外壳不换**，「首页」仍是 #home 形态；而锚点平滑滚动处理器（common.js 搜 sectionLinks）**先无条件 preventDefault 再找 #home 元素**——其他页面内容里没有 #home，点击被吞成毫无反应，document 级 pjax 拦截器见 `e.defaultPrevented` 也跳过，即用户所报"进入其他界面后点首页回不去"（logo 原先也是 #home，子页面点不动；**直达子页时「首页」本是真链接反而正常**，所以只有"从首页走进其他界面"才复现，隐蔽性强）。**修法**：①六页标记统一——「首页」一律 `<a href="/" class="nav-link" data-target="home">`（仅首页那份带 active/aria-current）、logo 一律 `href="/"`（坑 23 六处同步）；②sectionLinks 处理器改**先找目标、找不到不吞事件**（放行给 pjax）。修后场景矩阵实测全过：首页→pjax 留言→点首页回首页（高亮正确落「首页」、时钟模块重绑正常走动）、直达子页点首页回首页、logo 从文档页回家、首页内点首页平滑滚顶且 URL 不变、返回键正常、直达子页静态高亮不受 data-target 影响。**教训**：外壳跨页共用但标记每页一份，凡"假设自己在某个页面"的锚点形态链接在 pjax 下都是定时炸弹——导航链接一律写真链接，行为差异（滚顶 vs 换页）交给 JS 按当前页面状态判定；另 pjax 类改造验证必须覆盖"从首页出发"与"直达子页"两种外壳形态，只测一种测不出这类方向性差异

27. **子页整页加载会让"首页模块的模块级元素"为 null——依赖它的函数重置状态后早退，回首页时数据是空的（2026-09-06 用户实报）**："非主页强刷后点首页回主页，歌词不显示"。链路：子页整页加载时 lyricBar 查出来是 null → 播放器恢复上次播放（applyResume/restoreCachedTracks）调 musicLyricsLoad，函数先把 lyricLines/lyricPlain 重置再因 lyricBar 为 null 早退 → 歌词数据从此为空；pjax 回首页时 lyricRebind 见 audio.src 已恢复走了"播放中"分支，musicLyricsTick 又因没歌词数据静默早退 → 歌词条可见但完全空白。**修复**：lyricRebind 发现"有播放源但没歌词数据"时补载当前曲目歌词。**教训**：①模块级"重查重绑"只恢复 DOM 引用，不恢复数据状态——在元素缺席时早退的函数，其调用方（跨页的播放器恢复流程）跑在元素不存在的页面上时，目标页初始化必须能补载；②顺带发现 musicLyricsLoad 里 /music//.test(src) 守卫在曲目 src 改为 blob URL 后永远命中，静态曲目的同名 .lrc 拉取早已静默失效——守卫改按歌名一律尝试、404 回落占位；③runPageHook 的吞错 catch 现挂 window.__pageErrors 错误面包屑，排查这类"静默无反应"先看它。

28. **pageFade（fill:both）让页面容器持续构成层叠上下文，容器内 fixed 浮层的 z-index 出不来（2026-09-06 手机实报）**：AI 历史抽屉（#aiHistory，fixed z60 但 DOM 在 #aiView 里）被顶栏胶囊（z50）压住，手机上「新对话」按钮点不到。#aiView 的 pageFade 入场动画 fill:both 结束后仍在"filling"态，Chromium 据此让容器持续构成层叠上下文（computed opacity=1，常规启发式查不出来）——抽屉的 z60 只在容器内部生效，容器本身 z:auto 画在顶栏之下。**修法**：html[data-page=ai] #aiView { position:relative; z-index:60 }（容器盒从 y96 起与顶栏不重叠，无副作用）。**教训**：往页面容器里加 fixed 浮层时，层叠上限是容器自身的层级；fill:both 动画的容器默认就有这个问题，别用 computed opacity/transform 判断，直接查 getAnimations()。②position 从 static 改 relative 会激活老全屏层样式残留的 top/bottom 偏移——.ai-view 老样式里留有 top:52px/bottom:0（fixed 时代的定位），static 时被忽略、relative 下生效，导致整页内容下坠 52px（同日"头部上移"需求时发现）；relative 化时必须把 top/bottom/left/right 一并归零 auto。

29. **预览缩略图 iframe 是"会跑完整站逻辑的真页面"，一切基于 localStorage/自动播放的行为都会在里面再来一份（2026-09-06 用户实报）**："首页播放音乐后切换界面，越切越多路音乐同时响"。pjax 本身无辜（注入 play() 监听逐页切换零新建音频）——真凶是顶栏悬停预览的**实时缩略图 iframe**：按路径常驻缓存、同源**共享 localStorage**、完整执行 common.js，主页播放中时 `musicMeta.playing=true`，悬停过的每个导航项的预览帧都各自自动续播一整份曲库且不销毁（实测悬停 5 项 = 6 路音频）。**修法**：common.js 读 musicMeta 初始化 pendingMeta 处，`window.self !== window.top` 时整份丢弃恢复数据——预览帧不出声也不偷跑下载整首歌。**教训**：①同源 iframe 缩略图 = 第二个完整运行站点，凡"读共享存储后自动做事"的逻辑（续播、恢复草稿、预取）都要加嵌入守卫；②排查"多路声音"先数媒体元素：外壳只有迷你条 `new Audio()` 一份 + 悬浮播放器 DOM `<audio>` 一个，多出来的路数对着常驻 iframe 数；③给"自动播放类"行为写代码时先问一句"这段代码会在预览 iframe 里也跑一遍吗"。
30. **`window.innerWidth`（含滚动条）与 `getBoundingClientRect`/`clientWidth`（不含）混用——固定定位元素坐标 15px 级漂移（2026-09-08 外观圆钮拖动踩过）**：滚动条约 15px 宽，拖动/定位计算两者混写会导致「存的视口位置」与「回贴位置」差 15px，且二次拖动因起点错位看似失效。修法：同一功能内统一用 `document.documentElement.clientWidth/Height`（不含滚动条），面板相对偏移用实时 rect 换算（外观圆钮拖动模块即此写法）。
31. **本地 http.server 无 cache-control，浏览器走启发式缓存——改完代码不 Ctrl+F5 会跑到旧 JS/CSS/HTML（2026-09-08 反复踩）**：症状是「改了没生效/功能像坏了」，极易误判成代码问题（外观圆钮调试时曾因旧缓存 HTML 误判门控失效）。修法：调试一律强刷，或 `fetch(url, {cache:'reload'})` 后再 reload；以 curl 服务器响应为准。

32. **`filter()` 回调里对"将被丢弃的对象"累加状态——更新被静默丢掉（2026-09-09 审计第二轮）**：common.js `aiSaveHistory` 在 `aiConvs.filter` 回调里给命中的对话累加消息数，但回调对其 `return false`（该对象随即被移出数组），之后 unshift 的新侧栏条目又只取本次增量 `d.added`——历史抽屉「N 条」每存一轮对话就回落为 2、永不增长，全程无任何报错。修法：把累计后的值显式带进新对象。**教训**：对集合做「更新字段 + 挪到最前」时，别在 filter 里改对象又整体丢弃——filter 的语义是筛选不是原地更新，要改集合用 find + 改 + 挪，或把累计值显式传给新对象；这类 bug 的症状是"数值不对"而非"功能坏了"，靠 UI 数值随操作变化的常识（条数只增不减）才能察觉。

33. **跨模块复用返回对象时字段名笔误——`undefined` 拼进键名静默改变语义（2026-09-09 审计第二轮）**：`getUserSession` 返回 `{userId, username}`，api/ai/usage.js 的每小时上报限流处写成 `user.id` → undefined，限流键恒为 `'uundefined'`，**所有前台用户共享同一份 120 次/小时的配额**（一个活跃用户把别人的配额烧光），无报错无异常；同目录 chat.js 同位置写的是正确的 `user.userId`，属纯笔误，已改。**教训**：①同一返回对象的多处消费点要交叉对照（conversations.js/history.js 也都是 userId）；②`'u' + undefined` 这类拼接不会崩、只产生一个"奇怪的共享键"——凡按身份隔离的键（限流/存储/缓存），出现"互相影响额度/数据串了"的征兆，先查键的拼接来源有没有落空字段。

34. **CF Pages Pretty URLs 会把 `/offline.html` 308 到 `/offline`（2026-09-09 深夜批次）**：Pages 默认剥离 .html 后缀，直接请求 /offline.html 会 308 重定向——SW 的 `cache.addAll` 跟随重定向拿到内容后，**仍按原始 URL `/offline.html` 作键存**，所以 `caches.match('/offline.html')` 能正常命中，离线兜底不受影响；但这纯靠"跟随重定向后保留原 URL 作键"的行为。**对策**：往后凡新增纯静态兜底页（SW 要 precache 的），先 `curl -sI` 看一遍重定向链再写缓存键，别按"文件在哪键就是哪"想当然。

35. **`createMediaElementSource` 是永久接管——audio 元素一旦接进 AudioContext，声音就再也回不去默认输出（2026-09-09 音乐频谱）**：表现为不给它接 destination 就静音、`close()` 掉 Context 声音永久消失、也没有任何 API 撤销接管。**对策**（音乐频谱的取舍，见 §4）：source 建完立刻 `connect(analyser)` 且 `analyser.connect(destination)`；Context 建后全程不 close；面板开关只停 rAF 绘制、绝不 disconnect——初始化链路任何一步抛错宁可整体不启用频谱，不能让用户没声音。

36. **高频 DOM 写入会饿死一切「防抖等平静」的 MutationObserver（2026-09-11 首页内容区踩过，用户实报「切换到别的界面再点主页，添加的内容消失、刷新才出现」）**：滚动模糊揭示模块（common.js 顶部 IIFE）在 .page-main 上挂 MutationObserver(childList+subtree) + 80ms 防抖重扫（pjax/异步插图靠它重新 collect+show）；首页毫秒位每 50ms `textContent = …` **替换文本节点 = childList 突变**，防抖定时器每 tick 被清零重排 → 只要停在首页重扫永不落定 → pjax 回首页后新的揭示目标（.apple-card 等）不被采集不被揭示（opacity 0+blur 常驻，视觉即「消失」），滚动也没用（targets 数组还是上一页的），只有整页刷新（IIFE 启动时同步 collect+show）才显示。首页此前没有揭示目标内容，所以该饥饿机制潜伏未爆，直到首页内容区成为第一个目标。**修法**：startHomeClock 毫秒位改保留文本节点 `clockMsText.nodeValue` 写入——characterData 突变不在该 observer 的监听配置（childList/subtree）内，零扰动，时钟观感不变。**教训**：①textContent 轮询写常驻元素一律改常驻文本节点 nodeValue（顺带省掉每秒 ~20 次的节点重建）；②症状特征「整页刷新正常、pjax 回来就坏、滚动无济于事」+ 页面上存在高频写入源 → 先怀疑这个饥饿机制；③决定性排查手法：摘掉可疑高频写入源（如临时 `#clockMs.remove()`），症状立刻消失即是它；④周期小于防抖窗口的写入才饿得死（本例 50ms < 80ms；200ms 的 updateClock 写不饿）。

## 9.5 未解决 Bug

（当前无。原 §9.5「管理员绑定邮箱失败」已于 2026-09-04 根治，见 §9 坑 18；前台用户绑定邮箱走 index.html 真实文件、不经模板字符串，从未受此影响。）

## 10. 行为约定

* **前台内容合并显示**：后台清单排前面，静态文件夹（music/、video/、images/）内容跟在后面，同名去重；后台某类完全没内容时该类只显示静态内容；任一来源不可用时自动只用另一来源

* **上传同名查重**：标题带扩展名和不带扩展名都算同名（静态同步进来的标题带扩展名）

* **推送**：助手改完代码**只做本地修复和验证，不主动推送**；用户明确说"推送"时才推（推前确认要推送的文件）。2026-08-30 起生效（此前一度约定助手可直接推送，已收回）。**推送默认走系统代理（2026-09-06 用户指定）**：不用每次等用户报端口——按 §2 流程自查（先查注册表 ProxyEnable/ProxyServer 拿当前端口，curl 探测确认通再配 git），配代理 → 推 → **必须 unset**；端口不固定，别复用旧值（55734 只是 2026-09-06 当天的值）。网络差时 git push 失败就多重试几次（实测 3\~12 次内成功）

* **删库不可恢复**：后台删除同时删 KV 文件；解散相册只解除归属

## 11. 待办 / 扩展点

* 备份的**导入恢复**（配合已有导出，做恢复接口 + 后台入口）

* 用户密码重置（后台强制改密；前台自助改密已上线）

* 相册排序 UI（albums 表已建成、sort_order 列已备好——空相册不落库已根治，见 §9 坑 5；缺的只是后台拖拽排序）

* 外观偏好跟随账号存服务端（涉及与 localStorage 的合并优先级，想清楚再做）

* 前台用户个性化：评论等（收藏已上线）

* 访问趋势：按小时粒度、来源统计（需记录更多维度）

* 相册封面自定义（当前用前 3 张）

* AI 扩展：思考等级/上下文容量等客户端同款面板、AI 用量按模型/按小时的更细粒度图表（限速目前是内存近似值；本地持久化聊天记录已上线）

* 课表扩展：作息时间预设模板（早八/晚课等一键套用）、课表周视图导出图片、提醒改为多档提前量

* Resend 绑定自有域名（当前仅站长邮箱可收信，全员课表提醒/注册验证都受此限制）

* 歌词扩展：在线歌词 API（当前支持后台曲库歌词 + music/ 同名 .lrc 两条来源）、歌词区多行滚动样式

## 12. 常用操作速查

| 想做什么          | 怎么做                                                                                             |
| ------------- | ----------------------------------------------------------------------------------------------- |
| 改前台样式/功能      | 全站样式改 `assets/site.css`、脚本改 `assets/common.js`；单页内容改对应栏目页（index.html / tools|docs|ai|board|schedule|blog|notes 的 index.html）；本地 8000/8799 端口验证（python http.server 现在也能直接刷新 /tools/ 等子页） |
| 改后台界面         | 编辑 `functions/admin/index.js`，wrangler 8799 端口验证                                                |
| 加/改接口         | `functions/api/` 下按路径放文件，导出 `onRequestGet/Post/Put/Delete`                                      |
| **给文档区加一篇文档** | md 放进 `docs/`，在 `docs/docs.json` 加一条 {file,title,desc,date}，推送                                  |
| **给歌曲配歌词**    | 后台 KV 曲库：音乐页上传时同选同名 .lrc 自动配对，或歌曲行「歌词」按钮补传/替换（已配显示✓）；静态 music/：把 `.lrc` 放进 `music/` 与歌曲**完全同名**（只换扩展名）。两种都推送/保存后前台刷新生效                |
| 给更新日志补条目      | 编辑 `docs/changelog.md`                                                                          |
| 上线            | `推送更新.bat`（或 git push），等 1-2 分钟，Ctrl+F5                                                         |
| **改外壳（头部/浮层/页脚/播放器 markup）** | **八个页面同步改**：index.html + tools/ docs/ ai/ board/ schedule/ blog/ notes/ 的 index.html（坑 23）；漏同步会被 校验代码.bat 步骤[4] 当场报出 |
| **开代理后 git 仍连不上** | ① 查 Windows 系统代理：`reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings" //v ProxyServer`，把拿到的地址（如 127.0.0.1:55068）配给 `git config --global http.proxy http://地址`（端口会变，每次现查）② 推完 **必须 --unset 清掉** ③ 详见 §2 代理实践经验（TUN 模式则直连重试即可） |
| **课表提醒不发了排查** | ① 后台「邮件」→「立即执行一次」：现在会显示每个账号为什么没发（今天没课/邮箱没绑定/仅站长模式不符/早报未开/未到点/今天已发过/不在提醒窗口），按提示对症处理 ② 用「发送测试提醒」验证发送链路（能收到=发送正常，问题在触发侧）③ cron-job.org 任务是否 Active、最近执行是否 200（**401=重新生成过密钥，必须去 cron 任务里换新 URL**）④ 邮件服务是否启用 |
| 重置本地后台数据      | 关 wrangler 进程后删掉 `.wrangler/` 目录重启（进程不杀干净文件会锁住）                                                 |
| 看线上数据库        | Cloudflare 控制台 → D1 → yhuo-db → Console                                                         |
| 校验前台内联 JS 语法  | 双击 `校验代码.bat`（顺带校验全部 ESM 语法与 import 路径），或提取 `<script>` 后 `new Function()`                       |
| **推送被拒 non-fast-forward** | 远端可能有网页上传分叉：按 §2「网页上传重复的处理」四步走（fetch → 比对 → rebase → 核验后推）                    |

